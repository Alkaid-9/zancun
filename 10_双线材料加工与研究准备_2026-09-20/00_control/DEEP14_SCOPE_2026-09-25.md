# 14-paper deep-dive scope update · 2026-09-25

Status as of pause: USER-PAUSED / WAVE-1-TRANSPORT-FAILED / ONE-CONTRAGENT-SOURCE-PACKAGE-PARTIAL / NO-PAPER-FULLY-ACCEPTED / USER-LEARNING-UNTOUCHED. The `EXECUTING` line in earlier drafts described the start of this window; the later user request "停一下" supersedes it. Only handoff/archive writing is allowed during this closeout. No paper work, agent retry, research experiment, commit, or push is in progress.

## Authorization and authority

The user has replaced the earlier depth limit (11 P0 + PNULock/SBPN P1 + three incremental papers) with **deep, separate dissections of all 14 papers**. The original 11 and the incremental three remain distinct source pools. This file records the new scope; it does not retroactively mark older tasks complete or rewrite the historic D1/D2 dates (2026-09-22/23). `00_control/TASKS.tsv` still reports the prior execution receipt; no deep-dive status is inferred from its READY/PLANNED rows. This wave starts on 2026-09-25 (America/Los_Angeles); the learner's actual study time and EX-05 status remain separate.

Baseline: `HEAD=6f31e797b3cf6210ba4f5389b53c03ebcabf3c11`; branch `audit/redteam-external-ref-2026-09-20`. At start, pre-existing changes were tracked modifications in `.claude/gemini-guard/{test_guard.py,worker.py}`, `GEMINI_WORKER_CONTRACT.md`, and untracked `.claude/gemini-guard/README.md`, `DRAFT_00_OBJECTIVES_AND_CLAIMS.md`, `DRAFT_04_BRIDGE_LU_SUN_OPENAI.md`. Do not edit, reset, stage, or subsume these changes.

Only the controller writes new deep-dive receipts under this package's `00_control/`, `B/`, `research/`, or `runs/`. Workers read frozen PDFs/old analyses and return once; they do not edit, execute state-changing commands, read sealed/learner answers, or spawn agents. No commit, push, server/account/configuration changes, user-answer edits, PASS/OWNED decisions, or Gemini production retries are authorized. This environment fixes spawned workers to `gpt-5.6-sol / xhigh`; do not claim they ran as a different model.

## Acceptance separation

For each paper, distinguish source identity and claim coverage, source-grounded mechanism, falsification/strong simple baseline, and real executable evidence. The old package `P2=full reusable package` and the newer three-paper `P2=falsification` are different fields, never a shared completion label. Every nontrivial accepted claim needs a physical PDF page plus section/formula/definition/table/figure when applicable; unsupported content is UNKNOWN. A proposal for a counterexample is not a verified counterexample; a test specification is NOT_RUN until an actual command/log/oracle exists. Main controller reviews one paper at a time and records ACCEPTED_SCOPE/PARTIAL/REJECTED and unexamined sections before upgrading its task. The initial pass may return PARTIAL; deeper follow-ups are required to cover the whole paper.

Wave 1: independent frozen-input reviews of PNULock, SBPN, and ContrAgent. Concurrent read-only paper work does not mean concurrent main-controller acceptance. After the wave, rotate PetriBench/String Diagrams and the remaining original nine by independent paper-specific tasks; no paper is dropped because another stalled. The original A/D1 materials remain READY; A6/A7 still await genuine learner receipts. No EX-06 release.

The planned rotation did **not** happen: all three Wave-1 workers returned transport errors without research content. The controller then created a partial original-PDF ContrAgent source package, including 36 mapped claims and two *unrun* counterexample specifications; see `DEEP14_WAVE1_DISPATCH_2026-09-25.md` and `../B/papers/contragent/ACCEPTANCE.md`. The user subsequently requested an immediate pause and comprehensive archival handoff. For current state, consult `TASKS.tsv` and the closeout handoff, not this pre-wave scheduling paragraph.

## Stop/recovery

If a source hash changes, title/identity mismatches, a critical theorem/condition is unclear, citations cannot be located, or formal/learner answers would be exposed, stop only the affected paper and record the exact remaining page/claim. Preserve worker outputs as candidates, not accepted facts. Any rollback inspects task-owned paths individually and preserves all pre-existing work; never use whole-repo reset/clean.

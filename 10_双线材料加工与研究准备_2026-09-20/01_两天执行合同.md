# 两天双线材料加工与研究准备合同 v1.0

规划：Astra（gpt-6-astra / xhigh）；主线程依据原题面、论文输入和用户最新要求核对并补实。原稿见 [Astra规划原稿](Astra规划原稿.md)。本文件与 [逐篇施工卡](02_逐篇施工与专项验收.md)、[规划看板快照](03_两天任务看板.tsv)、[验收检查表](04_验收检查表.tsv) 共同保留范围、依赖与验收设计；进入执行后，当前任务状态只读 `00_control/TASKS.tsv`。细项以施工卡的具体对象和检查为准，不用通用微例替代论文机制。

本合同状态为 **EXECUTING / MATERIAL-BUILD-IN-PROGRESS**。D1：**2026-09-22**；D2：**2026-09-23**；记账时区：**America/Los_Angeles**。学习时段由本人在 D1 回执中记录，未填写不得虚构。执行证据只认 `00_control/`、`A/`、`B/`、`research/`、`runs/`与最终 manifest 中的实际产物。

首要目标是在每个学习块开始前交付能直接使用的 A 线材料，当前具体游标为 **R1最小缺口＋EX-05继续**；B 线在不阻塞A线的前提下加工后续论文资产。本人每天另有 **260 分钟净学习目标**，可按精力延长；时长用于排程，不是能力评分。具体近期路线见[近期学习优先执行表](06_近期学习优先执行表.md)。两天不承诺完成所有论文复现、形成创新成果或取得 OWNED。原 825 行合同未取得，本合同不声称已完整审阅或覆盖它。

**一、工作关系、角色与并发合同**

| 角色 | 职责与交付 | 权限及约束 |
|---|---|---|
| Astra 规划 | 本合同及必要的范围调整 | 当前仅输出文本；不成为后台常驻规划器 |
| 主控 M，兼任 A 线控制器 | 近期课程路由、全部文件归档、代码实现和运行、任务账本、结果合并及最终交接 | 唯一GPT/Codex渠道；唯一写入者和最终验收者，不承担可并行的机械抽取 |
| Claude worker池 | C-A准备下一学习块候选材料；C-PNU/C-SBPN回源P1；C-R做新上下文复核 | 最多3并发；只读、任务隔离、一次终态返回，不修改文件或任务状态 |
| Gemini worker池 | G-P0抽取P0题录/原句字段；G-SUN抽取孙猛来源中的日期、项目、成果、角色字段 | 最多4并发；每包新会话、单一问题、最多3个输入、20分钟、1次定点修正；只交DRAFT/PARTIAL/BLOCKED，主控逐字段回源 |
| 独立复核 | 检查来源、推导、接口和真实日志 | 使用新的Claude只读任务；占Claude池名额，以替换生产worker的方式进入，不突破并发上限 |
| 本人 | 学习、作答、解释、修改和调试 | 不计入 AI 生产线；AI 工作时长不能折算为本人学习时长 |

最新用户修订覆盖早先“总并发2”的限制。执行期并发硬上限为：**GPT/Codex主控1个，Claude worker最多3个，Gemini worker最多4个；Claude+Gemini worker合计最多6个，总活动上限7个**。Astra规划器不与执行期主控并开。每个worker必须有唯一任务ID、固定输入、独立输出缓冲和终止条件；只有M可写仓、运行会改变状态的命令、合并结果或改任务状态。

默认波次为：第一波`M + C-A + C-PNU + G-P0×4`；A线下一块已就绪后可转为`M + C-A + C-PNU + C-SBPN + G-P0/G-SUN×3`。需要复核时结束一个Claude生产worker，再开`C-R`，不把同一会话同时当生产者和复核者。M优先消费C-A，Gemini/论文worker即使已返回也只能排队等归档，不能打断下一学习块。

Gemini按用户最新要求取得一个**受限可选额度**，不取得控制权。只允许两类微包：`G-P0`从固定论文快照抽取题名、作者、年份、版本/DOI及带坐标原句；`G-SUN`从主控已取得的最多5个URL快照抽取人物身份、日期类型、项目/论文/软件名、角色及原句。它不得制作A线材料，不得解释完整论文、裁P1/P2、判断孙猛当前主线、做novelty/idea、写代码、运行工具、读取主仓或修改任何文件。

真实执行仍受根目录`GEMINI.md`与`GEMINI_WORKER_CONTRACT.md`约束，并只可走`claude-gemini-worker`专用无工具入口。T00后先用一个非敏感合法快照做一次格式烟测；成功才开放最多 **13个正式微包**：`G-P0-01..11`每包一篇P0，`G-SUN-01..02`各处理最多5个已取得URL快照。Gemini最多4包并发；每包目标5–10分钟、硬停20分钟，总硬上限260 worker-minutes，三至四波完成。时间从TB0/TR1/TB3–TB6的B线原预算内扣，超出对应任务预算就延期后排目标，不延长总窗口。烟测失败、限流或格式不合格时不连续重试，立即转Claude/Luna；任一伪造坐标、越界结论或状态自升，整包拒收并关闭本轮Gemini。Gemini输出不得直接落盘，M须对所有非空事实100%回源后才能接收；已接收字段不再让GPT/Claude重复抽取，它们只处理UNKNOWN、CONFLICT、P1及最终判断。

依本地派发规则，子任务使用 `fork_turns="none"`，不覆盖模型、角色或推理强度；任务必须自包含。派发后主控进入等待循环，不在同一检索范围重复工作；返回后再加工、归档。因此 A/B 是两条独立排队、交错推进的生产线，不能按两倍持续吞吐估算。当前会话的只读子代理不能直接成为后台文件生产者：W 返回可归档正文、规格和证据，M 负责落盘与实现。已结束的会话不会自动常驻；本合同没有创建定时任务或后台服务。进入执行后，M 在同一执行回合持续消费队列，暂停时交明确断点；另开窗口也共用两个槽位。B 的实质成果由只读分析与主控落盘共同完成，不能停在“给了检索建议”。

本人当前题目、参考答案和未来学习材料分开交付。B 可提前加工后续论文，不以本人通过当前学习站为条件；向本人释放哪些练习，仍遵守学习依赖和原站序。

**二、固定输入、写域与任务账本**

以下事实由规划阶段的定点读取和路径检查支持，但尚未形成执行期冻结快照。执行开始时仍须建立 `O00_输入登记`，重新记录实际路径、版本、哈希和使用范围；规划期可定位不等于执行期内容未变化。

| 输入 ID | 已知输入及定位 | 使用规则 |
|---|---|---|
| I01 | `01_进组总计划_OE1_材料交付/CURRENT_LEARNING_ROUTE.md:7-14,17-47`；同目录 `R1_R6_EVIDENCE_INVENTORY_2026-09-20.md:9-20` | 复用 EX WIP；下一指定交付为 R1。缺交件不等于不会，也不能由参考答案代替本人证据 |
| I02 | `02_四论文Ownership主线/00_共享合同_计划_验收/计划与决策/2026-09-13__research__four-paper-full-ownership-v3-sol-execution-contract.md`；该共享目录下 `学习与课程/ownership-v3/BUILD_STATUS.md` | 保持原站序、全文及主要主张覆盖要求；历史 BUILT/QA-PASS 不转写为当前验证或本人能力 |
| I03 | `03_鲁组其他论文与研究谱系/99_其他论文与盘点/论文原文与拆解/teardown-joint-20260813/09_FAMILY_SYNTHESIS_V2.md` | 定位旧拆解、critique、PDF 和旧候选矩阵；不把“约 7/18”解释成当前完整发表目录 |
| I04 | `03_鲁组其他论文与研究谱系/07_Deadlock_JOS2021/论文原文与拆解/teardown-joint-20260813/15_DEADLOCK_JOS2021.md`、`15b_DEADLOCK_JOS2021_kg_critique.md` 及其 PDF | 在新目录登记“总卡未入档、实际已有资产”的差分，不修改旧总卡 |
| I05 | 共享目录下 `论文原文与拆解/session-archive-20260906/ONLY_PAPER_FULL_SESSION_20260906.md`，对应旧会话 10403–11166 行；`/mnt/d/alkaid/desktop/导出的/六方向探索承接与证据审阅_2026-09-13.md` | 孙猛、六方向研究的已有起点；外部事实需刷新，不继承“确认不存在”的判断 |
| I06 | `04_Bridge与迁移研究/论文原文与拆解/teardown-bridge-20260904/13_BRIDGE_SYNTHESIS_20260904.md`；`06_九月研究刷新/02_学术增量与被引_RS/研究刷新/rescan_202609/00_SUMMARY.md`；`06_九月研究刷新/01_工业界_IND1/研究刷新/industry_scan_202609/IND1_minimal_scan.md` | 复用比较和工业扫描；开放项保持开放，不据此宣称新颖或已有部署 |

执行仓为 `/mnt/d/alkaid/desktop/zancun`，输入报告的分支为 `audit/redteam-external-ref-2026-09-20`，HEAD 为 `2513bd3072b2d5f27c1aa23aa789f10284f17fd3`。启动时记录实际值；不同则建立版本差分，不自行切换分支。

唯一新增写域：

`/mnt/d/alkaid/desktop/zancun/10_双线材料加工与研究准备_2026-09-20/`

原 MAS 仓 `/mnt/d/MyResearch/MAS_Safety_Project`、报告 HEAD `5d5bffc`，仅在明确复用需求下定点只读。不得回写原仓；不得修改旧 EX、本人答案、Ledger、sealed/holdout、审计、全局计划；不得纳入其他窗口的四份 09 修改、`.claude/` 或 `RESEARCH_AGENT_CONSTRAINT_v1.0.md`。邮件、简历、一页纸及发送继续冻结。

新目录按用途组织，不要求每个小项机械拆成文件：

```text
00_control/       合同、任务账本、输入版本、冲突与延期记录
A/                D1、D2 学习包，新模块 rubric 草案及后续冻结记录
B/papers/<slug>/  README、来源与主张表、机制材料、接口、fixture、代码
research/         方向证据、机制比较、候选与 novelty 记录
tools/            必要的运行捕获和 manifest 校验脚本
runs/<run_id>/    原始命令、stdout、stderr、退出码、环境与输入哈希
manifest.json    交付物、依赖、来源、哈希及限制
```

Markdown 使用 UTF-8；表格账本使用 TSV；机器输入使用 JSON。任务账本至少含 `task_id、output_id、input_ids、depends_on、owner、status、acceptance_ids、evidence、next_action`。本合同所有新任务初始为 `PLANNED`；历史资产状态另列，禁止混写。

**三、D1/D2 的交付与学习安排**

AI 的任务是提前准备下一学习块所需材料。本人等待模型/下载/安装的时间不计学习；每个学习块开头确认入口已就绪。未就绪时使用已确认的公开旧材料和纸面分支，不用未来答案填补时间。

每天目标 260 分钟；下面时间是净学习分钟，休息另加。已会的基础通过短检查后跳过，把时间回拨到当前站。时间到不自动 PASS；未完成的站留下可恢复断点。

| 时段 | 本人学习动作 | AI A 线提前交什么 | B 线同期队列 |
|---|---|---|---|
| D1 第1块 30分钟 | 找本人R1稿；没有则只开始五问空白框架。到点保存，不能让补档吞掉全天 | TA1：当前游标、原题入口、R1空白框架；不给答案、不重做DFG | TB0：11篇输入/旧资产队列 |
| D1 第2块 30分钟 | 对照题面判断`test_invariants.py`与`own.py`各自角色，写本日正式交件 | TA1/TA2：三个EX-05文件差分；不自动合并代码 | TB1：PNULock P1回源 |
| D1 第3块 80分钟 | 先写预测，再完成/检查正序、逆序、固定seed随机顺序；保留第一次失败 | TA2/TA4：worksheet、oracle/invariant/seed定点基础、运行预检 | TB2：SBPN P1回源 |
| D1 第4块 90分钟 | 接管`build_universe`：先凭记忆写representation，再核代码；改一个条件、预测、运行、观察、改回 | TA2/TA4：dataclass/Mapping/traceback/Git diff微单元；不写takeover答案 | TB1/TB2继续；其余论文只做P0入口 |
| D1 第5块 30分钟 | 解释今天测试了什么、没测试什么，交D1回执和明日首动作 | TA1/TA2：只记录动作、提示、错误和断点 | TV1先验A线；B新任务可暂停 |
| D2 第1块 20分钟 | 关材料回忆invariant、representation和一处失败/修改 | TA3：只依据D1实际回执给分支 | B线继续独立回源 |
| D2 第2块 100分钟 | 完成`build_universe`未完项并接管`classify_trace`：预测标签变化、真跑、改回 | TA3：题面入口、分级提示、预检；不提供目标结果 | TB1/TB2完成P1或留证据化断点 |
| D2 第3块 60分钟 | 用自己的话完成来源披露、两函数输入/状态/输出和判据重叠，填写`takeover.md` | TA3/TA4：结构空框与当前代码基础 | TB3–TB6按余量做P0/P1目标 |
| D2 第4块 40分钟 | 做`1.000`附录或最小EC/SR记录，只写证据上限 | TA3：证据类型、claim ceiling短卡 | TR1–TR3仅在A已就绪时推进 |
| D2 第5块 40分钟 | 脱稿解释并整理提交；验收者核前置。未满足则留在EX-05 | TA3：EX-06门禁判断与下一步，不代填T0 | TV2/TH1：A/B分开收口 |

EX-05 已读题面原本预计 4–6h。其冻结检查包括 10 个 universe 的正/逆两种顺序、对 full_features 的 S/E/R 比较、生成器/分类器 representation 与条件变更影响；具体题面以原文件为准。两天不预先承诺完成 EX-05、EX-06 和全部新桥接模块。

分支处理：R1最多占D1前30分钟；只缺指定交件而已有实质WIP时迁移本人证据，不重做整站。EX-06公开题面要求G0 note及EX-02/03/05 PASS，当前证据未闭合，因此默认保持LOCKED。若概念/实现确有缺口，只补当前代码所需的20–30分钟微单元再返回。正式能力检查前冻结适用rubric；未冻结时状态只记LEARNING/SUBMITTED。

**四、逐篇加工队列及两天承诺**

深度标签与执行状态分开；每篇独立交付、独立验收。准确输入见 [资料输入清单](资料输入清单.tsv)，逐篇具体施工与失败例见 [施工卡](02_逐篇施工与专项验收.md)。

- **P0 登记**：身份、原件、已有分析、缺口与排期清楚；仅P0不算“已加工论文”。
- **P1 论文机制片段可复用**：至少把选定的一个原文核心机制核到定义/公式/算法/证明坐标，提供对象、接口、前提、推演/反例和复用边界；旧全文分析保留入口和未重核部分。通用基础例子不算论文机制片段。
- **P2 完整可复用论文包**：满足第五节全部适用要求，全文论证链和主要贡献逐项核到位；至少一个有论文依据的机制可计算、可手推或可机械检查，结果独立复核。
- **P3 作者结果复现**：作者实现/版本、数据、配置、评价协议和差异均可核验。教学重建、有限枚举或表格重算不得升级为P3。

| 论文 | 任务 / 组件 | 两天必达 | 目标升级 | 关键验收 |
|---|---|---|---|---|
| EdgeIM | TA2 / EI-01 | 与EX-05直接相关的来源/机制映射和学习材料；不新开整篇P2重建 | 现有深拆回源补缺后再评P2 | S/E/R、频次与fixed-K边界准确；不能代写本人不变量题 |
| PNULock | TB1 / PNU-01 | P1：recover/合法终止或另一项回源机制片段 | P2全文链与作者工具小例 | 论文对象映射、使能/持锁不变量；等待图不顶替PN机制 |
| SBPN | TB2 / SBP-01 | P1：式(8)/(9)或关键理论方向的回源机制片段 | P2全文链与实验审计 | PDF公式逐符号对应、手算与参数边界；一般AND/OR不顶替 |
| SegLock | TB3 / SEG-01 | P0差分与P1施工入口 | P1/P2 | 两篇对象分开；未回源机制不称已加工 |
| Deadlock JOS2021 | TB3 / JOS-01 | P0并纠正新队列“未入档”误判 | P1算法片段 | 后作差分须双向回源，谱系图不抵机制 |
| MHP | TB4 / MHP-01 | P0差分与规则待核清单 | P1完整规则单元 | MHP不等于实际race；规则假设单列 |
| UAF-PN-VFG | TB4 / UAF-01 | P0差分与接口待核清单 | P1判定链 | 告警不等于已重演漏洞 |
| SBTPN | TB5 / STP-01 | P0及SBPN→SBTPN五项差分入口 | P1/P2 | 无参数不填数值；时序/根因语义保持OPEN |
| sigRank | TB6 / SIG-01 | P0及当前学习解锁/暴露边界 | P1评分片段 | 不提前释放课程；不伪作精确作者算法 |
| Ground Truth | TB6 / GT-01 | P0及world-contract后续入口 | P1生成/记录接口 | 真值独立于被测方法 |
| CrossEdgeIM | TB6 / CX-01 | P0及三层接口后续入口 | P1机制片段 | 未证幂等性、隐私和通信收益不补写 |

两天硬交只要求11篇队列P0对账、EdgeIM近期映射及PNULock/SBPN两个P1片段。其他论文仍有明确P1施工卡和后续顺序，但不再以牺牲近期学习材料为代价硬塞进两天。P0或普通教学小样不得改名为P1/P2。

旧“18篇”矩阵作为待对账目录，不作当前总数。SEFPN和苏旋采样等先核完整身份再纳入后续队列。CrossEdgeIM不是鲁法明署名成果；其他横向论文署名由题录决定，不统称“鲁组11篇”。

**五、可复用论文包的验收标准**

P2 包必须让后续执行者能够回答“原文说了什么、为何成立、在哪些条件下成立、我能抽取什么、哪些尚未验证”，而不是再读一遍长摘要。

| 组成 | 必须出现的内容 |
|---|---|
| 身份与来源 | 题名、作者、年份、版本、DOI/稳定链接；PDF 页码与印刷页码区分；来源哈希及取得时间 |
| 全文论证链 | 问题→假设→形式对象→机制→理论/实验支持→限制；按章节登记 coverage，主要贡献原文逐项编号 |
| 形式与推导 | 符号类型、单位、量词、前置条件；关键证明/推导的依赖、未证步骤、可反驳条件 |
| 实验与结论 | 数据、预处理、基线、指标、参数、随机性及威胁；明确哪些只是转录、复算或重新运行 |
| 资产接口 | 输入输出 schema、前后条件、不变量、异常、复杂度/成本、适用及禁止外推的范围 |
| 使用材料 | README、fixture、预期行为、运行方法或手工演算；可执行部分附真实运行证据 |
| 增量说明 | 旧资产已覆盖什么，本次纠正/补足什么；保留未解决项，不把重排版写成新增研究 |

理论论文可交经核验的推导、反例和机械可检查部分；没有代码的论文可交明确标记的机制重建。没有可执行主张时，运行项注明不适用及理由；有计算主张却缺实现时，登记缺口，不能借“不适用”取得同等完成度。

**六、三个填实的执行规格**

以下数值是计划中的验收预期，不是已运行结果；原文符号映射必须回源后填写。

**E01：EdgeIM 的结构覆盖不保留频次，关联 TA2 / OA2 / AC03–05。**

输入 `frequency_pair.json` 含两个各 10 条轨迹的日志：

- L1：9 条 `[a,b,z]`，1 条 `[a,c,z]`。
- L2：1 条 `[a,b,z]`，9 条 `[a,c,z]`。

教学合同定义 `S` 为首活动集合、`E` 为末活动集合、`R_DF` 为直接跟随边集合。两者均须得到 `S={a}`、`E={z}`、`R_DF={(a,b),(b,z),(a,c),(c,z)}`；边计数分别为 `9,9,1,1` 与 `1,1,9,9`。在活动 a 处归一化的分支频率分别为 `.9/.1` 与 `.1/.9`。

只有确认原文定义一致，才把 `R_DF` 映射到论文 R。演示仅支持“这些集合不能确定频次”，不声称任何指定下游算法必然产生不同模型。Stage1 不添加 fixed K；Stage1 集合 R 与 Stage2 weighted R 分别说明。

接口接受非空字符串轨迹，拒绝空轨迹及非法事件；重复边按发生次数累加，不能每条轨迹去重。计数部分复杂度为 O(事件数)，排序展示成本另列。测试包括上述反例、重复活动以及非法输入。该统计器只是公开教学例；EI-01还必须包含原Algorithm 1机制实现和独立参考核验，不能以统计器替代采样算法。缺作者代码时，README 必须标为教学统计器/机制重建。

**E02：PNULock 配套锁语义练习，关联 TB1 / OB1 / AC03–06。**

输入为线程、锁及事件序列：

```text
T0 acquire L0
T1 acquire L1
T0 blocked_request L1
T1 blocked_request L0
```

教学假设为互斥锁、无超时、无抢占、请求阻塞且持锁不自动释放。输出应含等待边 `T0→T1、T1→T0` 及循环集合 `{T0,T1}`。对照输入删除最后一行，只能报告“未发现等待环”，不能报告整个程序安全。

主控实现事件合法性检查和等待环检测；对“同一锁同时由两线程成功持有”“对空闲锁声明 blocked_request”等输入返回语义错误。另写一个改变超时/释放假设的边界例，说明原结论为何不再直接适用。

这是明确约束下的配套教学模型，只计 A 线基础素材，不满足 OB1/PNU-01 的论文机制交付。B 必须另给“教学对象—PNULock 原文对象”的逐项映射和差异；若原文语义不匹配，保留为 CS 练习，并重新限定论文片段。等待图演示通过，不能替代 PNULock 算法正确性或工具复现。

**E03：SBPN 概率与依赖练习，关联 TB2 / OB2 / AC03–05。**

输入边际概率 `P(A)=0.2、P(B)=0.3`：

| 条件 | 交集概率 | OR 概率 | AND 概率 |
|---|---:|---:|---:|
| 显式独立假设 | 0.06 | 0.44 | 0.06 |
| 指定相关联合分布 | 0.15 | 0.35 | 0.15 |

相关情形的联合分布为 `P11=.15、P10=.05、P01=.15、P00=.65`，四项和为 1，边际保持不变。程序校验非负、归一化及 Fréchet 边界，拒绝本例 `P(A∩B)=.25`。浮点断言容差为 `1e-12`；独立性必须显式传入，不能默认为真。

这个练习只计 A 线概率基础素材，不满足 OB2/SBP-01 的论文公式/推理交付。它验证概率合同，不能把 token firing 次数当作概率，不能据此宣称掌握 SBPN 的完整 RCA，也不能直接迁移到 SBTPN。原文门语义若有附加条件，保留一般概率练习并单列映射缺口。

运行统一由主控完成。示例命令规格如下；脚本须在执行阶段先实现：

```sh
DUAL_RUN="/mnt/d/alkaid/desktop/zancun/10_双线材料加工与研究准备_2026-09-20"
python3 "$DUAL_RUN/tools/capture.py" --out "$DUAL_RUN/runs/D1-edgeim-001" -- python3 "$DUAL_RUN/B/papers/edgeim/demo.py" --fixture "$DUAL_RUN/B/papers/edgeim/fixtures/frequency_pair.json"
python3 "$DUAL_RUN/tools/capture.py" --out "$DUAL_RUN/runs/D1-edgeim-tests-001" -- python3 "$DUAL_RUN/tools/run_checks.py" --package "$DUAL_RUN/B/papers/edgeim" --require EI-01
```

`capture.py` 用子进程直接捕获 stdout、stderr、真实退出码和参数，保留原始字节；不得由模型补写“成功日志”。run_checks.py 必须核对施工卡要求的命名检查实际发现并执行，零测试、跳过必验用例或预期失败未触发均返回非零。代码尚属执行阶段待建，当前不能照此声称已经测试。其余两个实例使用相同协议和独立 run ID。重跑递增编号，不覆盖旧运行。

**七、基础训练嵌入具体任务**

| 能力 | 两天本人练什么 | AI 先准备什么 | 错误诊断与通过条件 |
|---|---|---|---|
| 规格、接口、模块 | 为 E01 写输入、输出、异常；分开读取、计算、展示 | 下一批论文的类型及接口对照 | 若“代码能跑但输出含义不清”，先补规格；本人能解释模块职责并替换 fixture |
| 测试、调试、Git | 修复一次重复计数错误，先保留失败测试，再查看 diff | 最小复现、调试提示及安全的临时练习目录 | 需展示失败→定位→修复→回归；不要求提交或推送 |
| 数据结构、算法 | 用集合/字典统计，用图表达等待关系 | SCC、值流、MHP 的后续材料 | 能说明为何选该结构、时间/空间成本及一个反例；两天不要求背完整算法课 |
| 系统语义 | 区分 acquire、request、blocked、release；区分观察不到与没有发生 | 并发、内存、分布式/通信前提 | 能指出改变哪条假设会使死锁或过滤结论失效 |
| 数学统计 | 演算 E03，解释条件概率、独立性与联合分布 | RCA 推导、置信区间和误差分析 | 不混淆频次、概率、因果；能构造同边际不同联合的例子 |
| ML 实验 | 本轮只在EX-05不受阻且有余量时做一次数值前向；不挤占当前站 | AI把ML-01保留在未来队列，暂不建整套训练脚手架 | 延期不是删除；后台未来训练仍不计本人掌握 |

工程复现贯穿三个实例：本人应能根据 README 从固定输入得到相同确定性结果，并解释环境、参数与随机性。超出两天的数学、系统和 ML 内容进入有前置关系的课程队列，不作为当前开工门禁。

**八、孙猛与 OpenAI 方向证据刷新**

`TR1→OR1` 先读取 I05，纠正零起点判断，再做增量。时间窗以实际启动日期回溯 3 年为主、5 年补充；本轮输入日期对应的参考范围为 2023–2026、补充至 2021，不把启动日之后的内容当现有事实。

孙猛刷新顺序为：确认姓名及机构身份→官方主页/项目页→论文出版社或作者稿→软件仓库与版本→学生题目、报告和公开课程。Meng Sun 与 Jun Sun 分别建身份键。每条记录含题名、日期、角色、来源、原文支持句、方向标签、可信范围及最后核验时间。

| 方向分类 | 可支持的判断 | 不可直接推出的判断 |
|---|---|---|
| 智能体安全、形式化验证 | 某项目或论文明确研究的对象、性质和方法 | 所有在研工作均属 Agent 安全 |
| 余代数及系统语义 | 可复用的语义、组合或证明问题 | 与当前日志方法已建立迁移 |
| 企业合作、工具与学生题目 | 有公开证据的合作范围、实现或训练入口 | 未公开部署效果、持续招聘或个人适配 |
| 其他/归属未定 | 保留原文并登记待核 | 强行塞入预定方向 |

OpenAI 方向作为能力参照，转换成可执行动作：

- Oversight/System Mitigations：给定审查预算，设计监控接口、失效案例和覆盖指标。
- Training/Evals：建立数据切分、泄漏检查、强基线、误差分层和可复跑评价。
- CoT Monitorability：区分可见输出与内部过程，检查不可见信息假设及监控失效；不假定能取得私有思维链。
- Interpretability：要求操作性假设、可干预对象和反事实检验，不能用漂亮解释代替证据。

课程例子存 A，方向事实存 `research/`。TR1 还须给每个方向标 CURRENT_CORE_CANDIDATE / ACTIVE_SECONDARY / LEGACY_REUSABLE / HISTORICAL / UNKNOWN：判断“当前主线”至少同时有时间窗内重复成果与项目/学生/软件/报告中的另一类证据，并标为综合推断；缺材料不判方向衰退。B1组件协议、B2模型抽取、B3Agent评估各交“原成果—可复用机制—语义差异—新增前置—小产物—验收—失败条件”七栏桥接卡；模型抽取的历史工作可以作为方法基础，不自动代表导师当下重点。两天至少交已核增量、检索范围及仍未知项；外部访问失败时保留旧证据的时间边界和待查请求，不输出“近期没有相关研究”。

**九、从论文资产到研究的升级规则**

| 阶段 | 必交内容 | 升级条件与禁止事项 |
|---|---|---|
| 论文→机制资产 | P1/P2、来源坐标、接口、限制 | 无须每篇提出 idea；摘要或类比不能替代机制 |
| 机制→跨论文比较 | 对齐问题、输入信息、保证、成本、失败模式 | 图/Petri 网同形不等于语义相同 |
| 比较→候选 | 可反驳假设、受益场景、预期失效场景、最小验证 | 无机制差异或可测预测的“组合”不升级 |
| 候选→novelty 检查 | 最近邻及差异表，论文/开源/工业/负结果检索记录 | 未查到不等于不存在；窄范围差异不能写成领域首次 |
| novelty→研究 toy | 固定协议、独立真值、强基线、控制变量、资源预算 | 先通过方法检查再运行；toy 正结果不等于新颖性 |
| toy→后续研究 | 重复结果、失败解释、外部有效性和下一实验 | 不以候选数量、toy 数量或正结果验收 |

最近邻检索使用“问题、机制、保证、输入信息、预算”分别检索，并查引用/被引、开源 issue、企业机制说明及失败报告。记录具体检索式、日期、覆盖源和排除理由。企业材料只支持公开描述的机制，宣传指标不自动作为比较真值。

TR2/TR3在这两天只负责建立未来问题、检索和toy就绪门：输入必须来自已核P1或明确缺口，禁止预称创新。只有A线已就绪且PNULock/SBPN机制片段可用时，才把一个候选升级到最近邻初查；否则交`NOVELTY_QUEUE/READINESS_GATE`及下一动作。研究toy不在本轮硬运行，未运行不得算研究闭环。

“固定审查预算下选择轨迹”是待筛选示例，不是已选题。预注册规格至少包括：

- 固定各方法能看到的日志字段、历史范围、可用标签和预算；审查所得信息的使用时机一致。
- 真值由独立生成器的隐藏过程状态或穷举规则提供，不能由被比较方法自身打标签。
- 至少比较同信息同预算的规则/风险评分强基线、可运行的最近邻机制以及随机/分层参照；不确定性模型若未训练或拿到更多信号，不列为公平基线。oracle 仅作有特权上界。
- 协议必须冻结标签生成规则/对象状态机、每种失败族、正常/恶意条件、成本预算K、被测打分函数、参数搜索和退出阈值，不能只给样本数；具体协议见施工卡 RT-01。可按每种子100条开发、200条测试的起步规模，种子固定为 `11、23、37、51、89`；按过程配置分组，防止近重复泄漏。规模调整须在看测试结果前记录。
- 预算取同一可解释单位；若轨迹长度不同，额外报告事件读取及实际运行成本。参数只在开发集选择。
- 报告缺陷召回、漏检类型、成本和跨种子波动；逐项消融，不能只胜随机。
- 设置“可见日志相同、隐藏真值不同”的不可辨识对照。若方法无法区分，应解释信息限制；若优于基线仅来自额外信息，则候选失败。
- 强基线持平/更好、性能依赖单一种子或成本超限时，保留负结果，缩小或放弃候选。

**十、分轴验收矩阵**

`PASS` 只能是对具体检查的结论。事实真实性、代码可用性、方法正确性、教学就绪、本人能力、研究有效性分别记录，不能互相替代。

| 验收 ID / 对象 | 可执行操作及预期 | 证据位置 | 失败处置与状态上限 |
|---|---|---|---|
| AC00 写域、版本、并发 | 启动/收尾记录 Git HEAD、status；检查本任务每个写入路径都在新目录；核对活动模型数≤2 | `00_control/`、manifest | 越界立即停止该写入；其他窗口差异不归因给本任务 |
| AC01 身份与事实 | 逐项核题录及关键来源句，核作者消歧、日期和版本 | 各包 `sources.tsv` | 无一手支持标 UNKNOWN/旧证据；不作当前事实 |
| AC02 全文与主要主张 | 从摘要、贡献段、正文和结论建立主张全集；逐条对应原文、支持类型、分析与缺口 | `claims.tsv`、coverage | 未覆盖的主要主张不能因“抽查三条通过”消失；未完整则最高 P1 |
| AC03 语义和推导 | 核类型、单位、量词、前提；执行 E01–E03 的反例或手算，对照原文映射 | 机制说明、fixture、复核单 | 映射失败保留教学例，不称论文算法；错误理论段禁止发布为就绪 |
| AC04 代码可用 | 按 README 在干净输入副本运行 demo 和测试，保留真实退出码、stdout、stderr、环境 | `runs/<id>/` | 未运行为 UNRUN；依赖缺失为 BLOCKED；自填 PASS JSON 不算证据 |
| AC05 方法正确 | 将结果与手工真值/独立枚举核对；检查是否多看信息、偷换预算、遗漏边界 | oracle 说明、结果差分 | 单测通过不替代算法验证；论文参数未知不得标原实现 |
| AC06 教学就绪 | 执行者照 README 从入口完成公开例；检查练习依赖、术语、错误提示及答案分离 | `A/`、试走记录 | 必需步骤缺失则返工；运行材料未就绪时提供明确纸面分支 |
| AC07 本人能力 | 新 rubric 先冻结；本人无提示解释、修改新变体、诊断错误；保留原始作答 | 新模块评价记录 | AI 代写、题目暴露或 rubric 未冻结只算练习；不修改旧 R 状态 |
| AC08 学习安排 | 课表净学习计划合计260分钟，实际时长和产物如实登记；按前置分流 | 学习记录 | 时长不是能力PASS标准；少于目标调整课表，不惩罚或虚填，不用AI工时补足 |
| AC09 研究有效性 | 检查最近邻、信息合同、独立真值、强基线、切分、种子及失败条件 | `research/` | 缺项停在候选/协议草案；正结果不能绕过 novelty |
| AC10 交接可复用 | 复核者从 manifest 找到来源、命令、限制与下一步；校验文件和输入哈希 | `manifest.json`、`OH1` | 路径断裂、哈希变化或证据缺失则退回对应包，不全仓清零 |

独立复核由 W 在新的自包含上下文完成，检查全部阻断性问题及主要贡献清单，并抽查其他事实的出处。复核者读取实际代码和原始日志，不只看作者总结；发现关键错误后扩大到同类主张。独立模型复核不等于独立实验真值。

**十一、预算、失败分支与交付层级**

Gemini额度由用户报告为充足，但可见余额和真实吞吐仍须在执行回执中记录；额度多不提高其事实权限或包大小。其他模型token余额、外部数据库权限及可用 API 预算仍为 **UNKNOWN**。本合同是可审阅安排，不把它解释为新增付费权限；首包记录实际耗时和可见用量。默认使用已有本地环境和标准库；两天不安装大型框架、容器，不训练大模型，不开启付费批处理，不提交或推送。

排程参考为两天 **20–24 小时可用AI编排窗口**（估计，不是已测吞吐或自动运行承诺）：任务看板总计1340分钟（22小时20分钟）注意力预算；A及共同施工540分钟、复核135分钟、B及后续准备615分钟、控制/交接50分钟。W可在M制作A包时独立回源，但M不得为了归档B结果延迟下一学习块。本人学习另为两天各260分钟、共520分钟，不计入AI预算。

共18个有边界任务，见 [任务看板](03_两天任务看板.tsv)。这是任务数量，不是API请求次数。每个普通任务一轮首交、最多两次定点修复；Gemini微包最多一次定点修正且不续长上下文。同一阻断问题两次未解决，拆小或换独立任务并记录原因。每次来源定位25分钟检查一次替代入口，机制片段90分钟提交中间证据；这不是把复杂原文强行压成90分钟结案。模型限流最多一次延迟重试；Gemini烟测或微包失败则直接使用获准的Sol/Luna替代或转本地确定性任务，不偷偷升级到Astra批量生产。

| 层级 | 两天交付 |
|---|---|
| 必须交 | A0–A7近期学习包与真实试走；11篇输入/旧资产P0队列；EdgeIM→EX-05映射；PNULock、SBPN各一个P1机制片段；A/B分轴验收和交接 |
| 目标交 | 其他论文按优先级完成P1；PNULock/SBPN向P2推进；孙猛证据增量；未来novelty/toy就绪门。目标项不得延迟A线 |
| 延期但不能消失 | 其余P1/P2/P3、完整方向刷新、ML训练、完整novelty证据、toy真实运行、后续基础课和到期冷测；每项有输入/输出/前置/下一批顺序 |

若时间/额度不足，降级顺序：先停TR1–TR3和非优先论文P1，再停PNULock/SBPN的P2升级；保持TA1–TA4、TV1和两个P1片段。仍不足时先保证A线，B线如实记PARTIAL。质量定义不降级，延期不等于用户放弃。

单篇来源缺失时，可将B时段换给另一篇来源齐备的已确认鲁侧论文，但原篇保留BLOCKED和恢复动作。无法完成PNULock与SBPN两个P1片段时，B线标PARTIAL；不得用普通CS例子降标。B线失败不自动抹掉已验收的A线材料，反之亦然。

输入文件发生并行修改时，比较读取前后哈希；不一致则重新取得稳定版本或绑定已保存的输入快照。输出重名、写域冲突、额度限流时停止受影响包，保存来源坐标、最后验证步骤和下一命令；不覆盖其他窗口内容。发现旧合同冲突记录原文、作用范围和可继续任务，不扩大成全局停工。

交接 `OH1` 必须列明：已实际完成与未完成、各包最高层级、所有运行 ID、未解决错误、本人学习记录、后续依赖及恢复动作。manifest 校验只证明文件完整和对应关系，不能自证内容正确。

**十二、开工检查表**

1. 填写 D1/D2、实际时区及每日 260 分钟学习窗口。
2. 确认本轮进入执行范围；本规划文本本身不视为开工回执。
3. 核对活动池：GPT/Codex=1、Claude≤3、Gemini≤4、Claude+Gemini≤6、总活动≤7；每个worker有唯一任务ID和独立输出。
4. 记录仓库实际 HEAD、工作区背景状态和禁止写入范围。
5. 建立新写域、任务账本、来源版本和断点记录。
6. 定位 I01–I06，核实各论文 PDF/旧拆解；缺失项单独登记。
7. 先准备A0–A5并通过IM-Q01–IM-Q06，再派发不会阻塞A的长篇回源任务。
8. 检查参考答案、本人作答及未释放材料的隔离；保留旧 rubric 状态。
9. 确认运行捕获、可用 Python、资源余额 UNKNOWN 及逐包止损规则。
10. 所有状态从 PLANNED 开始，只有附实际证据后才升级。

Gemini微包只有在第1–7项完成、A线下一块已就绪后才可排队；先执行一次合法快照烟测，失败即走Sol/Luna分支，不把入口诊断变成主线工作。

**可复制给 A 线主控的提示词**

> 你是唯一GPT/Codex主控与唯一写入者；最高优先级是按`06_近期学习优先执行表.md`在每个学习块前交付A0–A7。当前游标是R1最小缺口＋EX-05继续：R1最多占本人D1前30分钟，不重学EX-01；对`test_invariants.py`、`own.py`、空`takeover.md`只做状态对账、worksheet、定点基础和安全预检，不修改或代写。EX-06前置未闭合时保持LOCKED。可并行使用Claude≤3、Gemini≤4，且worker总数≤6；worker全部只读、独立任务、一次终态返回。B返回内容只能在A下一块已就绪后归档。每项材料需通过IM-Q01–IM-Q08，不能把AI READY、历史QA或代码运行写成本人PASS。不安装、付费批量运行、提交或推送。

**可复制给 B 线只读执行者的提示词**

> 你承担一个B线只读包，不得阻塞或索取A线主控即时响应。先做11篇P0队列；深加工只优先PNULock、SBPN各一个P1机制片段。其他论文返回P0差分、P1所需输入和下一动作，不冒称已加工。派发必须附论文名、源路径、版本、旧拆解、单一问题和时限；未提供的事实写UNKNOWN。不得修改文件、执行状态变更命令、派生代理或声称运行成功。仅返回一次complete/partial/blocked，附准确file:line、PDF页码、符号名、关键原文、未覆盖范围和下一动作；普通等待图/AND-OR例不能替代论文机制。

**Gemini微包附加约束**

> 只处理任务单内提供的快照与一个问题；不读仓、不联网、不调用工具、不续接旧会话。逐条输出`claim_id/field/value/source_id/location/quote/uncertainty`，缺项写`null/UNKNOWN`。不得归纳方向、判断机制正确或新颖、宣布完成或生成仓库改动。首行只能是DRAFT/PARTIAL/BLOCKED；20分钟到点交断点。主控将逐字段回源，任何无出处事实不会进入交付。

# A3 EX-05 预测→运行→观察→解释

状态：`BLANK-WORKSHEET / USER-MUST-PREDICT / NO-EXPECTED-NUMBERS`

## 0. 本次运行身份

```text
日期/时间：
正式交件：
探索稿：
Python 路径/版本：
源文件哈希：
运行副本路径：
```

## 1. 三种顺序

运行前必须先填“预测”。运行后把终端实际输出定位到 run ID，不抄 AI 的预期数字。

| 顺序 | 预测：不变量是否保持，为什么 | 命令/run ID | 观察：实际看到什么 | 解释：预测与观察如何对应 |
|---|---|---|---|---|
| `candidate_index` 正序 |  |  |  |  |
| 逆序 |  |  |  |  |
| 固定 seed 随机顺序 |  |  |  |  |

另外分开写：

```text
预测 p1：
观察 p1：
对错与原因：

预测 p2：
观察 p2：
对错与原因：

预测 p3：
观察 p3：
对错与原因：
```

## 2. `build_universe` 接管

### 2.1 先凭记忆，后查代码

```text
我认为 universe 在内存中是：
输入：
输出：
状态/字段类型：
哪个信息是独立裁判：

查代码后需要修正的地方：
代码行锚：
```

### 2.2 改一个条件，只在临时副本

```text
我要改的单一条件：
改之前的预测：
对 |D'| 的预测：
对标签分布的预测：
实际命令/run ID：
实际观察：
若报错，traceback 第一个相关代码行：
我认为这只支持哪个解释：
还存在的竞争解释：
临时副本是否丢弃：
原文件哈希是否未变：
```

## 3. `classify_trace` 接管

```text
先凭记忆：它认为“真流程”由什么表示：
它使用哪几类判定：
我准备改掉的单一判定：
改之前的预测：
实际命令/run ID：
实际观察：
复原状态/原文件哈希：
这个裁判和 Algorithm 1 看到的信息有哪些重合：
它们各自看不到什么：
```

## 4. 证据类型与上限

| 证据 | 定理/证明 | 反例 | 有限测试 | 实现验证 | 当前最远能说到哪 |
|---|---|---|---|---|---|
| 三种顺序的运行 |  |  |  |  |  |
| 生成器条件变体 |  |  |  |  |  |
| 裁判条件变体 |  |  |  |  |  |

一次全绿不能写成“对所有日志已证明”。一次 traceback 也不能自动写成根因。

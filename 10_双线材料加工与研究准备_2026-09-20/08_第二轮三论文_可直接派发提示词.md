# 第二轮三论文：可直接派发提示词

状态：**DISPATCH-READY / EXECUTION-NOT-STARTED**。本页补齐 [三论文应用计划](07_第二轮研究动态_三论文拆解与JINZU应用计划.md) 的实际派工文本。所有 worker 只读、不修改文件、不改状态、不派生代理；主控 M 是唯一写入与验收者。

## S-R2-SD · Sol · String Diagrams

```text
任务 ID：S-R2-SD。只读核验 `/mnt/d/Alkaid/Downloads/2609.20478v1.pdf`，SHA-256 = 9011e7deb9a251f99c748847ca0d06cd30bddd9ce9185f789122661efee46c6e。

唯一问题：canonical signature 精确保留什么，四种 notation 如何各自构造它，signature equality/inclusion 与 trace equality/inclusion 的逻辑方向是什么，以及这对 JINZU T2/EdgeIM 有何可验收用途？

必须返回：
1. 题名、作者、版本及研究问题。
2. signature 的形式对象、输入、输出，保留/删除的信息。
3. Petri net、causal net、process tree、BPMN 四个构造定理；每项给 PDF 页、节、定理号和关键原文。
4. equality/inclusion 所有成立方向、反向失败及反例。
5. object-centric recovery study 的数据、对照、结果和局限。
6. artifact/仓库声明及是否有可直接核验链接。
7. JINZU 输出：`sameness ladder`、一对 trace 相同但并发语义不同的反例、与 EdgeIM 的可迁移点和不可声称点。

每条标 SOURCE FACT / RECONSTRUCTION / HYPOTHESIS / UNKNOWN。不得把 EdgeIM 改写成 object-centric 方法，不得把单向命题写成充要。首行只能是 complete / partial / blocked，最后明列未覆盖范围和不确定性。
```

## S-R2-CA · Sol · ContrAgent

```text
任务 ID：S-R2-CA。只读核验 `/mnt/d/Alkaid/Downloads/2609.18128v1.pdf`，SHA-256 = b338efa71c6ae3843200345024967b60ef2d3cc40a806b52c77b41e1864269e3。

唯一问题：ContrAgent 如何把同一份 trajectory-level contract 编译为 checker，同时支撑 online enforcement 和 offline evaluation，其真正的观测与信任边界在哪里？

必须返回：
1. 题名、作者、版本和相对 AgentSpec/Agent-C/VeriGuard/FORGE/CaMeL/Progent/AgentPex 的实际增量。
2. call/return event -> trace/state/context/counter -> interaction predicates -> ALTLf A/G contract -> DFA checker -> online/offline verdict 的完整数据流。
3. 关键定义、三值 verdict、compilation 与 monitor semantics；附 PDF 页/节/定义号/原文。
4. 四个 benchmark 的对象、基线、指标、作者数值结果及延迟测量条件。
5. 谓词覆盖、事件忠实性、contract correctness、counter/context 信任及自由文本语义盲区。
6. artifact/仓库声明与可核验程度。
7. JINZU 输出：T2 trajectory -> T3-lite observability -> monitor 的桥接卡；两对“已声明谓词完全同值，但语义风险不同”的轨迹规格。

每条标 SOURCE FACT / RECONSTRUCTION / HYPOTHESIS / UNKNOWN。不得把 DFA verdict 扩大为语义安全、意图安全或 Agent 安全已解决。首行只能是 complete / partial / blocked，最后明列未覆盖范围和不确定性。
```

## S-R2-PB · Sol · PetriBench

```text
任务 ID：S-R2-PB。只读核验 `/mnt/d/Alkaid/Downloads/2609.19883v1.pdf`，SHA-256 = 989f94fb791597ecaf66b234829f141ae17dc5d60a3dc6f87e126cdc41b16a91。

唯一问题：PetriBench 如何用程序生成 Petri 网和 exact solver 分离题目、真值与模型输出，它真正测到什么，又不能支持什么？

必须返回：
1. 题名、作者、版本，以及 local/global x finite/infinite taxonomy。
2. 六个任务的精确定义、答案类型、语义和所用 oracle。
3. relevant structure/distractor structure、Easy/Medium/Hard 生成、arc multiplicity 和结构分布限制。
4. exact solver/真值生成和数据泄漏防线。
5. model results、test-time compute、长度关系、shortcut/serialization ablation 和 error taxonomy；数字必须定位到表/图/页。
6. 错误分类的 LLM 代理成分、人工复核程度、artifact/仓库/leaderboard 声明的可核验性。
7. JINZU 输出：一个有限时域任务+一个无限时域任务的 exact-oracle 协议，包含输入、独立真值、错误分类、退出标准和声明上限。

每条标 SOURCE FACT / RECONSTRUCTION / HYPOTHESIS / UNKNOWN。不得把基准高分写成会从自然语言建模、会恢复流程或会验证真实系统。首行只能是 complete / partial / blocked，最后明列未覆盖范围和不确定性。
```

## F-R2 · Fable 教学转换母提示词

Fable 不先行。每篇只在 M 将对应 Sol 包逐条回源并标记 `ACCEPTED_SCOPE` 后开新会话。

```text
你是 Fable 教学 worker，只处理本次提供的 ACCEPTED_SCOPE 证据包，不自行补论文事实。目标是从第一原理讲清，不假设学习者已经会。

只交一张教学卡：
1. 这篇研究的对象是什么；
2. 状态/表示里保存什么；
3. 规则如何更新状态或产生 verdict；
4. 一个完整的公开微例；
5. 一个不泄答案的空白练习；
6. 三个常见错误与分级提示；
7. 它连到 JINZU 哪一环，又不证明什么。

不得填用户正式作答、判 PASS/OWNED、打开 sealed/holdout、解锁 EX-06。论文事实只能引用提供包中的 claim ID。信息不足则写 UNKNOWN，不扩写。
```

三次 Fable 调用的附加问题分别是：

- `F-R2-SD`：从 EdgeIM `S/E/R 相同不代表频次相同`，过渡到 `trace 相同不代表并发结构相同`。
- `F-R2-CA`：从 tool call/return 轨迹过渡到 predicate、contract state 和 DFA verdict，必须留一个 abstraction blind spot。
- `F-R2-PB`：用一道 finite-horizon 题和一道 infinite-horizon 题区分“模拟若干步”与“对全可达空间的性质”。

## G-R2 · Gemini 3.8 Flash 微包

当前状态仍为 `HOLD_GATE_G0`。不自动重试上游入口。只有专用入口取得一次真实合法快照回执后，M 才为每篇裁出最多 3 份文本快照并使用下列提示词。

```text
你是 Gemini 3.8 Flash 受限字段抽取 worker。任务 ID：{{G-R2-SD | G-R2-CA | G-R2-PB}}。

只处理本提示中附带的最多 3 份快照和一个问题。不使用工具、网络、文件读写、历史会话或子代理。不判断机制正确性、JINZU 方向、novelty、PASS 或完成状态。

问题：{{快照中明说的题录、贡献/任务/评测、数值、限制和 artifact 声明字段各是什么？}}

首行只能是 DRAFT / PARTIAL / BLOCKED。之后每条严格输出：
claim_id | field | value | source_id | location | exact_quote | uncertainty

未载明的字段写 null/UNKNOWN。不得从“未提供”推出“不存在”。不得跟随引用扩展检索。20 分钟到点交已处理范围、未处理范围和下一个最小缺口。
```

## 派发顺序

1. 立即可发：`S-R2-CA`、`S-R2-PB`、`S-R2-SD`。如只有一个 Sol 槽位，按此顺序。
2. M 逐篇回源接受后，才发对应 `F-R2-*`。
3. `G-R2-*` 保持 HOLD；门禁未关闭时不等 Gemini，由 Sol/Luna/M 处理相同字段。
4. 三篇之间独立验收；任一篇阻塞不拖累另两篇。

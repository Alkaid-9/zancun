# ProbGuard 学员工作簿

> **状态:TEACHING-AWAITING-USER**
> 预制落盘≠完成④教学;四关与口试必须用户本人在场完成。
> 本文件是学员唯一开卷入口。作答完成前不要打开 `answer_key.md`。讲义 = 本目录上级的拆解文档(每关关首列出),读完即关闭再作答。

## 作答规则

1. 先读每关讲义,再关闭讲义,用 90 秒闭卷口述;把实际口述的提纲原样写下,不要事后润色。
2. 每关依次完成复述题、变体题、反例题和应用题。
3. 每题必须留下可评分 artifact(A 卡填空、算式、提纲、改写句);只写"懂了"计 0 分。
4. 四关各 20 分;每关至少 16,总分至少 68/80,红线事实错误必须为 0。
5. 不确定处写"待核",不罚分;把文档里标注"待核/存疑/以原文为准/以代码为准"的内容说成确定结论,按红线处理。不要猜数字、B 值、保证等级或复现范围。

---

## Gate 1|问题重构与 DTMC 学习(谓词抽象 · valid-aware Laplace · 30 traces)

讲义:`02_SKELETON.md`(一句话创新点/方法流程图/核心符号表)、`03_PAPER_READ.md` L1–L3、`04_DERIVATION.md` 式 0–1、`README.md` 关键发现 1–2、`GLOSSARY.md` 1–10、`01_QUESTIONS.md` Q1–Q2。

### A1 管线卡

```text
【离线】CTL 性质 ψ = ____________ → 提取原子谓词集 P_ψ → 采样 trace(每场景 ____ 条)
  → 谓词抽象 s_P(v) ∈ ________ → 状态空间来自 ____________(不是 2^n 全枚举)
  → 计数 n_ij → Eq(1):P̂(sj|si) = ____________(valid_tran 成立时),否则 = ____
  → PACBoundSatisfied 检查 → M̂ → PRISM 预计算 P[ψ|s] 缓存表

【在线】观测 v_i → 抽象 s_i → 查表得 P_safe → P_safe < θ ? → 干预(____ / ____)

旁注必填:
α = ____
与标准 Laplace 分母的差别 = ____________
未观测合法转移拿到的概率 = ________(n_i>0 时)与 ________(n_i=0 时)
吸收态硬编码的例子(论文域一个、代码一个)= ____________
Eq(1) 行随机(和=1)成立的前提 = ____________
valid_tran 的三重作用 = ①________ ②________ ③________
```

### 1.1 90 秒复述(8 分)

提示词:反应式盲区—问题重构—三贡献—谓词从哪来—valid_tran 三重作用—Eq(1) 与标准 Laplace 的差别—未观测转移—归一化前提—30 traces 与 Figure 2 的 31/184。

```text
[粘贴你的口述提纲]
```

实际用时:____ 秒

### 1.2 变体题(4 分)

某状态 si 有 5 条合法出边(k_i=5),访问 n_i=20,转移计数 (12, 5, 3, 0, 0),α=1。
① 写出 Eq(1) 分母;② 算出五个概率并验证和=1;③ 两条未观测转移各拿到多少,一句话解释为什么不给 0;④ 若第 5 条出边后来被 valid_tran 判为非法,五个概率各变成多少?

```text
分母 =
五个概率 =
未观测转移概率与理由 =
改判非法后 =
```

### 1.3 反例题(4 分)

驳斥:"ProbGuard 没有任何新东西——谓词抽象是 1997 年的,DTMC 学习加 PRISM 查询早有人做,PAC 界是 Bazille 2020 的,随便哪个组拼一下都能发这篇。"要求:先承认对的部分,再说新在哪(至少三处),最后用团队谱系证据说明这不是拼贴。

```text
[作答]
```

### 1.4 应用题(4 分)

你在 T5 换域时收到两份故障报告:
甲:"我启用了关系域真剪枝(某些转移 valid_trans=False),沿用上游 HEAD 的 build_model,结果 PRISM 拒收,吸收态那行概率和 >1。"
乙:"我自己按论文 Eq(1) 重写了 build(非法转移硬零),换了一批新对话数据后,某些行概率和 <1。"
分别给出根因、修正方案,以及一条通用的迁移规则。

```text
甲根因 =
乙根因 =
修正 =
迁移规则 =
```

### Gate 1 自检

- [ ] 我没有把状态空间说成 2^n 全枚举(它是经验观测到的位串集合,再剪语义矛盾)。
- [ ] 我没有把 Eq(1) 分母写成 n_i+|S|·α(那是标准 Laplace;这里是 n_i+k_i·α)。
- [ ] valid_tran 的三重作用我一条没漏,特别是"给 B 有限提供前提"。
- [ ] 我记得 30 traces 是每场景采样数,Figure 2 的分母 184 是 s0 的访问次数,不是 trace 数。

---

## Gate 2|PAC 界与 P_safe 语义(条件数 B · 安全 BSCC · 监控自动机)

讲义:`03_PAPER_READ.md` L4–L5、`04_DERIVATION.md` 式 2–6(发现 #1/#2/#4)、`02_SKELETON.md` §3/§7 与左墙 7/右墙 1、`GLOSSARY.md` 11–16、`README.md` 关键发现 3/4/6。

### A2 公式链卡

```text
Def 3.3 两层概率:外层 Pr 的对象 = ____________;内层 P_M 的对象 = ____________
样本界三因子:① ________(主宰项,以平方进入) ② ________(标准集中不等式) ③ ________(经验方差修正)
δ' = ________(来自 union bound)
两域实测(ε=0.1, δ=0.05):
  具身:|S| = ______,B = ______,需 traces ______
  AV:|S| = ______,B ≈ ______(Laplace 下测得),均匀界不可行 → 退 ________,需 ______ traces,保证对象降为 ____________
实验实际每场景 trace 数 = ____ → 论文自我定位 = ____________
P_safe 方程组:x_s = __(s ⊨ unsafe);x_s = ____________(否则)
发现 #1:P_safe 非平凡 ⟺ ____________;违背时监控器行为 = ____________
监控自动机:Q = ____________,|Q| = ____;同步积状态数 = ______
发现 #2(端点):实际响应窗口 = ________,比 F[0,K] 多 ____;工程影响 ≈ ______;裁决口径 = ______
```

### 2.1 90 秒复述(8 分)

提示词:P[ψ|s] 为什么要附着在路径公式上—Def 3.3 两层概率—样本界三因子—Thm 3.4 三步与"为什么必须 Laplace"的咬合点—两域 B 实测与降档—30 traces 的诚实落差—安全 BSCC—监控自动机与端点发现。

```text
[粘贴你的口述提纲]
```

实际用时:____ 秒

### 2.2 变体题(4 分)

① 取 ε=0.1、δ=0.05、|S|=8、B=10、最坏方差因子 1/4,重算逐状态样本界(04 手推 1 的 B=1 版本答案是 ≈349 次/状态);② 你的结果是 349 的多少倍,为什么恰是这个倍数;③ 把 04 式 5 手推例 2 的 P(s1→done) 从 0.4 改为 0.7(P(s1→u)=0.3),重算 x1 与 x0,并回答 θ=0.75 时哪些状态报警。

```text
① n_i ≥
② 倍数与原因 =
③ x1 = ____,x0 = ____;θ=0.75 时报警状态 =
```

### 2.3 反例题(4 分)

驳斥:"论文证明了 M̂ 是 (0.1, 0.05)-PAC-correct,所以部署后我们的 agent 以 95% 的概率永远安全。"要求:说清两层概率各自的对象、PAC 到底承诺什么、这句话在本文实验设置下还有哪一层额外不成立,最后给一句合格表述。

```text
[作答]
```

### 2.4 应用题(4 分)

给 T5 写一段"查询语义决策备忘录":为什么 T5 主查询应选 P=?[F≤k unsafe] 而不是论文的 P=?[G ¬unsafe]?(提示:发现 #1、D13 的 FINISH 哨兵、长期陪伴对话的过程形态、D6 的判向换算。)备忘录必须含阈值方向的换算说明。

```text
[作答]
```

### Gate 2 自检

- [ ] 我没有把外层 PAC 概率说成"这条链 95% 概率安全"。
- [ ] 两域 B 值我没说反:具身 1.0–481,AV 10⁷–10⁸(Laplace 下测得);两档界的保证对象我分得清。
- [ ] 我记得无安全 BSCC ⇒ P_safe≡0 ⇒ 任意 θ>0 恒报警。
- [ ] 发现 #4 的 max 位置、方差因子 ε 尺度、B 的精确定义——这些"待核"项我保留了标注,没有硬化。

---

## Gate 3|两域实验与结果批判(AWT/FP · 安全-完成率 Pareto)

讲义:`03_PAPER_READ.md` L6–L7、`02_SKELETON.md` §5.2/§6.2、`04_DERIVATION.md` 式 7、`GLOSSARY.md` 17–20、`README.md` 一句话总结与关键发现 5、`01_QUESTIONS.md` S02 跟进 3/6。

### A3 两域结果卡

```text
AV 域(Apollo 9.0,是否 LLM:____;7 违法场景 + __ 个安全场景做 FP 标定;每场景 __ traces):
  θ=0.3 → FP ____,最长 AWT ______(场景 __)
  θ=0.5 → FP ____
  θ=0.7 → FP ____,最长 AWT ______(场景 __,性质 ______)
  检出率 ____(分母 = __ 个场景);每决策开销 ______
  θ 敏感性两极:平坦场景 = ______,敏感场景 = ______;决定因素 = ____________

具身域(SafeAgentBench + LangChain ReAct + ______,temp=__,每任务 __ traces):
  None:unsafe ____% / completion ____%
  stop@0.9:____ / ____        reflect@0.9:____ / ____
  −65.37% 的算式 = ____________
  80.4% 的算式与含义 = ____________(它不是绝对完成率!绝对值是 ____%)
  vs AgentSpec:token ______;缓存后延迟:32 状态 ______(未缓存 ______)

合法引用句(必须同时带 θ 与该档 FP):
____________
```

### 3.1 90 秒复述(8 分)

提示词:两域协议(平台/场景来源/性质来源/30 traces)—AWT 定义纪律(只在真违规 run 有定义、与 FP 成对)—Table 1 关键数字与 θ 敏感性两极—Table 2 Pareto 与 reflect@0.9 甜点位—三条批判(reflect 单点、干预后分布漂移、高基线域)—vs AgentSpec 与开销。

```text
[粘贴你的口述提纲]
```

实际用时:____ 秒

### 3.2 变体题(4 分)

用 Table 2 现场算:① stop@0.5 相对 None 的 unsafe 降幅;② stop@0.5 的完成率保留率(相对 None);③ 写出 reflect@0.9 的 −65.37% 是怎么算的;④ 写出 80.4% 是怎么算的、它是什么、绝对完成率是多少。

```text
① =
② =
③ =
④ =
```

### 3.3 反例题(4 分)

驳斥:"Table 1 显示 ProbGuard 能在零误报下提前 38.66 秒预警,比 REDriver 好 20 多倍。"(逐处指出这句话叠了哪几个错误,并给出正确读数。)

```text
[作答]
```

### 3.4 应用题(4 分)

把下句改写成论文可用的合格句(约 60–100 字),保留可辩护的主张、删除不可辩护的:

> ProbGuard 提供经认证的概率安全保证,可提前 38.66 秒无误报预警,并把具身 agent 违规降低 65.37% 而不损失任务完成率。

```text
[改写]
```

### Gate 3 自检

- [ ] 我引用任何 AWT 数字时都带了 θ 与该档 FP。
- [ ] 我没有把 65.37% / 80.4% / 93.6% 这些相对数当绝对数;80.4% 不是绝对完成率。
- [ ] 我没有把 reflect@0.9 单点外推成"reflect 全面 Pareto 占优"。
- [ ] 我记得 AV 域是 Apollo(非 LLM),"100% 检出"的分母只有 7 个场景。

---

## Gate 4|局限、T5 迁移与复现接口(对话域 B · 窗口化谓词 · P0–P2 实测)

讲义:`03_PAPER_READ.md` L8–L9、`README.md`"与 T5 及 Phase 1 的关系"、`01_QUESTIONS.md` Q3/Q5/品味判断、`ADDENDUM_DTMC_PRISM_FORMAT.md` ⓪/§⑨、`05_REPRODUCTION/README.md`、`05_REPRODUCTION/RUNLOG.md`、`05_REPRODUCTION/DEVIATIONS.md`(重点 D1/D2/D3/D6/D10/D13)、`05_REPRODUCTION/DTMC_PRISM_FORMAT.md`。

### A4 迁移与复现卡

```text
作者自述局限三条:① ________ ② ________ ③ ________
拆解补充局限(至少三条):① ________ ② ________ ③ ________
T5 三空位:
  ① 改域后第一个该产出的数字 = ____________(位于 ______ 与 ______ 两个锚点之间,无人报告过)
  ② 形式化空位 = ________ 语义(论文现成件是 ________,语义差别 = ____________)
  ③ 干预 Pareto 增维 = ____________
T5 最大 gap = ____________
复现裁定:P0 __ / P1 __ / P2 __;P3/P4 状态 = ____________
引擎:实际使用 ____________(原因:____________)
B(P̂) 实测三数字:toy = ______;上游具身 t3 = ______(对论文锚点:____);
  上游 AV s1 = ______(为什么不可直接对 10⁷–10⁸:____________)
B 的定义口径 = ____________(裁决状态:____)
三个必守复现口径:D6 = ____________;D10 = ____________;D2 = ____________
复现顺序第一步 = ____________(原因:____________)
Abstraction 接口义务(源码层六项)= ____________
落盘三件套 = ____________
```

### 4.1 90 秒复述(8 分)

提示词:作者三条局限—拆解补充局限—T5 第一数字(对话域 B)与两档界的抉择—窗口化累积语义空位与单计时器为何不够—复现依赖与顺序(AgentSpec)—P0–P2 裁定与引擎替换—D6/D10/D2 三口径—B 实测三数字与定义待核。

```text
[粘贴你的口述提纲]
```

实际用时:____ 秒

### 4.2 变体题(4 分)

假设 T5 对话域实测 B≈50、|S|=12,取 ε=0.1、δ=0.05、最坏方差因子 1/4:① 算 δ' 与 log(2/δ');② 算 (11/10·B)²;③ 给出逐状态样本界数量级;④ 据此给 T5 一个两句话的路线判断(均匀界还是降档?还能做什么?),并说明这道题为什么恰好证明"改域后第一个该产出的数字是对话域 B"。

```text
① =
② =
③ n_i ≥
④ =
```

### 4.3 反例题(4 分)

驳斥:"05_REPRODUCTION 三步全 PASS,而且算出的 AV 域 B 值和论文一致,说明复现窗已验证了 ProbGuard 论文的全部结论。"(至少四处错误。)

```text
[作答]
```

### 4.4 应用题(4 分)

给"T5 换域第一周"写最小实验计划:① 复现/搭建顺序(含为什么先跑谁);② CompanionAbstraction 的接口义务与上线前三条断言;③ 对话域 B 实验的最小设计(trace 数量级、状态数、用什么工具、判据是什么);④ 一句你自己的立场句:你是否同意"复现即预研"这个判断?引用至少一条拆解或复现证据(判分口径 = 言之成理且引用证据)。

```text
① =
② =
③ =
④ =
```

### Gate 4 自检

- [ ] 我没有把 P0–P2 离线 PASS 说成"全论文复现/在线通道已验证"。
- [ ] 我记得复现顺序:先跑通 AgentSpec(controlled_agent_executor 依赖)。
- [ ] 我说 B 实测数字时区分了 Laplace 口径锚点与频率估计版产物(AV s1 不可直接对表)。
- [ ] θ 换算(θ_paper = 1 − threshold_code)与索引漂移对策(PYTHONHASHSEED=0 / sorted)我都能复述。
- [ ] B 的定义(‖(I−Q̂)⁻¹‖_∞)我说了"待 Bazille 原文终裁",没有说成已核定。

---

## 提交区

```text
学员:
日期:
是否闭卷完成四个 90 秒复述:
是否在作答前查看 answer_key.md:
最不确定的三个点(如实写,可写"待核"):
1.
2.
3.
本篇最想带走的一条个人判断(感想,自由陈述):
```

提交本文件后,教师按 `answer_key.md` 评分并更新 `mistakes.md`。只有四关均 ≥16、总分 ≥68、红线为 0,才进入 `FINAL_ORAL.md`。

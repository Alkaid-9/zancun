# EmoAgent 学员工作簿

> **状态：TEACHING-AWAITING-USER**
> 本文件是学员唯一开卷入口。作答完成前不要打开 `answer_key.md`，也不要展开 `08_TEACHING_PACK.md` 中的“标准答案”。

## 作答规则

1. 先读每关讲义，再关闭讲义，用 90 秒口述；把实际口述的提纲原样写下，不要事后润色。
2. 每关依次完成复述题、变体题、反例题和应用题。
3. 每题必须留下可评分 artifact；只写“懂了”计 0 分。
4. 四关各 20 分；每关至少 16，总分至少 68，红线事实错误必须为 0。
5. 不确定处写“待核”，不要猜数字、并发性、保证或提前量。

---

## Gate 1｜EmoEval 协议与统计单位

### A1 协议图

请画/写不超过 12 节点的流程：

```text
[在此填写：CCD → …… → 输出/统计]
```

图旁必填：

```text
每格段数 N =
计数单位 =
共享/聚类结构 =
核心输出数 =
仓库 JSON 总数 =
额外输出来自 =
```

### 1.1 90 秒复述（8 分）

提示词：初始化—初测—八段对话—后测—统计。并列说明论文逐段前后测与冻结代码共享初测的差异,解释 N、非独立性、核心数、仓库总数,以及为什么统计必须从初后测分数重算。

```text
[粘贴你的口述提纲]
```

实际用时：____ 秒

### 1.2 变体题（4 分）

迁移版每格使用 4 个 CCD、每 CCD 6 个话题、2 个独立 seed。共有多少段级输出？聚类/分层单位如何设？

```text
[作答]
```

### 1.3 反例题（4 分）

把同一 CCD 的 8 段当成 8 个独立病人并做普通二项区间，有哪两个错误？最低成本修正是什么？

```text
[作答]
```

### 1.4 应用题（4 分）

审计“公开仓有 709 个可直接复算主实验样本”的报告：

```text
文件总数：
核心数：
额外数及来源：
统计单位：
```

### Gate 1 自检

- [ ] 我没有把段、轮、CCD、真人混为一谈。
- [ ] 我区分了仓库文件总数与核心设计数。
- [ ] 我没有声称同一后测 prompt 能保证偏差完全对消。

---

## Gate 2｜34.4%、R、MCID proxy 与分布

### A2 三层结果卡

```text
层 1｜R：定义、分母、解释边界

层 2｜MCID proxy：适用量表、阈值、解释边界

层 3｜分布：至少四个要报告的统计/图形

合法的 34.4% 引用句：
```

### 2.1 90 秒复述（8 分）

解释 34.4% 的来源、R 为什么松、MCID proxy 补什么、为何仍需全分布，并给一句论文可用表述。

```text
[粘贴你的口述提纲]
```

实际用时：____ 秒

### 2.2 变体题（4 分）

96 段中 42 段分数上升，11 段 PHQ-9 上升至少 5 分。计算两项比例，并写还需报告的两类分布信息。

```text
R =
MCID proxy 命中率 =
分布信息 1 =
分布信息 2 =
```

### 2.3 反例题（4 分）

A 臂 80% 的段都上升 1 分；B 臂 20% 的段上升 8 分，其余不变。只看 R 会如何排序？为什么可能和严重风险排序冲突？

```text
[作答]
```

### 2.4 应用题（4 分）

把下句改成约 50 字的论文合格句：

> EmoAgent 证明至少 34.4% 的心理疾病用户会被聊天机器人伤害。

```text
[改写]
```

### Gate 2 自检

- [ ] 我没有把 R 写成 MCID proxy 命中率。
- [ ] 我没有把 PHQ-9 阈值套到其他量表。
- [ ] 我的 34.4% 句子含模拟、敏化、松判据与禁止真人外推。

---

## Gate 3｜EmoGuard 内外环与公开仓缺陷

### A3 两泳道机制图

```text
运行时内环：

评测后外环：

调用顺序/并发性：

历史 profile 的源码语义：

提前量字段应填：
```

三项公开仓复现缺陷：

```text
1.
2.
3.
```

### 3.1 90 秒复述（8 分）

讲清内外环，并解释为何“并行三分析器”“全部历史 profile 累积”“提前量为 0”均不合格；背出三项源码缺陷。

```text
[粘贴你的口述提纲]
```

实际用时：____ 秒

### 3.2 变体题（4 分）

12 轮对话每 4 轮触发一次，沿用公开 `advise()`。额外 LLM 调用多少次？关键路径能否按 3 个分析器并发估时？

```text
[作答]
```

### 3.3 反例题（4 分）

有人因源码遍历所有历史 JSON 就断言“全部历史 profile 被拼进更新 prompt”。用赋值语义反驳，并给保守表述。

```text
[作答]
```

### 3.4 应用题（4 分）

现有数据只有每段最终量表和聊天记录，没有风险事件 onset/预警时间。实验表的提前量应填什么？为可比较需新增哪些定义或日志？

```text
现应填写：
新增定义/日志：
```

### Gate 3 自检

- [ ] 我区分了架构并列与源码串行。
- [ ] 我没有把 profile 更新叫参数训练。
- [ ] 我没有把未定义/未报告的提前量写成 0。
- [ ] 三项公开仓缺陷一项不少。

---

## Gate 4｜T5 迁移与伦理

### A4 一页预注册

```text
研究对象：

四个实验臂：
1.
2.
3.
4.

配对与统计单位：

结果通道：

过程通道：

R / MCID proxy / 分布计划：

seed 与不确定性：

提前量定义：

人格一致性、误干预与开销：

敏化敏感性分析：

形式保证边界：

伦理停止条件：
1.
2.
3.
```

### 4.1 90 秒复述（8 分）

按“保留哪些论文级行为结构—怎样 clean-room 重建—新增什么—保证到哪里—许可/伦理停在哪里”解释迁移。

```text
[粘贴你的口述提纲]
```

实际用时：____ 秒

### 4.2 变体题（4 分）

未来 C1 新增 2 个自架角色,使用 3 病种标签、3 个自造 profile、每个 8 个话题、2 个 style 标签、1 seed。新增多少段？若 style 仅是目录标签，能否声称复现 Meow/Roar？

```text
[作答]
```

### 4.3 反例题（4 分）

反驳：“T5 使用 PAC-iMDP，所以从模拟病人输入到自然语言回复都获得端到端形式保证。”

```text
[作答]
```

### 4.4 应用题（4 分）

设计最小实验：四臂、配对单位、三类主要结果、提前量定义、一个敏化敏感性分析、三个伦理停止条件。

```text
[作答]
```

### Gate 4 自检

- [ ] 我没有把本地 style 标签冒充平台风格。
- [ ] 我把保证限制在符号模型及其假设内。
- [ ] 我没有把模拟结果用于真人诊断、治疗或人群外推。
- [ ] 真人接触前的 IRB、人工升级与退出机制已写入停止条件。

---

## 提交区

```text
学员：
日期：
是否闭卷完成四个 90 秒复述：
是否在作答前查看答案：
最不确定的三个点：
1.
2.
3.
```

提交本文件后，教师评分并更新 `mistakes.md`。只有四关均 ≥16、总分 ≥68、红线为 0，才进入 `FINAL_ORAL.md`。


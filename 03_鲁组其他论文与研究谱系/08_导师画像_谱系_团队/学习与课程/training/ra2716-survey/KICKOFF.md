# ra2716 专属窗口协议（KICKOFF）

**本窗职责**：只管 `ra2716-survey` 训练包全生命周期（RA-27 + RA-16 两资产共享一个训练闭环：盲写画像 → 开封对账，现成报告即密封答案）。桌面域与报告原件全程只读。

## 阶段 1 · 赛前准备（本窗第一动作）

> **幂等检查（先做）**：若 `_sealed/EX-02_对账基准.md` 已存在且带 completion footer——赛前准备已由协调窗代理完成（8-12 晚并行发动），本阶段跳过，直接报"就绪"并贴禁读清单给用户确认。

1. 读 `BRIEF.md` 全文 + `../00_SYSTEM.md` §2/§3/§7。
2. **时点检查**：EX-01/02 随时可做；**EX-03 不早于 8-25**（BR-1 NSFC 放榜复核后素材最新鲜——已拍板）。今天日期若在 8-25 前，本窗先做 EX-01/02，EX-03 留断点。
3. 无环境前置。建工作目录 `EX-01/ EX-02/ EX-03/`。
4. **派 Setter（子代理后台）**：读三份 `_REPORT_*.md`（优先 MAS 仓 `progress/audits/2026/07/` 副本）+ 桌面 `调研中/` 的 SYNTHESIS + 矩阵版审计，抽成逐维对账基准表 → `_sealed/EX-02_对账基准.md`。回执只报「完成 + 路径 + 覆盖维度数」。**本窗对话里零报告内容引用**（引用即剧透 EX-01）。
5. 就绪报告一行 + 把 BRIEF §0 第 5 条禁读清单原文贴给用户过目（本包答案隔离范围最大，开工前必须确认）。

## 阶段 2 · 做题

- EX-01（盲写鲁法明画像，90 分钟硬时间盒）：主控计时 + 过程纪律提醒（每条带来源、`[MEM]`/`[VERIFIED]` 分离、60 分钟处提醒取舍）。允许用户联网检索（这就是训练内容），主控不代查。hint 封顶 H2（H1 提维度缺口、H2 给检索入口）。
- EX-02（开封对账 + 归因）：用户提交 v0 后主控开封基准表，陪走四栏对账（重合/你漏/你独有/冲突）；归因环节主控追问一轮："这条归因是方法问题还是这次的偶然？"
- EX-03（贴 8-25 后）：增量对照（bridge_scan 新档 + 3 个 untitled 清账 + BR-1 查证）→ 素材包合成。素材句硬验收：`[VERIFIED]` + 坐标 + 时效标记 + 话术分支，缺一不收。

## 阶段 3 · 批改与复盘

- EX-02 铸卡《调研画像方法》v0.1（六维清单 + 检索入口表 + 证据分级 + 时间盒分配）→ `../methodology/` + INDEX。
- EX-03 素材包是转正产出：落 `progress/research/oe1-materials/` + task_log 记一行 + 通知 outreach 线（OE-1 的输入）。
- EX-04（可选二刷）：用卡对孙猛速写，验证用时 ≤ EX-01 的 60%，卡升 v0.2。死线紧则 Agent 代做孙猛素材、用户只做卡复盘（LEDGER 记 `[降级→逆向训练]`）。
- LEDGER 只追加本包行。

## 断点恢复

读本档 + `../LEDGER.md` 查 ra2716 最后一行；特别注意 EX-03 的 8-25 时点闸门。

# 鲁组 PN 方法族四篇联卷 学员工作簿

> **状态:TEACHING-AWAITING-USER**
> 预制落盘 ≠ 完成④教学;四关与口试必须由用户本人在场完成。
> 本文件是学员唯一开卷入口。作答完成前不要打开 `answer_key.md` 与 `FINAL_ORAL.md`。
> 讲义 = 上级目录六个拆解文档(`../README.md`、`../01_SBTPN.md`、`../02_DEADLOCK_PNULOCK.md`、`../03_UAF_PN_VFG.md`、`../04_DEADLOCKSEG_SEGLOCK.md`、`../05_FAMILY_SYNTHESIS.md`),它们是唯一事实源。

## 作答规则

1. 每关先读对应讲义,再**关闭讲义**,用 2 分钟闭卷口述〔08-24 修:原 90 秒塞不下 8 个高密度给分点,放宽至 2 分钟,先 30 秒结论定调〕;把实际口述的提纲原样写下,不要事后润色。
2. 每关依次完成复述题、变体题、反例题和应用题。
3. 每题必须留下可评分 artifact;只写"懂了"计 0 分。
4. 四关各 20 分;每关至少 16,总分至少 68/80,红线事实错误必须为 0(红线清单见 `mistakes.md` R-01…R-12)。
5. 不确定处写"待核",不按错误处罚;不要猜数字、作者、年份、刊物、因果方向或精度/开销的相对大小。
6. 事实纪律:所有数字与结论以上述六个文档为准;README 已声明"PDF 文本提取存在乱码处已尽力校对,以原 PDF 为准",涉及公式细节与图中编号处沿用该标注。
7. **延迟再测〔08-24 新增〕**:错题闭环后 +1 天自测该关 B 题一次、+3 天再测一次,对照 B 题主考判分锚自评,两次均过才算销项(规则见 `EXAMINER_SOP.md` §4)。

---

## Gate 1|01 SBTPN:D1 概率根因线(讲义:`../01_SBTPN.md`)

### A1 卡|九元组 + 式 3–8 公式链卡(关首准备,作答前闭卷填写)

```text
九元组 SBTPN = (X, P, β, T, F, S, E, Pr, M),逐元一句:
X =            P =            β(映射性质)=
T(一个变迁建模什么)=
F =            S(两种协同各是什么、图上怎么画)=
E =            Pr =           M(初始标记来自哪)=

AND/OR 落位:同一变迁的多个输入库所之间 = ____;汇于同一效应库所的不同变迁之间 = ____

公式链(每式一句"算什么、判什么"):
式 3 μ(依赖强度指数;08-22 符号勘误:原文为 μ,初版误作 Θ)=
式 4 Φ(判 AND,阈值与方向要求)=
式 5 Ψ/Υ(判催化/抑制;η1 = __、η2 = __〔08-22 符号勘误:原文为 η〕;候选库所从哪取)=
式 6(催化调升;λ+ 公式)=
式 7(抑制调降;λ− 公式)=
式 8(综合)=
算法 4 双重过滤 = 概率打分(ρ 判据)+ ____________(两条剪枝)

idea(它的核心洞见,一句话):
novelty(相对谁、新在哪):
感想(一句主观判断 + 所引证据):
```

注:~~公式符号细节以原 PDF 为准(README 乱码校对声明)~~〔08-22 已对照原文核销(视觉读 PDF p.1058-1059):发现并改正两处符号错——式3 原文为 μ、阈值原文为 η1/η2;式4/5 结构、λ± 公式、g/q 平均、Ψ/Υ 均与原文一致〕。

### 1.1 90 秒复述(8 分)

提示词:按"**研究目的**(现有 RCA 的两个盲区)→ **contributions**(九元组 + 协同弧的建模选择及其动机)→ **方法**(TCDF→AND/OR→协同→条件概率的四步管线;式 6–8 修正与算法 4 双重过滤)→ **结果**(Syn 的 12 指标与 RCA 具体数字、TE 的 AND/催化/抑制规则与根因结论)→ **局限**(论文自认三条 + 拆解补充)"的顺序完整讲一遍。

```text
[粘贴你的口述提纲]
```

实际用时:____ 秒

### 1.2 变体题(4 分)|改协同弧重算概率

某规则下已算得 $Pr(\hat{p}_i \mid \hat{p}_j) = 0.6$。协同挖掘已判定:
- 催化库所 $p_x$:$\psi_1 = 0.5$、$\psi_2 = 0.8$、$Pr(\hat{p}_x \mid \hat{p}_j) = 0.5$;
- 抑制库所 $p_y$:$\psi_1 = 0.8$、$\psi_2 = 0.6$、$Pr(\hat{p}_y \mid \hat{p}_j) = 1.0$。

手算:①$\lambda_x^{+}$ 与式 6 调升值;②$\lambda_y^{-}$ 与式 7 调降值;③式 8 综合值;④按算法 4(ρ 门槛按 0.5 记)判断 $p_i$ 是否留在根因候选集,并说明"留在候选集"离"判为根因"还差哪一步。

```text
λx+ =            式 6 调升后 =
λy− =            式 7 调降后 =
式 8 综合 =
候选判定与缺的那一步 =
```

### 1.2B 备用变体题(重考用,4 分)|改协同弧重算概率·B 卷〔08-24 增〕

某规则下已算得 $Pr(\hat{p}_i \mid \hat{p}_j) = 0.4$。协同挖掘已判定:
- 催化库所 $p_x$:$\psi_1 = 0.4$、$\psi_2 = 0.8$、$Pr(\hat{p}_x \mid \hat{p}_j) = 0.6$;
- 抑制库所 $p_y$:$\psi_1 = 0.8$、$\psi_2 = 0.4$、$Pr(\hat{p}_y \mid \hat{p}_j) = 0.5$。

手算:①$\lambda_x^{+}$ 与式 6 调升值;②$\lambda_y^{-}$ 与式 7 调降值;③式 8 综合值;④按算法 4(ρ 门槛按 0.5 记)判断 $p_i$ 是否留在根因候选集;若不留,说明被哪道工序挡住。

```text
λx+ =            式 6 调升后 =
λy− =            式 7 调降后 =
式 8 综合 =
候选判定与挡住的工序 =
```

> **主考判分锚(B1,不向学员展示)**:①λ+x=(0.8−0.4)/(1−0.4)=2/3;式 6=0.4+(2/3)×0.6×0.6=**0.64**;②λ−y=(0.8−0.4)/0.8=**0.5**;式 7=0.4−0.5×0.5×0.4=**0.3**;③式 8=(0.64+0.3)/2=**0.47**;④0.47<0.5 → **不进候选集**,被算法 4 第一道工序([0.5,δ] 门槛)挡住;与 A 卷差异:A 卷留候选还差路径过滤,B 卷连候选都不进——④考门槛机制的反向情形。

### 1.3 反例题(4 分)

驳斥以下论断:

> "协同效应无非是把非因果变量也当成因。SBTPN 完全可以把催化/抑制库所直接用因果弧 F 连进变迁,不需要单独的协同弧 S。"

要求:说出经典 BN/PN 在协同因子上的二选一困境与论文的反例场景、S 与 F 的语义差别、把协同并入因果会复现哪类对手的错误、候选协同库所的结构来源。

```text
[作答]
```

### 1.4 应用题(4 分)|T4 挂载(面向 2026-09-10 进组)

面谈时要向鲁老师/王晓亮讲清"SBTPN 怎么借力到 T4(SBPN 腐败级联根因)"。写出:①MAS 概念到 SBTPN 元素的映射(至少四对);②式 6–8 在 T4 里的用途一句话;③T4 立项的第一个技术决策及备选方案;④一个可当"见面礼"的问题。

```text
①映射:
②式 6–8 用途:
③第一个技术决策:
④见面礼问题:
```

### Gate 1 自检

- [ ] 我没有把协同弧 S 混进因果弧 F,也没把催化/抑制说成"因/非因"。
- [ ] η1 = 0.51、η2 = 0.54 没记反,且记得"协同依赖噪声存在"。
- [ ] 我记得四个对手(TCDF/PCMCI/MTE/MLP-GC)的 AND 与协同指标是 N.A.,以及两种错法(误判为因果 vs 忽略)。
- [ ] 我没有声称 SBTPN 在真实工业数据上验证过(Syn 自造 + TE 仿真)。
- [ ] 身份卡:王晓亮一作、鲁法明+周孟初通讯、IEEE/CAA JAS 2026、代码开源(四篇中唯一)。

---

## Gate 2|02 PNULock:PN 展开死锁检测与重演(讲义:`../02_DEADLOCK_PNULOCK.md`)

### A2 卡|轨迹网→伴随网→展开流程卡(关首准备)

```text
流程逐格填:
运行轨迹(5 种同步操作)= { , , , , }
→ 程序轨迹网 Σ(α)=(P,T;F,β,γ,M0):P = ____库所 + ____库所;γ 的关键性质 = ________(解决哪个信息丢失?)
   锁弧编码了什么因果(解决哪个信息丢失?)=
→ 定义 4:合法死标记 =                         ;非法死标记 =
→ 定义 5 伴随网:recover 变迁 •recover = ________,recover• = ________;语义 = 合法____、非法____
→ 展开:McMillan/ERV 完全有限前缀;死标记 ⟺ 存在________________________的配置(定义 6 名称:)
→ 配置张成子网 → 每锁授权线程序列 δ(o) → CalFuzzer 探针( / / )+ park/unpark
→ 终态三分判定: →真死锁; →假死锁; →未知

定理 1 一句话:
定理 2 一句话:
定理 3 一句话:

idea(全文最干净的构造是什么、干净在哪):
novelty(相对锁图系模型、相对 DeadlockFuzzer 系重演):
感想(一句主观判断 + 证据):
```

注:~~论文示例的事件/库所编号(如配置 C1 与死标记 {p1, p12, p19})以原 PDF 图为准~~〔08-22 已核销:原文两处出现死标记 {p1,p12,p19},配置 C1={e1–e8,e26–e29,e38} 均与原文一致,示例忠实〕。

### 2.1 90 秒复述(8 分)

提示词:按"**研究目的**(现有模型两个信息丢失 + 重演痛点)→ **contributions**(轨迹网/伴随网构造 + 确定性一次重演)→ **方法**(定义 1/3/4/5、定理 1–3、展开检测、算法 2/3、跨运行映射)→ **结果**(Table VII:假阳性、重演次数与耗时、检测开销方向)→ **局限**(自认四条 + 拆解补充)"的顺序讲。

```text
[粘贴你的口述提纲]
```

实际用时:____ 秒

### 2.2 变体题(4 分)|小轨迹手推展开检测与重演

观测到如下正常终止的轨迹(main 先 fork 出 A、B,末尾 join;锁 x、y):

```text
fork(main,A); fork(main,B);
acq(A,x); acq(A,y); rel(A,y); rel(A,x); stop(A);
acq(B,y); acq(B,x); rel(B,x); rel(B,y); stop(B);
join(main,A); join(main,B)
```

手推:①锁库所数量与初始 token;整条轨迹对应多少个变迁、依据哪条性质;②这条轨迹本身没死锁,为什么轨迹网能检出死锁(引定理),非法死标记长什么样(哪些库所有 token、锁库所状态、为何不满足定义 4 合法条件);③deadlock-adjoint 配置中的锁获取事件与导出的 δ(x)、δ(y);④算法 3 重演的执行过程与终态三分判定结果。

```text
①
②
③ δ(x) =        δ(y) =
④
```

### 2.2B 备用变体题(重考用,4 分)|小轨迹手推展开检测与重演·B 卷〔08-24 增〕

观测到如下正常终止的轨迹(main 先 fork 出 A、B,末尾 join;锁 m、n):

```text
fork(main,A); fork(main,B);
acq(A,m); acq(A,n); rel(A,n); rel(A,m); stop(A);
acq(B,n); acq(B,m); rel(B,m); rel(B,n); stop(B);
join(main,A); join(main,B)
```

手推:①锁库所数量与初始 token;整条轨迹对应多少个变迁、依据哪条性质;②这条轨迹本身没死锁,为什么轨迹网能检出死锁(引定理),非法死标记长什么样;③deadlock-adjoint 配置中的锁获取事件与导出的 δ(m)、δ(n);④算法 3 重演的执行过程与终态三分判定结果。

```text
①
②
③ δ(m) =        δ(n) =
④
```

> **主考判分锚(B2)**:与 A 卷 2.2 完全同构(仅锁名 x,y→m,n),判分点逐条对齐 answer_key Gate 2 变体:①2 个锁库所各 1 token;变迁数=事件数(每次执行独立成变迁);②非法死标记=A 持 m 等 n 且 B 持 n 等 m 的组合标记,两锁均被持有故不满足定义 4 合法条件;③δ(m)=[A 的 acq(m)],δ(n)=[B 的 acq(n)];④配置导出调度器→一次重演成功→真死锁确认。

### 2.3 反例题(4 分)

驳斥以下论断:

> "轨迹网只忠实记录观测到的那一条轨迹。从一条不死锁的轨迹里检出死锁,说明模型在凭空捏造行为,报出来的都是假阳性。"

要求:引定理 1 说明"蕴含更多交错"的原理与合法性、承认 γ(σ) 不必然可执行这一真实的假阳性来源、说明论文用什么机制处理它(而不是否认它)、给出实验中的假阳性对照数字。

```text
[作答]
```

### 2.4 应用题(4 分)|T1 挂载(面向进组面谈)

鲁老师问:"你觉得 PNULock 这套机器怎么用到 MAS 安全?"写 60 秒版答案:①锁库所/acq-rel/非法死标记/deadlock-adjoint 配置/张成子网 → MAS 概念的映射;②T1"传播割集"在展开语言里是什么;③"确定性重演"为什么是审稿人可验证的硬卖点、δ(o) 换成什么;④recover 构造思想或跨运行映射假设在 MAS 里的一个具体风险点。

```text
①映射:
②割集:
③重演卖点:
④风险/构造迁移:
```

### Gate 2 自检

- [ ] recover 只复位合法死标记、非法保持死,我没说成"消除所有死标记"。
- [ ] 检测开销方向没记反:PNULock 检测比 iGoodlock 慢(用时间换精度),重演一次成功且比随机重演快。
- [ ] 重演判定是真/假/未知三分,不是二分;"未知"是诚实输出。
- [ ] 我记得基准全是教学级小程序,状态爆炸在实验里没被触发。
- [ ] 身份卡:鲁法明一作、包云霞通讯、IEEE Access 2024。

---

## Gate 3|03 UAF:分段 PN + 值流图(讲义:`../03_UAF_PN_VFG.md`)

### A3 卡|三约束兼容性卡(关首准备)

```text
三类约束(定义 2–4):
①控制流因果约束,三型 = 线程内 / ________ / ________(第三型是对 Canary 的增量贡献)
②UAF 触发约束,两型 = ________ / free 先于 use
③条件满足约束 =
检测原理一句话(§2.2 口径):若"UAF 触发所要求的执行序"与"控制流因果允许的执行序"____,则该源汇路径____。

双图:
分段 PN Σ = (P,T,F,S,M0,λ):相对经典 PN 新增 __ 与 __;伴随标签 α/β 刻画什么;输入是____(静态/动态),保留了 PNULock 轨迹网扔掉的什么结构;段概念承自哪篇前作
分段值流图 ℘ = (O,V̂,R,C,S,σ):变量节点 V̂ 的三元组 = ( , , );算法 4 从__节点出发找__节点

展开的两个用途:①判________;②cut-off 展开段 = ________摘要(替代 CBMC 的什么做法)
算法 5 判定链顺序:条件约束 → 同一展开段 → ________ → 并发 → ________ → 行号兜底

idea(核心洞见):
novelty(相对 Canary 新在哪、相对 Saber 新在哪;载体差异话术):
感想(一句主观判断 + 证据):
```

注:算法 5 最终报告判据的措辞按讲义 §2.2 检测原理口径作答;~~逐步判定链细节以原 PDF 为准~~〔08-22 已核销:教学骨架与原文 Algorithm 5 六分支逐位吻合,两空格答案="lock/fork 耦合(序判)""非并发(再序判)"〕。

### 3.1 90 秒复述(8 分)

提示词:按"**研究目的**(值流方法忽略并发因果;Canary 具体失效场景;CVE-2021-21148 动机)→ **contributions**(lock/fork 耦合因果 + cut-off 循环摘要 + 三约束兼容性框架)→ **方法**(12 种中间码语句 → 分段 PN + 分段值流图 → 展开 → 算法 5)→ **结果**(vs Canary 与 vs Saber 的 recall/precision 增量、耗时、数据集)→ **局限**(自认 + 拆解补充,特别是评测规模)"的顺序讲。

```text
[粘贴你的口述提纲]
```

实际用时:____ 秒

### 3.2 变体题(4 分)|改设定判报/不报

三个场景,逐个判"报 / 不报 UAF"并给出算法依据:

(a) 动机案例原样:主线程 acq(ι) → fork 子线程 → use(l9) → rel(ι);子线程内先 acq(ι) 再 free(l26)。
(b) 在 (a) 基础上,主线程在 rel(ι) 之后、join 之前新增一次 use(l9')。该 use 与子线程的 free 之间判什么关系、报不报?
(c) free 与 use 出现在同一个循环体内,对应同一个"伴随 cut-off 事件的 PN 展开段"。算法 5 对这对序判什么、能否据此剪枝,该展开段是怎么构造出来的、替代了谁的什么做法?

```text
(a)
(b)
(c)
```

### 3.2B 备用变体题(重考用,4 分)|改设定判报/不报·B 卷〔08-24 增〕

三个场景,逐个判"报 / 不报 UAF"并给出算法依据(与 A 卷 3.2 同构,锁名换 κ、行号平移):

(a) 动机案例原样(锁名换 κ):主线程 acq(κ) → fork 子线程 → use(l11) → rel(κ);子线程内先 acq(κ) 再 free(l28)。
(b) 在 (a) 基础上,主线程在 rel(κ) 之后、join 之前新增一次 use(l13')。该 use 与子线程的 free 之间判什么关系、报不报?
(c) free 与 use 出现在同一个循环体内,对应同一个"伴随 cut-off 事件的 PN 展开段"。算法 5 对这对序判什么、能否据此剪枝,该展开段是怎么构造出来的、替代了谁的什么做法?

```text
(a)
(b)
(c)
```

> **主考判分锚(B3)**:与 A 卷 3.2 逐条同构(仅锁名/行号换),判分点对齐 answer_key Gate 3 变体(含 08-14 audit F-02 修正后语句顺序口径):(a) lock/fork 耦合因果成立→不报;(b) rel 后的 use 与子线程 free 判并发→按并发约束判、报;(c) 同一 cut-off 展开段→按段内偏序判、可剪枝;展开段=cut-off 事件摘要构造,替代 CBMC 定次展开。

### 3.3 反例题(4 分)

驳斥以下论断:

> "03 篇的分段 PN 和 02 篇的轨迹网是同一张网换了个名字,这篇论文不过是把 PNULock 跑在 C 代码上。"

要求:从输入形态、网结构差异(保留/扔掉什么)、新增元素与段概念的真实出处、被移植的到底是什么(以及叠加了什么新载体)四个方面反驳,并给出 README 谱系的"分工"表述。

```text
[作答]
```

### 3.4 应用题(4 分)|T3 挂载 + 见面礼

写出:①T3(use-after-poison)的概念映射(free/use/值流图/lock-fork 耦合 → MAS 概念,至少四对);②W1-C 已核实的"相对 Canary 的载体差异"辩护话术;③与 T1 的接口("静态检出 + 动态确认"闭环)及其现状一句话;④一个问包老师/UAF 线学长的问题(工具栈方向)。

```text
①映射:
②辩护话术:
③T1 接口:
④问题:
```

### Gate 3 自检

- [ ] 数字没对调:vs Canary 是 recall +25%、precision +33.3%;vs Saber 是 recall +3%、precision +15.1%。
- [ ] 我没说 03 在 Chrome/Linux 等真实项目上评测过(CVE-2021-21148 只是动机;评测是 Juliet 393 UAF + 5 个自建程序)。
- [ ] 我记得"比 Canary 少 12.6 ms、比 Saber 少 21.3 ms"是平均耗时,论文写成 time complexity 属表述不严谨。
- [ ] lock/fork 耦合是 03 相对 Canary 的增量贡献;分段 PN 是静态构建、保留分支。
- [ ] 身份卡:李珊珊一作、包云霞通讯、IEEE Access 2025;刘聪(NOVA)是 D3 线人脉交点。

---

## Gate 4|04 SegLock + 05 横向综合(讲义:`../04_DEADLOCKSEG_SEGLOCK.md` + `../05_FAMILY_SYNTHESIS.md` + `../README.md` 谱系)

### A4 卡|序增广弧 + 历史锁因果图卡 + 谱系卡(关首准备)

```text
五种因果/互斥关系(定义 2;标出两种新增):
①<_thread ②<_start ③<_join ④________(新增) ⑤________(新增);另有互斥 #_lockSet
对应可杀的五类假阳性:已有方法能杀 = / / ;本文新杀 = / 

锁增广分段图 SegG_Lock 的两个增广:
①____________也成为分段点(规则 v 的触发条件一句话)
②段标签 φs 记录________;历史锁集怎么算出 = ________

序增广锁图弧标签 ⟨____, (____, ____), m, ____⟩^(k):m = ________,k = ________
它对 PNULock"每次执行独立变迁"的关系 = ________(代价对比)

检测 5 条件:环 / ________ / ________ / ________ / 历史锁耦合因果图 DependG_Lock_c ____
lockSet_OnceHeld(e) 定义一句话:
L_IS = ________ ∩ ________;DependG 有环 ⟹ ________

两阶段重演:DLAE(c) = ____;一阶集 vs 二阶集 = ____;SchdG 的边 = __种因果;排序方法 = ____;执行端与 02 共用什么

谱系卡(README/05):
2021 锁增广分段图 →(给了什么)→ 02 PNULock →(移植了什么)→ 03 UAF;02 →(被怎样重构)→ 04 SegLock
D1 与 D2 的互引状态 = ________;"闭环"的正确表述 = ________
重演公共资产三件套 = ________ / ________ / ________
方法论 DNA 四条(选题公式/建模判断标准/求解价值函数/评测惯式):

idea(04 的核心洞见):
novelty(04 相对 ConLock+、相对分段图系、相对组内 PNULock 各新在哪):
感想(对"重 vs 轻"路线之争的一句主观判断 + 证据):
```

注:~~论文段标签串示例(如段 4 的 G^(16)…)的具体序号以原 PDF 为准~~〔08-22 已核销:原文轨迹表 e16–e23 与拆解段4标签串八事件逐项一致(acq/rel 与上下划线一一对应)〕。

### 4.1 90 秒复述(8 分)

提示词:按"**研究目的**(分段图缺 lock-start 耦合、锁图丢历史锁、重演不直观)→ **contributions**(两种新增因果关系、两个图增广、历史锁耦合因果图判环、两阶段重演)→ **方法**(定义 2–5 + 检测 5 条件 + SchdG 拓扑排序)→ **结果**(精度持平 PNULock、时间 1/2–1/6、内存排序、重演对比)→ **局限**(五原语、覆盖面 vs 展开)→ 谱系衔接(2021→02→03→04;重演公共资产)→ 05 方法论 DNA 与 D1/D2 闭环现状"的顺序讲。〔⚠️ 08-24 勘误：原文“1/2–1/6”为整体范围宣称；共享 8 基准真实 1.70–6.35×（中位 2.76×），Table 5 对标数字整列错位，非逐项保证；见 02b §〇.2 与 _audit_remediation_20260824/A4_pdf_verification.md §终判。〕

```text
[粘贴你的口述提纲]
```

实际用时:____ 秒

### 4.2 变体题(4 分)|改锁序判环(历史锁耦合)

观测到如下正常终止轨迹(main 先 start 出 t1、t2,末尾 join;四把锁 G、H、x、y;事件编号见行内):

```text
t1: (1)acq(t1,H); (2)acq(t1,G); (3)rel(t1,G); (4)acq(t1,x); (5)acq(t1,y);
    (6)rel(t1,y); (7)rel(t1,x); (8)rel(t1,H); stop(t1)
t2: (9)acq(t2,G); (10)acq(t2,H); (11)rel(t2,H); (12)acq(t2,y); (13)acq(t2,x);
    (14)rel(t2,x); (15)rel(t2,y); (16)rel(t2,G); stop(t2)
```

手推:①序增广锁图中的两个有向环各是什么(顶点=锁);②对两个环分别过前四个条件(不同线程/段间无路径/持锁集不交),给出各请求事件的 lockSet;③对 (x,y) 环:写出两条 lock_held 耦合约束(用事件编号),加上线程内因果边,构造 DependG_Lock_c 并指出环;④结论:哪个环被杀、哪个环报为潜在死锁,报的那个用直觉验证是不是真死锁。

```text
①
②
③
④
```

### 4.2B 备用变体题(重考用,4 分)|改锁序判环(历史锁耦合)·B 卷〔08-24 增〕

观测到如下正常终止轨迹(main 先 start 出 t1、t2,末尾 join;四把锁 P、Q、u、v;事件编号见行内):

```text
t1: (1)acq(t1,P); (2)acq(t1,Q); (3)rel(t1,Q); (4)acq(t1,u); (5)acq(t1,v);
    (6)rel(t1,v); (7)rel(t1,u); (8)rel(t1,P); stop(t1)
t2: (9)acq(t2,Q); (10)acq(t2,P); (11)rel(t2,P); (12)acq(t2,v); (13)acq(t2,u);
    (14)rel(t2,u); (15)rel(t2,v); (16)rel(t2,Q); stop(t2)
```

手推:①序增广锁图中的两个有向环各是什么(顶点=锁);②对两个环分别过前四个条件,给出各请求事件的 lockSet;③对 (u,v) 环:写出两条 lock_held 耦合约束(用事件编号),加上线程内因果边,构造 DependG_Lock_c 并指出环;④结论:哪个环被杀、哪个环报为潜在死锁,报的那个用直觉验证是不是真死锁。

```text
①
②
③
④
```

> **主考判分锚(B4)**:与 A 卷 4.2 完全同构(G,H,x,y→P,Q,u,v):①环 (P,Q)(e2+e10)与 (u,v)(e5+e13);②两环均跨线程、段间并发、lockSet 不交均过;③(u,v) 环:OnceHeld(e5)∋Q 且 Q∈lockSet(e13) ⟹ L_IS={Q};对称 OnceHeld(e13)∋P ⟹ L_IS={P};加线程内边得 1→2→9→10→1 环→DependG_Lock_c 有环→被杀;④(u,v) 被杀、(P,Q) 报为潜在死锁;t1 嵌套序 P→Q、t2 反序 Q→P,互等=真死锁。数值与 answer_key Gate 4 变体逐条对应(08-14 audit 独立复算一致)。

### 4.3 反例题(4 分)

驳斥以下论断(两句话都错):

> "SegLock 之所以同时赢了 iGoodlock 和 PNULock,靠的是信息量更大、更重的模型,精度超过了 PNULock;而且组里已经把 D2 的检测结果接进 D1 的 SBTPN 做概率归因,四篇形成了检测→归因的闭环。"

要求:①精度关系的准确表述(Table 5/6 的内部对标结论);②"更重"方向的反驳(弧标签替代什么、求环算法、时间与内存的三方排序);③轻量化的真实代价(理论覆盖面、缺失的对照实验);④闭环论断错在哪(互引事实 + 正确表述)。

```text
①
②
③
④
```

### 4.4 应用题(4 分)|进组面谈综合(主考 05)

2026-09-10 面谈,鲁老师说:"谈谈你读完这四篇的整体收获,以及进组想做什么。"写 90 秒版提纲:①方法论 DNA 至少一条(准确复述其内容);②谱系一条(衔接链或重演公共资产);③把"共同局限"之一转述成"我能补的位",并挂到 T1/T3/T4 之一;④一个问学长的问题(指名对象与要解决的决策)。

```text
①DNA:
②谱系:
③补位 + T 挂载:
④问学长:
```

### Gate 4 自检

- [ ] 精度关系:SegLock 持平 PNULock(均零假阳性),不是超过;贡献是同等精度做进轻量模型。
- [ ] 三方排序没记反:检测时间 SegLock 最快(降为 PNULock 的 1/2–1/6);内存 iGoodlock < SegLock < PNULock。〔⚠️ 08-24 勘误：原文“1/2–1/6”为整体范围宣称；共享 8 基准真实 1.70–6.35×（中位 2.76×），Table 5 对标数字整列错位，非逐项保证；见 02b §〇.2 与 _audit_remediation_20260824/A4_pdf_verification.md §终判。〕
- [ ] 五种因果关系里 lock-start 与 lock-held 是新增两种;历史锁假阳性是 ConLock+ 遗留、本文首杀。
- [ ] D1 与 D2 零互引(四篇互引仅 03 引 02);"闭环是机会不是事实"。
- [ ] 重演公共资产三件套:CalFuzzer 探针调度(lockBefore/lockAfter/unlockAfter + park/unpark)、跨运行 ID 映射、真/假/未知三分。
- [ ] 身份卡:鲁法明一作、袁贵元通讯、王小宇二作(T1 接口人)、Tsinghua Science and Technology 2026。

---

## 提交区

```text
学员:
日期:
是否闭卷完成四个 90 秒复述:
是否在作答前查看 answer_key.md / FINAL_ORAL.md:
最不确定的三个点:
1.
2.
3.
```

提交本文件后,教师按 `answer_key.md` 评分并更新 `mistakes.md`。只有四关均 ≥16、总分 ≥68、红线为 0 且错题闭环,才进入 `FINAL_ORAL.md`。

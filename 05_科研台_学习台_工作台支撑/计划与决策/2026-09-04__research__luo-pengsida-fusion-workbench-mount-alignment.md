# 讨论存档 · Luo(Supervisor-Skills)+pengsida 融合与工作台挂载对齐（未拍板）

**Date**: 2026-09-04
**Status**: `ALIGNMENT-CHECKPOINT / 未拍板 / Q1（挂哪个工作台）仍悬空`——本档存的是对齐讨论的产出（隐藏约束+验收标准提案+A/B 两案设计层与落地层对比），不是决策记录。本档不设"拍板记录"表，因为没有拍板发生；不修改任何已冻结文件（契约 §13、WB-S 设计稿逐字不动）。
**触发**：用户 09-04 "先存档吧"——本轮对齐讨论到此为止，先落盘不丢。
**关联**：`progress/TODO.md:479-483` `[topic:supervisor-skills]`；`progress/decisions/2026-08-31__maintenance__task-unified-contract-v0-proposal.md` §13（frozen）；`progress/decisions/2026-09-01__maintenance__workbench-v2-skill-registry-design.md`（DESIGN-v1-FROZEN）；TASK-20260904-001/003 产出（B1-B4 + `研体系三源整合_20260904.md`）；`progress/handoff/2026-09-04__research-system__window-handoff-snapshot.md`（现已相对 TASK-20260904-003 陈旧，未更新）。

---

## 0. 用户原话链（按时间序，一字不改）

1. "因为容易记不住，港科大的博导，ok，我想整理一下彭、骆的经验库以及我们原本的科研学习体系"
2. （`/model` 命令执行后）"luo的整个仓库，不仅仅是pdf。我想直接整合一下，融合成我们自己的科研台，镶嵌在我们的工作台上的。先和我把需求对齐、深挖、还原、深挖出的隐藏约束、验收标准提案"
3. （三问三答之后）"想知道就是两个设计的具体对比情况之后再拍板选哪个"
4. （设计层对比给出之后）"我想问具体的方案落地之后的实用角度的对比"
5. （落地层对比给出之后）"先存档吧"（本档的触发点）

---

## 1. 已查明的冻结/既定事实（本轮据以推理的地基，均已核对盘面原文）

- **契约 §13「科研侧挂载点」**（`2026-08-31__maintenance__task-unified-contract-v0-proposal.md:136-149`，frozen）：定位判断原文"**不建第二个"科研工作台"，科研工作任务化接入同一脊柱**"。四挂载点：①学习/拆解任务=普通 Task+特型 acceptance；②24h 冷启动复测=`accepted{open_gates:[冷启动复测@日期]}`；③知识依赖图=P-06 第二类节点；④能力/贡献账本=派生视图。明确排除工作台侧决定："Supervisor-Skills 选用范围与 NC-SA 许可边界"不在工作台侧决定之列。
- **§14 拍板记录**（同档 :151-168）：§8-7 门类型枚举**已批准**，并"按 §13-2 增补`冷启动复测`枚举值"——这条枚举在契约冻结批（TASK-20260901-001）已经生效，**不是**排在 P1 才生效的东西（本轮对比中我曾误说"要等 WP5"，见 §9 纠错）。
- **WB-S 设计稿**（`2026-09-01__maintenance__workbench-v2-skill-registry-design.md`，DESIGN-v1-FROZEN，§10 用户原文"冻结，都接受"）：12 字段 schema；数据流=上游文件→`overrides.yaml`（唯一手改面）→generator→`skills.json`（生成视图）→SQLite→注册表页；试点三技能含"local/supervisor-skills 内一枚"；排位=WP-S，P1 与 WP5 同期，不阻塞 P0。
- **TODO.md:479-483** `[topic:supervisor-skills]`：08-23 用户原话"git@github.com:HKUSTDial/Supervisor-Skills.git这个我想融入进来！"的长期追踪行，含首次安装记录（11 技能实体拷贝）与首次使用评分（pre-submission-reviewer 7.5/10）。
- **今日（09-04）已有产出**：B1-B4（三库整理）、`B2b_luo_handbook_pdf_reading.md`（Luo handbook PDF 70 页读完+视觉核对）、`research_growth/方法论/科研体系三源整合_20260904.md`（六步框架+一页操作卡，`INTEGRATED-AS-ADAPTER`）、B4 Amendment A1（映射 Luo-PDF 机制到既有 L1-L4/M 编号，不解 B4 §6 原 12 项待拍板）。

---

## 2. 深挖出的隐藏约束（4 条）+ 1 处记录错误（未改，仅标记）

| # | 约束/错误 | 依据 |
|---|---|---|
| C1 | WB-S 12 字段 schema 是照"可触发技能"/"带 Pipeline 声明的方法论文档"设计的；Luo 的 16 篇 handbook 理论文与 pengsida 笔记不是这个形状（是读物，不是技能），硬塞会削足适履 | WB-S §3 字段表 vs handbook/pengsida 实测结构对比 |
| C2 | pengsida `external/learning_research` **零许可证**（无 LICENSE 文件、README 无许可声明），比 Supervisor-Skills（CC BY-NC-SA，至少给改编权）更严格——默认版权保留，只能读/引用出处，不能摘录进任何会展示/共享的构件 | 直接查该目录无 LICENSE 文件 |
| C3 | schema 的 `license` 字段本身已预留对策（"未知记 UNKNOWN，禁复制默认"），不需要新发明，只是没人真正按它走过一次；"整个仓库"这次范围正好能把这条规则第一次用上 | WB-S §3 字段表原文 |
| C4 | `drawio-reconstruction` 这一个 skill 自带 **MIT**（版权方 VCG-Bench Authors），不是仓根的 CC BY-NC-SA；整合"全部 11 个 skill"时要单独标注，不能继承默认值 | 逐目录查了 11 个 skill 的 LICENSE 文件，仅此一个有自己的 LICENSE |
| E1（错误，未改） | `2026-09-01__maintenance__workbench-v2-contract-freeze-batch.md`（"尚未完成"栏）与 `2026-09-01__maintenance__workbench-v2-skill-registry-design.md`（库存表+§6 试点理由）两处都写"Supervisor-Skills 逐 skill 许可证 7×CC-BY-4.0／4×CC-BY-NC-SA-4.0 分裂，待 Bridge 窗回传确认"——本轮逐目录核实后为**假**：只有 drawio-reconstruction 有自己的 LICENSE（MIT），其余 10 个继承仓根单一 CC BY-NC-SA，没有 7:4 分裂。这条待办在等一个不存在的确认，需要 amendment 撤销或改写，尚未处理 |

---

## 3. 验收标准提案（草案，未拍板；针对"这次整理回合"，不是工作台最终功能）

1. **来源清单闭环**：Luo（11 skill + 16 handbook + repo-root `handbook/`/`handbook-en/`/`assets/`/`scripts/`）与 pengsida（全部笔记+PDF+Notion 摘录）每一条都能查到"属于哪个源、许可证是什么、目前挂在 §13 哪个挂载点（或标注'挂不上，需要新类型'）"。
2. **不重复造轮子**：不新增契约 §4/§5 字段，不解冻 L0-L5（B4 §6 第 1 项，未拍板），不新建第二个技能触发机制（`.claude/skills/` 的 frontmatter 发现链路本来就生效，与本次整理无关）。
3. **许可证边界写死**：pengsida 任何内容默认"仅指针，禁摘录复制"直到另行拍板；Luo 的 CC BY-NC-SA 每条改编都带署名+出处；drawio-reconstruction 单独标 MIT。
4. **产出形态匹配现实排期**：本次产出是纸面提案（§13 扩展附录+清单），不是能点开的工作台页面——UI 要等对应批次真正开工。
5. **给未来一个可执行钩子**：提案写清楚"WB-S 真开工时，generator 脚本直接读这份清单建卡"，不是一份读完就扔的静态文档。

---

## 4. AskUserQuestion 三问三答（原文，09-04）

### Q1 · "镶嵌在我们的工作台上"具体指哪个工作台？

选项：①工作台 v2（§13/WB-S 已设计，推荐）②现在跑着的 8899/hub ③独立的"科研台"新入口

**用户答（非选择，原话）**："我在思考是直接放在工作台上还是单独一个科研台" —— 这是用户自己仍在权衡的问题，**不是**对三选项之一的选定。截至本档落盘，**Q1 仍未解决**。

### Q2 · "Luo 的整个仓库，不仅仅是 PDF"——整合到什么颗粒度？

选项：①方法论内容摘要 ②连同 skill 本体一起接入 ③全部，包括 scripts/assets

**用户答**："全部，包括scripts/assets" —— **已选定**：完整颗粒度，含 drawio-reconstruction 的批处理脚本、figure-designer 的工具链等非文本资产。

### Q3 · 这次工作是否要改动已冻结的 §13/WB-S 设计本体？

选项：①严格按已冻结设计执行，不改设计（推荐）②在冻结设计基础上扩展 ③先不管工作台挂载，只做整理评估

**用户答**："在冻结设计基础上扩展" —— **已选定**：扩展而非改写、也非搁置。

---

## 5. Q1 与 Q3 的逻辑缺口（本轮发现，已提醒用户，未被回应/未解决）

Q3 选的是"在冻结设计基础上扩展"，但 §13 原文的定位判断句就是"**不建第二个科研工作台**"——这不是留白，是已拍板的立场句。若严格遵守"扩展不推翻"，Q1 的答案在逻辑上已被 Q3 锁定为**同一脊柱路线**（工作台 v2），因为"独立科研台"选项字面上就是在改写 §13 的核心立场句，不是扩展它。

本档记录这个缺口，**不代为决定**——用户随后转向"先看两个设计的具体对比"，暂未回头处理这个缺口本身；缺口依然存在，留给 Q1 最终拍板时一并考虑。

---

## 6. 设计层对比（第一轮，"选哪条路径"层面）

| | A：挂 §13/WB-S（扩展冻结设计） | B：独立科研台 |
|---|---|---|
| 今天新增什么 | §13 扩展提案（新 content_type 类型+Luo/pengsida 全量清单+许可证表）+ WB-S schema 微调 | B1=独立文档/索引（零代码）；B2=8899 新面板（真代码，走现成三步食谱） |
| 多快能点开看 | UI 层今天不能点开——WP5+WP-S 排 P1，且是门 4 开工后**最后一批**才落地 | B1 今天写完即可读；B2 走完三步食谱+重启，今天也能看——两个选项里唯一今天能产出"点得开的网页"的路径 |
| 占哪条 line 配额 | 不占——产出是 research 线纸面提案，不碰 `tools/dashboard/` 代码 | B1 不占；B2 占 infra line，受 R07 约束 |
| 撞不撞"不建第二个科研工作台"这条已批原则 | 不撞——这正是该原则本身定义的路径 | B1 撞得最直接；B2 稍轻但 hub 本身是 v2 要取代的旧架构，严格说也不是"同一脊柱" |
| 门 4 真开工后要不要再搬一次 | 不需要——照 §13/WB-S 形状写的，generator 直接读现成清单建卡 | B1/B2 都需要——门 4 开工后要重新映射进正式契约挂载点，等于多做一次搬迁 |
| 要不要碰冻结文档/要不要用户再批一轮 | 要——§13 是冻结文档，扩展提案需追加新章节+拍板记录，走一次轻量再批 | 不要——完全绕开冻结契约 |

---

## 7. 落地层对比（第二轮，"落地之后的实用角度"，用户 09-04 追问触发）

本轮对比前核实到三条改变结论权重的盘面事实：

- **工作台 v2 是零行代码落主干**。WP0 唯一有实现的部分躺在隔离 worktree `wb2-wp0-20260901` 里，状态 TECH-PASS/CONTROLLER-PASS 但**未合并未提交**；WP1-WP6（含 WP5/WP-S）纯设计稿。
- **8899/hub 是活的**：14 面板在跑，`ARCHITECTURE.md §10` 给了现成五步"加面板食谱"（写 `panel_xxx()` → 加入 `PANEL_SPECS` → 补测试 → 跑全套 pytest → 受控重启），今天就能挂新面板；但它是 v2 要"绞杀者式"取代的旧架构（v3.1），不是 v2 本体。
- **"门 4=09-10 进组后"是过期触发条件**：施工方案（08-31）与 rule-registry 的硬节点表仍钉着"09-10"，但 CLAUDE.md 自己记着"2026-09-03 用户统一改月底"——这次改口没有传导进门 4/R07 的任何判定文件，二者不一致，未澄清。

| | 零建设基线（现状） | A：挂 §13/WB-S | B1：纯文档 | B2：hub 加面板 |
|---|---|---|---|---|
| 今天点开看到什么 | TODO:479 一行 + PENDING 待拍板项 + 看板任务卡 | 无新增可点页面——产出是一份 markdown 提案 | 一份/一组 markdown 清单（内容整合卡今天已有） | `/hub` 多一块方块：几个数字+几个链接，点开还是跳回同一批 markdown |
| 验收怎么判 | 自己读、自己写 `[x]` | 专项验收模板（rubric/闭卷复述）渲染在任务页——这一层排在 WP5（P1），门 4→WP0→WP1/WP2→WP3→验收→WP4→四仓复核之后才轮到 | 人工判断，同基线 | 人工判断，同基线（`render.py` 只有转义文本+指标表+链接，无交互控件） |
| 24h 冷启动复测会不会自动提醒 | 不会，自己翻密封题 | **修正**：门类型枚举本身已随契约冻结批（§8-7）生效，只要 P0（WP0+WP1）落地，挂这个门类型的任务就会自动出现在收件箱——不需要等 WP5；WP5 只决定"学习任务专项验收模板长什么样"这一 UI 细节 | 不会 | 不会 |
| license 踩线会不会被自动挡 | 不会 | 有路径——`license` 字段预留"UNKNOWN 禁复制默认"，一旦清单被 generator 读取建卡即可插入提示；但这一环仍要等 generator 跑起来（P1） | 不会，只是文档里一句提醒文字 | 不会，同左 |
| 谁负责保持更新 | 自己手改 | generator 幂等重跑（S1 建成后），来源变了自动重生成 | 自己手改 | 自己手改（面板只转发现状链接，不做内容级解析） |
| 这周占不占配额 | 不占（已在跑） | 不占——research 线纸面提案 | 不占 | 占 infra line，受 R07 约束；"09-10 vs 月底"歧义未澄清前，这周是否合规不确定 |

**结论要点**："能不能自动提醒/自动判分"这件事 B1、B2 两条路径都给不了——不是暂时给不了，是 hub 面板（只读信号灯，无表单无判分逻辑）和纯文档的性质决定了给不了；这类能力依附的运行时（任务状态机、`accepted{open_gates}`、专项验收模板）是整个工作台的同一套机脚，单独在面板里复刻等于在防"第二个科研工作台"这条原则本身要防的事。反过来，"今天能点开看到东西"这件事 A 给不了——WP0 未合并主干，WP5 排在四仓复核之后的 P1 批次。**"现在就能看"与"以后自动记"两件事，当前没有一条路径能同时给；B 系列买前者，A 买后者且 A 的后者不用二次搬迁，B 系列的"以后"部分完全买不到。**

---

## 8. 仍然悬着的问题（本档不解决，留给下一步）

1. **Q1 本身**：工作台 v2（同一脊柱）vs 独立科研台，用户仍在权衡，未拍板；§5 的逻辑缺口（Q3 的"扩展"隐含锁定同一脊柱）也未被回应。
2. **"09-10 进组"vs"09-03 改口月底"的硬节点歧义**：影响 R07 配额档位（≤14 天=止损类窗；15-30 天=≤2 窗/周），进而影响 B2 这周是否合规——未澄清。
3. **E1 记录错误**（7×CC-BY/4×CC-BY-NC-SA 许可证分裂claim）未改正，仍挂在两份 09-01 文档里等一个不存在的确认。
4. **C2**（pengsida 零许可证约束）尚未写入任何正式决策/证据文档，只在本档和此前的对话回复中提及。
5. `progress/handoff/2026-09-04__research-system__window-handoff-snapshot.md` 相对 TASK-20260904-003 的产出（B2b、整合卡、B4 Amendment A1）已陈旧，未更新——若用户仍打算把该交接文档带去另一窗口，需要先补这块。

---

## 9. 本档对上一轮对比的自我纠错

上一轮口头对比中，我曾把"24h 冷启动复测的自动提醒"归入"要等 WP5 才有"——这是不准确的。核实 §14 拍板记录后确认：门类型枚举本身（含冷启动复测这一值）已经随 08-31 契约冻结批（TASK-20260901-001）批准生效，真正要等 P1/WP5 的只是"学习任务专项验收模板"这个 UI 呈现细节，门本身能不能响应与 WP5 无关。本档 §7 表格已按纠正后的事实重写，不沿用上一轮的说法。

---

## 10. 恢复入口 / 下一步选项

**恢复入口**：本档 + `2026-08-31__maintenance__task-unified-contract-v0-proposal.md` §13/§14 + `2026-09-01__maintenance__workbench-v2-skill-registry-design.md` + TODO:479-483 + `研体系三源整合_20260904.md`。

**下一步选项（供参考，未替用户选）**：
- A. 先解决 Q1（工作台 v2 同一脊柱 vs 独立科研台），再决定要不要写 §13 扩展提案；
- B. 先澄清"09-10 vs 月底"硬节点歧义，这会直接影响 B2 这周是否合规，可能反过来影响 Q1 的判断；
- C. 先处理 E1 记录错误（两份 09-01 文档的许可证分裂 claim）与 C2（pengsida 无许可证）的正式落盘，这两项与 Q1 选哪条路径无关，随时可以独立处理；
- D. 暂停本线，先做别的——本档已把当前所有已知信息钉住，随时可续。

**未做的事（本档只存档，不越权）**：未改动任何冻结文件；未写 TODO/PENDING/INDEX；未创建 task log；未 commit。

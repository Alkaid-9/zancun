# WB-1 两份独立交付对照表

2026-09-10 · 本文件不替用户选定哪份交付"算数"，只做事实对照。两份交付彼此不知道对方存在，
各自独立完成同一范围（Sheet1 + Sheet18，各选 3 段），结论互相印证但记录严谨度不同。

## 基本信息

| 维度 | `wb1-20260910-sol/` | `wb1-20260910-sol-xhigh/` |
|---|---|---|
| 完成时间 | 18:48 前后 | 19:04–19:07（晚约15分钟） |
| 执行身份自述 | 未在回执中单独说明具体模型/强度 | 回执明确写"会话只暴露 Codex，基于 GPT-5，未暴露可独立核验的具体 SKU"；并明确声明"目录名 sol-xhigh 不作为模型或强度证据" |
| 是否读取/依赖对方 | 无从判断（先完成，无对照对象） | 回执明确写"未读取或依赖既有 `wb1-20260910-sol/` 正文" |
| 产物完整性 | SOURCE_MAP.md / WALKTHROUGH.md / RECEIPT.md 三件齐全 | 同三件齐全 |
| 自评状态 | `complete`（文档包）/ `partial`（应用运行验收） | 同：`complete`（文档包）/ `not implemented`（应用接线） |

## 核心结论收敛点（两份独立得出、完全一致）

- Sheet18 的显示表名（Sheet18）与其底层 OOXML 关系不是同一数字：sheetId=18、rId12、实际文件 `xl/worksheets/sheet12.xml`，资产尾号为12——两份都强调"不能由显示名推断XML/ID"。
- Sheet1 存在一张真实嵌入图片（`image1.png`），Sheet18 在文件格式层面**没有**任何 drawing 关系，`A17`/`A45`/`A53`/`A72` 等"image.png"字样均为纯文字占位，不是可恢复的真图。
- 两个资产（`asset:workbook1:sheet:01`、`:12`）均为 `adapted=false`、`used=false`，指导库和 continue 记录均未引用。
- 三类语义必须分存：`L0/L1/L2`（领域尺度）≠ `global/topic/workspace`（操作入口）≠ 拖拽位置/目标归属/正式语义关系。
- AI 提案被接受时，Proposal 状态与正式 Relation 须同一事务保存；SQLite 迁移仍是待审建议，两份都未创建数据库、未迁移原件。
- 均未虚构学习效果、能力、PASS 或真实用户反馈；用户填写区均留空。

## 严谨度差异

| 维度 | `wb1-20260910-sol/` | `wb1-20260910-sol-xhigh/` |
|---|---|---|
| 单元格级核验方法 | 文档间比对叙述（原文↔导出↔导航逐项核对），未见独立重新解析原始 xlsx 的命令记录 | 独立用 Python 标准库重新解析 `workbook.xml` + `.rels`，抽查 42 个单元格与导出 JSON 逐一比较，全部 `MATCH=True`；回执附实际执行的 shell/Python 命令 |
| 对现有应用的实跑验证 | 无 | 实跑 `research_desk.py check-links`，返回 `ok=true, 0 errors, 0 warnings, 2432 assets`（回执明确注明"这只验当前导航数据，不等于新功能验收"） |
| git 状态自查 | 无 | 回执附 `git status --short --branch` 实际输出 |
| 对 DATA_CONTRACT 的补齐建议 | 走查末尾给出交互缺口的文字描述（未成表） | SOURCE_MAP.md §5 给出五项结构化补齐建议（精确SourceRef、融合阶段回执字段、尺度与入口分离、三动作分离交互、提案确认原子可见），并各自标注与现有契约哪一行对应 |
| 图片锚点区间问题 | 未单独提出 | 明确指出 Sheet1 图片目前"只存了锚点起点，未把完整 `Q26:AF65` 区间存成可回溯的 SourceRef"，本轮 DATA_CONTRACT.md 修订即采纳自此条 |
| 篇幅 | SOURCE_MAP.md 54 行 | SOURCE_MAP.md 106 行（约两倍） |

## 两份都遗漏的同一件事

均未登记进 `/mnt/d/MyResearch/CROSSWINDOW.md`——协议要求"开工先登记一行"，两个执行窗口都没有做。
本会话已按跨窗协议补登一行（标明"事后补登，非原执行窗口自登"），但补登不能替代原窗口自证身份；
两份回执里"实际模型/推理强度"这类字段本该由执行窗口自己填，现在都写着"未暴露/unknown"，
这是诚实的记录，不是缺陷。

## 未决问题（留给用户判断，本文件不擅自选边）

1. 是否两份都保留（作为交叉验证证据），还是选其一作为"正式"WB-1 交付、另一份归档？
2. `wb1-20260910-sol-xhigh/` SOURCE_MAP.md §5 的五项 DATA_CONTRACT 补齐建议，本轮只采纳了第1项（SourceRef 结构化）；
   其余四项（融合阶段回执字段、domain_scale/entry_kind 分离、三动作分离交互、提案确认原子可见）是否也要采纳，
   需要用户对施工范围表态，不在本次"只列对照表"的授权范围内处理。

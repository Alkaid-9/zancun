# WB-1 实际来源支持的页面操作走查

这是基于现有真实工作簿的文档走查，不冒充已运行的应用验收；没有用户实际作答，用户填写区保持空白。

## 场景：从 Sheet1!R6 进入论文工作区

1. 打开原表 `/mnt/d/Alkaid/Desktop/工作簿1.xlsx`，显示名选择 `Sheet1`，定位 `R6`。原文是：“hv-analysis [论文小主题]（纵向研究追时间深度，横横截面研究追同期广度，最后交汇出判断。）”。记录来源 `asset:workbook1:sheet:01` 与 cell `R6`。不要把 Sheet1 的数字和 XML 文件名混用。
2. 在 P2 建立专题草稿：题目暂写“结构摘要保留什么？”，保留来源视角，建立“纵向谱系”和“同期比较”两个操作入口。这里是操作入口，不等同于 L0/L1/L2 领域尺度；节点位置也不产生学术归属。
3. 进入 P3，保存一个来源引用和本次问题。原文区保留上面的原句；助手改编区可写：“先分别列出时间前后继承链与同年替代方案，再记录交汇处的判断条件。”这是助手改编，不是工作簿原文。
4. 用户填写区：

   - 本次实际论文/问题：
   - 纵向检索范围与结果：
   - 同期检索范围与结果：
   - 交汇判断、证据、未知：
   - 本次反馈：

5. 挂起工作区，分别保存 selected、collapsed、positions、resume_note 和来源引用。返回原位置时打开 `Sheet1!R6`，核对原文未被后来解释覆盖。位置、目标归属和正式语义关系分别记录；拖动本身不建立关系。

## 另一条方法引用：Sheet18!A78-A102

在同一 P3 工作区的指导侧栏打开 `asset:workbook1:sheet:12`，定位 `Sheet18!A78-A102`（实际 XML 为 `xl/worksheets/sheet12.xml`）。原文保留“核心创新点/背景/假设”的区分和 A98-A102 prompt；助手只能把它改成待核对的提问模板。用户填写区记录：实际论文、原文证据、AI 输出与原文不一致处、反馈和继续点。不能执行 prompt，不能写“已掌握”。

## 交互缺口

当前仓库没有本包授权的编辑/关系/3D实现，因此上述步骤可审但尚未运行验收。后续实现至少需要：精确 cell anchor（含 worksheet relationship）、原文与改编/反馈的分离、图片原位置可达性、挂起状态可恢复，以及提案接受时 proposal + decision + formal relation 的同事务保存。

Author: Fable 5 subagent (xhigh, Agent tool)
Date: 2026-09-03
Layer: EVIDENCE, not a decision

# EX-01 读前导航包（只导航，不解读）

## A4 入口（2026-09-04，优先于下方历史导航）

**给谁**：已完成 EX-00 核心题、准备做 EX-01 的学习者。
**用来做什么**：只解决“原文在哪一页、读到哪一行停、证据怎样记”；不提供 G0 五问的判断。
**不得做什么**：不得把本页的章节名、栏位推断或停靠问题抄成 G0 答案；不得在 G0 note 落盘前打开拆解包的结论段。

按以下次序走：

1. 不开 PDF，先在自己的 note 写三条 `PREDICTION`，并建表 `G0 问号 | 印刷页 | PDF 页 | 章节/算法行 | 证据标签 | 尚缺什么`。
2. 只用下方页码地图定位；PDF 是判定原件，TXT 只用于搜索关键词。页码或栏位对不上时，以 PDF 可见内容为准并记 `UNKNOWN`。
3. 读 Def. 1-4 时只完成事件、trace、直接跟随关系、权重、DFG 的定位；读 §IV.A-D 时按输入行、状态/更新行、输出行逐处停靠。
4. 每找到一处证据，只记录页码和最短必要原文；“原文直接写了什么”标 `PAPER-TEXT`，“我据此推出什么”另标 `USER-INFERENCE`。
5. 合上 PDF，脱稿写 G0 五问；在这一步完成前不打开 `REUSE_from_teardown-joint.md` 中任何要求 G0 后才读的材料。

下方 (a)-(c) 是更细坐标表。出现看似带判断的措辞时，只把它当“去哪里核”的搜索提示，不把它升级为答案。

**原件**：`research/papers_lu/EdgeIM-2025-ICWS.pdf`（7 页；印刷页 404–410 = PDF 物理页 1–7，`07_EDGEIM.md:3`）。
**定位辅助**：`research/papers_lu/teardown-joint-20260813/_launch/pdftxt/EDGEIM.txt`（双栏乱序抽取，只用于找位置，判定以 PDF 为准——v1.1 作业书 A1 第 2 条）。
**本包不含**：G0 五问任何一问的答案；bridge 卡的结论。第 1 站产出由你自己写进 `_scratch/seg0/G0_user_paper_note.md`。

## (a) 页码地图

来源：`07_EDGEIM.md` 的 `[PDF p.xxx]` 标注 + `EDGEIM.txt` 行号（分页符在 TXT:77/147/246/326/391/464）。**左右栏按 TXT 字符偏移推断，未在原件上复核。**

| 对象 | 印刷页 | PDF 页 | 栏 [推断] | TXT 行 | 07 卡标注 |
|---|---|---|---|---|---|
| Def 1（事件日志/迹）+ **十条四变体例子** L=[⟨x,a,b,c,e,y⟩², ⟨x,a,c,b,e,y⟩⁴, ⟨x,b,d,a,f,y⟩³, ⟨x,b,a,d,f,y⟩] | 405 | 2 | 右 | 100-105 | `:23`（例子本身 07 卡未抄） |
| 式(1) 投影 ↾Q | 405 | 2 | 右 | 106-110 | `:184` |
| Def 2 start/end | 405 | 2 | 右 | 112-117 | `:24` |
| **Def 3 直接跟随 + 权重式(2)(3) + 例子** | 405 | 2 | 右 | 118-146 | `:25-26`, `:185-186`, `:189` |
| Def 4 DFG | 406 | 3 | 左 | 147-152 | `:27` |
| **Fig.1** (a) 转移系统 (b) DFG 例（边 x→a 权重 6） | 406 | 3 | 左 | 155-198 | 07 卡无；`R2:151` 提 (a) |
| Def 5 Petri 网 + Fig.2 | 406 | 3 | 左 | 202-235 | `:28` |
| §IV.A 概览 + Fig.3 + Stage 1/2/3 列表 | 406→407 | 3→4 | 右→左 | 246-263 | `:71`, `:97` |
| **§IV.B Stage 1 + Algorithm 1** | 407 | 4 | 左 | 265-307 | `:101` |
| §IV.C Stage 2 + Algorithm 2 | 407 | 4 | 右 | 275-320 | `:126` |
| §IV.D Stage 3 + Algorithm 3（标题误印为 Feature-Preserving Sampling） | 408 | 5 | 左 | 326-390 | `:153-155` |
| §V.A 实验设置 | 408 | 5 | 右 | 326+ | `:195` |
| Table I 日志统计 | 409 | 6 | 上 | 391 | `:201` |
| Table II 质量 | 409 | 6 | 中 | 408 | `:215` |
| §V.C 时间 + Table III（无单位） | 409-410 | 6-7 | — | 439, 464 | `:229` |

## (b) 两小时读法（零基础，只读 Stage 1–3）

压缩自 `PAPER_ANALYSIS_SOP.md:117-124`（合上论文凭记忆写）、`paper-reading` skill Stage 2a/2b/4（技术理解→模型拆解→教/讲验证）、OS §10.6 七字段、OS §15 六问、`证据分层写作.md:11`（先立标签表再动笔）。

| 段 | 分钟 | 做什么 | 落笔 |
|---|---|---|---|
| 0 | 10 | **不翻论文。** 在 note 顶部先立四个标签：`PAPER-TEXT / USER-INFERENCE / AGENT-SUMMARY / UNKNOWN`；写"读前预测"：我猜 Stage 1 的输入是什么、输出是什么、有没有一个数字要我事先给它 | 七字段之③ |
| 1 | 20 | p.405 Def 1–3。用**论文自己的十条例子**手算 S、E、R 和每条边的 w；翻到 p.406 Fig.1(b) 对答案（x→a 应为 6） | 一张手算表 |
| 2 | 45 | p.407 左栏 Algorithm 1，一行一行。每行写：读了哪个变量 / 写了哪个变量。然后把十条例子按两种 CaseID 顺序各跑一遍 | G0 第 1、2 问 |
| 3 | 25 | p.407 右栏 Algorithm 2 只看**输入行**和 `+= 1` 那几行；p.408 Algorithm 3 只看**签名**和 merge 那几行。不试图看懂递归 | G0 第 3、4 问（允许 UNKNOWN） |
| 4 | 15 | 填七字段（OS §10.6 `:290-298`），每句打标签；用六问审 Stage 1 的那句"zero loss" | note 正文 |
| 5 | 5 | 合上 PDF，三到五句话向一个不懂过程挖掘的人讲 Stage 1（skill Stage 4） | note 末尾 |

## (c) 停靠点

读到这里就停，把问题写进 note 再往下：

1. p.405 Def 3 例子那句"(a,b) occurs twice, (a,c) occurs 4 times"——停：按我自己算的，对吗？（`07_EDGEIM.md:189` 有意见，先别看）
2. p.405 Def 3 之后 Def 4 之前——停：R 是集合还是带次数的东西？w 是 R 的一部分还是另一个函数？
3. p.407 §IV.B 第一段"sorted in a total order"——停：谁在排序？排序需要看见多少数据？
4. p.407 Algorithm 1 的 Input/Output 行——停：输入里有没有任何数字型参数？输出有几样东西？
5. Algorithm 1 的 if 判据行——停：这一行比较的是"有没有"还是"有几次"？三个条件之间是 and 还是 or？
6. Algorithm 1 保留后更新全局集合的那几行——停：更新用的是并集还是加法？
7. §IV.B 末尾"zero loss"那句——停：zero loss 是相对**谁**说的？相对哪一**层**（元素在不在 / 出现几次）？
8. Algorithm 1 之后的复杂度句——停：句中的 n 是哪份日志的事件数？
9. p.407 Algorithm 2 的 Input 行——停：进来的是哪份日志？
10. Algorithm 2 的 `+= 1` 行——停：这个计数是在哪份日志上数出来的？
11. p.408 Algorithm 3 的 `MineModel(...)` 签名——停：签名里传了 DFG，那 DFG 上的 w 后面有没有被读？找到证据写 PAPER-TEXT，找不到写 UNKNOWN，不要猜
12. 合上论文前——停：我刚才写的每一句，能指到页码吗？指不到的改标签

## 读后（不是本站要求）

G0 note 落盘后，再按 `REUSE_from_teardown-joint.md` 一节开密封件：先 `07_EDGEIM.md:122-123`，再 `bridge/EDGEIM_T2_bridge.md:25-27`，逐句对照你自己的标签。

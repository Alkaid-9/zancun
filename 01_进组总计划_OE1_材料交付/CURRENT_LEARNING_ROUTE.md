# 进组学习当前路由（证据对账版）

日期：2026-09-20。状态：`ROUTER / RECORDED-AT-EX-05 / CURRENT-EVIDENCE-UNVERIFIED`。

本页只把现有权威接成一个可执行入口，不修改题面、PASS、冷测或 Ledger。学习规则以[四论文 v3 合同](../02_四论文Ownership主线/00_共享合同_计划_验收/计划与决策/2026-09-13__research__four-paper-full-ownership-v3-sol-execution-contract.md)为准；EdgeIM 站序以[公开学习入口](../02_四论文Ownership主线/01_EdgeIM/学习与课程/START_HERE.md)为准。

> **09-20 原课程源对账结果**：见 [R1–R6 本人证据库存](R1_R6_EVIDENCE_INVENTORY_2026-09-20.md)。原仓存在 EX-01/02/03、EX-05a/05/06 的实质 WIP、本人作答片段和可复跑代码；09-12 checkpoint 还记录两个脚本退出码 0。与此同时，R1–R3/R5 的指定提交仍未找到，R4 仍缺正式 `SR/EC` 记录，R6 rubric 未冻结且无 EdgeIM Ledger PASS 行。下一份缺失交付是 R1，但不是让用户从 EX-01 重学。

## 1. 当前能确定什么

- 09-17 历史记录把位置记为 `EX-05`；原仓 09-12 checkpoint 把 EX-02/03 和 EX-05a 归为 `WIP`，明确 `EX-03 USER-REPORTED / NOT PASS`。本轮检查了文件和状态记录，但没有按冻结 rubric 重新验收，因此仍不是新 PASS 裁定。
- 原仓课程不是空白：EX-01 有 DFG/预测作答，EX-02/03 有学习记录，EX-05a/05 有代码。它们的来源混合程度、提示暴露和独立性各不相同，不能批量升级，也不能批量抹掉。
- 当前不是十五题 D2。D2 是四篇全文包和延迟复测后的最终诊断。
- 当前也不是直接打开 ownership-v3 全部模块。其 rubric 仍 `NOT-FROZEN`，释放须看前置和用户确认。
- 进组材料可以先诚实表达“正在学习”；只有“我掌握/实现/复现”的句子需要对应本人证据。

## 2. 下一动作：先做六项证据对账

这一步只确认“已有、缺失、已暴露、待验收”，不要求立刻重写答案，不读取密封参考。

| 项 | 查什么 | 对账输出 | 关闭条件 |
|---|---|---|---|
| R1 | EX-01 G0 五问 paper note | 先复用已有 DFG/预测 WIP；只找或补五问正文，记录本人/AI边界 | 本人五问可定位；来源/推断/未知分开；第 3 问标提示暴露 |
| R2 | C00/C01/C05a/C02 CONNECT | 每卡 `已有 / 缺失 / 已见答案` | 已有项可定位；缺失项排队；已见答案项换新变体，不倒签 |
| R3 | sigRank L-S1 一页对照 | `已有 / 缺失 / 未解锁` + 路径 | 前置 PASS 经复核，且 coverage/importance 等比较有本人判断 |
| R4 | EX-02 `SR-__` 与 EX-03 `EC-__` | 每项一条可定位记录或 `MISSING` | system reading 与 evidence closure 均有本人记录 |
| R5 | 已学站全文回接句 | 每站一句的路径，或列缺失站 | 每句说明该站如何改变对整篇 EdgeIM 的理解 |
| R6 | rubric、验收与 Ledger | 只报当前验收记录是否存在；不读密封答案、不倒填 | 由验收者按冻结 rubric 复核；只有真实结果才可写 Ledger |

最小提交格式：

```text
R1..R6：已有 / 缺失 / 未知 / 已暴露
证据：文件路径 + 标题（没有就写无）
贡献边界：本人完成 / AI辅助 / Agent先行 / 团队工作
请求：只核路径 / 按rubric复核 / 需要零基础教学
```

## 3. 对账后的分流

1. `已有但未验收`：提交路径和标题复核，不机械重做；脚本运行通过只记 scoped runtime evidence。
2. `指定交付缺失、但过程 WIP 已有`：只迁移或补最小缺口，不重写整站；按 R1→R6 的依赖一次只开一项。
3. `已暴露答案`：保留 exposure，换未见变体；旧答案最多支持 `SEEN`。
4. `基础概念不会`：先教学微例，再本人尝试；不因急用跳过。
5. 六项关闭后：回到记录中的 EX-05，并按 v3 依赖安排 B-S2→B-S3A→B-S3B、B-EVAL；进入 EX-07 最终 verdict/D1 前完成相应模块。

本次库存把 R1 定为下一份缺失交付，而非“当前唯一能做的事”。R2 的受控卡已确认存在于原课程源，但本次未读正文，必须按前置从原课程源授权释放；R3 已有助手内容暴露且解锁状态未知；R4/R5 等 R1 和对应前置明确后，从现有 WIP 提取最小记录，不并行重写整站。进组急用材料可并行做个人事实确认，但只能消费已经核实的证据状态。

## 4. 当前明确不做

- 不做十五题 D2，不把旧 `MASTERY_GATE` 的 09-06 游标恢复成当前游标。
- 不开研究实验，不从候选矩阵直接选题，不把 toy 计划写成结果。
- 不读取或导入密封答案来“补齐”证据。
- 不因课程资产 `BUILT/QA-PASS` 推出本人 `PASS/OWNED`。

## 5. 本路由的完成标准

- R1–R6 每项都有 `已有/缺失/未知/已暴露` 和可定位依据；
- 只生成下一项最小动作，不强制重做已有证据；
- 下一项的输入、本人产物、允许帮助和验收者清楚；
- 进组材料只消费已核事实与本人已确认的能力证据。

本页完成的是导航修复，不是学习完成。

# 付费墙三篇（SBPN / EdgeIM / MHP）教学考核 学员工作簿

> **状态：TEACHING-AWAITING-USER**
> 预制落盘 ≠ 完成④教学；全部 18 题与口试追问必须由用户本人在场完成。
> 本文件是学员唯一开卷入口。作答完成前不要打开 `answer_key_sealed.md`（该文件已密封）。
> 讲义 = 上级目录六张拆解卡（`../06_SBPN.md`、`../06b_SBPN_kg_critique.md`、`../07_EDGEIM.md`、`../07b_EDGEIM_kg_critique.md`、`../08_MHP.md`、`../08b_MHP_kg_critique.md`），它们是唯一事实源。
> 本包与旧四篇联卷包（`../teaching/`）互相独立：那边考 01–05，这边考 06/07/08，判分逻辑同源但互不解锁。

## 作答规则

1. 每章先读对应两张卡（主卡 + b 质疑卡），然后**关闭讲义**作答；手推题的计算过程必须闭卷独立完成。
2. 每题必须留下可评分 artifact（提纲、算式、表格均可）；只写"懂了"计 0 分。
3. 全卷 18 题，每题 5 分，共 90 分。每章 ≥24/30、手推 6 题合计 ≥24/30、总分 ≥76/90、红线错误 0 才算通过（红线清单见 `mistakes.md` R-P1…R-P10）。
4. 不确定处写"待核"，不按错误处罚；不要猜数字、作者、刊物、定理方向、精度/开销的相对大小。
5. 公式一律纯文本书写（如 Φ(x,y,z) = 1/(1+exp(ωa−ωb))）；卡内标注"推断/以原 PDF 为准"的内容，复述时必须保留该标注。
6. 卡壳时可向主考申请提示，按三级提示阶梯扣分（见 `EXAMINER_SOP.md` §3），不可自行翻卡。

---

## 第一章 SBPN（讲义：`../06_SBPN.md` + `../06b_SBPN_kg_critique.md`）

### 1.1 概念题（5 分）｜八元组与三步管线

回答以下五问：

① SBPN 八元组 = (D, P, beta, T, F, Pr, lambda, M)，逐元一句话说明；重点讲清：beta 的映射性质、lambda 的取值范围与符号语义、M 与经典 PN 初始标记 M0 的区别。
② AND 与 OR 在图上分别落在哪里？（提示：一个落在变迁的输入侧，一个落在库所的输入侧——说准确。）
③ 定义 1 三条附注中"协同效应生效的必要前提"是哪一条？它意味着协同改变的是什么、不改变什么？
④ 相对经典 PN 四元组 (P, T, F, M0)，SBPN 做了哪三个替换？为什么 SBPN 必须是 DAG？付出的代价是什么？
⑤ 三步结构挖掘管线：算法 1 / 算法 2 / 算法 3 各用什么统计工具、各产出什么？

```text
[作答]
```

### 1.2 概念·迁移题（5 分）｜SBPN(2024) → SBTPN(2026) 差分

SBPN 是 SBTPN 的前身。按 `06_SBPN.md` §9 的差分口径，回答：

① 形式元组怎么变？（八元组 → 九元组：哪个集合改名、新增哪两样、lambda 去了哪里）
② 数据对象与因果发现底座怎么变？推理方向怎么变（正向预测 → 什么）？
③ 协同判定量怎么变？（滤波函数的去留、阈值的显式化、候选集的收窄——各说准确）
④ 理论保证与复杂度报告在两篇之间发生了什么？（定理 1 的命运；复杂度分析的有无）
⑤ 列出至少两条"两篇不变的方法论 DNA"，并指出式(9) 的哪一个结构骨架被 SBTPN 原样继承。

```text
①
②
③
④
⑤
```

### 1.3 手推题（5 分）｜小 CPT 算 Φ 判 AND/OR

设定：z 严格由某二元门决定（无噪声），前提仅 x、y 两个；x、y 独立、各以 0.5 概率取 1；样本量充分大。观测得条件概率表：

```text
Pr(z=1 | x=0, y=0) = 0
Pr(z=1 | x=0, y=1) = 0
Pr(z=1 | x=1, y=0) = 0
Pr(z=1 | x=1, y=1) = 1
```

计算约定：互信息用自然对数；ln2 ≈ 0.693，exp(−ln2) = 0.5，exp(ln2) = 2。判定式：Φ(x,y,z) = 1/(1+exp(ωa−ωb))，ωa = I(y;z|x=0)，ωb = I(y;z|x=1)。

① 写出 I(y;z|x=0) 与 I(y;z|x=1) 的数值，并各用一句话说明依据（哪个条件下 z 恒定、哪个条件下互信息等于 y 的条件熵）。
② 算出 Φ(x,y,z) 与对称方向 Φ(y,x,z)，按双向阈值 ε = 0.5 判定 AND 还是 OR，并点名这一步用到定理 1 的哪个性质（单向还是充要）。
③ 若观测 CPT 换成 0, 1, 1, 1（即 x=0,y=1 与 x=1,y=0 时 z 也为 1），重算 Φ(x,y,z)，判定变成什么？
④ 复合闭包：设 Parents(G2, x7) = {x1,…,x6}，成对判定得 AND(G2, x7) = {{x1,x2}, {x2,x3}, {x1,x3}, {x4,x5}}，lmax = 3。写出升级与删子集后的 ANDOR(G2, x7)，并写出对应的推理规则组。
⑤ 一句话回答：真实数据上为什么不能直接查条件概率表判 AND？（用 `06_SBPN.md` 表 2 反例的机制作答：真值是什么门、表面像什么门、谁把脚印稀释了。）

```text
① I(y;z|x=0) =        I(y;z|x=1) =
② Φ(x,y,z) =          Φ(y,x,z) =        判定 =
③ Φ =                 判定 =
④ ANDOR(G2,x7) =      规则组 =
⑤
```

### 1.4 手推题（5 分）｜协同强度 Θ/Ψ 判定 + 式(8)(9) 推理合成

**(a) 协同判定。**规则变迁 t2：IF p2 THEN p6。已算得 e1 = Pr(p6=1 | p2=1) = 0.5；候选协同库所 p9 使 e2 = Pr(p6=1 | p2=1, p9=1) = 0.875。滤波参数 k = 2π/3。判定式：Θ(t,p) = 1/(1+exp(σa−σb))，Ψ(t,p) = 1/(1+exp(σb−σa))，σa = tan(k×(e1−0.5))，σb = tan(k×(e2−0.5))。本题阈值按 γ = δ = 0.7 记（原文只给区间 0.5<γ,δ<1，未给数值——作答时注明这一点）。

① 算 σa、σb（提示：tan(0) = 0，tan(π/4) = 1），再算 Θ(t2,p9) 与 Ψ(t2,p9)（exp(−1) ≈ 0.368，exp(1) ≈ 2.718），按阈值判催化还是抑制；顺手验证 Θ+Ψ = 多少，这说明催化/抑制强度实际是什么关系。
② 定理 2 是单向还是充要？这对"按阈值判协同"意味着什么精度缺口？用 D5-1000 上的哪一个数字佐证？

**(b) 推理合成。**库所 p6 有输入变迁 t1、t2，参数：泄漏项 Pr(p6=1 | t1=0, t2=0) = 0.1；r1 = Pr(p6=1 | 仅 t1 使能) = 0.5；r2 = Pr(p6=1 | 仅 t2 使能) = 0.6。t1 受抑制库所 p4（lambda(p4,t1) = −0.7，Pr(p4=1) = 1）与催化库所 p5（lambda(p5,t1) = +0.6，Pr(p5=1) = 1）作用。t1、t2 均使能。合成式：Pr(p6=1) = 1 − (1−泄漏) × (1−r1) × (1−r2)。

③ 算四种情形的 r1 修正值与合成值：Case A 无协同；Case B 仅 p4 抑制生效；Case C 仅 p5 催化生效；Case D 催化抑制并存（式(9)：逐协同修正后取算术平均）。
④ 给出四个合成值的大小排序，并对照 `06_SBPN.md` 油罐四案例说明这个排序形状说明了什么。
⑤ "取算术平均"这一设计决策的行为含义是什么？一个强抑制配多个弱催化会发生什么？原文对该选择做过消融吗？

```text
①(a) σa =    σb =    Θ =    Ψ =    判定 =    Θ+Ψ =
②
③ Case A =    Case B（r1' = ，合成 = ）   Case C（r1'' = ，合成 = ）   Case D（r1''' = ，合成 = ）
④
⑤
```

### 1.5 批判题（5 分）｜评测自洽循环：替作者辩护 + 补实验

b 卡（`06b`）最硬的质疑：**评估自证循环**——合成数据的生成机制与 SBPN 的建模假设同构，"真实世界结构"实验的数据仍由同一机制合成，泌尿数据的协同真值与式(7) 同义反复，推理实验的预测目标按"受协同影响"筛选。

假设你是作者，在返修信里回应这条质疑：

① 先诚实复述质疑（生成机制三处同构各是什么：OR 用什么生成、AND 用什么编码、协同怎么注入；再加"真实结构仍合成"与"真值同义反复"两点），不许弱化。
② 给出至少两条站得住的辩护（提示方向：bnlearn 为什么不可用、UCI 定性对照的存在、论文对 40% 误报与噪声-性能反向关系的诚实报告——每条要引卡内证据）。
③ 给出至少三条可执行的补实验方案，其中必须包含"负控实验"一条（在什么网络上测什么率）。
④ 一句话划出辩护的边界：哪一条批评你必须直接认账、不能辩？

```text
①
②
③
④
```

### 1.6 变体/反例题（5 分，Transfer 型）｜"协同挖掘普适论"驳斥 + 两条迁移

驳斥以下论断：

> "SBPN 的协同挖掘是普适机制——只要有数据，催化/抑制就能挖出来；把它搬到强一致执行的软件流水线日志上也照常工作。"

要求：

① 用"噪声是协同效应的前提"的机理驳斥：确定性规则下协同为什么在原理上不可观测？
② 给出卡内实验证据（哪个数据集全表最差、误报率多少、噪声维度上协同挖掘的方向性结论是什么）。
③ 指出定理 2 的单向性如何加重这个问题（阈值法只有哪个方向的依据）。
④ 迁移一：若必须在近确定性系统上挖协同，`06_SBPN.md` §6.3 给出的替代路线是什么？（把"协同 = 概率移动"改成什么？）
⑤ 迁移二：SBPN 架构在时序轴上的剩余空位是什么类型的数据？该空位对 MAS 轨迹意味着哪三组概念映射（防御机制、共谋、信任通道各对应 SBPN 的什么）？

```text
①
②
③
④
⑤
```

### 第一章自检

- [ ] 我没把协同弧当因果弧；记得候选协同库所必须与变迁无直接/间接因果连线（UP/RP）。
- [ ] 定理 1 = 充要（无噪双亲前提）、定理 2 = 单向，我没说反。
- [ ] 记得 ε=0.5、lmax=3、k=2π/3；γ/δ 正文只给区间没给数值。
- [ ] 我没把 SBPN（八元组、Information Sciences 2024）与 SBTPN（九元组、JAS 2026）的元素混装。
- [ ] 身份卡：王晓亮一作、鲁法明通讯、周孟初/曾庆田/包云霞、Information Sciences 680 (2024) 121019、代码数据仓库 github.com/netlearninglabs/SBPN（仓库只有数据无代码）。

---

## 第二章 EdgeIM（讲义：`../07_EDGEIM.md` + `../07b_EDGEIM_kg_critique.md`）

### 2.1 概念题（5 分）｜三阶段流水线与充分统计量恒等式

① 三阶段各做什么？每阶段一句话（Stage 1 排序+采样 / Stage 2 边缘局部特征 / Stage 3 中心合并+发现）。
② 写出整篇论文赖以成立的"立足恒等式"：(S, E, R) 三元组是什么的充分统计量？该恒等式的两个方向分别为哪两个阶段提供合法性？
③ EdgeAlpha 的三条明确缺陷是什么？EdgeIM 用哪三个设计一一回应？
④ EdgeIM 与标准 IM 的关键分叉点在哪里？（递归对象的差别；这对应文献中哪个变体；该分叉在表 II 上造成了什么样的 precision 偏移——几升几降？）

```text
①
②
③
④
```

### 2.2 概念题（5 分）｜覆盖采样判据与时间戳冲突处理

① 写出 Algorithm 1 的保留判据（原文的三条件析取形式），并说明第 1 行全排序的双重作用。
② "关键结构特征零丢失"的保证边界在哪一层？到不了哪一层？这个边界的后果是什么？
③ 时间戳冲突分支：t(ak) = t(ak+1) 时 Algorithm 2 做什么？双向对进的是哪个桶（按什么哈希）？这背后的洞见是什么（EdgeMiner 把冲突当什么、EdgeIM 当什么）？
④ 覆盖采样在噪声日志上的结构性缺陷是什么？（"带来新特征就保留"这一判据在噪声迹上如何精确反转？下游 IM-on-DFG 为什么接不住？）

```text
①
②
③
④
```

### 2.3 手推题（5 分）｜小日志走一遍三阶段

小日志（3 条 case；活动 a, b, c, e；括号内为时间戳）：

```text
case01: <(a,1), (b,2), (c,3), (e,4)>
case02: <(a,1), (b,2), (c,3), (e,4)>        （与 case01 完全相同）
case03: <(a,1), (c,2), (b,2), (e,3)>        （c、b 时间戳同为 2；全排序对同戳事件给出确定序：c 在 b 前）
```

设 k = 2 个边缘节点 N1、N2，哈希映射给定：活动 a、c → N1；活动 b、e → N2。

① Stage 1：按 case 序逐条判"保留/过滤"并给理由（引用保留判据），写出最终 D′ 与全局 (S, E, R)。
② Stage 2：对 D′ 逐事件走 Algorithm 2，写出 N1、N2 各自的局部三元组 (S_i, E_i, R_i)（R 带权重；注意 case03 中 (c,b) 同戳对的双向注入落在哪个桶）。
③ Stage 3：写出聚合后的全局 DFG 边表（带权），然后走 Algorithm 3：第一层检测什么模式、怎么切；第二层对中间块检测什么模式；最终 Petri 网的块结构是什么（用"顺序/并行/选择/循环"描述）。
④ 该模型中"b 与 c 并行"的证据来自哪两处？若这批日志的时间戳粒度只有"天"，Algorithm 2 会发生什么、模型质量会怎样？（用卡内"粒度悖论"的机制作答。）

```text
①
②
③
④
```

### 2.4 手推题（5 分）｜表 II/III 数字核算与主张对账

表 II 摘录（每格 fitness / precision / F-measure）：

```text
Sepsis:    IM = 1 / 0.0866 / 0.1594     EdgeIM = 1 / 0.1213 / 0.2166    AM = EdgeAlpha = 0.16 / 0.4272 / 0.2328
BPI13:     IM = 1 / 0.2672 / 0.4217     EdgeIM = 1 / 0.2837 / 0.4420
ICP.anon:  IM = 1 / 0.5489 / 0.7088     EdgeIM = 1 / 0.443  / 0.6140
exercise:  AM = EdgeAlpha = 1 / 0.6458 / 0.7848    EdgeIM = 1 / 0.3008 / 0.4625
```

表 III 摘录（原文未标时间单位；EdgeIM sum = sampling + discovery）：

```text
exercise:  EdgeAlpha 86.5827    EdgeIM sum 90.6906 = sampling 2.125 + discovery 88.5656
Sepsis:    EdgeAlpha 1957.3217  EdgeIM sum 174.2978
AllTrace:  EdgeAlpha 2013.5815  EdgeIM sum 21.0402
```

① 核算原文主张"对复杂日志（Sepsis 与 BPI2013Incidents），EdgeIM 的 precision 比 IM 高约 40%"：两个日志分别算增幅，判断该主张成色。
② 原文断言"Across all datasets, EdgeIM outperforms EdgeAlpha in total execution time"——用摘录数据找出反例，并指出原文自己两句之后写了什么。
③ 对 exercise 日志：把 EdgeIM 的采样开销全部扣除后与 EdgeAlpha 全程比较，原文"固定采样开销占比高"的解释站得住吗？这个反例真正说明了什么？
④ ICP.anon 上 EdgeIM 相对 IM 的 precision 变化方向与幅度是多少？原文怎么处理这一格？
⑤ EdgeIM 与 IM 在哪三个日志上三项指标逐位相同？这说明 EdgeIM 的质量增益从哪来、不从哪来？

```text
①
②
③
④
⑤
```

### 2.5 批判题（5 分）｜提速归因混淆：替作者辩护 + 补实验

b 卡（`07b`）最硬的质疑之一：**提速归因混淆**——表 III 是"EdgeIM（含采样）vs EdgeAlpha（无采样）"的非受控比较，加速无法在"采样剪数据"与"算法差异"之间归因；exercise 反例提示当采样失效时 EdgeIM 的 Stage 2–3 本身不比 EdgeAlpha 快；更简洁的替代解释是"EdgeIM ≈ 覆盖式采样 + 分布式 DF 计数 + IMd"，三个组件都不是本文发明。

假设你是作者，回应这条质疑：

① 先诚实复述质疑链（非受控比较缺哪两个对照；exercise 的两步核算证据；替代解释的三组件分解）。
② 给出至少两条站得住的辩护（提示方向：两个基线全灭日志上的产出能力；换 miner 底座本身是明示贡献；"过滤冗余"在 Insight 层面本来就是贡献的一部分——每条引卡内证据）。
③ 给出补实验清单（至少四项，必须覆盖：两个缺失对照、采样保留率报告、集中式端到端对比、k 扫描或通信账本任一）。
④ 划辩护边界：通信开销与隐私两条主张，你能辩吗？为什么？

```text
①
②
③
④
```

### 2.6 变体/反例题（5 分，Transfer 型）｜移植到多 agent 行为日志

场景：把 EdgeIM 移植为"多 agent 系统行为日志的分布式摘要与流程发现"——每台宿主上传本地 agent 行为的 (S, E, R) 摘要，中心聚合后挖掘交互流程模型。

① 写出概念映射至少四对（活动、case、时间戳冲突、边缘节点各映射到 MAS 的什么）。
② 指出覆盖采样在**对抗性** agent 场景下的致命缺陷：为什么"新颖轨迹全保留"在对抗场景比在普通噪声场景更危险？
③ 给出卡内 §6.3 的修复方向（新颖性与什么联合决策？两个具体机制是什么？）。
④ 判断题：EdgeIM 对 T2 课题（agent 轨迹 conformance）是竞争者还是基础设施？依据是它做了什么、没做哪三块？

```text
①
②
③
④
```

### 第二章自检

- [ ] 我记得 (S,E,R) 恒等式的保证只到集合层面、不到频次层面。
- [ ] 数字方向没错：+40% 只在 Sepsis 成立（BPI13 是 +6.2%）；exercise 是 EdgeIM 更慢的反例；ICP.anon 是 precision 下降。
- [ ] 我没把通信/隐私主张当成已测事实（单机模拟、k 未报告、零通信测量、隐私零机制）。
- [ ] 我记得 EdgeIM 代码未公开、数据脚注是 4TU 门户通用搜索链接（数字不可复现）。
- [ ] 身份卡：苏旋一作、刘聪二作（NOVA 双署名）、鲁法明通讯、ICWS 2025 pp.404–410（CCF-B 会议短文 7 页）。

---

## 第三章 MHP（讲义：`../08_MHP.md` + `../08b_MHP_kg_critique.md`）

### 3.1 概念题（5 分）｜STCFG、六属性、六场景与判定总则

① 列出六种开段场景 A–F（每个一短语）。
② 六个段属性集 F / J / FL / LF / L / IL 各记录什么？每个一句话（注意 FL 与 LF 的元素方向：谁是 (t,l)、谁是 (l,t)）。
③ 写出 MHP 判定总则（"排除式默认"），并说明这个默认方向的系统性代价：每一个未建模的同步原语（如 barrier、条件变量）会自动变成什么？
④ 语句对怎么由段对提升？multi-forked 线程的定义 1 两个条件是什么？它导致哪两个后果（线程内 HB、与自身的关系）？
⑤ 相对 TCFG 的四属性 + 一锁集，STCFG 的三点增强是什么？

```text
①
②
③
④
⑤
```

### 3.2 概念·迁移题（5 分）｜家族三方"分段"对照

`08_MHP.md` §9 给出了同名"分段"的三套坐标（03 分段 PN / 04 SegLock / 08 STCFG）。回答：

① 三条线在输入域、切分依据、并发判定引擎、判定目标四个维度上各是什么？（可列 4×3 小表。）
② lock-fork 耦合这一个祖传洞见在三篇里的三种形态分别是什么载体？
③ 本篇（08）真正独有的增量是什么？（哪个二分 + 哪个条款；三方中只有它处理了什么问题？）
④ "重模型 vs 轻模型"路线之争在静态侧怎么复刻？（03 之于 08 恰如哪两篇的关系？）
⑤ 本篇参考文献里的反常是什么？这对你复述"lock-fork 是组内祖传洞见"提出了什么表述纪律？

```text
①
②
③
④
⑤
```

### 3.3 手推题（5 分）｜3 线程小例：切段 → 属性 → 判 MHP

程序（一把锁 l；w(gX) 表示写全局变量 gX 的普通语句；五种建模语句之外均为普通语句；无循环、无分支）：

```text
tm:  lock(l); fork(t1); w(g1); fork(t2); unlock(l); w(g2); join(t1); join(t2)
t1:  lock(l); w(g3); unlock(l); w(g4)
t2:  w(g5)
```

① 切段：给出三个线程的段划分，每段标注触发场景（A–F）与所含写语句。
② 属性：写出含写语句各段的 L / FL / LF / F / J（含 Algorithm 2 第 22 行的 FL 下传；说明本例为什么**不**生成过程间锁 IL——差在哪个条件）。
③ 判定 (w(g1), w(g4))：逐规则检查，命中哪条？这一对正是哪类方法会误判为并发的展示对（对应运行例的哪种颜色标记）？
④ 判定 (w(g1), w(g3))、(w(g2), w(g3))、(w(g3), w(g5))、(w(g1), w(g5)) 四对，各注明命中/不命中的规则编号与最终结论（MHP 真/假）。
⑤ 对照运行例：DRB005（Figure 2a 程序）的真值是恰好 4 对并发修改对——列出这 4 对，并指出其中哪一对依赖 multi-forked 自并发、哪一对是基线因"t2 全体受 l 保护"的错误假设而漏掉的真并发。

```text
①
②
③
④
⑤
```

### 3.4 手推题（5 分）｜过程间锁与祖先例外（DRB006 型场景）

程序（一把锁 l；无循环、无分支）：

```text
tm:  lock(l); fork(t1); fork(t2); w(g1); join(t1); join(t2); unlock(l); w(g2)
t1:  w(g3)
t2:  w(g4)
```

① 按 Table 2 写出 tm 各段属性演化（重点：两次 fork 后的 F 与 LF；join(t1) 行对 F/J/LF 各做什么；unlock 行做什么）。
② 本例满足 IL 生成条件吗？写出条件本身与 IL(seg(w(g3)))、IL(seg(w(g4)))。（对照 3.3 题：那边为什么不生成？）
③ 判定 (w(g3), w(g4))：规则 2a 的祖先例外条款如何生效？最终结论是什么？这个结论的直觉语义是什么（伞防谁、不防谁）？
④ 判定 (w(g1), w(g3))：规则 2b 的祖先例外如何生效？再排查 HB 各规则（规则 5 为什么不触发、规则 6 为什么不触发），给最终结论与直觉语义。
⑤ 判定 (w(g2), w(g3))：命中哪条规则？再答追问：若 fork(t1) 在循环里（t1 成为 multi-forked），按规则 4 的字面守卫（只查哪一方？）判定会怎样？这暴露卡内指出的什么缺口？最后：本题场景对应 Table 3 哪一行，三方法数字各是什么，基线的失败机制是什么？

```text
①
②
③
④
⑤
```

### 3.5 批判题（5 分）｜自建靶场：替作者辩护 + 补实验

b 卡（`08b`）最硬的质疑：**真实负载零增益 + 基准自选**——STCFG 相对 RRG 的全部精度优势只出现在 8 个自研用例中的 3 个（恰好一一对应自家新机制）；在全部 4 个 SPLASH2 真实内核上与 RRG 逐值相同；SPLASH2 十二个程序只挑 4 个 kernel 且无一字解释；DRB 命名与 DataRaceBench 撞名；两个基线均为作者自行复现。

假设你是作者，回应这条质疑：

① 先诚实复述质疑（五个要点各一句，数字要准：真实内核四行的三方数字、优势三行是哪三行）。
② 给出至少三条站得住的辩护（提示方向：DRB006 修的是什么方向的错误、其价值为什么高于普通降误报；真实程序上剩下什么增益、数字是什么；论文哪两处措辞体现了自我声明的克制）。
③ 给出补实验清单（至少四项，必须覆盖：真实代码中两种锁模式的普遍性统计、有真值标注的外部基准、三机制消融、barrier 支持后的扩展评测）。
④ 划辩护边界：哪一条你必须认账？（提示：SPLASH2 假阳性归因的"all"为什么很可能过强——哪个未建模原语在这四个程序里是主要同步手段？）

```text
①
②
③
④
```

### 3.6 变体/反例题（5 分，Transfer 型）｜LF 并集缺口 + 组内缝合迁移

**(a) 反例。**Algorithm 1 的五个差量方向：F 取并集、J/L/FL 取差集、LF 取并集（与 F 联动）。

① 解释为什么"LF 取并集"与规则 6 的 HB 语义之间存在健全性缺口（may 方向的原料喂给了什么性质的结论？）。
② 按卡内骨架构造反例场景：双路径汇合、仅一条路径在持 l 期间 fork 了 t——沿另一条路径执行时会发生什么（虚构什么 → 漏报什么 → 下游漏检什么）？
③ 这个缺口与原文"选择性更新策略"的自我辩护（F 宁多加防漏报）形成什么张力？张力的根源是 LF 同时背负哪两种模态？卡内为这条批判标注的撤回条件是什么——为什么必须保留该标注？

**(b) 迁移。**

④ 把 MHP 基座喂给组内 03（UAF 分段 PN + 值流）：谁出什么、谁补谁的短板？（提示：08 自认的假阳性根源是什么，恰好是 03 的哪套能力覆盖的？）这条闭环目前是既成事实还是机会？表述时必须带哪个事实（互引状态）？

```text
①
②
③
④
```

### 第三章自检

- [ ] FL 与 LF 的元素方向没写反：FL 记 (t, l) = 线程创建后获取过锁；LF 记 (l, t) = 持锁期间 fork 了线程。
- [ ] 祖先例外条款方向没说反：伞只防外人、不防伞内（后代 vs 祖先自身、同伞后代之间不判冲突）。
- [ ] 我记得 SPLASH2 四内核上 STCFG 与 RRG 逐值相同（684/169/169/205），精度优势只在 DRB005/006/007。
- [ ] 我记得基线是作者复现、DRB 八个用例是自研（非 DataRaceBench）、"True 列确定方式全文未述"。
- [ ] 我没把"lock-fork 是组内祖传洞见"当论文内容复述（本篇零自引，家族联系是拆解外部知识）。
- [ ] 身份卡：鲁法明一作、王小宇二作（T1 接口人，横跨 04/08）、袁贵元+包云霞双通讯、CCPE 37(21-22) e70203, 2025。

---

## 提交区

```text
学员：
日期：
是否闭卷完成全部手推计算：
是否在作答前打开过 answer_key_sealed.md：
使用过的提示（题号 × 级别）：
最不确定的三个点：
1.
2.
3.
```

提交本文件后，主考按 `answer_key_sealed.md` 逐点判分并更新 `mistakes.md`。通过线：每章 ≥24/30、手推 6 题合计 ≥24/30、总分 ≥76/90、红线 0；之后进入口试追问（`EXAMINER_SOP.md` §6）。

# Mistake Log · lu-edgeim-algo1

> 只收可迁移的认知模型错误（OS §4C）。六字段。"看懂了解析"不算关闭。

## #1 · 把一遍过滤器当成了采样器 — `OPEN`

| 字段 | 内容 |
|---|---|
| 登记日期 | 2026-09-03 |
| 原来相信什么 | EdgeIM 的 coverage sampling 是一个可以在固定预算 K 下与随机采样对比的采样器；因此可以直接操作化成 fixed-K greedy sampler，用它的结果讨论 EdgeIM coverage 的性质 |
| 哪个验收暴露它错了 | 重读 `EDGEIM.txt:269-277` Algorithm 1 原文：伪码只有排序 + 初始化空集；判据是 `Si ⊆ S ∧ Ei ⊆ E ∧ Ri ⊆ R`，命中即滤、否则留。无 K、无 ranking、无 score、无 seed |
| 为什么错 | 混淆了"为了公平比较而外加的 matched-budget 抽象"和"论文方法本身"；没有先问原算法为什么不需要预算参数、补上这个参数后丢了什么语义（保留量内生、保留谁由 CaseID 序决定）。整条链上 A2 §9 已标"不足以授权代码"，但没有一步负责消费这条 flag |
| 正确模型 | EdgeIM Stage 1 是有排序和状态更新的 one-pass feature-preserving filter。要做 matched-K 比较，必须明确称之为额外的 surrogate/截断规则并单独论证，不能把 surrogate 的结果写成 EdgeIM 结果。"首次携带新非法边的 trace 必留"是判据的推论；"D′ 噪声占比高于随机"是道计数题，不是恒等式 |
| 下次识别信号 | 当出现"必须先给它补一条 X 规则/参数才能比较"的需求时，先问：原方法为什么没有这条规则？这是公平比较的外加抽象，还是已经换了研究对象？ |
| 复测 / 关闭条件 | 不看本文件与任何对话：空白纸写出 Algorithm 1 的输入、状态、保留判据、输出；对 EX-00 的 6 条 trace 两种顺序各预测一次保留集合并解释差异；说清 K 在哪里、为什么不在那里。= `EX-00` PASS。之后 D+2、D+7 冷启动复测各一次 |

**状态变更记录**：
- 2026-09-03 登记 OPEN（起草：Claude 主窗 + Sol 复审收窄"恒真"措辞；最后一格由用户在 EX-00 完成时亲自改写为 CLOSED）

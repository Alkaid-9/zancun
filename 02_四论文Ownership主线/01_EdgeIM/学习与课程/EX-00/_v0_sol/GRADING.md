# EX-00 批改 · Fable 主窗 · 2026-09-03 晚

**判定**：**NOT PASS YET**。差五个空，估计 20–30 分钟。Mistake Log #1 保持 OPEN，LEDGER 今晚不记。
**核对底稿**：重答-2（`README.md:146-149`）+ 脱稿（`README.md:171-178`）+ 第一轮任务 5-4（`README.md:115`）。
**原文重核**：Algorithm 1 伪码 `EDGEIM.txt:283-305`（双栏抽取，行号约）。

---

## 先认一个我的错

论文 Algorithm 1 第 12 行是 `return D′, S, E, R`——**两样都返回**。EX-00 第四部分我的转述只写了"输出 D′"，漏了 (S,E,R)。密封答案同处已订正，题面不动。
这不改变你该补什么（见下），但你写"(S,E,R)"不是凭空来的，有一半在论文里。

## 任务 5

| 问 | 判 | 说明 |
|---|---|---|
| 5-1 | ✓ | 4 条 vs 3 条；{1,2,3,6} vs {6,5,4}，只有 case6 两边都在；**最终 (S,E,R) 相同**。这句原样带进脱稿。 |
| 5-2 | ◐ | 对的部分：判据那一行本身不看顺序。没答的：顺序从哪一步进来；一个"不看顺序"的判据是怎么跑出"看顺序"的结果的。→ 追问 A |
| 5-3 | ✓ | "没有哪一步说要留几条""加了就不是论文的 Alg.1"——对。这两句就是 `MISTAKE_LOG.md` #1 最后一格要你亲手写的东西。你反问"加什么？"——不告诉你，跑变体 V1 自己看。 |
| 5-4 | ✓ | 第一轮已答对（case1 正序留 / 逆序滤，case4 反之，"因为 R 已经有了"）。欠一句话 → 追问 B |

## 脱稿

| 项 | 判 | 说明 |
|---|---|---|
| 输入 | ✓ | 日志 L：(case, activity, timestamp) 事件表，按 case 归成 trace。 |
| 状态 | ✗ | 你写的 `Si ⊆ S 且 Ei ⊆ E 且 Ri ⊆ R` 是**判据**——拿着状态问的那个是非题——不是状态。状态是跑到某条 case 之前，你必须写在纸角上、下一条来了要拿出来对照的东西。→ 追问 C |
| 更新 | ◐ | 你写"遍历的每一个 case 都 S ← S ∪ Si…"；论文把并入写在 `if` 里面（第 6–9 行），只有留下的才并入，而且第 6 行并入的那样东西你整份脱稿都没出现。→ 追问 D（这题比看起来有意思） |
| 判据 | ✓ | "含有当前集合里没有的 S、E、R 中的任意一项 → 留"。对。措辞收一下："至少一项"；三个条件之间是"或"。 |
| 终止 | ✓ | 扫完所有 trace。 |
| 输出 | ◐ | 论文返回两样，你写了一样；漏的那样是一个**过滤器**的主产物。追问：一个过滤器跑完，最要紧的输出是什么？ |
| 为什么没有 K | ✗ | "没提到 K、不需要别的参数"是**观察**，不是**原因**。→ 追问 E |
| 保留谁由什么决定 | ✓ | 同判据。 |

## 追问（只答这五个空，别重写整份）

**A（5-2 补）** 判据是个两边比较：左边是这条 case 自己的 (Si, Ei, Ri)，右边是什么？右边那个东西是谁写进去的、在什么时候写的？把 case4 在正序、逆序里各自面对的"右边"写出来比一下。然后回到四步伪码：哪一步的产物是一个**有序**列表？顺序就是从那一步进来的。
写成一句："顺序从第 __ 步进来；判据不看顺序，但判据的右边是由 __ 写出来的，所以 __。"

**B（5-4 补一句）** case1 和 case4 内容一模一样，所以决定它们命运的东西不在 trace 里面。在哪里？一句话。

**C（状态）** 跑完 case2、还没看 case3 的时候，你纸角上写着什么？全写出来——不止三样。那就是状态。

**D（更新）** 你的写法是每条 case 都并入，论文是只有留下的才并入。这两种写法跑出来的 D′ 和最终 (S,E,R) **一样吗？为什么？**（想清楚这题，判据就真懂了。）另外：第 6 行并入的是什么，它去了哪个集合？

**E（没有 K）** 跑之前我问你"这 6 条会留几条"，你答得出来吗？要先知道什么才答得出？那"留几条"这个数，对这个算法来说是**输入**还是**结果**？一句话写出来，就是"为什么没有 K"。

**V1（5-3 变体，不用交，跑一遍就知道"加了丢什么"）** 在第 3 步里加一句"D′ 里已有 2 条就停"。正序跑：留谁？跑完的 R 有几条，和全日志的 8 条比？

## 记账口径（诚实起见，不是罚）

- Sol 的批改（`README.md:243-251`）把**状态、更新、输出、没有 K** 四项的答案句子直接写出来了。按 BRIEF §3「开了再做不算 PASS，记 hint 级」：今晚五个空补对 → EX-00 记 **PASS（hint 级，四项，来源 Sol 批改）**；这四项真不真，看 BRIEF §5 本来就有的 **D+2（09-05）冷复测**：空白纸重写 + 5 条新 trace 预测。
- `MISTAKE_LOG.md` #1 的关闭条件写的是"不看本文件与任何对话"——Sol 那段是对话。所以 #1 的 CLOSED **等 D+2 干净过了再由你亲手改**，不是今晚。
- 第 1 站第 3 问 Sol 也给了强提示（`README.md:447-452`）。第 1 站 PASS 条件要求页码级 PAPER-TEXT，提示不替代翻 PDF；同记 hint。
- 五个空补完发我。判定 → 通过则 LEDGER 记一行（hint 列如上）。

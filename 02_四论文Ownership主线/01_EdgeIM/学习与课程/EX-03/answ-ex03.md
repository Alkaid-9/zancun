algo只能看到单个的


|  | 有新边 | 没有新边 |
|---|---|---|
| 合法 | ___q2___ | ___q1___ |
| 非法 | ___q4___ | __q3____ |


不能预测 L1 正式题里哪条 trace 会留，也不能估计真实日志的噪声比例。


EX-00的六条trace：
ABDF
ACDF
ABDEDF
ABDF
ACDEDF
ABCDF
        AB AC BD BC CD DE ED DF


a）：AB BE ED DF .BE新 不合法
（b）AB BD DE EF,EF新，不合法
（c）
(solver)
# alkaid @ YaoGuang in /mnt/d/MyResearch/MAS_Safety_Project on git:master x [7:28:29]
$ /home/alkaid/miniconda3/envs/solver/bin/python /mnt/d/MyResearch/MAS_Safety_Project/learning/training/lu-edgeim-algo1/
EX-05a/algo1.py
[('case1', ['A', 'B', 'D', 'F']), ('case2', ['A', 'C', 'D', 'F']), ('case3', ['A', 'B', 'D', 'E', 'D', 'F']), ('case6', ['A', 'B', 'C', 'D', 'F']), ('tst_a', ['A', 'B', 'E', 'D', 'F']), ('tst_b', ['A', 'B', 'D', 'E', 'F'])] {'A'} {'F'} {('B', 'E'), ('B', 'C'), ('B', 'D'), ('E', 'D'), ('A', 'C'), ('A', 'B'), ('E', 'F'), ('D', 'E'), ('C', 'D'), ('D', 'F')}
['case1', 'case2', 'case3', 'case6', 'tst_a', 'tst_b']
['tst_b', 'tst_a', 'case6', 'case5']
True
(solver)

（d）？有新边

——————————————————————————————————
非法trace只能带来至少一个新的S、E、R，就会被留下。同一个非法trace，到达时间和novelty状态不同，处理就不同
————————————————————————————————————————————
```
⟨x, a, b, c, e, y⟩ × 2
⟨x, a, c, b, e, y⟩ × 4
⟨x, b, d, a, f, y⟩ × 3
⟨x, b, a, d, f, y⟩ × 1
```
S = X
E = Y
R = xa, xb, ab, ac, ad, da, bd, ba, bc, be, af, cb, ce, df, fy, ey

(a) 试试 `⟨x, a, b, c, b, e, y⟩`（在 c 后面多插了一个 b）。写出它的 r，圈新边。有没有新边？
xa, ab, bc, cb, be, ey
没有……吗？等等我写一下
(solver)
# alkaid @ YaoGuang in /mnt/d/MyResearch/MAS_Safety_Project on git:master x [7:38:09]
$ /home/alkaid/miniconda3/envs/solver/bin/python /mnt/d/MyResearch/MAS_Safety_Project/learning/training/lu-edgeim-algo1/
EX-05a/algo1.py
[('3_1', ['X', 'A', 'B', 'C', 'E', 'Y']),
('3_2', ['X', 'A', 'C', 'B', 'E', 'Y']),
('3_3', ['X', 'B', 'D', 'A', 'F', 'Y']),
('3_4', ['X', 'B', 'A', 'D', 'F', 'Y'])]

{'X'} {'Y'}

{('C', 'E'), ('D', 'A'), ('X', 'B'), ('A', 'C'), ('X', 'A'), ('A', 'B'), ('B', 'C'), ('B', 'E'), ('A', 'D'), ('E', 'Y'), ('D', 'F'), ('B', 'D'), ('F', 'Y'), ('B', 'A'), ('A', 'F'), ('C', 'B')}

['3_1', '3_2', '3_3', '3_4']
['tst_c', '3_4', '3_3', '3_2', '3_1']
True







(b) 这条 trace 合法吗？（四种变体里没有一条长 7；按"流程"的常识，b 做了两次。）

emmmm？不合法

(c) 把它追加到日志末尾跑 Algorithm 1。留了吗？
不留
逆序有
(d) 一句话：任务 1(d) 的那个条件，反过来说是什么？——什么样的非法 trace 是 Algorithm 1 **看不见**的？

如果一个非法trace的s e r都已经出现过了，那algo1就看不见了，会直接过滤掉

————————————————————————————
如果一个非法trace的Si Ei Ri全部出现过，algo1无法将它所观察到的representation中区分
——————————————————————————————————



### 任务 3 · |D′| 的上界（30 分钟，写成一页推导）

问：不管什么日志、不管什么顺序，Algorithm 1 最多能留多少条？



引导（按顺序答，每步一两句）：

1. 每留一条 trace，S、E、R 三个集合里**至少**多一个元素。为什么？（回看判据）

至少增加一个啊，至少有一个新的，这条trace才会被留下来

2. S、E、R 只增不减。跑完之后它们各有多少个元素？记作 |S|、|E|、|R|


3. 所以留的条数 ≤ ______。写出不等式并说明理由

——————————————————————
！！
一共有多少个“第一次出现”？
|S|、|E|、|R|
|D'| ≤ |S|+|E|+|R|
又
|D'| ≤ |L|

|D'| ≤ min（|L|，|S|+|E|+|R|）

——————————————————————

4. 在 EX-00 日志上，这个上界是多少？实际留了几条（正序 / 逆序）？

实际上是四、三

5. 在论文的十条例子上，上界是多少？实际留了几条？


6. 一句话：这个上界跟日志有多少条 trace（|L|）有关系吗？这意味着日志再大 100 倍，|D′| 最多变多大？
（第 2 站追问里提过 REUSE 表第 10 项有一个更紧的上界，做完这站再开。）

上界与|L|有关系吗？
|S|+|E|+|R|的上界不直接由trace数决定。
只有distinct feature universe变大，feature bound才变大



### 任务 4 · 用论文的例子手算频次塌平（40 分钟）

还是那十条。按论文列出的顺序（先 2 条变体 1、再 4 条变体 2、再 3 条变体 3、最后 1 条变体 4）：

(a) 正序跑 Algorithm 1，写出留了哪几条（用 σ1…σ10 编号）

正序跑能留下四条不同的
1、3、7、10


(b) 列一张 16 行的表：`边 | D 里的 w | D′ 里的 w`。D 里的 w 就是 Def 3 的公式 (2)，也就是 EX-01 的数法。自检：D 的总和 = 事件总数 − 10；D′ 同理

捏？这里算法怎么写？


边 | D 里的 w | D′ 里的 w

xa	6	2
ab	2	1
bc	2	1
ce	2	1
ey	6	2
ac	4	1
cb	4	1
be	4	1
xb	4	2
bd	3	1
da	3	1
af	3	1
fy	4	2
ba	1	1
ad	1	1
df	1	1

totle 50  20




(c) 翻 p.406 Fig. 1(b)，找 x→a 那条边的数字。跟你 D 表里的对上了吗？D′ 表里它变成几？
D'

6-->2

(d) 顺便核一下 p.405 Def 3 后面那个小例子（`⟨x,a,b,c,e,y⟩², ⟨x,a,c,b,e,y⟩², ⟨x,a,b⟩`）：原文说 (a,b) 出现 2 次、(a,c) 出现 4 次。你数一遍。数对了记 `PAPER-TEXT 有误`，这是 EX-01 停靠点 1 的答案，你现在自己有了

ab两次，但
？ac怎么出现4次的
会不会是我们整错了话说，是不是哪里漏看了？？

(e) 一句话：过滤之后 16 条边的 w 分布长什么样？"哪条边常见、哪条边罕见"这个信息还剩多少？
长上边的表格那样，常见和罕见的区别被大幅损失掉了
------------------
对，我刚把 **EdgeIM 原文和公式重新核了一遍**。这里不是我们漏看了，**论文这句确实算错了，而且不只 `a→c` 一个错。**

Def. 3 明确定义 \(w(a,b)\) 是在整个 event log 中逐条 trace、逐个相邻位置累计 occurrence count；紧接着的小例子确实写的是

$$
L=[\langle x,a,b,c,e,y\rangle^2,\;
\langle x,a,c,b,e,y\rangle^2,\;
\langle x,a,b\rangle]
$$

然后原文却声称 `(a,b)` 出现 2 次、`(a,c)` 出现 4 次。

我们按它**自己的公式**重新数：

$$
\langle x,a,b,c,e,y\rangle^2
$$

两条，每条有一次 `a→b`：

$$
+2\;ab
$$

$$
\langle x,a,c,b,e,y\rangle^2
$$

两条，每条有一次 `a→c`：

$$
+2\;ac
$$

最后还有：

$$
\langle x,a,b\rangle
$$

又贡献一次：

$$
+1\;ab
$$

所以正确答案其实是：

$$
\boxed{w(a,b)=3}
$$

$$
\boxed{w(a,c)=2}
$$

不是论文写的：

$$
2,\;4
$$

而且我们还有一个交叉验证：论文的大十条日志确实是第一种变体 ×2、第二种 ×4、第三种 ×3、第四种 ×1。 Fig. 1 又明确说那个大日志的 `x→a` 权重是 6，说明他们确实是按 multiplicity 算的，并不存在什么“指数不是次数”的特殊解释。

所以这里可以正式记：

> **PAPER-TEXT 有误：Def. 3 示例按公式应为 \(w(a,b)=3,\;w(a,c)=2\)，原文写成 2 和 4。**

甚至能再往前推一个很像的编辑痕迹——这是**推断，不是论文明说**：`2,4` 恰好就是前面主例中第一、第二 variant 的 multiplicity。很可能作者改成这个小例子时，把第二项从 ×4 改成 ×2、又新增了 `<x,a,b>`，但后面的文字 `2,4` 没同步改。这个解释很自然，但只能标成 reconstruction。

你 `(c)` 也对，不过写完整最好是：

> Fig. 1(b) 的 `x→a` 为 **6**，与原始 \(D\) 的手算一致；过滤后 \(D'\) 中变成 **2**。

论文自己也明确写 Fig. 1 的 `x→a` 权重为 6。

至于 `(e)`，你说：

> 常见和罕见的区别被大幅损失掉了

**判断正确。** 但为了过这一站，我会要求你再精确半层，因为不是“所有频次信息都没了”。

原始 \(D\) 有：

$$
w\in\{1,2,3,4,6\}
$$

过滤以后：

$$
w'\in\{1,2\}
$$

于是原来：

$$
6,\;4,\;3,\;2,\;1
$$

这么多层频率差异大量塌成了：

$$
2,\;1
$$

比如：

$$
ac:4\to1,\quad bd:3\to1,\quad ab:2\to1,\quad ba:1\to1
$$

原来四种完全不同的频率，现在全都变成 **1**。

所以更好的 `(e)` 一句话是：

> **过滤保持了 16 种 directly-follows relation 的存在性，但大量边权被压平为 1 或 2，原日志中常见/罕见关系的相对频率、比例和排序大量丢失；\(D'\) 的 weight 更反映“被保留代表 trace 中出现多少次”，而不是原日志中的真实行为频率。**

这里其实已经把 EX-03 最重要的一刀砍出来了：

$$
\boxed{\text{structure-preserving}\neq\text{frequency-preserving}}
$$

甚至再精确：

$$
\boxed{R(D')=R(D)}
$$

但一般：

$$
\boxed{w_{D'}\neq w_D}
$$

而且没有简单的统一比例：

$$
6\to2,\quad4\to1,\quad3\to1,\quad2\to1,\quad1\to1
$$

这就是为什么不能拿 \(D'\) 的 raw weight 当原日志真实频率。

**任务 4 到这里可以算完成。** 现在正好进这站最后的脱稿验收，你别翻前面，直接空白回答这三句：

1. “现有字母表插非法位置首次必留”——**什么条件下对？给一个反例说明为什么不总对。**
2. \(|D'|\le\underline{\qquad}\)，**理由一句。**
3. 十条例子过滤后 `x→a` 从 **__ → __**；\(D'\) 上的 \(w\) **反映什么，不反映什么？**

这三句过了，我们再开 C03，不提前偷答案。


----------------

- "现有字母表插非法位置首次必留"——这句话对的条件是什么、不对的例子是什么，各一句
前提是SER中有一个是新的，是新的就会留。
不对的是非法、合法的都会留下来

- |D′| ≤ ______，理由一句
min(|L|,|S|+|E|+|R|)。理论最松上界+主要约束变量的理论上界，超过就一定是错的

- 十条例子过滤后 x→a 的 w 从 __ 变 __；这说明 D′ 上的 w 反映的是什么、不反映什么
从D'上 w 不再反应该cut在原日志的trace中出现多少次的真实行为频率，而是过滤之后的日志中出现多少次


——————————————————————————————————————

（）对不起。是我偷懒了，现在改。

纠正后：

第一问：

**插入后必须产生至少一条当前 \(R\) 中没有的新 directly-follows relation，才会必留。 你后半句“非法、合法的都会留下来”**
反例：
\[
\langle x,a,b,c,b,e,y\rangle
\]
非法，但它的 \(S,E,R\) 全都已经出现过，所以 Algorithm 1 看不见，追加到末尾会被过滤。这个例子正好推翻“非法插入首次必留”。



第二问：

\[
\boxed{|D'|\le \min(|L|,\ |S|+|E|+|R|)}
\]

这个式子可以。理由最好别写“理论最松上界+主要约束变量”这种偏元语言，改成【**直接证明**】：

> 每保留一条 trace，都至少首次引入一个新的 \(S/E/R\) 元素；这样的元素总共至多 \(|S|+|E|+|R|\) 个，同时显然也不可能保留超过 \(|L|\) 条。


\[
\boxed{x\to a:\ 6\rightarrow2}
\]

然后你的解释方向是对的，但可以再精确一点：

> \(D'\) 上的 \(w\) 反映的是被保留的 representative traces 中某条边出现了多少次，不再直接反映该边在原始日志 \(D\) 中的真实频率、比例或常见/罕见排序。


即：

1. **条件：**现有字母表里插入非法位置，若因此产生至少一个此前未见的 \(S/E/R\) 元素形成新边，则会被留。
反例：x a b c b e y 非法，但其 \(S,E,R\) 都已出现，所以会被过滤。


2. |D'|\le \min(|L|,\ |S|+|E|+|R|) 因为每条被保留 trace 至少首次贡献一个新的 \(S/E/R\) 元素，而这样的元素总数有限


3. x→a 的 \(w\) 从 6 变 2；\(D'\) 上的 \(w\) 反映过滤后**保留样本中的出现次数**，**不反映原日志中的真实行为频率分布**。

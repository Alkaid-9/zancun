思考了 1m 31s

对，你现在问到的其实是这篇里最值得偷的设计动作了。它真正漂亮的地方不是“写了个 if”，而是先把问题改写成：

> 我到底需要从完整日志 \(D\) 里保住什么，才能让后面的模型发现继续工作？



然后才有 Algorithm 1。

先把一个特别关键的地方钉死：EdgeIM 并没有把被删掉的原始日志“还原回来”。它做的是另一件事——先选定一个作者认为关键的结构投影

\[
F(D)=(S,E,R)
\]

也就是起点类型、终点类型、直接跟随关系的存在性，然后保证这个投影在 \(D\to D'\) 后完全一样；至于原始日志里“每种行为发生了多少次”，并没有被这个 zero-loss 保证覆盖。论文自己把 Stage 1 描述成 dynamic feature coverage，明确说保的是 key process structure features；Algorithm 1 就是检查 \(S_i,E_i,R_i\) 是否已经被全局集合覆盖。

这个保证为什么这么简单又这么硬？

你可以把整份日志想成一张布尔覆盖表。

每一行是一条 trace，每一列不是“第几条 trace”，而是某个特征：

trace	start=A	end=F	A→B	A→C	B→D	B→C	…

case1	1	1	1	0	1	0	…
case2	1	1	0	1	0	0	…
case3	1	1	1	0	1	0	…


Algorithm 1 干的事情几乎就是：

> 扫每一行；只要这行有至少一个以前没亮过的列，就保留。否则整行删掉。



于是最终所有“亮过的列”必然一列不少。

写得再抽象一点。设一条 trace 的特征为

\[
F(\sigma)=\{\text{start}\}\cup\{\text{end}\}\cup R_\sigma
\]

当前已经保留的特征集合为 \(U\)。

如果：

\[
F(\sigma)\not\subseteq U
\]

说明它带来至少一个新东西，留下，然后：

\[
U\leftarrow U\cup F(\sigma)
\]

反过来，如果：

\[
F(\sigma)\subseteq U
\]

那就算你把这条 trace 扔了，

\[
U\cup F(\sigma)=U
\]

什么都没变。

所以它的 zero-loss 根本不是靠实验观察“好像没掉”，而是直接长在这个更新规则里面。论文 Algorithm 1 正是这个判据：新 start、新 end、或至少一条新 directly-follows relation，任意一种出现就保留。

这里还有一个我觉得特别值得你记的更深层解释：

> 它利用的是集合并集的幂等性。



因为：

\[
x\cup x=x
\]

一个特征你见过 100 遍，在“有没有”这个问题上跟见过 1 遍完全一样。

但是频次用的是加法：

\[
1+1\neq1
\]

所以一旦你的问题从

> “B→C 有没有出现？”



换成

> “B→C 出现了多少次？”



刚才那套删除逻辑立刻不再 lossless。

这就是为什么你刚刚算出了：

\[
1:6\rightarrow1:4\rightarrow1:3
\]

集合层是幂等聚合，计数层是非幂等聚合。

我觉得这比单纯记“R 不变，weight 变了”值钱多了。以后你看到一种数据压缩方法，可以直接问：

> 它依赖的 aggregation operator 是不是 idempotent？
如果不是，删掉重复 observation 以后拿什么补偿？



这个问题相当可迁移。


---

那它后面到底怎么从 \(D'\) 继续搞？

这篇的三阶段现在就能串起来了。

Stage 1 不是最终模型，它只是把原始 trace 大量压掉，得到 \(D'\) 和结构集合 \(S,E,R\)。

到了 Stage 2，作者才开始真正构建带权的局部 DFG。活动通过 hash 被分给不同 edge node，然后系统遍历的是 filtered \(D'\)；每看到一条相邻关系，对对应的 \(R_i(a,b)\) 做 +1。如果两个连续事件 timestamp 相同，还会把两个方向都加进去，用来保留潜在并发关系。也就是说，权重是在 \(D'\) 上重新数出来的，不是把原始 \(D\) 的计数藏在 Stage 1 里带过去。

到了 Stage 3，中心节点把各边缘节点的：

\[
S_i,\ E_i,\ R_i
\]

聚回来。\(S,E\) 用并集，局部 DFG 的边权则相加：

\[
w(a,b)=\sum_i R_i(a,b)
\]

然后对这个 global DFG 做类似 Inductive Miner 的递归分解：识别 sequence、parallel、choice、loop，把 activity set 分区，对每个 sub-DFG 递归，然后把 subnet 合成 Petri net。

所以流程其实是：

\[
\boxed{
D
\xrightarrow[\text{保 support}]{\text{Stage 1}}
D'
\xrightarrow[\text{在 }D'\text{ 上计数}]{\text{Stage 2}}
\text{weighted local DFGs}
\xrightarrow{\text{Stage 3}}
\text{global DFG}
\xrightarrow{\text{recursive IM}}
\text{Petri net}
}
\]

注意这里特别容易误解的一点：

它“还原”的是过程结构模型，不是原始日志。

它不是：

\[
D'\to D
\]

而是：

\[
D'\to\text{DFG}\to\text{Petri net}
\]

论文的 Stage 3 明确就是聚合 DFG 后递归检测四种基本模式并生成过程模型。


---

那原来被删掉的次数还能还原吗？

不能，从 \(D'\) 本身不能。

这是信息论意义上的不能，不是“作者没写恢复算法”。

拿最简单的例子：

\[
D_1=1\times\langle A,B,D,F\rangle
\]

和

\[
D_2=10001\times\langle A,B,D,F\rangle
\]

Algorithm 1 最后都可能只保：

\[
D'=\{\langle A,B,D,F\rangle\}
\]

只看这个 \(D'\)，你根本没办法知道原来是 1 条还是 10001 条。

也就是说：

\[
D_1\neq D_2
\]

却有可能：

\[
T(D_1)=T(D_2)=D'
\]

映射是 many-to-one，没有逆函数。

而且就算给你原始 DFG 的所有 edge weights，也仍然通常不能唯一还原 trace multiset，因为不同 trace 组合可以产生相同的边计数。DFG 本身就是日志的一个投影。

所以如果你问：

> “它先压掉，后面又怎么把原始信息完整恢复？”



答案就是：

> 它没有，也做不到；它只声明某一类信息不需要恢复，因为那一类信息从来没有丢。



这就是“lossless relative to what?”为什么这么重要。


---

但这也立刻暴露出一个很自然的改法

你刚才已经自己走到一个挺像 research idea 的地方了：

> 既然结构类型用集合保，次数又是可计量的，那为什么不把两者分开？



完全可以设计成：

\[
\text{witness set} + \text{sufficient statistics}
\]

一边继续用 Algorithm 1 保一个很小的 \(D'\)，专门充当“每种结构至少有一个 witness”的代表集；

另一边扫描完整 \(D\) 时，不管 trace 最后留不留，都更新：

\[
C[a,b]\leftarrow C[a,b]+c_\sigma(a,b)
\]

最后只需要保存：

哪些 start/end/edge 存在；

每条 edge 的原始总 count；

一个很小的 witness trace set。


这样你不需要保存巨大 \(D\)，却能精确保住原始 weighted DFG。

甚至可以写成：

\[
\boxed{\text{structure witnesses }D' \quad+\quad \text{global counters }w_D}
\]

这不是论文的方法——这是从论文机制反推出来的一个自然扩展。论文实际 Stage 2 是在 \(D'\) 上 +=1。

而且这里还有个细节：单纯给“保留下来的 trace”加一个 multiplicity 权重未必够，因为一条被滤掉的 trace 可能不是某一条 retained trace 的重复；它的不同 edge 可能分别被好几条 retained trace 覆盖。所以如果目标是恢复 DFG frequency，更自然的是per-edge counter，而不是 per-trace multiplicity。

这个 distinction 很漂亮。


---

作者可能是怎么想到 Algorithm 1 的？

这一段我要明确标成反向 genealogy 推断，不是论文作者自述。论文没有说“我们受到 set cover / test reduction 的启发”，也没有在 Algorithm 1 旁边给一个这样的来源引用。

但从论文自己给出的“压力场”，很容易反推出一条非常自然的思路链：

原问题是 IoT 边缘环境里日志巨大，集中传输和处理的 storage / computation / communication overhead 很高；他们又希望最后能用 Inductive Miner 式的 DFG 递归发现复杂结构。论文 Introduction 和 Overview 都明确把“减少冗余数据、保关键结构、最后从 global DFG 做 IM 式递归分解”串在一起。

于是很容易产生这个问题：

> 如果后面主要吃的是 start/end/directly-follows 这些结构特征，那为什么我要把 10000 条提供完全相同结构证据的 case 全送下去？



然后进一步：

> 一条 case 到底有没有“新增信息”？



最简单的定义就是：

\[
\Delta F_i=F(\sigma_i)\setminus U
\]

若：

\[
\Delta F_i=\varnothing
\]

它对当前结构集合没有任何增量。

那就删。

所以我会把这篇的 idea genealogy 重建成：

\[
\text{大日志通信贵}
\rightarrow
\text{下游真正依赖什么表示？}
\rightarrow
(S,E,R)
\rightarrow
\text{哪些 trace 对这些特征是冗余 witness？}
\rightarrow
\text{只留首次带来新 feature 的 trace}
\]

这就是一个非常典型的：

> 先找 downstream-relevant invariant，再围绕 invariant 做压缩。



而不是先说：

> “我要 sample 20% 的日志。”



这也解释了它为什么没有 \(K\)。


---

跟什么思路特别像？

最像的其实未必是“sampling”。

我第一反应反而是测试集约简 / coverage preservation。

想象软件测试：

一条 test case = 一条 trace；

一条程序 branch / transition = 一个 \(a\to b\)；

目标不是保留所有 test；

目标是保留一个更小 test suite，但 branch/transition coverage 一项不少。


那算法最朴素的版本就是：

> 这个测试有没有覆盖以前没覆盖的 branch？
有 → 留。
没有 → 可以删。



这跟 EdgeIM Algorithm 1 几乎是同一个认知动作。

再往抽象层走，它还和几个东西同族：set cover / coverage greedy 是“用少量集合覆盖 universe”，不过 EdgeIM 不是先知道全集后求近似最小 set cover，而是按固定顺序 one-pass 留新特征 witness；coreset / sufficient statistics 是“不要保完整数据，只保对下游 query/objective 足够的信息”，但 EdgeIM 只对 feature support exact，不是对所有 downstream objective exact；形式化验证里的 abstraction / quotient 也是“扔掉 concrete detail，但保证某个关心的 property/invariant 不丢”；graph sparsification 也是压掉巨大原对象、保某类 graph property，不过通常保的是 cut/spectrum 的近似，不是 EdgeIM 这种 exact edge-support coverage。

所以它们真正共享的母题是：

\[
\boxed{
\text{Compress the object, preserve the invariant that the consumer actually needs.}
}
\]

但 EdgeIM 现在最值得你警觉的也正是下一问：

> consumer 真只需要这个 invariant 吗？



如果后面的算法对 frequency / ratio / threshold 敏感，那么：

\[
(S,E,R)\text{ support preserved}
\]

并不能推出：

\[
\text{downstream behavior preserved}
\]

你现在算出来的 \(1:6\to1:4\to1:3\) 已经是在给这个问题埋雷了。

所以这篇如果拿来学“作者怎么想到”，我觉得最值钱的不是 Algorithm 1 这十行，而是这一个研究动作：

> 先问昂贵原始对象里，哪一层信息是下游真正需要的；找到一个可证明保持的投影，再围绕这个投影删冗余。



然后紧跟一个审稿人式的问题：

> 你证明保持的是不是恰好就是下游真正使用的全部信息？



前一句能生 idea。

后一句能防止 idea 把自己骗死。（）

思考了 16s

啊，这句我前面压得太狠了。你可以先完全忘掉 Petri 网，把它理解成：

> 拿到一张大 DFG 后，不直接一口气“画模型”，而是像拆表达式一样，先判断最外层是什么结构，把图切成几块；每一块再继续判断、继续切；切到只剩单个活动后，再按刚才识别出的结构一层层拼回去。



这就是所谓“递归分解”。论文原文也是这个顺序：DETECTBASEPATTERN → SPLITDFG → 每个 partition 取 SUBGRAPH → 对 subgraph 再 MINEMODEL → 最后 MERGESUBNETS。

用一个新的小例子看，不用你现在练习那份日志

假设 global DFG 是：

B
      ↗ ↘
A            D
      ↘ ↗
       C

也就是有：

A→B
A→C
B→D
C→D

你肉眼看它，会觉得整个流程大概是：

先 A
然后 B 或 C 选一个
最后 D

所以可以写成一种“结构表达式”：

\[
\rightarrow(A,\ \times(B,C),\ D)
\]

这里：

→ = sequence，顺序

× = choice，二选一


现在看算法怎么得到它。


---

第一次：看整张图

它先问：

> 这张大图最外层是什么关系？



发现可以按顺序切成三块：

{A}  →  {B,C}  →  {D}

注意这里所谓：

> “把 activity set 分区”



就只是把活动集合

\[
\{A,B,C,D\}
\]

分成互不重叠的几个小集合：

\[
\{A\},\quad\{B,C\},\quad\{D\}
\]

这就是论文里的 partitions ← SPLITDFG(...)。


---

第二次：分别处理每一块

{A} 只有一个节点。

不用再拆：

A

{D} 也一样：

D

但 {B,C} 里面还有两个活动，于是算法调用自己：

> MINEMODEL({B,C})



这就是“递归”。

然后它再看 B、C 之间是什么关系。

这个例子里它们代表一个 choice，于是得到：

\[
\times(B,C)
\]

现在三个子结果都有了：

A

×(B,C)

D


---

第三次：按照第一层识别出来的 sequence 拼回来

第一层不是已经判断：

{A} → {B,C} → {D}

了吗？

那现在把三个小模型塞回去：

\[
\rightarrow(A,\times(B,C),D)
\]

完事。

所以所谓：

> “subnets 合成完整 process model”



本质上就是：

> 我切的时候记住“这一刀是 sequence / choice / parallel / loop 中的哪一种”；
下面的小块各自处理完以后，再按照这个运算符拼回来。



论文 Algorithm 3 的第 13–21 行就是这么写的。


---

你可以把它类比成“解析数学表达式”

比如：

\[
A\times(B+C)\times D
\]

你不会把所有字母一次处理。

你会先看最外面：

乘法
├── A
├── (B+C)
└── D

然后发现 (B+C) 还能拆：

加法
├── B
└── C

最后形成一棵树。

Inductive Miner 干的事情很像：

sequence
├── A
├── choice
│   ├── B
│   └── C
└── D

只不过它的四种“运算符”是：

→  sequence
∧  parallel
×  choice
⟲  loop

所以你现在甚至可以暂时把 Petri net 完全放旁边。

先理解成：

\[
\boxed{\text{DFG} \rightarrow \text{一棵由四种结构组成的语法树}}
\]

然后那棵树再有规则地转成 Petri net。


---

那四种 pattern 大概是什么意思？

现在只要直觉，不要学检测规则：

sequence：A 做完才能做 B，像 A → B

choice：B/C 二选一，像 A → (B or C) → D

parallel：B/C 都要发生，但先后可以交换

loop：某一部分做完可以回去再做一轮


EdgeIM 论文这里没有展开 DETECTBASEPATTERN 到底如何判四种 cut 的全部规则，它只是说这里沿用 classical Inductive Miner 的 recursive decomposition，并在伪代码里把检测封装成这个函数。所以现在别从 EdgeIM 这几行硬猜“看到双向边就一定 parallel”之类的规则；那属于你后面真正学 IM 时再拆。


---

而且这样你就能看懂 EdgeIM 三阶段为什么接得起来了：

原始日志 D
   ↓
Stage 1
删掉“没有新增结构类型”的重复 case
   ↓
D′
   ↓
Stage 2
把 D′ 变成各 edge node 的局部 DFG
   ↓
Stage 3 前半
把局部 DFG 合成一张 global DFG
   ↓
Stage 3 后半
不断：
看最外层结构 → 切图 → 递归 → 拼回来
   ↓
过程模型

所以 global DFG 不是最终答案。

它更像是一个供 Inductive Miner “读结构”的中间表示。

你刚刚那句“不懂”主要卡的应该就是：“一张边图怎么突然变成 Petri net 了？”——中间其实少写了一大坨：

\[
\boxed{
\text{DFG}
\rightarrow
\text{识别 cut}
\rightarrow
\text{递归形成 process tree 式结构}
\rightarrow
\text{再拼成 process model}
}
\]

论文把这一坨用 DETECTBASEPATTERN / SPLITDFG / MINEMODEL / MERGESUBNETS 四个函数名压过去了。

有，而且这一层其实应该正式纳入训练框架。不然很容易变成“把一篇论文顺着读懂”，但没有长出你真正想要的科研 taste：怎么定位它在方法空间里的位置、怎么产生联想、怎么把联想重新压回可验证的问题。

我会把我们后面的学习从“两张图”升级成 四张图 + 一个探索闭环。


---

1. 专业知识图谱：我现在在学什么

这是刚才那张：

event
→ trace
→ event log
→ S/E/R
→ DFG
→ weighted DFG
→ cut
→ Inductive Miner
→ process tree
→ Petri net

它解决的是：

> 我缺哪些前置？这个概念依赖什么？



默认零基础逐节点补。


---

2. 方法地图：同一个问题，别人还怎么做？

这是你说的横向对比。

以后碰到 EdgeIM Stage 1 这种“日志太大，想减少数据”的方法，不能只学：

> Algorithm 1 怎么跑。



我们还会问：

> “减少日志”这一目标还有哪些基本路线？



比如先粗略分成：

日志太大
│
├─ 随机抽
│
├─ 分层/按 variant 抽
│
├─ 按频率保代表
│
├─ 按 coverage 保代表        ← EdgeIM Stage 1 很接近这一类
│
├─ 保统计量，不保原始样本
│
├─ 做近似摘要 / sketch
│
└─ 根本不压日志，改成分布式计算

然后不是列名词，而是统一拿一张表审：

方法	保什么	丢什么	需要先知道什么	是否有预算 K	顺序敏感吗	对频次友好吗	保证是什么



这张表特别重要。

因为科研里经常不是：

> A 方法好，B 方法坏。



而是：

> A 选择保 invariant X，B 选择保 statistic Y，所以二者适合的 downstream 根本不同。



你以后看到一个新方法，第一反应就会逐渐从“涨了几个点”变成：

> 它交换掉了哪一类信息，换来了什么？




---

3. Idea genealogy：它从哪里长出来，后来又长去哪？

这是纵向对比。

比如对 EdgeIM，我们会分清两个完全不同的问题：

A. 有文献证据的 genealogy

要去看论文：

Related Work 引了谁；

作者明确说改进了谁；

哪个 bottleneck 是前人留下的；

后续谁又改了它。


这属于：

> 可考证的学术谱系。



必须有 citation。

B. 我们从机制反推的 genealogy

例如刚才我们猜：

> “日志很大 → downstream 主要消费 S/E/R → 能否只留首次带来新 feature 的 witness？”



这个是：

> USER-INFERENCE / idea reconstruction



可以很有价值，但不能写成：

> “作者就是受 set cover 启发。”



除非论文真写了。

这个区分本身就是科研素养训练：

> plausible story ≠ historical fact。




---

4. 跨领域启发图：这个结构在哪儿还出现过？

这一层我非常同意你要加。

不是为了“万物类比”，而是练：

> 抽掉领域名词以后，当前方法的数学/计算结构是什么？



拿 EdgeIM Stage 1 举例。

它表面是 process mining，抽象以后却是：

> 有很多对象，每个对象携带一组 feature；
顺序扫描；
若带来未覆盖 feature 就保留；
否则删除；
最终要求 feature universe 不变。



一下就能联想到：

软件测试：test-suite reduction

1000 个测试里很多覆盖同样 branch。

想保：

> branch coverage



而不是保：

> 每个 test case。



那么“有新 coverage 才留”与这里非常接近。


---

Set cover / maximum coverage

每条 trace 都对应一个 feature set。

问题变成：

> 用少量集合覆盖 feature universe。



但要马上追一句：

> EdgeIM 不是标准 set cover 求最小解。



它是按顺序 one-pass，首次贡献就保。

所以：

> 结构相似，不等价。



这就是类比训练里非常重要的一步。


---

Streaming algorithms

数据流只能扫一遍。

你不想保存所有 observation，只维护某种 summary/state。

EdgeIM 的：

\[
S,E,R
\]

本质上就是状态摘要。

于是可以继续问：

> 如果目标改成保 frequency，state 应该换成什么？



自然就会想到 counter / sketch。

这个联想就已经有研究生成能力了。


---

Coreset / sufficient statistics

机器学习里常问：

> 能不能不保全部数据，只保一个小集合，使某类 downstream query 近似不变？



这与 EdgeIM 的：

> 小 \(D'\) 保 structural support



同属一大类思想：

> 压缩输入，保存 downstream sufficient information。



但再次注意：

EdgeIM 对什么 sufficient，要逐项证明，不能借“coreset”这个名字替它扩大 claim。


---

形式化验证里的 abstraction

这个和你以后方向尤其值得连接。

Concrete state space 很大，就构造 abstraction：

\[
\text{concrete system}\rightarrow\text{abstract system}
\]

重点从来不是：

> 抽象以后什么都没丢。



而是：

> 关于我要验证的 property，丢掉的信息是否 irrelevant？



这与我们刚刚钉的：

> lossless relative to what?



是高度同构的科研动作。


---

Graph sparsification

大图太大，只留一个更小的图。

但真正核心问题不是“删边”，而是：

> 要保 connectivity？ cut？ spectral property？ shortest path？ reachability？



不同 sparsifier 对不同 query 才叫“保真”。

这也会反过来提醒你：

> EdgeIM 说 zero loss，保的究竟是哪个 graph property？




---

所以以后每次跨领域拓展，我们都不会只说：

> “诶，这个很像 X！”



而是必须继续填：

共同抽象结构：
哪里相同：
哪里不同：
这个类比产生了什么新问题：
哪个问题可以实验：
哪个只是漂亮比喻：


---

5. 最重要的：Idea → Evidence 的闭环

你提到：

> 怎么 explore，产生联想以后怎么回来验证？



这个我觉得要变成固定动作。

以后你突然说一句：

> “诶，那是不是可以把 frequency 单独保存？”

“是不是和 coreset 很像？”

“如果 downstream 只看 R，那完全可以删更多？”

“能不能换成 k-hop relation？”



我不会直接顺着把它吹成 research idea。

我们走一个固定的 8 步探索环：

① Observation

我到底观察到了什么？

比如：

> Algorithm 1 保住 \(R\)，但 \(w\) 发生变化。



先只写事实。


---

② Analogy / Idea

脑子冒出了什么？

> “那能不能一边压 trace，一边单独维护原始 edge count？”



这时候可以大胆。

这一阶段允许发散。


---

③ Abstraction

为什么会想到它？

抽成：

> witness 用于结构； counter 用于统计； 两类信息没必要绑在同一个 representation 里。



这一步很重要。

否则只是灵感碎片。


---

④ Claim

把 idea 改写成一句可错的话：

> “若 downstream 只需要原始 weighted DFG，那么保留 coverage witness \(D'\) 加完整日志扫描得到的 per-edge counters，可能足以代替完整 D。”



现在开始有科研价值了。

因为它能被打脸。


---

⑤ Counterexample first

先问：

> 什么情况下一定不够？



比如 downstream 需要：

trace-level ordering beyond adjacency；

variant frequency；

long-range dependency；

case attributes；

duration distribution。


那：

\[
D' + edge\ counts
\]

就显然恢复不了。

于是 claim ceiling 自动缩小。


---

⑥ Minimal discriminating experiment

不要立刻做“大实验”。

找一个最小实验，能区分：

\[
H_0:\text{这个信息足够}
\]

和：

\[
H_1:\text{不够}
\]

比如当前训练里，一个非常自然的问题就是：

> 构造两份拥有相同 \(S/E/R\)、但不同 multiplicity 的日志，看看 downstream miner 是否输出相同结果。



如果普通 IM 相同、带 threshold 的 IMf 不同，那我们立刻知道：

> support preservation 不是所有 miner 的 sufficient condition。



你现在的训练包本来就在往这个接口走：第 2 站安排 IM/IMf 和 noise_threshold，目的之一正是把“边还在”和“比例还在”区分开。


---

⑦ 回原文 / 代码 / 数据

这一步防止脑洞自嗨。

回去问：

> 论文真这么做了吗？ Algorithm 输入是什么？ 哪个变量真的被 downstream 读取？ 源码到底用 support 还是 weight？ 作者实验有没有覆盖这个 operating regime？



也就是说：

> 联想负责生 hypothesis，原文和实验负责杀 hypothesis。




---

⑧ Update

最后不是只记：

> “实验成功/失败。”



而要记：

原假设：
证据：
被打掉的是哪一层：
留下来的 claim：
新的边界条件：
下一步：

这其实和你现在 Mistake Log 的逻辑完全兼容：错误必须经过 evidence 和正确 credit assignment 才算经验。你现有训练 OS 也已经把 cold replay、独立重做和 mistake closure 放进去了。


---

所以后面每学一个关键节点，我们可以固定做这六个面

例如未来学 sequence cut，不只会定义。

我会带你走：

Core

sequence cut 到底是什么，手画、手切。

Horizontal

choice / parallel / loop 和它怎么区分。

Vertical

为什么 Inductive Miner 要从 alpha-style relation discovery 走向 block-structured recursive cuts；具体历史等我们查论文时再有证据地补。

Cross-domain

像 parser 的 recursive decomposition、divide-and-conquer、hierarchical clustering 哪里像、哪里不像。

Research

这个 cut 的识别依赖哪些 representation？ 哪些信息一旦在 Stage 1 被删掉就可能判断错？

Explore

你自己改一条边、改一个频次、复制十条 trace：

> cut 会不会变？



然后先预测，再跑。

这时候你学到的就不是：

> “sequence cut 定义背完了。”



而是：

> 我知道这个判断为什么成立、依赖什么、什么会让它坏、和旁边方法是什么关系、怎么自己试探边界。




---

最后我们其实会维护四张长期地图

① Knowledge Map
我知道什么 / 缺什么前置

② Method Landscape
同一问题有哪些解法，各自 trade-off 是什么

③ Idea Genealogy
这个方法从什么 bottleneck 长出来，前后怎么演化

④ Transfer Map
别的领域哪里有同构结构，可以借什么、不能借什么

再加一条不断循环的：

\[
\boxed{
Observe
\to
Analogize
\to
Abstract
\to
Hypothesize
\to
Attack
\to
Experiment
\to
Verify
\to
Update
}
\]

我觉得这才比较接近你说的“一边补知识，一边真的培养科研能力”。

而且以后我会有意允许你先乱联想。科研 taste 不是把联想压死；真正要训练的是第二阶段：

> 联想可以大胆，claim 必须收敛；idea 可以免费，evidence 必须付账。



这条我们可以一直贯穿整个 EdgeIM 包。


我觉得我知道你为什么一直觉得“差一点”。

前面那套已经覆盖了很多显性的科研能力：学习、抽象、反例、实验、claim–evidence、taste、ownership、更新、fit……但它还是有点像在描述一个“能力很强的研究执行器”。

真正的科研人还有几层更难测、但导师其实非常在意的东西，我们还没正式放进去。

我会补成下面这几块。


---

第一块：不是“会不会研究”，而是你为什么会长期追这个问题

科研潜力里有一类东西不是 skill，而是驱动力结构。

导师会慢慢判断：

你是真的对“为什么”有持续兴趣，还是主要对结果/title/认可有兴趣；

一个问题没人催、短期没收益时，你还会不会自己回去想；

你是喜欢解已有问题，还是会自然地开始质疑“为什么问题要这样定义”；

你对什么类型的不确定性有耐受甚至兴奋感；

哪类痛苦你能长期忍：证明卡住、实验失败、工程 debugging、读大量文献、反复改定义……


这决定的不只是“适不适合科研”，还决定适合哪种科研。

有人可以一天 debug 十二小时，但让他证明一个 lemma 会疯。

有人特别喜欢把定义抠到没有漏洞，但面对 systems 工程会迅速失去兴趣。

有人最享受造东西。

有人真正上瘾的是：

> “等等，这个问题是不是一开始就问错了？”



这其实是 research identity 的雏形。

所以我们的图里还应该有：

> Research Motivation / Research Identity Map



不是“我喜欢 AI safety”这种领域标签，而是：

> 我究竟喜欢科研活动中的哪一种认知动作？




---

第二块：我们前面说了 research taste，但还少了更高一级的——agenda setting

现在的 taste 更多是：

> 给我十个问题，我知道哪个更值得先做。



但真正逐渐独立以后还要多一步：

> 为什么问题集合里只有这十个？第十一个问题是谁提出的？



博士生后期和独立 researcher 的分水岭很大程度就在这里。

低一级：

> 老师说研究 EdgeIM，我把 EdgeIM 做好。



再高一级：

> 我发现问题其实不是 EdgeIM performance，而是 feature-preserving compression 和 downstream statistic dependency 的错位。



再高一级：

> 我发现这是一个更一般的问题：
什么时候一个压缩 representation 对 downstream procedure 是 sufficient 的？



这时对象已经开始从一篇 paper 脱离出来。

所以我们后面必须训练的不只是：

\[
\text{solve problem}
\]

而是：

\[
\text{frame problem}
\rightarrow
\text{reframe problem}
\rightarrow
\text{choose problem}
\rightarrow
\text{abandon bad problem}
\]

这个能力前面提到过，但没有单独立柱。


---

第三块：还有一种我觉得特别关键的——epistemic character

这比“严谨”范围大。

导师真正长期相处会很在意：

> 当事实和你喜欢的结论冲突时，你站哪边？



例如：

你做了两个月，结果说明自己的 idea 根本没有优势。

会不会开始：

换 metric；

挑 seed；

找一个能讲故事的 slice；

把 negative result 解释成“差一点就成功”？


还是能说：

> “这个 hypothesis 没站住。”



再比如：

AI 帮你做了很多。

你能不能清楚分：

> 哪些是我亲手验证的； 哪些是 agent 给的； 哪些只是合理推测； 哪些我其实不知道。



这种东西很难通过知识题训练。

它更像科研中的“人格约束”：

不把 UNKNOWN 写成 known；

不因为自己投入很多就保护 hypothesis；

不为了漂亮 narrative 偷换 claim；

允许 negative evidence 真正改变方向；

能承认“这个我没弄懂”。


形式化验证和 AI safety 尤其需要这个。

我甚至会把它单独叫：

> Epistemic Integrity



而不是笼统放进“科研素养”。


---

第四块：我们还缺了研究工作中的社会维度

科研不是一个人和论文单挑。

导师会判断：

你会不会提一个别人能回答的问题；

会不会接受 critique 而不把 critique 当人格攻击；

会不会 critique 别人但不胡乱扩大 claim；

合作时能不能把 interface 说清楚；

遇到争议能不能回到 evidence；

能不能告诉别人“这里我不确定，需要你检查什么”；

会不会把别人的贡献正确 credit；

会不会让 collaborator 知道当前真正的 blocker。


这和所谓“会社交”不是一回事。

这是：

> research collaboration bandwidth



很多特别聪明的人科研推进非常痛苦，不是智力问题，而是接口烂。

而导师带学生本身也是一个 distributed system（）。

如果每次学生汇报都是：

> “我做了很多，然后有点问题……”



导师很难帮。

如果是：

> “当前有 A/B 两种解释；证据 X 排除了 A 的一种情况，但还区分不了 B1/B2。我希望你帮我判断这个 identification 是否值得继续追。”



导师的信息增益会高很多。

这个能力我们还没正式练。


---

第五块：还有研究节奏和资源管理

这也非常像科研潜力，但常被误认为“自律”。

其实不是。

研究资源包括：

时间；

注意力；

GPU；

人类导师反馈；

collaborator 时间；

verification budget；

literature-reading budget。


好的 researcher 会慢慢学会：

> 哪个 uncertainty 值得花 3 天？



而不是：

> 我能不能把所有东西都做到最完整？



这和你前面很喜欢的 DSpark 那种思想是一模一样的：

> verification resource 应该花在 expected decision impact 高的地方。



科研中也一样。

一个导师会观察：

> 这个学生会不会把两周花在一个不会改变结论的细节上？



这其实是研究经济学。

我觉得我们之前虽然谈过 taste，却没有把：

\[
\text{value of information}
/
\text{cost of investigation}
\]

明确纳入训练。

应该加。


---

第六块，也是最大的缺口：一次面试其实根本测不准科研潜力

这个非常重要。

前面我设计了一个“75 分钟科研面试”，但如果我是导师，我不会真相信一次面试能完成判断。

科研潜力最好的测试其实不是 interview。

是：

> 共同做一个小问题。



也就是 work sample / mini-project / trial collaboration。

因为真正的信息来自时间序列：

第一天不会。

第三天学会。

第五天踩坑。

第六天得到反例。

第七天推翻原计划。

第十天重新提出更好的问题。

我要看的不是某个 snapshot，而是：

\[
\boxed{\text{learning trajectory}}
\]

也就是：

> feedback 进去以后，这个人的状态怎么变。



同样两个学生第一次都答错。

A 三天后又犯同一个错。

B 三天后不仅不会再犯，还自己发现了同类错误。

他们的 snapshot 一样，derivative 完全不同。

所以科研潜力更像测：

\[
\frac{\Delta\text{capability}}{\Delta\text{feedback}}
\]

甚至：

\[
\frac{\Delta^2\text{capability}}{\Delta\text{experience}^2}
\]

而不只是当前能力值。

这可能就是你一直觉得“少了什么”里最大的一块：

> 我们一直在列能力，但科研潜力本质上是动态量。




---

那培养也不能只靠“训练技能”

真正好的导师培养学生，应该至少同时干四件不同的事。

第一是教显性知识：定义、方法、文献、技术。

第二是示范 tacit knowledge：为什么这个 experiment 不值得做、为什么这个 reviewer comment 很致命、为什么一句话看起来合理但不能写。

第三是控制脚手架撤除速度：不是永远带，也不是第一天就扔进海里。

第四是让学生进入真实科研共同体：组会、paper discussion、review、合作、失败项目、别人怎么做 decision。

很多科研 taste 根本不是一本《科研方法论》教出来的。

它是：

> 你跟着一个成熟 researcher 看了几十次
“这个问题我为什么不追”
“这个结果为什么不能说”
“这个失败为什么反而很重要”



慢慢压缩出来的。

这是 apprenticeship。

我们之前的训练系统比较像 curriculum，但还缺：

> 科研学徒制 / tacit knowledge transfer



这一层。


---

所以我现在会把整个框架重新整理成 七张图，这次我觉得比较完整了：

1. Knowledge Map
我知道什么，缺什么前置。


2. Method Landscape
同一个问题有哪些方法，它们交换什么。


3. Idea Genealogy
方法从什么 residual / bottleneck 长出来。


4. Transfer Map
跨领域有什么同构结构。


5. Researcher Competency Map
抽象、实验、反例、更新、ownership、表达……


6. Research Identity & Fit Map
我喜欢什么认知活动、适合什么问题和导师风格。


7. Research Trajectory Map
我的能力如何随 feedback 改变；哪些能力已经从“跟练”长到“陌生迁移”。



而所有这些图上方还有两个约束：

> Epistemic integrity：我对“知道”这件事有多诚实。



以及

> Research economy：我把有限研究资源花在哪里。




---

然后每一个学习节点，我们都不需要把这七张图全部刷一遍，那会把学习搞疯。

而是根据当前材料挑最自然的训练点。

比如刚才 EdgeIM 的 DFG：

专业层：

> support vs frequency。



taste 层：

> downstream 到底需要哪个 representation？



idea 层：

> witness + counter 能不能拆开保存？



跨域层：

> test-suite reduction / coreset / abstraction。



epistemic 层：

> “zero loss”到底允许说到哪里？



实验层：

> 怎么造最小反例？



trajectory 层：

> 今天你是我解释以后能理解，还是明天面对另一个 compression 方法会自己问“lossless relative to what?”



最后那个问题，才真正是在测科研潜力有没有开始长。

所以我觉得你刚才那种“总感觉还少点什么”的感觉是对的：少的不是另一个知识模块，而是从“科研能力列表”再往上升一层，看到研究者是一个会在长期反馈中形成问题品味、认知习惯、价值判断、合作接口和独立研究身份的动态系统。

这才接近导师真正想培养的“人”，而不只是一个会做科研题的人。


思考了 24s

对，我觉得这里最重要的一点是：**不要一开始画一棵“科研百科全书树”然后按目录学。**那样大概率三天以后枝杈爆炸，主线失踪。

更适合我们的，是一棵任务驱动、证据驱动、会自己长的树。而且严格说，它“看起来像树”，底层其实会越来越像一张图——因为同一个概念会同时连到算法、形式化验证、实验设计和科研能力上。

你现在的训练包已经把主线顺序冻住了，而且明确要求每天只开少量节点、防止顺手加坑；所以我们不应该为了这套新框架重排 0 → 1 → 5a → 2 → 3 → 5 → 6 → 7。新树应该长在主线旁边，而不是替换主线。

我会这样长。


---

一、先有一根“主干”

目前主干不是“计算机科学”，也不是“AI Safety”，而是我们正在真正解决的对象：

EdgeIM
│
├─ Stage 1: 日志过滤
│    ├─ event / trace / log
│    ├─ S / E / R
│    ├─ Algorithm 1
│    └─ D′
│
├─ Stage 2: DFG
│    ├─ edge support
│    └─ edge weight
│
├─ Stage 3: process discovery
│    └─ Inductive Miner
│          ↓
│       process tree
│          ↓
│       Petri net
│
└─ downstream question
     “Stage 1 保住的东西，
      真够后面用吗？”

这就是对象主干。

任何新东西先问：

> 它跟当前主干哪个节点有关系？



答不出来，先不学。

这样就不会发生：

> DFG
→ 图论
→ 谱图理论
→ 拉普拉斯矩阵
→ GNN
→ Transformer on graphs
→ 两小时以后忘了 EdgeIM 在哪。



（笑）


---

二、每个主干节点允许长 6 种枝

比如我们现在站在：

> DFG：support vs weight



这个节点旁边，不是只继续往下学 IM，而是可以长出六类枝。

┌─ ① 前置知识
                    │
                    ├─ ② 横向方法
                    │
DFG / support ──────┼─ ③ 纵向谱系
vs weight           │
                    ├─ ④ 跨域迁移
                    │
                    ├─ ⑤ 科研能力
                    │
                    └─ ⑥ Research identity

但这六类不是每次全学。

它们只是“允许生长的方向”。


---

① 前置枝：我为了理解它，现在必须补什么？

例如 DFG：

DFG
├─ directed graph
├─ node / edge
├─ weight
├─ set
└─ count

其中集合、边、权重你已经基本够用了。

所以不继续挖图论。

到了 Inductive Miner，如果碰到：

> connected component 是什么？



那再即时补。

这叫：

> just-in-time prerequisite



不是：

> “先学完离散数学再看论文。”




---

三、② 横向枝：别人处理同类问题还有什么办法？

这是培养方法空间感。

例如 Algorithm 1 的核心问题可以抽成：

> 日志太大，怎么减少需要继续处理的数据？



那以后可以逐渐挂：

日志压缩 / selection
│
├─ random sampling
├─ stratified / variant-based selection
├─ coverage-preserving selection   ← EdgeIM
├─ statistics-only summary
├─ streaming sketch
├─ coreset-like approaches
└─ distributed processing without dropping data

但每挂一个方法，不是记名字。

我们统一比较：

目标是什么？
representation 是什么？
保什么？
丢什么？
需要什么假设？
预算是外生还是内生？
有没有 exact guarantee？
顺序敏不敏感？
downstream 对什么敏感？
失败模式是什么？

于是方法树不是：

> A、B、C、D 四篇论文。



而是：

> 四种不同设计选择。



这才会长 taste。


---

四、③ 纵向枝：这个 idea 是怎么一步步长出来的？

这就是 idea genealogy。

但我要特别限制：

文献真正支持的谱系

写：

PAPER-EVIDENCE
A → B → EdgeIM

需要作者引用、related work、后续论文等证据。

我们自己反推的“可能思路”

写：

RECONSTRUCTED-GENEALOGY
日志太大
→ 重复结构多
→ downstream 使用结构特征
→ 那能不能只保新 coverage？
→ Algorithm 1

不能混在一起。

你之前特别感兴趣的是：

> “作者到底怎么想到的？”



这个问题我们以后会常做，但必须把：

> 历史事实



和

> 机制重建



分开。

否则很容易讲出一个特别漂亮但根本不是历史的故事。


---

五、④ 跨领域枝：在哪些地方见过同一个“骨架”？

这里是最好玩的。

但我们的原则是：

> 跨域类比不是找名词相似，而是找结构相同。



例如 Algorithm 1：

很多对象
↓
每个对象携带 feature set
↓
只保首次带来新 feature 的对象
↓
最终 coverage 不变

那么会挂：

Algorithm 1
                    │
        ┌───────────┼─────────────┐
        ▼           ▼             ▼
 test-suite     set cover     streaming
 reduction      family        summary
        │                         │
        ├─────────────┬───────────┤
        ▼             ▼           ▼
    coreset      abstraction    graph
                              sparsification

然后每条边必须问：

> 像在哪里？

不像在哪里？



例如：

> EdgeIM 和 set cover 都有“对象覆盖 feature universe”。



但：

> EdgeIM 不是标准最小 set cover；它按顺序一遍扫描。



所以知识图里可以连边：

EdgeIM ── shares coverage structure ── set cover

不能直接画：

EdgeIM = set cover

这就是科研上的“类比纪律”。


---

六、⑤ 每个知识节点同时长一根“科研能力枝”

这是最重要的。

我们不是学完 DFG 才另外上《科研方法论》。

而是在 DFG 这个具体东西上锻炼科研动作。

你刚才这个节点已经自然长出了：

DFG
│
├─ 专业知识
│    ├─ support
│    └─ weight
│
└─ Research skills
     ├─ representation awareness
     ├─ lossless relative to what?
     ├─ invariant identification
     ├─ claim ceiling
     ├─ counterexample construction
     ├─ downstream dependency
     └─ minimal experiment

比如：

\[
R(D)=R(D')
\]

但：

\[
w_D\neq w_{D'}
\]

你学到的不只是一个 EdgeIM 事实。

同时在磨：

> 一个“无损” claim 到底在哪个 representation 层成立？



以后换成模型压缩、抽象解释、形式化验证、数据压缩，你都应该自动冒出这句。

如果能迁移出去，这个能力节点才真正“长成”。


---

七、⑥ 还有一根很特别的枝：Research Identity

这个不是考试分数。

我们会慢慢记录：

> 哪些研究动作你明显特别有兴趣？



例如未来可能出现：

你对……
├─ 证明 guarantee        ?
├─ 找反例                ?
├─ debug system          ?
├─ 找 bottleneck         ?
├─ 换 representation     ?
├─ 设计 experiment       ?
├─ 建 formal model       ?
├─ 追 idea genealogy     ?
└─ 连接不同领域          ?

不是现在就给你贴标签。

而是经过很多真实任务后，看行为轨迹。

有可能半年以后我们会发现：

> “每次遇到 representation / hidden assumption / bottleneck，你都会主动追；纯调参反而没什么兴趣。”



那这就是很真实的 fit evidence。

这会慢慢帮助回答：

> 你适合哪类课题？ 理论 / systems / empirical / formal / interdisciplinary 哪种比例？ 什么导师风格适合你？




---

八、但如果每个节点都长六条，还是会爆炸

所以需要一个非常重要的机制：

枝条有状态。

我建议以后统一只有五种状态：

SEEN
见过，知道它存在

OPEN
当前需要学

PARKED
有意思，但现在不学

OWNED
能独立重建/使用

TRANSFERRED
能在陌生问题上迁移

例如现在：

DFG                           OWNED-ish
│
├─ support vs weight          OPEN → 快完成
├─ Inductive Miner            NEXT
├─ Petri net                  PARKED
├─ graph sparsification       SEEN / PARKED
├─ set cover                  SEEN
├─ test-suite reduction       SEEN
└─ “lossless relative to what” OPEN

PARKED 不是丢掉。

这是在练 research economy：

> 有意思 ≠ 现在值得追。




---

九、树什么时候允许“升级”一个枝？

不能因为：

> “这个好有趣！”



就升级成主线。

我们设三个升级门槛。

一个旁枝至少满足其中一个：

A. Dependency

不学它，当前主线走不下去。

例如：

> 不懂 sequence cut，就看不懂 IM。



→ 升级。

B. Repeated recurrence

同一个抽象在不同地方连续出现。

例如你在：

EdgeIM；

formal abstraction；

coreset；

model compression


反复撞到：

> sufficient representation



那它可能不是一个小枝，而是你的核心方法论节点。

→ 升级。

C. Research leverage

它产生了一个足够重要、可证伪、会改变当前研究判断的问题。

比如：

> “Stage 1 保 support，但 IMf 用 frequency threshold；这会不会改变 downstream model？”



这直接打到研究问题。

→ 升级。

其他的：

> 先 PARK。




---

十、Idea 也要长在树上，但不能直接变成果实

以后你脑子冒一句：

> “那是不是可以把 edge counter 单独保存？”



我们不会把它写成：

IDEA ✅

而是长成一条小芽：

OBSERVATION
R 保住，w 变了
    │
    ▼
IDEA
w 能不能旁路保存？
    │
    ▼
MECHANISM
witness 与统计量分离
    │
    ▼
PREDICTION
如果 downstream 只消费 weighted DFG，
D′ + exact edge counters 可能足够
    │
    ▼
ATTACK
什么情况下不够？
    │
    ▼
MINIMAL TEST
造两份日志
    │
    ▼
EVIDENCE
    │
    ├─ 支持 → claim 升级
    └─ 反驳 → 更新模型

只有走过后半段，才从“灵感枝”升级成“研究枝”。

这个特别重要：

> 我们鼓励树疯狂发芽，但不允许每个芽都宣布自己是一棵论文。



（笑）


---

十一、错误怎么长？

错误不应该成为一堆红叉。

它应该变成树里的修枝记录。

你现有 Mistake Log 已经是这个逻辑：记录原 belief、什么验收击穿它、正确模型、下次识别信号，而且“看懂解析”不算关闭。

例如之前：

fixed-K sampler
      ✗
      │  evidence
      ▼
one-pass feature-preserving filter

这个错误真正留下的不是：

> “以后记住 EdgeIM 没有 K。”



而是长出一个更高层节点：

Research cue:
“如果为了比较必须给原方法额外补参数，
先检查自己是不是换了研究对象。”

这就叫错误向上抽象。

一次错误 → 一个可迁移 recognition cue。

这棵树才会越来越聪明，而不是越来越厚。


---

十二、还有一个“高度”概念

一个节点不是“学过/没学过”二元。

我建议以后按 6 层长：

L0  听过
 ↓
L1  能解释
 ↓
L2  能手算/使用
 ↓
L3  能改条件
 ↓
L4  能造反例/验证边界
 ↓
L5  能迁移到陌生问题
 ↓
L6  能产生并检验新问题

比如 Algorithm 1：

你已经不在 L1 了。

你正在往：

> L3/L4



走。

因为你已经开始问：

> 保了什么？ 丢了什么？ 为什么这么设计？ 后面怎么恢复？ 有没有类似方法？



真正达到 L5 的测试会是：

某天我给你一个完全不是过程挖掘的压缩方法。

你自己冒出来：

> “等等，它所谓 lossless 是相对什么 statistic？downstream 真只需要这个吗？”



那我就会把这个科研能力节点标：

> TRANSFERRED



这才叫长在你身上。


---

十三、所以整棵树其实会变成这个样子

【研究方向/真实问题】
                                │
                         Formal + AI Safety
                                │
                    ┌───────────┴───────────┐
                    │                       │
              当前研究对象               长期能力
                 EdgeIM                     │
                    │             ┌─────────┼──────────┐
             Algorithm 1          │         │          │
                    │        epistemic   research    identity
                   DFG        reasoning     taste       / fit
                    │             │
          ┌─────────┼────────┐    │
          │         │        │    │
       support    weight     IM    ├─ claim/evidence
          │         │        │    ├─ counterexample
          │         │        │    ├─ experiment
          │         │        │    └─ credit assignment
          │         │        │
       横向枝     横向枝     前置枝
          │
      sampling
      coreset
      sketch
          │
       跨域枝
          │
      test reduction
      abstraction
      sparsification

注意这不是两棵独立树。

专业知识和科研能力是交织着长的。


---

十四、我们实际学习时，不需要天天看这整张巨图

每次我只给你一个“小视窗”。

比如接下来正式进入 IM，我们只打开：

【当前】
weighted DFG
     │
     ▼
为什么光看边还不够？
     │
     ▼
什么叫 cut？
     │
     ▼
sequence cut

同时只挂两三个科研问题：

Research lens:
1. 这个判断依赖 DFG 的什么信息？
2. weight 参与了吗？
3. 改掉一条边，会发生什么？

你学会后再展开：

> choice / parallel / loop。



然后再自然长横向、纵向和跨域枝。

这样不会出现“还没学 sequence cut，我们已经开始讨论 14 种 graph abstraction”。


---

我现在会把我们的原则压成一句：

> 主干按真实研究任务向前长；前置只按需补，旁枝先登记不追；反复出现或能改变研究判断的枝才升级；每个知识节点同时锻造一个可迁移科研动作。



这样长几年以后，最理想的结果不是你脑子里有一棵特别巨大的“知识树”。

而是你面对陌生论文时已经会自己长树：

\[
\text{这是什么？}
\rightarrow
\text{依赖什么？}
\rightarrow
\text{邻居是谁？}
\rightarrow
\text{它从什么问题长出来？}
\rightarrow
\text{保什么、丢什么？}
\rightarrow
\text{有什么相似结构？}
\rightarrow
\text{我产生了什么假设？}
\rightarrow
\text{怎么最快把它打一下？}
\]

到了那个时候，“长树”本身就已经变成你的科研能力了。


思考了 22s

对，而且我觉得不只是“可以借一点思路”。你飞机上那几篇放在一起，已经能抽出一套很像**科研元操作符库（research operators）**的东西。

先说明一下：下面“这些论文各自强调什么”是基于你之前整理进来的那些笔记；而“怎么迁移到我们的学习树、EdgeIM、科研训练”这一部分，是我在这些笔记之上的抽象与迁移，不是原论文自己这么写的。

我会把它们压成几种非常值得长期保留的元动作。

来源	可以迁移的元动作	以后遇到问题时自动问

Mapping Networks	换表示 / 压自由度	“为什么一定在原来的空间解决？”
IndexShare / LayerSplit / HiSparse	追 residual bottleneck	“刚解决一个瓶颈后，新的第一瓶颈去哪了？”
DSpark	拆依赖：重活并行，关键链串行	“哪些真的有因果依赖，哪些只是习惯性串行？”
DSpark	verification budget 按价值分配	“这个不确定性值得花多少验证成本？”
Chain-of-Experience / Manus	失败 + 外部反馈 + credit assignment	“这次失败到底打掉了哪条 belief？”
GLM-5.3 environment	先造可信反馈环境，再 scale	“我的 verifier / metric 本身靠谱吗？”
DataMagic / DVSpec	语义 state 显式化 + provenance	“重要语义是不是藏进黑箱了？”
DataMagic	先广泛生成，再全局编排	“局部探索能否并行，最终决策再集中？”


1. Mapping Networks：不是先想“怎么解”，先问“这个问题为什么要这么表示？”

你之前那份笔记抓得很准：它真正有意思的不是“降维”三个字，而是把

\[
\min_{\theta\in\mathbb R^P}L(\theta)
\]

改成

\[
\min_{z\in\mathbb R^d}L(g(z)),\quad d\ll P
\]

也就是质疑“这 \(P\) 个表面自由度为什么都必须独立优化”。而且笔记同时保留了必要的反问：你凭什么认为低维 sufficient representation 存在？被压掉的自由度会不会正好含有任务信息？ 

这个可以直接挂到我们树的一个高层元节点：

> Representation surgery：先改问题空间，再解问题。



以后读 EdgeIM，就可以自然问：

原始 event log 真的是 downstream 最需要的表示吗？

\(S/E/R\) 是不是一个更小的 structural representation？

这个压缩 representation 对哪个 downstream sufficient？

support 足够还是 weight 也需要？

有没有更高阶关系没被 DFG 表达？


注意，这不是说“EdgeIM = Mapping Networks”。

只是它们共享一个研究动作：

> 先质疑默认 representation。




---

2. IndexShare / LayerSplit / HiSparse：创新不是凭空想 idea，而是追着瓶颈跑

你那份笔记里最值得长期保留的是这个循环：

> 已有系统
→ profile
→ 当前最大浪费
→ 找 redundancy / invariant
→ 利用它
→ 新方法引入什么代价
→ 控制新代价
→ 再 profile。 



这个我甚至想正式挂成：

> Residual Bottleneck Loop



以后我们做研究，不能只问：

> “这个方法有没有问题？”



而是：

> 旧方法已经解决了什么？
解决以后剩下最大的 residual 是什么？



比如现在 EdgeIM：

日志太大 / 边缘资源有限
↓
Stage 1 去重复结构
↓
通信/计算压力下降
↓
新的问题冒出来：
frequency distribution 被扭曲了吗？
↓
如果 downstream 对 frequency 敏感：
这可能成为新的 residual

这就很像：

> attention 便宜了 → indexer 成瓶颈
indexer 便宜了 → KV / CPU / communication 成瓶颈。



而 IndexShare 那个“局部 similarity proxy 看着特别合理，结果不如直接看 end-to-end LM loss”更值得偷：proxy 很漂亮不等于它测到了真正 objective。 

这正好升级我们当前的科研问题：

> \(R\) preservation 是一个漂亮 proxy。
但真正 objective 是 downstream discovered model 的 fidelity 吗？
如果是，就必须验证二者是否对齐。



这就是非常实在的迁移。


---

3. DSpark：不要把问题锁死在“全串行 vs 全并行”这种假二选一里

你之前抓的“重计算并行，依赖修正串行”本质上是一种更一般的分解：

> 把真的有依赖的部分留下，把其余昂贵工作解耦出去。 



这个特别适合我们的学习和科研工作流。

例如 paper exploration：

查 related work ─┐
找实现           ├─ 可以并行
找类似方法       ┤
搜反例           ┘

↓ 汇总

claim 冻结
→ measurement contract
→ 实验解释
→ 是否改 hypothesis

后半段不适合“八个 agent 各说各的”。

因为它有 epistemic dependency。

所以可以形成一个元问题：

> 哪些步骤的正确性依赖前一步的结果？哪些只是计算/搜索量大？



这比“多 Agent 越多越好”高级很多。


---

4. DSpark 还有另一刀：科研验证也应该有 budget

你笔记里还有个我觉得特别适合迁入我们树里的东西：

> 值不值得验证，不只是对象属性，而是对象价值 × 当前资源状态。 



科研里完全可以改成：

\[
\text{verification priority}
\approx
\frac{
P(\text{当前假设错})
\times
\text{错了以后对决策的影响}
}{
\text{验证成本}
}
\]

不用真的精确算。

但习惯要长出来。

例如现在有三个 uncertainty：

A. DFG 表里 A→B 是 3 还是 4
B. Algorithm 1 是否保 \(R\)
C. downstream IMf 是否依赖被扭曲的 frequency

如果 C 一旦成立会直接改变研究结论，那显然不能把两小时全花在 A 的格式漂亮不漂亮上。

这可以挂到之前说的：

> Research economy / Value of Information



而且它能直接防你那种很容易“全部核完、全部搞完整”的倾向。


---

5. Chain-of-Experience：我们的树不能只保存“正确知识”，还应该保存“知识是怎么更新出来的”

这个其实已经和我们现在的 Mistake Log 很接近。

你之前的笔记把它压成了：

> attempt
→ environment feedback
→ diagnosis boundary
→ revised attempt



而不是简单：

> “错误轨迹保留下来就有价值。” 



所以我们的知识树不应该只有：

Algorithm 1
└─ 没有 K

而应该保：

原 belief：
它是 fixed-K sampler
     │
     │ 被什么击穿？
     ▼
Algorithm 1 原文没有 K
     │
     │ credit assignment
     ▼
错误来源：
把公平比较 surrogate
误当论文原方法
     │
     ▼
新的 recognition cue：
“如果必须额外补规则才能比较，
先检查是不是换研究对象”

这样树里长出来的不是一个孤立事实。

而是一个以后能触发的研究反射。

这就是为什么我们的树最好允许“错误边 / belief-update 边”，而不是只有“知识依赖边”。


---

6. GLM-5.3 那条更狠：训练能力之前，先训练“产生可信反馈的环境”

你之前的笔记里最有迁移价值的一句其实是：

> 不是有 feedback 就够了，feedback 本身也必须被验收。 



这对我们科研训练特别重要。

比如我给你一个小测。

如果我的答案本身错了，你：

> 做错 → 被判错 → 修改 belief



那不是学习。

那是错误 credit assignment。

所以我们的训练系统不能只有：

练习 → 判分 → 重做

还应该问：

> 判分依据是什么？
密封答案从哪里来？
能不能手算 / 代码双验？
verifier 测的是目标能力吗？
有没有 reward hacking？



这和 formal verification / AI safety 甚至天然接得上：

> 如何验证 verifier？



这是个很强的元问题。

所以以后我们做“导师模式面试”，也不该只问：

> 你答对了吗？



而要问：

> 这道题到底有没有 discriminative validity？

它真能区分科研潜力，还是只区分背题程度？



这就是把 GLM environment 那套思维迁到人的科研训练环境设计里。


---

7. DataMagic：知识树本身不能只是自然语言聊天记录，它需要一个“显式中间表示”

这个我现在觉得和你问“树该怎么长”非常直接相关。

你之前的 DataMagic 笔记里，DVSpec 的核心不是“做视频”，而是：

> Agent 可以自由 reasoning，
但真正的系统状态落成一个可检查、可编辑、可 diff、可 provenance 的 declarative spec。 



那我们的学习树也可以偷这个。

不要只靠：

> “GPT 应该记得我学到哪了。”



而是每个节点有一个小 schema：

NODE: DFG-support-vs-weight

STATUS:
  OWNED-L2

KNOWLEDGE:
  R = edge existence
  w = occurrence count

EVIDENCE:
  EX-01 task 1–4

RESEARCH-CUES:
  lossless relative to what?
  downstream uses support or frequency?

LINKS:
  -> Inductive Miner
  ~ test-suite reduction
  ~ coreset
  ~ abstraction

OPEN-QUESTIONS:
  does IMf change under distorted ratios?

IDEAS:
  witness set + exact edge counters

CLAIM-STATE:
  hypothesis only / untested

这就已经很像一个Research Learning IR。

自然语言可以随便聊。

但知识状态不要只存在聊天里。

这会让我们的树真的可维护。


---

8. DataMagic 的 Generate-then-Orchestrate 也能迁进“怎么 explore”

你之前问过：

> 我有联想以后怎么 explore，又怎么回来？



现在可以更精细一点：

探索阶段不需要过早串行压缩。

可以：

当前节点：EdgeIM frequency distortion

并行发散：
├─ 找 process-mining 同类方法
├─ 想形式化 abstraction 类比
├─ 想 coreset
├─ 想 streaming counter
├─ 找反例
└─ 想 downstream failure

这叫：

> generate broadly



然后不能六条全开工。

回来统一 orchestration：

哪个与当前 claim 最相关？
哪个可证伪？
哪个验证成本最低？
哪个会改变研究决策？

最后只升 1–2 条：

> orchestrate globally



DataMagic 的笔记本身就强调了“候选 scene 可以并行生成，但 global coherence 要集中编排”。

这个可以直接变成我们的 explore protocol。


---

把它们合起来以后，会出现一套非常漂亮的“科研动作语言”

以后碰到陌生问题，我们不是只会问“这是什么方法”。

我们可以像拿工具箱一样切换：

Representation operator

> 为什么一定在这个空间解决？



Redundancy operator

> 哪些东西重复做了，其实不必重复？



Invariant operator

> 哪一点必须保，其他能不能砍？



Dependency operator

> 哪些真的要串行？



Residual operator

> 上一刀砍完以后，新的瓶颈在哪？



Feedback operator

> 什么 evidence 才真正区分两个解释？



Verifier operator

> 我的反馈机制本身可信吗？



Budget operator

> 哪个 uncertainty 值得先花验证资源？



State operator

> 哪些语义必须显式落盘而不能藏在 narrative 里？



Experience operator

> 这次失败到底更新了哪条 belief？



我甚至觉得这比“科研能力图谱”还更具体。

因为“抽象能力”“科研 taste”这些词太大。

而这些是你遇到实际问题时可以执行的 mental moves。


---

而且 EdgeIM 正好可以成为第一块训练场

比如当前这个节点：

> Algorithm 1 保住 \(S/E/R\)，但 frequency 改变。



我们可以同时调用五个 operator：

Mapping Networks 式：

> 原日志是不是过高维？\(S/E/R\) 是不是 sufficient representation？



IndexShare 式：

> 去掉 trace redundancy 后，新的 residual 是不是 frequency distortion？



DSpark 式：

> structural witness 和统计计数是不是可以拆开处理？



Chain-of-Experience 式：

> 哪个反例把“zero loss = 什么都没丢”这个错误 belief 击穿？



DataMagic 式：

> support、weight、claim、evidence 能不能拆成显式状态，而不是混在一句“DFG 没变”里？



你会发现：

> 同一个 EdgeIM 小问题突然成了五种科研思维的训练器。



这就很有意思了。


---

我现在反而不想把这些论文只挂在树上当“跨领域阅读”。

更合适的是把它们抽成一个单独的：

> Research Operator Library



论文只是这些 operator 的典型案例 / provenance。

例如：

Operator: QUESTION-THE-REPRESENTATION
典型案例: Mapping Networks
触发信号:
  search space 巨大
  大量自由度可能相关
行动:
  寻找 sufficient lower-dimensional representation
防呆:
  必须验证被压掉的信息是否 irrelevant

再如：

Operator: CHASE-THE-RESIDUAL
典型案例:
  IndexShare / LayerSplit / HiSparse

触发信号:
  已经优化掉一个主瓶颈

行动:
  re-profile
  找新的 dominant cost

防呆:
  不要继续优化已经不是 bottleneck 的组件

随着以后再读论文，一个 operator 可以不断挂新的案例。

到最后你得到的不是：

> “我看过 300 篇论文。”



而是：

> 我见过 30 种反复出现的研究动作，而且知道它们什么时候可能有用、什么时候会骗人。



我觉得这个才真正接近你飞机上看那些论文时真正觉得“有东西可以偷过来”的那种感觉。

而且它正好回答我们上一轮“树怎么长”的遗漏：

> 知识节点在长；方法节点在长；科研能力也在长；但还应该有一层跨论文复用的“研究操作符”。



这层以后很可能会变成整棵树里最值钱的一层。

思考了 40s

对，你这个“怎么有点像控制/自动化”的感觉不是错觉。把两篇放在一起看以后，谱系会清楚很多，而且这里刚好能长出一条很有价值的跨学科枝。

先纠正一个很容易混的点：**两篇不都是鲁老师署名的论文。**如果你说的“鲁老师”是 Faming Lu，那么 EdgeIM 是：作者是 Xuan Su、Cong Liu、Faming Lu、Long Cheng、Qingtian Zeng、Shouli Zhang，而且 Faming Lu 是通讯作者。
但 CrossEdgeIM 不是：它的作者是 Xuan Su、Cong Liu、Qingtian Zeng、Jinglin Zhang、Long Cheng，没有 Faming Lu。它和 EdgeIM 属于高度重叠的合作团队——Xuan Su、Cong Liu、Qingtian Zeng、Long Cheng 四个人重合——而且 CrossEdgeIM 在正文里直接把 EdgeIM 当作前序方法来讲。

所以更准确的关系不是“鲁老师连续发了两篇 EdgeIM”，而是：

\[
\boxed{\text{同一研究合作谱系：EdgeIM }\rightarrow\text{ CrossEdgeIM}}
\]

CrossEdgeIM 自己的 Introduction 就说，EdgeMiner 和 EdgeIM 已经可以在 edge node 构造局部特征、集中聚合出全局过程模型，但主要面向 single-organization IoT；它觉得这个框架还不适合“跨组织、交互式机器人行为发现”，于是才引出 CrossEdgeIM。

	EdgeIM	CrossEdgeIM

主要场景	分布式 IoT 事件日志	分布式 IoRT / 多机器人组织
原始痛点	完整日志集中处理太贵	还多了组织隔离、机器人实时交互
最底层处理	case 先过滤，再分到 edge nodes	activity node 在线接收事件
传什么	局部 \(S_i,E_i,R_i\) 等结构信息	增量 \(\Delta DFR,\Delta Start,\Delta End\)
模型在哪里挖	central node 汇总 global DFG 后做 IM	organization node 就做 IM，得到 Org-Petri-net
最顶层合并什么	features / DFG	已经发现好的 Petri nets
最终结果	一个全局过程模型	多组织局部模型结构合并成 global robotic model


所以它其实发生了一个挺漂亮的计算位置迁移。

EdgeIM 大概是：

日志
→ 压缩
→ edge 上提特征
→ central 汇总特征
→ central 做过程发现

CrossEdgeIM 变成：

机器人事件流
→ activity node 在线提特征
→ organization node 汇总特征
→ organization node 已经做过程发现
→ central 只合并模型

CrossEdgeIM 明确把自己写成三层：Activity Node Processing、Organization Node Modeling、Central Node Merging；organization node 构造 Org-DFG 后直接运行 Inductive Miner 得到 Petri net，central 收到的是各组织的 Petri net，而不是原始日志。

这已经很像一种我们前面聊过的 Research Operator：move computation to where the information lives。


---

那为什么你会觉得它像控制论/自动化？

因为它们确实在一个很大的交叉地带里，但要把“控制论”拆开。

如果你脑子里的控制论是经典这种：

\[
\dot x=f(x,u)
\]

传感器测状态 → controller 算控制输入 \(u\) → 系统动态变化 → 再反馈，

那么 EdgeIM 本身并不是在做这种控制。

它没有设计控制律，没有问：

> “我给机器人什么输入才能稳定到目标状态？”



它问的是：

> “我观察到大量事件以后，这个系统实际上是按什么流程运行的？”



这是 model discovery / system behavior inference。

但它跟**自动化、离散事件系统（Discrete Event Systems, DES）**就真的非常近了。

你现在看到的：

A发生
→ B发生
→ C/D二选一
→ E

不是连续变量随时间平滑变化，而是：

> 一个个离散事件发生，系统状态随事件跳转。



自动化里大量系统恰好就是这种东西：

制造流水线；

机器人任务；

工作流；

通信协议；

仓储物流；

故障/恢复流程。


于是很自然会出现：

\[
\text{event}
\rightarrow
\text{state}
\rightarrow
\text{transition}
\rightarrow
\text{concurrency}
\rightarrow
\text{choice}
\rightarrow
\text{loop}
\]

……这不正是我们现在遇到的东西么。

而 Petri net 又正好特别擅长描述：

> 离散事件 + 并发 + 同步 + 资源约束 + 因果顺序。



所以它横跨过程挖掘、自动化、形式化方法，并不奇怪。

甚至作者单位本身就能看到这个交叉：EdgeIM 的 Long Cheng 来自 School of Control and Computer Engineering；CrossEdgeIM 又加入了来自山东大学 School of Control Science and Engineering 的 Jinglin Zhang，Long Cheng 仍在作者中。 


---

这里其实可以画出一条特别漂亮的知识桥

你现在原来的树大概是：

event log
→ DFG
→ Inductive Miner
→ Petri net

现在旁边可以长一条 PARKED 的跨学科枝：

离散事件系统 DES
                         │
              ┌──────────┼──────────┐
              │          │          │
         Petri Net    自动化系统   形式化方法
              │          │          │
              │          │        verification
              │          │
event log → process mining
              │
              ├─ discovery
              ├─ conformance checking
              └─ enhancement

注意这里有几个方向完全不同，但它们共享模型。

比如：

Process Mining：

\[
\boxed{\text{行为数据}\rightarrow\text{模型}}
\]

我看发生过什么，反推系统结构。

Formal Verification：

\[
\boxed{\text{模型}+\text{性质}\rightarrow\text{这个性质是否成立？}}
\]

我已经有模型，检查安全性、可达性等。

Supervisory Control / 自动控制中的离散事件控制：

\[
\boxed{\text{系统模型}+\text{规范}\rightarrow\text{如何限制系统行为？}}
\]

我不只观察，还要合成一个 supervisor，让非法行为不要发生。

突然就连起来了：

真实系统
   │
   │ observation
   ▼
event log
   │
   │ process mining
   ▼
behavior model
   │
   ├──────── verification ───────→ “它安全吗？”
   │
   └──────── control/synthesis ──→ “怎么让它只做允许的行为？”

这个桥我觉得对你以后特别有价值。

因为你想走的形式化验证 + AI safety，本身就在不断碰：

> 从行为得到模型

和

拿模型去验证/约束行为

这两个方向。
---

而 CrossEdgeIM 又比 EdgeIM 更往自动化那侧迈了一步，因为它的对象直接变成了：

> 多机器人、实时 event stream、edge/fog/cloud 三层计算、organization-level controller、协作行为模型。


论文甚至明确说 central node 可以部署在 cloud 或 supervisory controller 上。

所以你闻到的那股“自动化味”在 CrossEdgeIM 里确实更浓。

但这里要有一个科研上的边界意识：

> 用了机器人、Petri net、supervisory controller 这些词，不等于论文就在做控制理论。


它核心贡献还是：

\[
\textbf{distributed process model discovery}
\]

而不是 controller synthesis。

这个 distinction 非常值得练。


---

更有意思的是，两篇连起来刚好能看一次“瓶颈迁移”

EdgeIM 解决：

> complete centralized log 太重
→ edge 上提 feature
→ central 再重建 model。



CrossEdgeIM 接着问：

> 但真实 IoRT 不是只有一个组织啊，而且事件还是持续来的。



于是：

> case-level preprocessing
→ online incremental feature maintenance



以及：

> central 重建全部模型
→ organization 本地先建模，central 只做 model merging



这就是我们飞机论文里 IndexShare 那种 research move：

> 上一代解决一个瓶颈以后，重新看真正的大头跑到哪儿去了。



但它又带来下一代问题。

CrossEdgeIM 的 central merge 把每个 organization model 当成一个 logically independent branch，然后用共同 split/join 让这些组织模型并行组合。论文自己同时承认：目前没有显式捕获多机器人之间的协作关系，组织之间的 interaction 还是 implicit 的。

所以 genealogy 很漂亮：

Traditional centralized IM
       │
       │ 数据规模 / 通信 / 隐私
       ▼
EdgeMiner / EdgeAlpha
       │
       │ 效率、复杂结构、时间戳冲突
       ▼
EdgeIM
       │
       │ single organization
       │ streaming / robotic setting
       ▼
CrossEdgeIM
       │
       │ 新 residual
       ▼
“真正的跨机器人/跨组织 interaction
到底怎么显式建模？”

这个已经不是单纯“看两篇 paper”。

它在教你一个科研动作：

> 看后继论文，不只看它多加了什么；还要看它把上一代哪个 bottleneck 移走了，以及新架构又把什么东西暴露成了下一代 bottleneck。



我会把这两篇在我们的树上这样挂：

> EdgeIM = 主干当前对象
CrossEdgeIM = vertical genealogy / downstream extension，暂时 PARKED
DES–Petri net–automation–formal verification = 一条跨学科桥，SEEN，等我们学到 Petri net 时正式 OPEN。



这样现在不会被自动化理论整个拖走，但你刚刚闻到的这个连接也不会丢。


能，而且我觉得这条线跟 Multi-Agent Systems 的关系其实比表面上还近。

但要先区分两种“能用”：

第一种是直接拿过程挖掘/Petri net 去分析 Multi-Agent 行为；第二种是进一步把它变成Multi-Agent 的监控、验证、协调甚至协议设计工具。前一种已经很自然，后一种就开始进入你会感兴趣的研究空间。

CrossEdgeIM 本身其实已经走到门口了：它把对象换成了跨组织、多机器人行为，activity node 在线收事件，organization node 建局部模型，central node 再合并模型。也就是说，“多个自治实体产生事件 → 汇总行为结构 → 得到全局模型”这个框架已经非常 MAS 味了。


---

先从最简单的 Multi-Agent 例子开始

假设有两个 Agent：

Agent A:
observe_task
→ propose_plan
→ send_plan

Agent B:
receive_plan
→ check_plan
→ send_feedback

Agent A:
receive_feedback
→ revise_plan
→ execute

如果系统把这些动作都记成 event log：

case1:
A.observe
A.propose
A.send
B.receive
B.check
B.feedback
A.receive
A.revise
A.execute

那它已经天然满足过程挖掘的输入形式：

> 一串带 case / activity / timestamp / actor 的事件。



所以最直接可以做：

\[
\text{Multi-Agent trajectories}
\rightarrow
\text{event logs}
\rightarrow
\text{DFG / process model}
\]

然后问：

> 这些 Agent 实际上是怎么协作的？



而不是只看 prompt 里“理论上应该怎么协作”。

这个 distinction 非常重要。


---

第一种用途：发现 Multi-Agent 的“真实协作流程”

你写了一个 workflow：

Planner
→ Critic
→ Executor

你以为系统是这么跑的。

但真实运行 1000 次以后可能变成：

Planner
→ Critic
→ Planner
→ Critic
→ Planner
→ Executor

甚至：

Planner
→ Executor
→ Error
→ Critic
→ Planner

Process Mining 做的就是：

> 从实际 trajectories 反推出真实行为模型。



于是可以发现：

哪些 Agent 总是先于哪些 Agent；

哪里经常反复循环；

哪些路径只在失败 case 出现；

哪些 Agent 实际上几乎没贡献；

哪些“并行”任务事实上被某个依赖串行化；

哪些 delegation pattern 最常出现。


这已经挺适合现在的 LLM multi-agent systems 了。


---

第二种：Conformance Checking —— “设计的协议”和“实际行为”对不对得上

这个比单纯 discovery 更有意思。

你可以有一个预期协议：

Planner
   ↓
Reviewer
   ↓
Executor

规定：

> Executor 必须在 Reviewer approval 后才能执行。



然后拿真实 agent log 检查：

Planner
→ Executor
→ Reviewer

这就是 deviation。

所以形成：

\[
\text{desired protocol}
+
\text{actual event log}
\rightarrow
\text{conformance checking}
\]

对 Multi-Agent safety 特别自然。

例如：

> 某个高风险 action 是否一定经过 verifier？



> 两个 agent 是否出现了不允许的 communication sequence？



> 执行 agent 有没有跳过 approval？



这个已经开始和形式化验证接上了。


---

第三种：Petri net 特别适合表示 Multi-Agent 的“并发 + 同步”

这是为什么你会觉得它和 MAS 很搭。

Multi-Agent 的麻烦之一就是：

> 多个东西同时跑。



例如：

┌→ Agent B: search literature ─┐
Agent A plan ├→ Agent C: inspect code       ├→ Integrator
             └→ Agent D: design experiment ─┘

这里不是普通 sequence。

你需要表达：

1. B/C/D 可以并行；


2. Integrator 必须等若干结果；


3. 某些 branch 可选；


4. 某些失败后回去重试；


5. 有共享资源可能不能同时使用。



Petri net 本来就擅长这几个东西：

> concurrency
synchronization
choice
loops
resource constraints



所以它比单纯“画 Agent graph”丰富。

一个普通 directed graph：

Planner → Critic → Executor

很难告诉你：

> Critic 和 Searcher 是不是必须都完成才能执行。



Petri net 可以明确表达这种同步条件。


---

第四种：Runtime Monitoring

这就开始有你形式化 + AI Safety 那种味道了。

假设我们已经得到/规定一个合法 Multi-Agent protocol：

Researcher
→ Evidence Checker
→ Experiment Runner
→ Claim Reviewer

系统在线跑的时候，每来一个 event：

Researcher.propose
Checker.approve
Runner.execute
...

runtime monitor 就更新当前状态。

如果下一步出现：

Runner.execute

但此前没有：

Checker.approve

就可以立刻报：

> protocol violation。



所以：

\[
\text{Petri-net / automaton model}
+
\text{live agent event stream}
\rightarrow
\text{runtime verification}
\]

这已经不是“事后看日志”。

是：

> 边跑边检查 Agent collective behavior。




---

第五种：异常 / 失败模式发现

这个我觉得特别值得你以后看。

比如你跑 10000 条 multi-agent trajectory。

成功 case：

Plan → Review → Execute

失败 case 经常：

Plan → Review → Replan → Review → Replan → ...

那 process mining 可能发现：

> 一个明显的 review-replan loop 与 failure 强相关。



于是你得到的不是：

> “失败率 22%。”



而是：

> 失败是怎样长出来的。



这就和你前面特别喜欢的 Chain-of-Experience 接上了。

从：

\[
\text{outcome statistics}
\]

升级到：

\[
\text{behavioral failure structure}
\]

这个可能比最后 accuracy 更有解释力。


---

但 CrossEdgeIM 直接迁到 LLM Multi-Agent 有一个大坑

而且这个坑刚好就是 CrossEdgeIM 自己的 limitation。

它目前上层合并组织模型时，本质上更接近：

> 每个 organization 有自己的 Petri net，最后把它们作为相对独立的 branch 合起来。



但真正 Multi-Agent 最重要的往往恰恰是：

> agent-to-agent interaction。



CrossEdgeIM 自己也承认，目前并没有显式捕获 multi-robot collaborative relations，跨组织 interaction 还是 implicit 的。

这对 LLM agents 会更严重。

因为一个 agent 的行为往往取决于：

A sends(message m)
       ↓
B receives(m)
       ↓
B updates belief
       ↓
B sends response

如果你只记录：

A.send
B.receive

而不记录：

message identity；

causal link；

sender/receiver；

shared artifact；

dependency；

tool/resource state；


那你会丢掉大量真正的协作语义。

所以如果真往 MAS 扩，我第一反应不会是：

> “直接把 CrossEdgeIM 跑一遍。”



而是：

> Multi-Agent 的 feature representation 应该是什么？



这一下又回到我们那个 Research Operator：

> Question the representation.




---

例如，普通 DFG 可能不够

普通 DFG 的 edge 是：

\[
activity_i\rightarrow activity_j
\]

但 Multi-Agent 可能更需要：

\[
(agent_i,activity_i)
\rightarrow
(agent_j,activity_j)
\]

甚至还不够。

可以继续扩成：

\[
(agent_i, action_i, artifact/message)
\rightarrow
(agent_j, action_j)
\]

例如：

(A, send, plan_17)
        ↓
(B, review, plan_17)

这样才能知道：

> B review 的到底是不是 A 刚发的那个 plan。



再进一步，你可能需要区分：

control flow：谁先谁后；

message flow：谁给谁发了什么；

data flow：共享了哪个 artifact；

authority flow：谁能批准谁；

resource flow：谁占了哪个工具；

epistemic flow：哪个 agent 知道什么。


这时候就已经变成一个很漂亮的跨学科问题了。


---

而且这里能接好几个领域

你刚才问“能不能用在 Multi-Agent”，往下一层其实会把我们树上好多枝突然连起来：

Process Mining
     │
     ├── event log → behavior discovery
     │
Multi-Agent Systems
     │
     ├── communication / coordination
     │
Discrete Event Systems
     │
     ├── concurrency / synchronization
     │
Petri Nets
     │
     ├── behavioral model
     │
Formal Verification
     │
     ├── safety / liveness / reachability
     │
Runtime Verification
     │
     ├── online protocol checking
     │
AI Safety
     │
     └── agent interaction assurance

这个交叉其实很自然。


---

再往研究问题走，能长出什么？

我现在随手就能从这个连接里重建几类可研究问题，不过先都标成 idea，不是 claim。

A. Multi-Agent Process Discovery

> 从 agent trajectory 自动发现协作协议。



输入：

\[
\text{agent interaction logs}
\]

输出：

\[
\text{interaction process model}
\]


---

B. Safe Multi-Agent Protocol Mining

只看 successful/safe trajectories：

> 能否归纳出一个“安全协作流程”？



然后检测新轨迹是否偏离。


---

C. Failure-specific process mining

把 success / failure 两类分开：

> 哪些结构只在失败轨迹中出现？



例如：

> verifier bypass
excessive replan loops
cyclic delegation
deadlock
premature tool use




---

D. Online MAS Monitoring

Agent 正在运行时维护增量 DFG/Petri-net state：

> 一旦进入危险行为结构，提前报警。



这个就和 CrossEdgeIM 的 online incremental update 很贴近。


---

E. Agent orchestration optimization

如果发现：

A → B → C

实际上：

B 和 C 没有依赖

那能不能改成：

A → (B || C)

这就是：

> process discovery → bottleneck diagnosis → orchestration redesign



一下又接回你飞机上 DSpark 那条：

> 真依赖串行，不依赖的重活并行。




---

一个我觉得特别有意思的方向

甚至可以把 process model 当作 Multi-Agent harness 的显式 IR。

这和 DataMagic 的 DVSpec 有点像。

现在很多 agent workflow 是自然语言：

“You are planner...
Then ask reviewer...
If approved call executor...”

这其实很难验证。

如果编译成一个显式 process specification：

sequence(
  planner,
  parallel(searcher, checker),
  reviewer,
  choice(
    approve → executor,
    reject  → planner
  )
)

甚至进一步变成 Petri net / state machine，

那么：

可执行；

可视化；

可验证；

可 diff；

可 runtime monitor；

可 provenance；

可以检查 deadlock/liveness。


这就很有意思了。

> 自然语言 Agent orchestration → formal process IR



这个连接我觉得非常值得在我们 Transfer Map 上登记。

不是说它一定能成为论文。

但它是个很好的研究芽。


---

不过现在要立一个边界

我们目前只能比较有把握地说：

> Process-mining / Petri-net / DES 这套思想很适合用于建模和分析 Multi-Agent interaction。



不能现在就跳成：

> “EdgeIM 能解决 Multi-Agent safety。”



中间还有很多 identification gap：

1. 什么算 event？


2. case 怎么定义？


3. agent interaction 的 causal relation 怎么表示？


4. message semantics 是否需要进入模型？


5. DFG 是否 sufficient？


6. stochastic / nondeterministic agent 如何建模？


7. LLM agent 的 hidden state 怎么处理？


8. process conformance 和“安全”到底是什么关系？



这些正是以后可以探索的。

所以这条枝我会现在标成：

> TRANSFER NODE — OPEN/SEEN

Process Mining / Petri Nets → Multi-Agent behavior discovery & runtime assurance



然后下面先挂三个醒目的 research cues：

> representation：什么叫一个 Multi-Agent event？



> interaction：跨 Agent causality 怎么显式表示？



> claim：流程合规 ≠ 自动等于安全。



这三个问题比“能不能用”本身更有研究价值。

而且我觉得你刚才这个联想挺好：你已经开始不是在问“CrossEdgeIM 是什么”，而是在自动做：

\[
\text{方法结构}
\rightarrow
\text{抽象}
\rightarrow
\text{新领域映射}
\rightarrow
\text{边界问题}
\]

这其实就是我们前面说的 L5 transfer 开始冒头了。



思考了 1m 16s

有，而且截至 **2026-09-05**，这条线已经明显开始“汇流”了。只是它现在还没有统一挂着一个叫 **“LLM Multi-Agent Process Mining & Formal Assurance”** 的招牌；研究者分别从 process mining、agent trace/observability、formal methods、protocol security、workflow synthesis 这几个社区往中间靠。

我刚扫了一轮 arXiv、OpenReview 和预印本，最值得我们挂进树里的大概是这些：

| 工作 | 状态 | 它在做什么 | 和我们刚才想法的距离 |
|---|---|---|---|
| **TraceCompiler: Skill-Guided Mining and Compilation of LLM Agent Traces into Mostly Deterministic Workflows** | arXiv, 2026-08 | 从大量 noisy agent traces 里挖依赖关系，编译成可执行 workflow | **非常近，当前第一优先** |
| **Lean4Agent** | arXiv, 2026-06 | 用 Lean4 显式建模 agent workflow + trajectory，验证并定位失败，再修改 workflow | 很近：formal IR / verification / repair |
| **AgentRFC / AgentThread** | arXiv preprints, 2026-03/06 | agent protocol → typed IR / TLA+ → model checking → counterexample replay 到真实 SDK | 很近：protocol formalization + compositional safety |
| **Do LLM Agents Negotiate Rationally?** | arXiv, 2026-07 | 把 A2A/MCP 上的多 Agent 协商协议写成约束，在线验证/修复消息 | 很近：cross-agent interaction + runtime verification |
| **Mission-Level Runtime Assurance for LLM-Assisted ISR Swarms** | arXiv, 2026-07 | platform→squad→mission 三层 compositional runtime verification | 很近：local safety ≠ global safety |
| **AutoRAS** | OpenReview / ICLR 2026 投稿版本 | 用 symbolic primitives 表示 sequence/parallel/branch 等 agent workflow，执行后分析 trace，再优化结构 | 很近：formal-ish workflow IR + trace feedback + synthesis |
| **ProtocolBench** | OpenReview 版本 | 比较 A2A/ACP/ANP 等协议，显式记录 workflow/message-flow 和结构化运行 traces | 邻近：给在线 discovery/monitoring 提供 telemetry substrate |

其中 **TraceCompiler 是最让我觉得“草，这已经踩到我们刚才那根枝上了”的一篇。**

它 2026 年 8 月才挂出来，目标就是：tool-using LLM agents 会产生大量带有 retry、探索、偶然顺序、重复查询的 noisy traces，作者希望从这些轨迹中**挖出真正可复用的程序结构，再编译成 mostly deterministic workflow**。更关键的是，他们不满足于：

\[
A\text{ 后面经常出现 }B
\]

而是要求类似：

> B 的输入里确实包含一个可以唯一追溯到 A 输出的值。

才建立 hard dependency；不确定的关系标成 suspected，而不是强行加顺序约束。citeturn537755view0turn537755view1

这和你现在刚学的 EdgeIM/DFG 发生了一个非常漂亮的碰撞。

TraceCompiler 甚至拿 **adjacency / frequency-thresholded directly-follows** 当 baseline。在他们 T1 数据上，基于直接相邻/频率的这种关系只有约 **0.71 F1**，而 producer-consumer dependency rule 达到 0.928 precision / 0.943 recall。citeturn537755view0

也就是说，我们刚刚自己问的：

> “普通 DFG 到 LLM Multi-Agent 会不会不够？”

现在已经有一份非常新的 empirical warning：

\[
\boxed{\text{directly-follows} \neq \text{true dependency}}
\]

为什么？

比如：

```text
Agent A: search()
Agent B: summarize()
```

运行日志里 A 总在 B 前面，不代表 B 真的依赖 A。

可能只是 orchestrator 恰好这么调度。

反过来：

```text
A: produce artifact_17
...
C: inspect artifact_17
```

中间隔着五个动作，但 C 对 A 有非常真实的 data dependency。

这一下就把我们之前的：

\[
(agent_i, action_i)\to(agent_j,action_j)
\]

继续升级成：

\[
\boxed{
control\ flow
\neq
data\ flow
\neq
message\ flow
\neq
causal\ dependency
}
\]

这个我觉得应该直接进入我们的 **Research Operator Library**：

> **Adjacency is not causality.**

---

另一条正在快速长的是 **formal workflow / protocol IR**。

**Lean4Agent** 直接把 agent workflow 和实际 execution trajectory 用 Lean4 建模，检查 semantic consistency，并利用失败 trajectory 定位 workflow 的问题；他们还做了 LeanEvolve，根据形式化检查结果修改 workflow。也就是说：

\[
\text{workflow}
\rightarrow
\text{formal model}
\rightarrow
\text{trajectory evidence}
\rightarrow
\text{verification}
\rightarrow
\text{workflow repair}
\]

这个闭环已经跟我们之前脑补的“自然语言 Agent orchestration → formal process IR → verify → repair”高度重合了。citeturn181585academia0

但它选的是 **Lean/dependent types**，不是 Petri net。

这说明一个特别值得研究的问题不是：

> “要不要形式化？”

而是：

> **什么 formal representation 最适合什么 assurance question？**

Petri net 擅长 concurrency/synchronization/deadlock；TLA+ 擅长分布式协议状态转换和 invariant；Lean 可以表达更丰富的语义性质；MDP/model checking 适合概率性行为。

这就是我们 Method Landscape 可以开始长的地方。

---

更靠 **Multi-Agent protocol + formal verification** 的，是 AgentRFC/AgentThread 这条。

AgentThread 把 agent protocol specification 中的要求形式化成 **TLA+ invariants**，先 model-check，然后把得到的 executable counterexample 再通过 protocol adapter 重放到真实 SDK 上。最有意思的是，他们发现不少问题**单独看一个协议不存在，协议组合以后才出现**：报告了 30 个只在 composition 下出现的额外 failures。citeturn537755view3turn537755view4

这个跟 CrossEdgeIM 那个：

> local organization model 都正常  
> ↓  
> global merge 呢？

简直是同一个元问题的现代 agent-protocol 版本：

\[
\boxed{
P(A)\land P(B)
\not\Rightarrow
P(A\parallel B)
}
\]

所以我们之前说的 **local–global gap** 不是一个泛泛的 safety slogan，现在是非常活跃的前沿问题。

---

而 **Mission-Level Runtime Assurance for LLM-Assisted ISR Swarms** 更直接。

它研究的就是：

> 每个单体 agent 看起来全部 compliant，但是几个 agent 联合起来却完成了一个 mission-level prohibited behavior。

于是做 platform → squad → mission 三层 compositional runtime verification，并且保留 provenance，指出究竟哪些 platform **共同导致** violation。citeturn243228academia27

这个对我们特别重要，因为它给“Multi-Agent safety”一个很具体的 failure model：

```text
Agent 1 做的事合法
Agent 2 做的事合法
Agent 3 做的事合法

但是

Agent1 + Agent2 + Agent3 的组合
= 非法目标
```

也就是说：

> **collective behavior 本身就是一个新的 verification object。**

这跟传统 MAS、Petri net composition、CrossEdgeIM 以及 AI safety 四条线真的开始接起来了。

---

还有一条很有趣的是 OpenReview 上的 **AutoRAS**。

它不做 process mining，但它做了一个很像我们 DataMagic/DVSpec + Petri-net 思维的东西：不用“一个静态 Agent DAG”定义 agentic system，而是定义一套 symbolic primitives，里面明确有 **sequential chaining、parallel grouping、branch merging** 等结构操作，再把 primitive sequence 编译成 executable workflow。运行以后 monitor execution traces，把 failure/safety signal 再反馈给下一轮系统设计。citeturn754842view1

它实际上在走：

\[
\boxed{
\text{formal-ish design IR}
\rightarrow
\text{execute}
\rightarrow
\text{trace}
\rightarrow
\text{diagnose}
\rightarrow
\text{redesign}
}
\]

这已经不是简单：

> “Multi-Agent workflow 怎么写？”

而是在研究：

> **workflow structure 本身是不是一个可搜索、可优化、可反馈学习的对象？**

这跟我们前面“process discovery → orchestration redesign”那条研究芽高度相关。

---

然后还有一个方向我会标成**反向箭头**，不要混淆。

2026 年 7 月的 **Using Process Mining to Generate AI Agents from Software Engineering Process Records** 已经真的在做：

\[
\text{software event logs}
\rightarrow
\text{process mining}
\rightarrow
\text{发现项目中的角色/流程}
\rightarrow
\text{生成 AI agent specification}
\]

也就是不是：

\[
Agents\rightarrow traces\rightarrow process
\]

而是：

\[
process\ traces\rightarrow Agent\ design
\]

作者结合 object-centric、imperative 和 declarative process mining，根据真实软件仓库行为去决定 agent role 的划分和粒度。citeturn243228academia26

所以“process model 反过来指导 Multi-Agent orchestration”也已经有人在动了。

---

## 那还有没有真的没被吃掉的缝？

有，而且我现在比上一轮更敢说“**确实存在一个正在形成、但尚未完全合流的缝**”。

你会发现现在各社区拿的 representation 是分裂的：

```text
Process Mining
    → DFG / Petri Net / process model

TraceCompiler
    → def-use / producer-consumer dependency graph

Agent protocol security
    → typed Protocol IR / TLA+

Lean4Agent
    → dependent-type formal workflow

Runtime assurance
    → MDP / temporal properties / compositional monitors

Agent workflow design
    → DAG / symbolic primitive sequence
```

大家其实都在处理：

\[
\text{Agent interaction traces}
\leftrightarrow
\text{explicit behavioral structure}
\leftrightarrow
\text{verification / optimization}
\]

但**中间表示还没有收敛**。

我目前没看到一条已经成熟地把下面整个闭环吃掉的主流工作：

\[
\boxed{
\begin{array}{c}
\text{LLM Multi-Agent telemetry}\\
\downarrow\\
\text{online interaction/process discovery}\\
\downarrow\\
\text{显式区分 control/data/message/causal flow}\\
\downarrow\\
\text{compositional formal model}\\
\downarrow\\
\text{runtime verification}\\
\downarrow\\
\text{violation provenance}\\
\downarrow\\
\text{protocol/workflow repair}
\end{array}}
\]

现在基本是每篇吃其中两三块。

这就是很典型的**fragmented frontier**。

---

尤其对我们当前 EdgeIM 这条线，TraceCompiler 给了一个很强的提醒：

我们原先很自然会想：

> EdgeIM / incremental DFG  
> → 搬到 LLM MAS  
> → online monitor。

现在不该这么快。

更好的第一问已经变成：

> **DFG 到底是不是适合 Agent interaction 的 sufficient representation？**

因为最新工作已经提示：

> agent trace 里的 adjacency 含有大量 accidental ordering。

那么一个更合理的 research design 可能是比较：

\[
DFG
\]

vs.

\[
DFG + actor
\]

vs.

\[
message-aware DFG
\]

vs.

\[
dataflow / def-use graph
\]

vs.

\[
hybrid interaction graph
\]

然后问：

> 对 failure localization、protocol discovery、runtime verification，哪个 representation 真正 sufficient？

这个比：

> “把 EdgeIM 用到 MAS！”

已经收敛很多了。

这就是刚刚一次很标准的 **prior-art feedback → idea update**。

---

还有一组工作可以当“基础设施层”看。

**TRAIL** 已经在收 single/multi-agent 的长 execution traces，并做 failure taxonomy 和 issue localization；他们发现即使长上下文模型，对复杂 agent trace 的 debugging 也非常弱。citeturn740082academia0

而 2026 的 trace/provenance 工作开始围绕 OpenTelemetry、execution lineage、evidence tracing 搭统一 observability substrate。比如最近的综述明确把 claim、tool output、memory、environment observation、action 和 final answer 之间的 provenance 关系当作核心研究对象。citeturn740082academia2

这意味着未来这种：

> process mining / formal verification of agents

可能不必自己从零发明日志格式。

**Agent telemetry 正在逐渐成为自己的研究基础设施。**

---

所以如果给我们这条树现在打状态，我会这么标：

**HIGH-PRIORITY TRANSFER：TraceCompiler。**  
它和 EdgeIM/DFG 的接口太直接，尤其那个 directly-follows baseline 值得以后专门拆。

**FORMAL BRIDGE：AgentRFC/AgentThread + Lean4Agent。**  
看大家怎么把自然语言 workflow/protocol 变成真正可检查的 formal object。

**COMPOSITION BRIDGE：Mission-Level Runtime Assurance。**  
专门训练“local property 为什么不自动组合成 global property”。

**DESIGN LOOP：AutoRAS。**  
看 trace feedback 怎么反过来优化 Agent architecture。

CrossEdgeIM 暂时还是我们的 process-mining 主谱系；这些是从另一边正在逼近它的 **2026 agent frontier**。

所以你前几轮那个直觉——

> “Process Mining / Petri Net / Formal Verification / Multi-Agent 好像开始能接起来？”

现在我的判断会从原来的：

> “理论上挺自然。”

升级成：

> **“对，2025–2026 已经能看到多个研究社区独立地向同一个交叉区域收敛；但 representation、discovery、formal assurance、runtime intervention 还没有被一套统一方法真正打通。”**

这个节点值得在我们的树上从 **SEEN** 升到 **PARKED-HIGH** 了。（）

每周扫描 LLM Multi-Agent × Process Mining / Formal Verification 新预印本

已创建。它会每周扫描一次 LLM Multi-Agent × Process Mining / Formal Verification 方向的新预印本、OpenReview 投稿、tech report 等，并优先关注：

agent interaction traces、process/behavior discovery、Petri net / workflow、protocol formalization、runtime verification、compositional safety、provenance，以及 workflow/protocol repair。

我还让它每次顺手判断这些新工作和 EdgeIM / CrossEdgeIM 的关系：是直接 prior art、邻近工作，还是能挂进我们研究树的新节点。


对。真正难的其实不是“能联想到”，而是：

> 怎么把一个“这俩东西好像很像”的 sense，筛成“这里真的存在可迁移的研究结构”。



我觉得我们应该专门训练一种能力：Transfer Judgment / 迁移判别力。

不是看到“都在做图”“都在压缩”“都在反馈”就硬连，而是判断：到底是词像、形状像、机制像，还是问题本身真的同构。

我会先给你一个非常实用的分层。

层级	你看到的相似性	研究价值

L0 词汇相似	都叫 graph / agent / feedback	几乎没价值
L1 现象相似	都有“重复”“瓶颈”“并行”	可作为灵感
L2 结构相似	对象—关系—约束有对应	值得继续挖
L3 机制相似	源方法有效所依赖的机制，在目标里也存在	真正可迁移
L4 干预相似	不只机制存在，而且可以用类似手段改变系统	很有研究价值
L5 可验证迁移	能设计便宜实验区分“迁移成立/不成立”	可以进入研究队列


关键是：

> 绝大多数“sense”停在 L1/L2。真正值钱的是能一路走到 L3–L5 的。




---

最核心的一问：源方法到底“靠什么”才有效？

这是判断迁移价值的中心。

比如你看到 EdgeIM：

> 从大量日志里抽结构。



然后想到：

> Multi-Agent 也有很多 traces，那是不是也能抽结构？



光到这里其实只是 L1/L2。

真正要往下追：

> EdgeIM 这一刀为什么成立？



不是因为它“用了 DFG”。

而是因为它隐含假设：

> 某种局部事件关系能够作为 downstream 需要的结构表示。



于是迁到 Agent 时，真正应该问的不是：

> 能不能也画 DFG？



而是：

> Agent 系统里，什么 observable relation 才承担那个“结构信息”的角色？



可能不是：

\[
A\text{ 后发生 }B
\]

而是：

\[
B\text{ 消费了 }A\text{ 产生的 artifact}
\]

或者：

\[
A\text{ 的 message causally enabled }B
\]

于是这里出现一个特别重要的 distinction：

> 方法可能不能搬，但研究操作符可以搬。



EdgeIM 的具体 DFG 未必适合 LLM agents。

但：

> 从行为痕迹中恢复 downstream-relevant latent structure



这个 operator 很可能能迁。

这就是我们应该挖的东西。


---

我会给每个 sense 做一次“机制剥皮”

以后你突然说：

> “这个跟 XX 很像诶？”



我不会马上搜论文。

我们先写：

\[
\text{source phenomenon}
\rightarrow
\boxed{\text{why does it work?}}
\rightarrow
\text{abstract mechanism}
\]

例如 Mapping Networks：

> 高维参数空间很大
→ 但好解可能只占低维有效自由度
→ 压缩搜索自由度



IndexShare：

> 每层都重新算 selection
→ 相邻层 selection 高度冗余
→ 重复计算可共享



DSpark：

> 全串行慢、全并行丢依赖
→ 真正必须串行的只有一小部分依赖
→ dependency-critical part serial, expensive independent work parallel



Process mining：

> 大量执行 traces 太复杂
→ 行为里存在重复、稳定结构
→ recover latent behavioral structure



这一步特别重要，因为我们要迁的是中间那个机制，不是两边的表面实现。


---

然后做一次“目标域验尸”

假设我们抽到了：

> recover latent behavioral structure from traces



再看 Multi-Agent。

不能问：

> “像不像？”



要问四件事情。

第一：这个 latent structure 在目标域里真的存在吗？

Agent traces 可能非常随机。

如果每次策略完全不同、没有稳定组织规律：

> 那就没有值得 mining 的 structure。



sense 到这里直接死。


---

第二：我们能观察到它吗？

真实 dependency 可能存在，但日志只记：

Agent A called tool
Agent B called tool

没有 message ID、artifact provenance、sender/receiver、causal link。

那结构存在也没用。

因为：

\[
\text{存在} \neq \text{可识别}
\]

这是很多跨域 idea 会死掉的地方。


---

第三：识别出来以后能干嘛？

这个特别重要。

如果我们发现：

> A 经常在 B 前面。



然后呢？

如果这不能改变：

verification；

orchestration；

failure diagnosis；

compilation；

monitoring；

resource allocation；


那它可能只是一个漂亮 visualization。

科研价值通常来自“结构改变了什么决策”。


---

第四：能不能证明自己不是在看幻觉？

比如：

> 我挖出了一个 Agent dependency graph。



怎么知道它是真的 dependency，不是偶然 adjacency？

必须能设计 discriminating test。

比如：

> 打乱两个 independent action 的顺序，模型应该认为结构不变；



而：

> 换掉 producer artifact，consumer 应该失效。



这样才从：

> “图挺像”



升级成：

> “这个 representation 捕捉到了 operational dependency。”




---

所以我现在会用一个很简单的“迁移价值公式”

不是数学定理，只是判断器：

\[
\text{Transfer Value}
\approx
\frac{
\text{mechanism overlap}
\times
\text{observability}
\times
\text{actionability}
\times
\text{testability}
}{
\text{translation cost}
}
\]

这里每一项都很实际。

mechanism overlap：源方法成功依赖的东西，目标里有没有。

observability：你拿得到相关信号吗。

actionability：识别以后能不能改变系统。

testability：有没有最小实验快速证伪。

translation cost：为了迁移是不是得重造半个领域。

最后这一项很多人会忘。

一个 idea 再漂亮，如果：

> 要先造新的 logging infrastructure、新 benchmark、新 formal semantics、新 runtime……



你可能面对的不是“小迁移”，而是三篇博士论文的工程量。

那就要判断值不值得。


---

还有一种特别常见的“假相似”

比如：

> control system 有 feedback
RL 也有 feedback
human learning 也有 feedback
Agent 也有 feedback



如果只到这里：

> 没意义。



因为 feedback 这个词太宽。

真正要抽：

控制里：

\[
state\rightarrow controller\rightarrow action\rightarrow environment\rightarrow new\ state
\]

科研学习里：

\[
belief\rightarrow experiment\rightarrow evidence\rightarrow belief\ update
\]

这时共同结构可能是：

> 闭环系统通过外部响应修正内部状态。



不错，到了 L2/L3。

然后还要问：

> 控制里的 stability、observability、delay、noise 这些概念，在科研学习里有没有可操作对应？



有些可能有。

有些只是诗意类比。

我们必须敢于写：

> analogy stops here.



这也是研究素养。


---

一个特别好的判断标准：能不能产生“新预测”

这是我觉得最有杀伤力的一条。

如果两个领域真的共享机制，那么迁移过去以后应该不只是：

> “解释起来很像。”



而应该能推出一个原本没想到的 prediction。

比如我们从 DSpark 偷：

> 长 dependency chain 的可靠性会累积衰减。



迁到 Agent workflow：

于是预测：

> verification checkpoint 不应该平均插，而应该优先插在高风险 dependency boundary。



这个可以实验。

这就是有价值的迁移。

再比如从 process mining 偷：

> behavior 中存在稳定 structure，可以被显式恢复。



迁到 Agent：

预测：

> successful traces 与 failure traces 在 dependency topology 上应该存在系统性差异。



这也能实验。

如果一个类比永远只能产生：

> “这让我想到 XX。”



但无法产生：

> “因此我预期在条件 C 下会看到现象 Y。”



那它大概率还只是 inspiration。


---

所以我们“怎么挖”其实可以固定成一个闭环

以后出现 sense，我们这样走：

① 闻到相似
   ↓
② 去表面化
   “两边真正共享的结构是什么？”
   ↓
③ 机制剥皮
   “源方法到底靠什么成立？”
   ↓
④ 目标域检查
   “那个机制在那里也存在吗？”
   ↓
⑤ Representation check
   “我能观察/表示它吗？”
   ↓
⑥ Intervention check
   “知道它以后能改变什么？”
   ↓
⑦ 生成新预测
   ↓
⑧ 找 prior art
   ↓
⑨ 最小实验 / 反例
   ↓
⑩ 决定：DROP / PARK / PROMOTE

注意 prior-art search 不一定非要最早。

有时候我们先自己抽 10 分钟，会更清楚到底应该搜什么。

否则一开始搜：

> “multi-agent process mining”



很容易被关键词把思路锁死。


---

related work 也要换搜索单位

你以后不能只搜领域名。

更应该搜动作 + 对象 + 结果。

比如我们现在这个 sense：

> traces 里有隐藏结构，可以抽出来再改变执行。



那么搜索就不只是：

> process mining LLM agent



而是沿着几个动作族去搜：

agent trace dependency inference
workflow induction from execution traces
program synthesis from traces
agent trajectory compilation
protocol inference from logs
causal dependency discovery agent workflow
runtime monitor learned from traces
workflow repair from execution feedback

你会发现很多真正有价值的 adjacent work 根本不叫 process mining。

这就是：

> search by mechanism, not by tribe.




---

我特别建议我们以后给“跨域联想”建一种 Transfer Card

非常短，不搞成官僚文档。

比如：

SENSE
EdgeIM → Multi-Agent

SOURCE MECHANISM
从 execution traces 中恢复稳定行为结构

TARGET CANDIDATE
Agent interaction traces

SHARED STRUCTURE
重复执行 + dependency + concurrency

POSSIBLE MISMATCH
DFG adjacency ≠ causal/data dependency

NEEDED OBSERVABLES
actor / message / artifact / tool / provenance

ACTION IF SUCCESSFUL
monitor / verify / compile / optimize orchestration

NEW PREDICTION
failure traces should exhibit distinct dependency structures

CHEAPEST TEST
构造两个相同 adjacency、
不同 causal dependency 的 toy workflows

STATUS
PROMISING / PARKED

这个东西会特别适合我们现在的研究树。

因为它能把：

> “我感觉像。”



一路压成：

> “哪里像、哪里不像、为什么可能有用、怎么证伪。”




---

更进一步，我觉得要训练的是一种“二次抽象”能力

第一次抽象：

> EdgeIM 是日志压缩。



比较浅。

第二次：

> EdgeIM 是 coverage preservation。



更深。

再抽：

> 在保留 downstream-relevant invariant 的前提下删除冗余实例。



这时它突然可以连接：

test-suite reduction；

coreset；

graph sparsification；

abstraction；

sufficient statistics。


但这还没完。

然后要做反向具体化：

> “如果我拿这个 operator 去另一个领域，那个 invariant 到底是什么？”



这一步最关键。

否则就会变成万物皆 coreset、万物皆控制论、万物皆信息压缩。（）

所以完整能力其实是：

\[
\boxed{
\text{具体}
\rightarrow
\text{抽象}
\rightarrow
\text{迁移}
\rightarrow
\text{重新具体化}
}
\]

很多人很会前三步。

真正科研上值钱的是第四步。


---

我会把一个 sense 什么时候从 PARKED 升成 PROMISING 定得很严格：

> 它必须不仅告诉我们“两个东西很像”，还至少带来一个新的 representation、一个新的可证伪 prediction，或者一个新的 intervention。



满足一个，我会继续挖。

满足两个，很值得做小实验。

三个都满足，我会开始认真查 novelty / prior art / feasibility。

这样我们就不会把所有跨学科联想都开成坑；但也不会把真正有价值的嗅觉因为“这只是联想”而扔掉。

这其实就是你现在特别想学的那种科研 taste 的一个非常具体版本：

> 大胆地产生 analogies，苛刻地审判 mechanisms，快速地杀掉假迁移，把验证资源留给真正能产生新预测的结构同构。



我觉得这个可以正式成为我们研究树的一条“迁移审计协议”。


思考了 4m 49s

有，而且我刚把鲁法明老师本人近年的产出、山科大这一支 Petri 网/流程挖掘谱系、以及曾庆田—刘聪—苏轩—程龙这圈公开合作网络重新串了一遍。这条线比“鲁老师现在做形式化验证 + AI”要清楚得多。

先给你一句总判断：

> 鲁法明老师并没有从 Petri 网/形式化方法“转行”去做 AI。更像是一直守着一个很稳定的内核：复杂系统的“结构化建模 → 行为/知识发现 → 分析验证 → 决策/优化”，然后不断把新的技术和新的真实系统接进这个内核。

所以你现在看到的 Petri 网、流程挖掘、并发程序、因果/根因分析、EdgeIM、矿山 AI、时序大模型、多模态，看似散，其实能串起来。



而且这不是我强行替他编学术人格。鲁老师以前自己说得非常明确：Petri 网是相对小众的方向，但他认为需要坚持相对稳定的科研方向；“时髦的科技可以借鉴”，可以让传统方向有新的用武之地，但并不意味着抛弃过去。

这个表态几乎就是理解他现在发展路径的钥匙。


---

先把“鲁法明本人”和“我们最近看到的合作网络”分开

这个必须先钉死，否则特别容易误判“鲁组现在在干啥”。

鲁老师目前是山东科技大学计算机学院教授、博导、公共安全与应急管理研究院副院长；官方当前列出的方向就是应急决策支持、人工智能、知识图谱与大模型、Petri 网理论与应用，成果介绍又明确加入 BPM。

而我们最近读的那一大圈：

\[
\text{曾庆田}
\leftrightarrow
\text{鲁法明}
\leftrightarrow
\text{刘聪}
\leftrightarrow
\text{苏轩}
\leftrightarrow
\text{程龙}
\]

更准确应该叫长期公开合作谱系/研究网络，不能全部叫“鲁老师自己的组”。

例如：

EdgeIM：有鲁法明；

2025 healthcare duplicate-task discovery：有鲁法明；

CrossEdgeIM：没有鲁法明；

2026 sigRank event-log sampling：没有鲁法明；

2026 IM imprecise-subprocess optimization：没有鲁法明；

2026 causal process anomaly RCA：也没有鲁法明。


但这些工作与他长期的曾庆田/刘聪/process-mining 网络高度相连，所以如果想判断“他们这整个学术圈正在往哪里长”，这些又必须一起看。EdgeIM 的作者就是苏轩、刘聪、鲁法明、程龙、曾庆田等。 CrossEdgeIM 则是苏轩、刘聪、曾庆田、张景林、程龙。


---

一、截至 2026-09，鲁法明老师本人最近到底在做什么？

我现在会把他的可见产出分成 四条同时在跑的轨道。

轨道	2025–2026 代表工作	它真正解决的问题

A. Petri 网 / 因果知识 / RCA	2026 JAS 多元时序知识挖掘与根因分析	从时间序列恢复 AND/OR、协同效应、因果/时间结构
B. 并发程序分析 / 形式化	MHP、UAF、deadlock detection & replay	怎样更准确表示并发因果关系并验证真实 bug
C. Process Mining / BPM	EdgeIM、医疗 duplicate-task discovery	从大规模/复杂事件日志恢复高质量流程模型
D. AI / 大模型 / 多模态工业智能	不规则时序 + LLM、精煤灰分 LLM、多模态、煤岩识别、跨语言情感	把新 AI representation 引入真实工业/安全数据


这四条不是同一个项目，但它们之间存在很强的共同认知动作。


---

A. Petri 网已经从“画流程”走到了“知识 + 因果 + 根因”

2022 年他们做的是 synergy-effect-incorporated fuzzy Petri net。

2024 年进一步变成 Synergy-incorporated Bayesian Petri Net：开始把 Petri 网和概率推理结合起来，显式处理 “AND/OR” relation 和 synergy effect。鲁老师官方主页把这两篇都列成代表性成果。

到了 2026 年，这条线继续长成：

A New Knowledge Mining and Root Cause Analysis Methodology for Multivariate Time Series

作者是 Xiaoliang Wang、Faming Lu、MengChu Zhou、Qingtian Zeng，鲁法明和孟初周为通讯作者。方法把 Petri net + Bayesian network 融合为 synergy-incorporated Bayesian time Petri net，希望从多元时序里挖 temporal causal structure、AND/OR relations、synergy effect，然后做 root-cause analysis；他们在太阳能板质量异常和 Tennessee-Eastman process 上验证，前者报告比对比 RCA 方法高出超过 11% 的准确率。

注意这个演化：

Petri Net
“行为结构是什么？”
        ↓
Fuzzy PN
“关系有不确定性怎么办？”
        ↓
Bayesian PN
“能不能进行概率推理？”
        ↓
Bayesian Time PN
“能不能从时序数据里挖知识、解释异常、找根因？”

这不是单纯“Petri 网继续发论文”。

它在往：

> structured causal knowledge representation + reasoning



走。

而且这也是为什么你觉得他们跟自动化/控制特别近——这篇甚至发在 IEEE/CAA Journal of Automatica Sinica，共同作者 MengChu Zhou 本身就是 Petri nets、automation、robotics、IoT/edge computing 这一大圈的人。


---

B. 另一条老线完全没有停：并发程序形式化分析

这个很重要，因为它说明“AI 论文变多了 ≠ formal methods 被扔了”。

2025 年有：

Segment-Based May-Happen-in-Parallel Analysis for C Programs

它不是用一句“两个语句可能并发”糊过去，而是构造 Segmented Thread-sensitive Control Flow Graph (STCFG)，把语句划成 segment，把 fork/join、锁等上下文信息显式编码进去，再判断 Happens-Before 和 conflict relations。核心目标就是同时提高 precision 和 efficiency。

2025 年还有：

A Use-After-Free Vulnerability Detection Method for Multi-Threaded Programs Based on an Improved Petri Net and Value Flow Graph

这里更漂亮：

\[
\text{程序}
\rightarrow
\text{segmented Petri net}
+
\text{value-flow graph}
\]

Petri 网负责控制流/并发因果约束，value-flow graph 负责值传播/UAF 触发条件，再检查约束兼容性。

然后 2026 年 6 月又有：

Deadlock Detection and Replay of Multi-Thread Programs Based on Refined Segmentation and Lock Graphs

它直接从运行轨迹里提取更细的 causal dependence 和 lock information，降低 false positive；发现潜在死锁以后，还进一步产生 deterministic scheduling 进行 replay，确认这个 deadlock 是真的可以触发的。

这条发展特别有味道：

Petri-net theory
    ↓
reachability / unfolding
    ↓
deadlock analysis
    ↓
program running traces
    ↓
data race / UAF / MHP / deadlock
    ↓
不仅 detect
而且 replay / reproduce

是不是又回到我们之前说的：

> representation → verification → externally checkable evidence



了。

而且你注意一个非常有意思的共同问题：

EdgeIM 在问：

> execution log 里哪些结构信息值得保？



并发程序线在问：

> execution trace 里哪些 causal / locking structure 必须显式表达，否则就会误判？



其实都是 recover the right structure from behavior。

领域不一样，元操作高度相似。


---

C. Process Mining 这条线：从“能挖”逐渐走到了“复杂、分布式、大规模、脏日志还能不能挖”

这条才是你现在真正踩着的主线。

早期非常经典：

2014 的 Hierarchy Modeling and Formal Verification of Emergency Treatment Processes；

2015 的跨部门 collaborative process Petri-net modeling/verification；

2015 并行 heuristic process mining；

2015 case cluster + synchronization core process discovery；

2016 synchronization-core-based process discovery；

2016 BPMN model fusion。鲁老师官方主页现在仍然把这些当代表性成果。

而且他的博士论文名字本身就是：

> 《业务流程的分层建模验证与挖掘方法研究》



导师是曾庆田，2015 年获山东省优秀博士论文。曾庆田官方页面也明确记录了这一导师关系；同一页面还记录刘聪的硕士论文是《跨组织业务流程挖掘及其隐私保护方法研究》。

所以这一支的 genealogy 非常清晰：

曾庆田：
Petri Net / workflow / process mining
          │
          ├──── 鲁法明：
          │     分层建模、验证、挖掘
          │
          └──── 刘聪：
                跨组织 process mining
                privacy / discovery

后来几条线又不断重新交叉。


---

到 2024–2026，process-mining 网络明显在打“规模和真实复杂性”

2024 年苏轩、刘聪、曾庆田等已经有：

Sampling Business Process Event Logs with Guarantees

直接把 DFR equivalence 当 sampling quality 的结构保证，提出 7 种 sampling strategies。

然后 2025 年：

EdgeIM

进一步把问题搬到 distributed edge / IoT：

> feature-preserving sampling → edge local feature extraction → central global DFG → IM。



同一届 ICWS 他们还有：

Enhancing Healthcare Process Model Discovery Through Duplicate Task Identification

因为医疗日志里 duplicate task 很多，会把流程发现搞坏，所以先辨认/relabel duplicate task，再构造 DFG，用 IM 生成 PN。

这个 pattern 很明显：

> 不是一直发明“新的矿工”。



而是在问：

\[
\boxed{
\text{真实日志有什么特殊结构，导致标准 process discovery 失效？}
}
\]

然后逐个处理：

大日志 → sampling；

distributed data → EdgeIM；

duplicate tasks → preprocessing；

IM 局部 overgeneralization → subprocess optimization。



---

D. 到 2025–2026，AI / 大模型这条明显加速了

这是最近最显眼的变化。

2026 年 3 月：

Irregularly Sampled Time Series Classification via Fusing Language Models and Multi-Channel Image Representations

把 irregular time series：

\[
\text{time series}
\rightarrow
\text{text prompt}
\rightarrow
\text{LLM embedding}
\]

同时用 message passing 补缺失数据，再把时序变成 multi-channel image，最后做 text/image multimodal alignment。鲁法明是通讯作者。

然后 2026 年 7 月：

一种大语言模型驱动的多模态精煤灰分预测方法

这就从 generic benchmark 直接落进煤炭生产：

\[
\text{选煤工艺时序}
\rightarrow
\text{LLM + domain knowledge structured text}
\]

再加 message passing、multi-channel line images、multi-head attention multimodal fusion，而且还加了 physical-constraint loss。在真实重介质选煤数据上，RMSE 比次优方法低 3.1%。该工作明确由新一代人工智能国家科技重大专项和国家自然科学基金支持。

这里的演化非常明显：

2026-03
“LLM 能不能帮助表示 irregular time series？”
        ↓
2026-07
“拿进真实选煤系统，
再加入 domain knowledge + physical constraint。”

这已经开始从：

> “LLM 当 feature extractor”



向：

> domain-informed industrial AI



长。


---

还有同时间的其他支线。

2026 的 SiamWT-CRNet 是真实放顶煤场景的动态煤岩识别，用多种 wavelet representation + Siamese/cross-domain feature fusion。官方主页列为 2026 代表成果。

2026 年 8 月 advance online 的 Cross-lingual Speech Emotion Recognition 又在做 multi-wavelet augmentation + language-invariant emotion feature disentanglement。

2025 还有 autonomous-vehicle collaborative perception，官方主页也列为了代表作。

以及一个挺偏 AI architecture 的 Dynamic Global Query Fusion：用 cross-layer persistent global context 去增强 CNN，2025 年发表。

所以现在的 portfolio 明显比 2015 年宽很多。


---

那“团队现在都在干啥”？

这里我更愿意画成公开合作网络的研究簇，而不是胡猜谁是谁的学生。

山科大的“计算机模型与算法研究所”本身长期稳定列出三块：

> Petri 网理论及应用、软件建模与分析、信息资源整合与共享。



鲁法明是研究所公开成员之一。

从近两年的 coauthor/output 看，现在大致可以看到这样的结构：

曾庆田
                 Petri Net / Process Mining
                         │
          ┌──────────────┼──────────────┐
          │              │              │
       鲁法明          刘聪            其他成员
          │           NOVA / BPM
          │              │
 ┌────────┼───────┐      ├──────────────┐
 │        │       │      │              │
程序分析  AI/工业  BPM   苏轩/采样       程龙/Edge
 │        │       │      │              │
MHP      LLM     EdgeIM sigRank       distributed
UAF      时序     医疗PM CrossEdgeIM   systems
deadlock 煤矿                 │
 │        │                   │
包云霞   林泽东/李子辰       causal PM / IM优化
原桂远   刘毅……

这个图只是从公开论文 coauthorship 抽出来的结构，不是实验室组织架构。

但它非常能解释现在为什么产出这么“多头”。


---

更广的 process-mining 合作网络在 2026 年干得相当明显

例如 2026 的 sigRank：

不是 EdgeIM 那种：

> 顺序扫描，出现新 DFR 就保留。



而是给 trace 算 significance，显式输入 sampling ratio，然后选择高价值 traces，目标同时优化 efficiency 和 sample-log quality；12 个真实日志、ProM 实现。

这个对你当前 EX-01 特别有意思。

因为我们可以把谱系画成：

LogRank / ranking sampling
        │
        ├──── 2024 DFR-equivalence sampling
        │
        ├──── EdgeIM 2025
        │       one-pass feature preservation
        │
        └──── sigRank 2026
                significance + sampling ratio

于是你之前那个“是不是 fixed-K？”的错误，反而碰巧踩到了另一类真实 sampling formulation：EdgeIM 本身不是，但他们整个研究网络确实一直存在“预算/比例型 sampling”这条线。

这个以后非常值得横向对比。


---

另一个 2026 工作：

Enhancing Process Discovery by Optimizing Imprecise Sub-processes

已经不是压日志，而是看 IM 挖完以后：

> 哪些局部 subprocess 因为 overgeneralization 太不精确？



定位这些 sublogs，只在局部进行 frequency filtering 和重新 discovery，最后挑 fitness/precision 最好的 candidate；相对 IMi 报告平均 F-measure 绝对提升 0.173。

这就是很典型的：

> 不要推翻整个 IM，只修 residual bad region。



是不是又有点我们飞机论文那股 residual bottleneck 的味道（）。


---

还有一篇 2026：

Detecting Root Causes for Process Performance Anomalies Using Causal Inference

这边的刘聪/曾庆田/程龙合作网络开始直接：

> 从 event log 提取 contextual factors
→ 建 causal hypotheses
→ traceability
→ meta-learning causal inference
→ root cause。



这就和鲁老师自己的：

> Bayesian Time Petri Net → multivariate time-series RCA



在根因/因果这里开始从两边靠拢了。

这个我觉得特别值得标。


---

所以如果把二十年的脉络压成一棵树，我会这么画

Petri Net
                  离散行为结构
                       │
          ┌────────────┴────────────┐
          │                         │
     Business Process            Program
          │                      Concurrency
          │                         │
   建模 / 验证 / 挖掘         reachability / unfolding
          │                         │
  hierarchy / cross-org      race / deadlock / UAF
          │                         │
          │                     replay
          │
       Process Mining
          │
   ┌──────┼─────────────┐
   │      │             │
sampling complex logs distributed
   │      │             │
   │ duplicate task   EdgeIM
   │ IM precision       │
   │                    ▼
   │                CrossEdgeIM*
   │
   └─────────────┐
                 │
         knowledge / reasoning
                 │
           fuzzy Petri net
                 ↓
          Bayesian Petri net
                 ↓
      temporal knowledge + RCA
                 │
                 │
          ┌──────┴────────┐
          │               │
      industrial AI      LLM
          │               │
      mine/safety     text embedding
      perception      multimodal TS
          │               │
          └──── domain/physics ────?

* CrossEdgeIM 属于同一公开合作谱系，但不是鲁法明署名论文。


---

我觉得真正的“稳定内核”不是 Petri 网三个字

这个区别特别重要。

如果只看技术名，你会觉得：

> 这人怎么 Petri net → CNN → LLM → wavelet → process mining 到处跑。



但如果抽高一层：

\[
\boxed{
\text{复杂系统产生数据}
\rightarrow
\text{找到合适 representation}
\rightarrow
\text{恢复结构/关系}
\rightarrow
\text{分析、验证或预测}
\rightarrow
\text{服务真实决策}
}
\]

就非常稳定。

比如：

应急流程

事件/业务行为
→ Petri net
→ formal verification。

并发程序

running trace/code
→ segmented graph/Petri net
→ causality
→ bug detection/replay。

process mining

event log
→ DFR/DFG
→ process model
→ discovery/conformance。

RCA

multivariate TS
→ Bayesian time Petri net
→ causal structure
→ root causes。

最近工业 AI

irregular industrial TS
→ LLM text embedding + image representation
→ multimodal prediction
→ physical/domain constraint。

你会发现：

> representation → structure → downstream reasoning



这个动作一直没怎么变。

这跟我们前两天刚抽出来的 Research Operator 完全吻合。


---

为什么会形成现在这个发展路径？

我觉得这里可以分成有公开证据的原因和我们的推断。

公开证据一：学术惯性是有意保留的。

鲁老师自己明确说过，坚持相对固定方向非常重要，新技术应该给传统方向“新的用武之地”，而不是因为流行就抛弃原方向。

所以现在：

> Petri nets 没死； program verification 没死； process mining 没死； 但 AI/LLM 进来了。



这恰好符合他自己很多年前描述的 strategy。


---

公开证据二：他的学术出身本来就是 BPM / Petri-net / workflow。

他的博士论文就是分层业务流程的建模、验证和挖掘，导师曾庆田；而同一个曾庆田谱系又长出了刘聪的跨组织流程挖掘。

所以 EdgeIM 并不是 2025 突然心血来潮。

你现在正在读的是一条十几年线的现代节点。


---

公开证据三：现实应用场景一直在拉着方向走。

官方当前写的是：

> 矿山、化工、公共安全、海洋安全。





而且 2026 年 4 月，鲁法明也在山科大计算机学院赴“露天煤矿灾害防治与生态保护全国重点实验室”的 AI 赋能矿山安全技术对接团队中。

于是最近大量：

煤岩识别；

矿工语音情绪；

精煤灰分预测；

多模态工业数据；

physical constraints；


就一点也不奇怪。

这不是纯追热点，它还有非常明显的application pull。


---

我们的进一步推断：现在应该是“三股压力同时发生”。

传统 formal / process core
            │
            │
            ├── 现实系统越来越大
            │       → sampling / edge / distributed
            │
            ├── 数据越来越复杂
            │       → causal / probabilistic / multimodal
            │
            └── AI 能力快速上升
                    → LLM / representation learning

所以现在看到的不是一条线，而是一个稳定内核向三个方向扩张。

这个推断与公开成果高度吻合，但它不是老师本人公开写过的一份“科研路线图”。


---

那他们“可能想做，但现在公开成果里还没有真正做透”的是什么？

下面这部分我要全部打上：

> INFERENCE：根据公开论文结构和当前缺口推出来，不代表他们已经在做，也不代表他们计划做。



这里反而非常值得你学，因为就是我们说的：

> 已有工作 → residual → plausible next step。



1. Formal Methods × LLM/Agent 还没有真正接起来

这是我现在觉得最明显的一条。

你看鲁老师当前可见的 LLM 工作：

LLM
→ embedding / domain knowledge
→ time-series / multimodal prediction

而他的另一只手：

Petri Net
→ formal model
→ deadlock / causal reasoning / verification

目前公开成果里，我还没看到真正变成：

LLM / Agent execution
        ↓
formal behavior model
        ↓
verification / runtime assurance

也就是说：

> AI 已经进入 prediction/representation 侧，但 formal core 还没有大规模进入 foundation-model/agent assurance。



这条缝很明显。

恰好也是你现在很感兴趣的那条。


---

2. Process Mining × LLM Multi-Agent 的 interaction semantics

尤其整个合作网络现在已经：

\[
EdgeIM\rightarrow CrossEdgeIM
\]

做到 distributed robotic behavior discovery。

但 CrossEdgeIM 当前的 global merge 对真正跨组织 interaction 的表达依然相对粗。

下一步非常自然的问题就是：

> message flow？ data dependency？ causal visibility？ agent-to-agent protocol？ interaction property？



而不是简单把局部 PN 并起来。

这也是我们前面挖 Agent frontier 时看到大量 2026 work 正在撞的区域。


---

3. 从 offline discovery 到 online runtime assurance

他们已经分别拥有很多“零件”：

CrossEdgeIM：
streaming / incremental features

鲁老师程序分析：
runtime trace
+ detection
+ replay

process mining：
conformance / discovery

Petri net：
verification

但公开成果里还没有看到它们被组装成：

\[
\boxed{
\text{stream}
\rightarrow
\text{online discovered model}
\rightarrow
\text{runtime property checking}
\rightarrow
\text{intervention}
}
\]

这就是一个非常自然的未合流节点。


---

4. “相关结构”进一步变成“因果结构”

现在这两条已经越来越近：

鲁法明这边：

> Petri + Bayesian → root cause。



合作网络另一边：

> process logs + causal inference → performance anomaly root cause。



那么一个自然问题就是：

> process control-flow structure 和 causal intervention structure 能不能统一？



因为：

\[
A\rightarrow B
\]

作为流程先后关系，并不自动意味着：

\[
A\ causes\ B
\]

这跟我们刚才：

> adjacency ≠ causality



简直一模一样。

我会把这个节点标成很值得观察。


---

5. AI 里的“约束”可能进一步 formalize

最新精煤灰分论文已经不是纯黑盒，它加入了 physical-constraint loss。

这其实是一个很有意思的信号：

> 只靠 data fitting 不够； 引入 domain constraint。



如果继续沿鲁老师传统 taste 往前推，下一层很自然就是：

\[
\text{physical constraints}
\rightarrow
\text{process constraints}
\rightarrow
\text{formal constraints}
\]

例如：

> 模型预测不仅数值准，还必须符合流程/物理/安全 invariant。



但目前这是我们的推论，不能说他们已经准备这么做。


---

还有一个我觉得你现在应该特别盯的“近身脉络”

就是：

\[
\boxed{
\text{DFR-equivalence sampling}
\rightarrow
\text{EdgeIM}
\rightarrow
\text{sigRank}
}
\]

这跟你现在正在学的东西距离太近了。

2024 的 sampling-with-guarantees 明确：

> 把 DFR 当 fundamental behavioral unit，要求 sample log 和 original log DFR-equivalent。



EdgeIM 2025：

> 把类似 structural feature preservation 搬进 distributed edge process discovery。



sigRank 2026：

> 不再只问“DFR 有没有”，而是给 trace 排 significance，输入 sampling ratio，在 sample quality 和 efficiency 之间做更明确的 trade-off。



这其实就是一个特别漂亮的研究演化：

第一问：
怎么抽样？

↓
第二问：
怎么保证 sample 仍保关键 behavior？

↓
第三问：
怎么把这种保证用于 edge discovery？

↓
第四问：
保证结构还不够，
怎么在有限 budget 下优先保“重要”的 trace？

↓
【下一问？】
这些 surrogate 到底和 downstream model fidelity
有多强的 alignment？

最后一句是我们的 research question，不是他们已经做出来的结论。

但你看——

这就开始解释为什么我们现在死磕：

> support、frequency、ratio、downstream sensitivity



并不是在某个随机角落钻牛角尖。

它正好卡在这条 genealogy 的接口上。


---

所以如果让我给“鲁法明现在在干嘛”一个比官网研究方向更有信息量的描述

我会写成：

> 他从 Petri 网/流程模型出发，长期研究复杂离散系统中“行为结构如何表示、如何从数据恢复、如何验证和分析”；之后分别向并发程序验证、跨组织/大规模流程挖掘、概率与因果知识推理扩展。最近又把 AI、LLM、多模态表示学习引入工业时序和安全场景，但传统的形式化/程序分析线仍持续产出。



这比：

> “研究 Petri 网、人工智能、大模型。”



信息量大很多。

而如果再把公开合作网络也算上，现在整个圈子的 trajectory 很像：

\[
\boxed{
\begin{array}{c}
\text{Formal structure}\\
\downarrow\\
\text{Process discovery}\\
\downarrow\\
\text{large / distributed / cross-org process}\\
\downarrow\\
\text{causal / probabilistic reasoning}\\
\downarrow\\
\text{edge / robotics / industrial AI}\\
\downarrow\\
\text{？ agents / runtime assurance / trustworthy AI}
\end{array}}
\]

最后那一行我特意打问号。

公开成果目前支持前五层；最后一层是我们根据 residual 和外部 frontier 推出来的可能连接。


---

而这其实也解释了一个你之前可能一直觉得有点奇怪的地方：

为什么鲁老师的方向不像某些 AI lab 那样：

> Transformer → LLM → Agent → Agent RL。



因为他的 genealogy 根本不是从 AI 起家的。

他的 genealogy 是：

> Petri net → workflow/process → formal analysis → mining → complex systems → data/AI。



所以他吸收 LLM 的方式，很可能天然跟纯 foundation-model lab 不一样。

纯 AI 组看到 LLM，第一反应可能是：

> 怎么训、怎么对齐、怎么扩展能力？



这条传统看到 LLM，更自然的问题可能是：

> 它进入一个复杂系统以后，行为怎么表示？知识怎么组织？过程怎么发现？约束怎么表达？结果怎么验证？如何用于真实决策？



至少从过去十几年的公开轨迹看，这种理解比“他现在也开始追大模型热点了”更符合证据。

而且这也让我对你现在应该怎么准备他的面试有了一个更明确的判断：别只学 EdgeIM 这一个算法。要逐渐学会看见这整条 research grammar。

因为你以后如果真在他面前说一个 idea，真正有价值的不是“我也会 LLM / Agent”。

而是能说清：

\[
\text{真实问题}
\rightarrow
\text{representation}
\rightarrow
\text{structural assumption}
\rightarrow
\text{method}
\rightarrow
\text{property / evidence}
\rightarrow
\text{failure boundary}
\]

我现在越来越觉得，这个才是这条学术脉络里非常稳定的一种“口音”。


对，但不是“四篇里只学 EdgeIM，另外三篇放着”。更合适的结构是：

> 一篇主干 + 一篇横向对照 + 一篇实验方法论 + 一篇纵向后继。



这四篇其实刚好组成一个很漂亮的小型训练闭环。

论文	我们怎么处理	它主要训练什么	深度

EdgeIM	主干，拆到能独立重建	DFG、sampling、representation、distributed discovery、IM、claim 边界、算法审计	★★★★★
sigRank	横向方法对照	“sampling 到底优化什么”、预算、ranking、model-quality objective、fair comparison	★★★★
Ground Truth Approach	实验与证据方法论	ground truth 怎么造、noise/deviation 怎么区分、一个实验到底能支持什么 claim	★★★★
CrossEdgeIM	纵向后继 / genealogy	EdgeIM 后来往哪里长、架构为什么变、旧瓶颈解决后新瓶颈在哪里	★★★，现在先浅，后面再深


所以不是平均用力。

我会把它叫：

\[
\boxed{\text{EdgeIM 是“学习对象”，另外三篇是三个观察镜头。}}
\]


---

EdgeIM：真的要重点整

这个不用变。

因为它同时满足三个条件：它是你现在最直接的鲁法明署名对象；它本身足够短，可以真正拆穿；而且里面刚好包含我们后面大量概念的接口：

\[
\text{event log}
\rightarrow
\text{sampling}
\rightarrow
S/E/R
\rightarrow
DFG
\rightarrow
\text{distributed aggregation}
\rightarrow
IM
\rightarrow
Petri\ Net
\]

论文的核心三阶段本身就是 preprocessing + feature-preserving sampling、edge-side feature construction、central aggregation/model discovery。

所以 EdgeIM 不是“读懂摘要”级别。

我要把它做到你能：

> 不看论文重建 Algorithm 1；
解释每一个 representation 为什么存在；
自己造反例；
改一个 assumption 后预测哪里坏；
重实现最小版；
审它的 claim；
拿别的方法来攻击它。



也就是从：

> paper comprehension



一路做到：

> research object ownership。



现在我们正在做的 DFG support/weight、Stage 1 的 invariant、后面 IM cut，全部属于这个主干。Algorithm 1 明确只按 \(S,E,R\) 的集合覆盖决定是否保留 trace，而 Stage 2 又从过滤后的 \(D'\) 重新累计关系次数；这正是值得深挖的接口。 


---

sigRank：不是第二篇 EdgeIM，而是拿来“打 EdgeIM”的

这篇我反而觉得非常重要。

但不应该像 EdgeIM 一样从第一页逐句教学。

它的作用是等你真的理解 EdgeIM sampling 后，突然把旁边另一种 sampling philosophy 摆出来。

EdgeIM 问的是：

> 这个 trace 有没有带来以前没有的新结构 feature？



如果没有，就丢。

而 sigRank 明确接受一个 user-defined sampling ratio，给 trace 做 significance ranking，再按预算选高价值 trace。

所以两边非常适合横向审：

EdgeIM
“只要没有新 coverage，就不要了”
          │
          │ VS
          ▼
sigRank
“预算有限时，哪些 trace 更值得留下？”

突然就出现一堆真正的研究问题：

\[
\text{coverage}\quad vs\quad \text{importance}
\]

\[
\text{endogenous sample size}\quad vs\quad
\text{fixed sampling ratio}
\]

\[
\text{structural preservation}\quad vs\quad
\text{downstream model quality}
\]

而 sigRank 自己的质量评价又非常值得你看——它不是只数 sample 里还剩多少 edge，而是把 sample log 拿去做 process discovery，然后用原日志评价发现模型的质量；论文还专门展示了同样 15% 的样本，得到的模型质量可以差非常大。

这篇会专门训练你一个能力：

> 别把一个方法自己的 surrogate objective，当成最终 objective。



EdgeIM 说保住 \(S/E/R\)。

很好。

sigRank 会逼我们继续问：

> 那最终 process model 呢？



这也是你现在的 Stage-1 审计为什么不是一个凭空长出来的问题。

所以 sigRank 是：

> EdgeIM 的 sparring partner。



不是配角。


---

Ground Truth 那篇：不是学新算法，是学“你凭什么说实验说明了这个？”

这篇的重要性跟前两篇完全不在同一维。

Sommers 等人的问题本身就是：

> 真实日志虽然真实，但我们往往不知道真实 underlying process 到底是什么。



于是一个算法发现了模型 \(M\)，你看到 fitness/precision 很漂亮，可你并不知道：

> 它到底恢复对了没有？



也不知道某个 deviation 是真实业务偏差还是 logging error。

论文因此保留一整套链条：

\[
\text{initial ground-truth model}
\]

经过已知的 behavioral deviation，

\[
\downarrow
\]

得到 deviating model，

再注入 recording errors，

\[
\downarrow
\]

得到 imperfect event log。

这样 ground truth 没有丢掉，才能真正评估 process-mining technique 哪里对、哪里错。

这篇对我们的意义非常大，因为以后你很容易提出：

> “EdgeIM 这个 sampling 会不会扭曲模型？”



然后跑一个 BPIC/Sepsis：

> F-measure 0.91！



完事。

不行。

Ground Truth 这篇会逼你问：

> 原来真实 model 是什么？

缺的 behavior 是 sampling 造成的还是原日志本来就没有？

recording error 和 true behavioral deviation 能分吗？

我测的是 robustness、fidelity、conformance 还是别的东西？



作者自己也明确把 process-mining assessment 拆成 correctness/validation、quantitative & qualitative evaluation、reliability/reproducibility、robustness、performance/scalability、usability 等不同层。

这篇其实是在教：

> 怎么造一个能真正击中 claim 的 world。



和我们飞机上 GLM-5.3 那个：

> verifier/environment 本身也要验收



是可以迁移的同一种 research operator。

所以它以后不会变成：

> “背 Sommers 方法细节。”



而会变成我们的 Experimental Design / Claim–Evidence 教材。


---

CrossEdgeIM：现在不需要深挖，但它很适合学“论文是怎么长出下一篇的”

它是另外一种用途。

CrossEdgeIM 明确把 EdgeIM 当 predecessor：EdgeIM 在 edge node 构造 local features、central aggregate global model，但主要针对 single-organization IoT；CrossEdgeIM 把对象改到跨组织的 IoRT/robotic behavior discovery。

所以我们读它不是主要为了再学一遍 DFG。

而是问：

> 为什么 EdgeIM 做完以后，还需要 CrossEdgeIM？



然后看架构怎么变：

EdgeIM
case filtering
→ edge feature
→ central global DFG
→ central discovery

变成：

CrossEdgeIM
activity node 在线维护 feature
→ ΔDFR / ΔS / ΔE
→ organization-level DFG
→ organization 已经做 model discovery
→ central 合并 local models

它明确把 activity node 作为最小 edge computing unit，维护直接跟随关系并向 organization node 发增量更新；organization node 构造自己的 DFG。

这篇最适合训练：

> idea genealogy + residual bottleneck。



EdgeIM：

> distributed data 很大
→ 别把全部日志搬中央。



CrossEdgeIM：

> 等等，真实机器人是持续流式、跨组织、多自治实体啊。
→ computation 再向下沉、模型分层。



然后我们又会问：

> 但真正跨组织 interaction 怎么表示？



于是又出现下一代 residual。

这就是你之前特别想学的：

> “他们到底怎么想到下一篇的？”



CrossEdgeIM 就是现成教材。

而且它没有鲁法明署名，所以对“鲁老师本人面试”的直接权重低于 EdgeIM；但它属于高度相关的公开合作谱系，所以拿来理解这个圈子怎么继续往 robotics / distributed behavior / multi-agent-like systems 扩，非常值。


---

所以四篇实际上形成一个十字架

我觉得你可以这样记：

CrossEdgeIM
                         【往后长】
                            ↑
                            │
                            │ genealogy
                            │
sigRank  ←──── compare ── EdgeIM
【横向方法】              【主干】
                            │
                            │ evaluate
                            ↓
                  Ground Truth Approach
                    【证据/实验地基】

这个结构特别漂亮。

EdgeIM 回答：

> 方法是什么？



sigRank 回答：

> 还有没有另一种解决法？设计选择差在哪？



Ground Truth 回答：

> 你怎么知道自己的比较真的说明问题？



CrossEdgeIM 回答：

> 这个方法做完以后，研究路线为什么还继续往下长？



这四个问题正好对应四种科研能力。


---

所以学习节奏我不建议改成“四篇一起啃”。

还是：

\[
\boxed{\text{EdgeIM 深挖为主}}
\]

但我们每走到一个合适节点，就打开另外一篇当镜子。

例如现在学完 Algorithm 1 / DFG：

> 打开 sigRank 一次，看“它为什么不按 coverage 选”。



等做到实验设计：

> 打开 Ground Truth，问“我们的 synthetic experiment 怎样才真有 ground truth”。



等 EdgeIM 全链、IM/Petri net 都理解后：

> 打开 CrossEdgeIM，做一次 predecessor → residual → redesign → new residual 的完整 genealogy reconstruction。



这样三篇不会把主线打断，但又不会沦为“以后再说”。

我甚至觉得，这比“四篇都逐页精读”更接近真正导师会培养学生的方式：一篇让你建立 ownership，其他论文分别用来逼你学会比较、验证和追踪研究演化。

所以 EdgeIM 是主教材；另外三篇其实是研究训练器。



“怎么有点像控制/自动化”的感觉不是错觉。把两篇放在一起看以后，谱系会清楚很多，而且这里刚好能长出一条很有价值的跨学科枝。

  先纠正一个很容易混的点：**两篇不都是鲁老师署名的论文。**如果你说的“鲁老师”是 Faming Lu，那么 EdgeIM 是：作者是 Xuan Su、Cong Liu、Faming Lu、Long Cheng、Qingtian
  Zeng、Shouli Zhang，而且 Faming Lu 是通讯作者。
  但 CrossEdgeIM 不是：它的作者是 Xuan Su、Cong Liu、Qingtian Zeng、Jinglin Zhang、Long Cheng，没有 Faming Lu。它和 EdgeIM 属于高度重叠的合作团队——Xuan Su、Cong
  Liu、Qingtian Zeng、Long Cheng 四个人重合——而且 CrossEdgeIM 在正文里直接把 EdgeIM 当作前序方法来讲。

  所以更准确的关系不是“鲁老师连续发了两篇 EdgeIM”，而是：

  \[
  \boxed{\text{同一研究合作谱系：EdgeIM }\rightarrow\text{ CrossEdgeIM}}
  \]

  CrossEdgeIM 自己的 Introduction 就说，EdgeMiner 和 EdgeIM 已经可以在 edge node 构造局部特征、集中聚合出全局过程模型，但主要面向 single-organization IoT；它觉得这
  个框架还不适合“跨组织、交互式机器人行为发现”，于是才引出 CrossEdgeIM。

        EdgeIM  CrossEdgeIM

  主要场景      分布式 IoT 事件日志     分布式 IoRT / 多机器人组织
  原始痛点      完整日志集中处理太贵    还多了组织隔离、机器人实时交互
  最底层处理    case 先过滤，再分到 edge nodes  activity node 在线接收事件
  传什么        局部 \(S_i,E_i,R_i\) 等结构信息 增量 \(\Delta DFR,\Delta Start,\Delta End\)
  模型在哪里挖  central node 汇总 global DFG 后做 IM    organization node 就做 IM，得到 Org-Petri-net
  最顶层合并什么        features / DFG  已经发现好的 Petri nets
  最终结果      一个全局过程模型        多组织局部模型结构合并成 global robotic model


  所以它其实发生了一个挺漂亮的计算位置迁移。

  EdgeIM 大概是：

  日志
  → 压缩
  → edge 上提特征
  → central 汇总特征
  → central 做过程发现

  CrossEdgeIM 变成：

  机器人事件流
  → activity node 在线提特征
  → organization node 汇总特征
  → organization node 已经做过程发现
  → central 只合并模型

  CrossEdgeIM 明确把自己写成三层：Activity Node Processing、Organization Node Modeling、Central Node Merging；organization node 构造 Org-DFG 后直接运行 Inductive
  Miner 得到 Petri net，central 收到的是各组织的 Petri net，而不是原始日志。

  这已经很像一种我们前面聊过的 Research Operator：move computation to where the information lives。


  ---

  那为什么你会觉得它像控制论/自动化？

  因为它们确实在一个很大的交叉地带里，但要把“控制论”拆开。

  如果你脑子里的控制论是经典这种：

  \[
  \dot x=f(x,u)
  \]

  传感器测状态 → controller 算控制输入 \(u\) → 系统动态变化 → 再反馈，

  那么 EdgeIM 本身并不是在做这种控制。

  它没有设计控制律，没有问：

  > “我给机器人什么输入才能稳定到目标状态？”



  它问的是：

  > “我观察到大量事件以后，这个系统实际上是按什么流程运行的？”



  这是 model discovery / system behavior inference。

  但它跟**自动化、离散事件系统（Discrete Event Systems, DES）**就真的非常近了。

  你现在看到的：

  A发生
  → B发生
  → C/D二选一
  → E

  不是连续变量随时间平滑变化，而是：

  > 一个个离散事件发生，系统状态随事件跳转。



  自动化里大量系统恰好就是这种东西：

  制造流水线；

  机器人任务；

  工作流；

  通信协议；

  仓储物流；

  故障/恢复流程。


  于是很自然会出现：

  \[
  \text{event}
  \rightarrow
  \text{state}
  \rightarrow
  \text{transition}
  \rightarrow
  \text{concurrency}
  \rightarrow
  \text{choice}
  \rightarrow
  \text{loop}
  \]

  ……这不正是我们现在遇到的东西么。

  而 Petri net 又正好特别擅长描述：

  > 离散事件 + 并发 + 同步 + 资源约束 + 因果顺序。



  所以它横跨过程挖掘、自动化、形式化方法，并不奇怪。

  甚至作者单位本身就能看到这个交叉：EdgeIM 的 Long Cheng 来自 School of Control and Computer Engineering；CrossEdgeIM 又加入了来自山东大学 School of Control
  Science and Engineering 的 Jinglin Zhang，Long Cheng 仍在作者中。


  ---

  这里其实可以画出一条特别漂亮的知识桥

  你现在原来的树大概是：

  event log
  → DFG
  → Inductive Miner
  → Petri net

  现在旁边可以长一条 PARKED 的跨学科枝：

  离散事件系统 DES
                           │
                ┌──────────┼──────────┐
                │          │          │
           Petri Net    自动化系统   形式化方法
                │          │          │
                │          │        verification
                │          │
  event log → process mining
                │
                ├─ discovery
                ├─ conformance checking
                └─ enhancement

  注意这里有几个方向完全不同，但它们共享模型。

  比如：

  Process Mining：

  \[
  \boxed{\text{行为数据}\rightarrow\text{模型}}
  \]

  我看发生过什么，反推系统结构。

  Formal Verification：

  \[
  \boxed{\text{模型}+\text{性质}\rightarrow\text{这个性质是否成立？}}
  \]

  我已经有模型，检查安全性、可达性等。

  Supervisory Control / 自动控制中的离散事件控制：

  \[
  \boxed{\text{系统模型}+\text{规范}\rightarrow\text{如何限制系统行为？}}
  \]

  我不只观察，还要合成一个 supervisor，让非法行为不要发生。

  突然就连起来了：

  真实系统
     │
     │ observation
     ▼
  event log
     │
     │ process mining
     ▼
  behavior model
     │
     ├──────── verification ───────→ “它安全吗？”
     │
     └──────── control/synthesis ──→ “怎么让它只做允许的行为？”

  这个桥我觉得对你以后特别有价值。

  因为你想走的形式化验证 + AI safety，本身就在不断碰：

  > 从行为得到模型

  和

  拿模型去验证/约束行为



  这两个方向。


  ---

  而 CrossEdgeIM 又比 EdgeIM 更往自动化那侧迈了一步，因为它的对象直接变成了：

  > 多机器人、实时 event stream、edge/fog/cloud 三层计算、organization-level controller、协作行为模型。



  论文甚至明确说 central node 可以部署在 cloud 或 supervisory controller 上。

  所以你闻到的那股“自动化味”在 CrossEdgeIM 里确实更浓。

  但这里要有一个科研上的边界意识：

  > 用了机器人、Petri net、supervisory controller 这些词，不等于论文就在做控制理论。



  它核心贡献还是：

  \[
  \textbf{distributed process model discovery}
  \]

  而不是 controller synthesis。

  这个 distinction 非常值得练。


  ---

  更有意思的是，两篇连起来刚好能看一次“瓶颈迁移”

  EdgeIM 解决：

  > complete centralized log 太重
  → edge 上提 feature
  → central 再重建 model。



  CrossEdgeIM 接着问：

  > 但真实 IoRT 不是只有一个组织啊，而且事件还是持续来的。



  于是：

  > case-level preprocessing
  → online incremental feature maintenance



  以及：

  > central 重建全部模型
  → organization 本地先建模，central 只做 model merging



  这就是我们飞机论文里 IndexShare 那种 research move：

  > 上一代解决一个瓶颈以后，重新看真正的大头跑到哪儿去了。



  但它又带来下一代问题。

  CrossEdgeIM 的 central merge 把每个 organization model 当成一个 logically independent branch，然后用共同 split/join 让这些组织模型并行组合。论文自己同时承认：目
  前没有显式捕获多机器人之间的协作关系，组织之间的 interaction 还是 implicit 的。

  所以 genealogy 很漂亮：

  Traditional centralized IM
         │
         │ 数据规模 / 通信 / 隐私
         ▼
  EdgeMiner / EdgeAlpha
         │
         │ 效率、复杂结构、时间戳冲突
         ▼
  EdgeIM
         │
         │ single organization
         │ streaming / robotic setting
         ▼
  CrossEdgeIM
         │
         │ 新 residual
         ▼
  “真正的跨机器人/跨组织 interaction
  到底怎么显式建模？”

  这个已经不是单纯“看两篇 paper”。

  它在教你一个科研动作：

  > 看后继论文，不只看它多加了什么；还要看它把上一代哪个 bottleneck 移走了，以及新架构又把什么东西暴露成了下一代 bottleneck。



  我会把这两篇在我们的树上这样挂：

  > EdgeIM = 主干当前对象
  CrossEdgeIM = vertical genealogy / downstream extension，暂时 PARKED
  DES–Petri net–automation–formal verification = 一条跨学科桥，SEEN，等我们学到 Petri net 时正式 OPEN。



  这样现在不会被自动化理论整个拖走，但你刚刚闻到的这个连接也不会丢。



› 能，而且我觉得这条线跟 Multi-Agent Systems 的关系其实比表面上还近。

  但要先区分两种“能用”：

  第一种是直接拿过程挖掘/Petri net 去分析 Multi-Agent 行为；第二种是进一步把它变成Multi-Agent 的监控、验证、协调甚至协议设计工具。前一种已经很自然，后一种就开始进
  入你会感兴趣的研究空间。

  CrossEdgeIM 本身其实已经走到门口了：它把对象换成了跨组织、多机器人行为，activity node 在线收事件，organization node 建局部模型，central node 再合并模型。也就是
  说，“多个自治实体产生事件 → 汇总行为结构 → 得到全局模型”这个框架已经非常 MAS 味了。


  ---

  先从最简单的 Multi-Agent 例子开始

  假设有两个 Agent：

  Agent A:
  observe_task
  → propose_plan
  → send_plan

  Agent B:
  receive_plan
  → check_plan
  → send_feedback

  Agent A:
  receive_feedback
  → revise_plan
  → execute

  如果系统把这些动作都记成 event log：

  case1:
  A.observe
  A.propose
  A.send
  B.receive
  B.check
  B.feedback
  A.receive
  A.revise
  A.execute

  那它已经天然满足过程挖掘的输入形式：

  > 一串带 case / activity / timestamp / actor 的事件。



  所以最直接可以做：

  \[
  \text{Multi-Agent trajectories}
  \rightarrow
  \text{event logs}
  \rightarrow
  \text{DFG / process model}
  \]

  然后问：

  > 这些 Agent 实际上是怎么协作的？



  而不是只看 prompt 里“理论上应该怎么协作”。

  这个 distinction 非常重要。


  ---

  第一种用途：发现 Multi-Agent 的“真实协作流程”

  你写了一个 workflow：

  Planner
  → Critic
  → Executor

  你以为系统是这么跑的。

  但真实运行 1000 次以后可能变成：

  Planner
  → Critic
  → Planner
  → Critic
  → Planner
  → Executor

  甚至：

  Planner
  → Executor
  → Error
  → Critic
  → Planner

  Process Mining 做的就是：

  > 从实际 trajectories 反推出真实行为模型。



  于是可以发现：

  哪些 Agent 总是先于哪些 Agent；

  哪里经常反复循环；

  哪些路径只在失败 case 出现；

  哪些 Agent 实际上几乎没贡献；

  哪些“并行”任务事实上被某个依赖串行化；

  哪些 delegation pattern 最常出现。


  这已经挺适合现在的 LLM multi-agent systems 了。


  ---

  第二种：Conformance Checking —— “设计的协议”和“实际行为”对不对得上

  这个比单纯 discovery 更有意思。

  你可以有一个预期协议：

  Planner
     ↓
  Reviewer
     ↓
  Executor

  规定：

  > Executor 必须在 Reviewer approval 后才能执行。



  然后拿真实 agent log 检查：

  Planner
  → Executor
  → Reviewer

  这就是 deviation。

  所以形成：

  \[
  \text{desired protocol}
  +
  \text{actual event log}
  \rightarrow
  \text{conformance checking}
  \]

  对 Multi-Agent safety 特别自然。

  例如：

  > 某个高风险 action 是否一定经过 verifier？



  > 两个 agent 是否出现了不允许的 communication sequence？



  > 执行 agent 有没有跳过 approval？



  这个已经开始和形式化验证接上了。


  ---

  第三种：Petri net 特别适合表示 Multi-Agent 的“并发 + 同步”

  这是为什么你会觉得它和 MAS 很搭。

  Multi-Agent 的麻烦之一就是：

  > 多个东西同时跑。



  例如：

  ┌→ Agent B: search literature ─┐
  Agent A plan ├→ Agent C: inspect code       ├→ Integrator
               └→ Agent D: design experiment ─┘

  这里不是普通 sequence。

  你需要表达：

  1. B/C/D 可以并行；


  2. Integrator 必须等若干结果；


  3. 某些 branch 可选；


  4. 某些失败后回去重试；


  5. 有共享资源可能不能同时使用。



  Petri net 本来就擅长这几个东西：

  > concurrency
  synchronization
  choice
  loops
  resource constraints



  所以它比单纯“画 Agent graph”丰富。

  一个普通 directed graph：

  Planner → Critic → Executor

  很难告诉你：

  > Critic 和 Searcher 是不是必须都完成才能执行。



  Petri net 可以明确表达这种同步条件。


  ---

  第四种：Runtime Monitoring

  这就开始有你形式化 + AI Safety 那种味道了。

  假设我们已经得到/规定一个合法 Multi-Agent protocol：

  Researcher
  → Evidence Checker
  → Experiment Runner
  → Claim Reviewer

  系统在线跑的时候，每来一个 event：

  Researcher.propose
  Checker.approve
  Runner.execute
  ...

  runtime monitor 就更新当前状态。

  如果下一步出现：

  Runner.execute

  但此前没有：

  Checker.approve

  就可以立刻报：

  > protocol violation。



  所以：

  \[
  \text{Petri-net / automaton model}
  +
  \text{live agent event stream}
  \rightarrow
  \text{runtime verification}
  \]

  这已经不是“事后看日志”。

  是：

  > 边跑边检查 Agent collective behavior。




  ---

  第五种：异常 / 失败模式发现

  这个我觉得特别值得你以后看。

  比如你跑 10000 条 multi-agent trajectory。

  成功 case：

  Plan → Review → Execute

  失败 case 经常：

  Plan → Review → Replan → Review → Replan → ...

  那 process mining 可能发现：

  > 一个明显的 review-replan loop 与 failure 强相关。



  于是你得到的不是：

  > “失败率 22%。”



  而是：

  > 失败是怎样长出来的。



  这就和你前面特别喜欢的 Chain-of-Experience 接上了。

  从：

  \[
  \text{outcome statistics}
  \]

  升级到：

  \[
  \text{behavioral failure structure}
  \]

  这个可能比最后 accuracy 更有解释力。


  ---

  但 CrossEdgeIM 直接迁到 LLM Multi-Agent 有一个大坑

  而且这个坑刚好就是 CrossEdgeIM 自己的 limitation。

  它目前上层合并组织模型时，本质上更接近：

  > 每个 organization 有自己的 Petri net，最后把它们作为相对独立的 branch 合起来。



  但真正 Multi-Agent 最重要的往往恰恰是：

  > agent-to-agent interaction。



  CrossEdgeIM 自己也承认，目前并没有显式捕获 multi-robot collaborative relations，跨组织 interaction 还是 implicit 的。

  这对 LLM agents 会更严重。

  因为一个 agent 的行为往往取决于：

  A sends(message m)
         ↓
  B receives(m)
         ↓
  B updates belief
         ↓
  B sends response

  如果你只记录：

  A.send
  B.receive

  而不记录：

  message identity；

  causal link；

  sender/receiver；

  shared artifact；

  dependency；

  tool/resource state；


  那你会丢掉大量真正的协作语义。

  所以如果真往 MAS 扩，我第一反应不会是：

  > “直接把 CrossEdgeIM 跑一遍。”



  而是：

  > Multi-Agent 的 feature representation 应该是什么？



  这一下又回到我们那个 Research Operator：

  > Question the representation.




  ---

  例如，普通 DFG 可能不够

  普通 DFG 的 edge 是：

  \[
  activity_i\rightarrow activity_j
  \]

  但 Multi-Agent 可能更需要：

  \[
  (agent_i,activity_i)
  \rightarrow
  (agent_j,activity_j)
  \]

  甚至还不够。

  可以继续扩成：

  \[
  (agent_i, action_i, artifact/message)
  \rightarrow
  (agent_j, action_j)
  \]

  例如：

  (A, send, plan_17)
          ↓
  (B, review, plan_17)

  这样才能知道：

  > B review 的到底是不是 A 刚发的那个 plan。



  再进一步，你可能需要区分：

  control flow：谁先谁后；

  message flow：谁给谁发了什么；

  data flow：共享了哪个 artifact；

  authority flow：谁能批准谁；

  resource flow：谁占了哪个工具；

  epistemic flow：哪个 agent 知道什么。


  这时候就已经变成一个很漂亮的跨学科问题了。


  ---

  而且这里能接好几个领域

  你刚才问“能不能用在 Multi-Agent”，往下一层其实会把我们树上好多枝突然连起来：

  Process Mining
       │
       ├── event log → behavior discovery
       │
  Multi-Agent Systems
       │
       ├── communication / coordination
       │
  Discrete Event Systems
       │
       ├── concurrency / synchronization
       │
  Petri Nets
       │
       ├── behavioral model
       │
  Formal Verification
       │
       ├── safety / liveness / reachability
       │
  Runtime Verification
       │
       ├── online protocol checking
       │
  AI Safety
       │
       └── agent interaction assurance

  这个交叉其实很自然。


  ---

  再往研究问题走，能长出什么？

  我现在随手就能从这个连接里重建几类可研究问题，不过先都标成 idea，不是 claim。

  A. Multi-Agent Process Discovery

  > 从 agent trajectory 自动发现协作协议。



  输入：

  \[
  \text{agent interaction logs}
  \]

  输出：

  \[
  \text{interaction process model}
  \]


  ---

  B. Safe Multi-Agent Protocol Mining

  只看 successful/safe trajectories：

  > 能否归纳出一个“安全协作流程”？



  然后检测新轨迹是否偏离。


  ---

  C. Failure-specific process mining

  把 success / failure 两类分开：

  > 哪些结构只在失败轨迹中出现？



  例如：

  > verifier bypass
  excessive replan loops
  cyclic delegation
  deadlock
  premature tool use




  ---

  D. Online MAS Monitoring

  Agent 正在运行时维护增量 DFG/Petri-net state：

  > 一旦进入危险行为结构，提前报警。



  这个就和 CrossEdgeIM 的 online incremental update 很贴近。


  ---

  E. Agent orchestration optimization

  如果发现：

  A → B → C

  实际上：

  B 和 C 没有依赖

  那能不能改成：

  A → (B || C)

  这就是：

  > process discovery → bottleneck diagnosis → orchestration redesign



  一下又接回你飞机上 DSpark 那条：

  > 真依赖串行，不依赖的重活并行。




  ---

  一个我觉得特别有意思的方向

  甚至可以把 process model 当作 Multi-Agent harness 的显式 IR。

  这和 DataMagic 的 DVSpec 有点像。

  现在很多 agent workflow 是自然语言：

  “You are planner...
  Then ask reviewer...
  If approved call executor...”

  这其实很难验证。

  如果编译成一个显式 process specification：

  sequence(
    planner,
    parallel(searcher, checker),
    reviewer,
    choice(
      approve → executor,
      reject  → planner
    )
  )

  甚至进一步变成 Petri net / state machine，

  那么：

  可执行；

  可视化；

  可验证；

  可 diff；

  可 runtime monitor；

  可 provenance；

  可以检查 deadlock/liveness。


  这就很有意思了。

  > 自然语言 Agent orchestration → formal process IR



  这个连接我觉得非常值得在我们 Transfer Map 上登记。

  不是说它一定能成为论文。

  但它是个很好的研究芽。


  ---

  不过现在要立一个边界

  我们目前只能比较有把握地说：

  > Process-mining / Petri-net / DES 这套思想很适合用于建模和分析 Multi-Agent interaction。



  不能现在就跳成：

  > “EdgeIM 能解决 Multi-Agent safety。”



  中间还有很多 identification gap：

  1. 什么算 event？


  2. case 怎么定义？


  3. agent interaction 的 causal relation 怎么表示？


  4. message semantics 是否需要进入模型？


  5. DFG 是否 sufficient？


  6. stochastic / nondeterministic agent 如何建模？


  7. LLM agent 的 hidden state 怎么处理？


  8. process conformance 和“安全”到底是什么关系？



  这些正是以后可以探索的。

  所以这条枝我会现在标成：

  > TRANSFER NODE — OPEN/SEEN

  Process Mining / Petri Nets → Multi-Agent behavior discovery & runtime assurance



  然后下面先挂三个醒目的 research cues：

  > representation：什么叫一个 Multi-Agent event？



  > interaction：跨 Agent causality 怎么显式表示？



  > claim：流程合规 ≠ 自动等于安全。



  这三个问题比“能不能用”本身更有研究价值。

  而且我觉得你刚才这个联想挺好：你已经开始不是在问“CrossEdgeIM 是什么”，而是在自动做：

  \[
  \text{方法结构}
  \rightarrow
  \text{抽象}
  \rightarrow
  \text{新领域映射}
  \rightarrow
  \text{边界问题}
  \]

  这其实就是我们前面说的 L5 transfer 开始冒头了。


后面我会彻底按零基础模式来。刚才我直接说“递归分解、process tree、Petri net”，实际上已经默认你知道三四层前置了，这种讲法不适合现在这个阶
  段。

  而且我们不只是“把 EdgeIM 看懂”。更合适的目标是：

  > 借 EdgeIM 这篇论文，把过程挖掘、算法设计、实验思维、论文阅读和科研 reasoning 一起长出来。



  你现在这套训练包本来就是从“过程挖掘零基础”起步，而且站序已经冻结为 0 → 1 → 5a → 2 → 3 → 5 → 6 → 7；我们不用推翻它，而是在它上面再铺一层知识图谱 + 科研能力图谱。


  ---

  以后我怎么教

  每遇到一个新概念，我默认你一个前置术语都不知道，按这个顺序走：

  1. 它解决什么问题

  为什么会需要这个东西？

  没它会怎样？



  2. 最小例子

  先不用公式。

  用 2～4 个活动手画出来。



  3. 正式定义

  这时才引入符号。

  每个符号都解释“它装的是什么”。



  4. 亲手操作

  数一次、画一次、切一次、跑一次。



  5. 接回 EdgeIM

  论文这里为什么突然需要它？

  输入是什么，输出是什么？

  前一阶段给它什么，后一阶段拿它干什么？



  6. 科研训练

  作者在优化什么？

  保了什么，丢了什么？

  哪个 claim 是论文写的，哪个是我们的推论？

  有什么反例？

  如果改条件，会坏在哪里？

  这个 idea 可能从什么 bottleneck / representation / invariant 长出来？




  每走完一个节点，我们顺手更新：

  > 已掌握 → 当前节点 → 前置依赖 → 后继概念 → 暂时不用学



  这样不会发生“为了懂一句话，又被迫打开十个新坑”。


  ---

  先把我们现在的知识地图搭起来

  你目前真正走到的位置其实是这里：

  现实中的过程
      │
      ▼
  事件 Event
      │
      ▼
  活动 Activity
      │
      ▼
  轨迹 Trace
      │
      ▼
  事件日志 Event Log
      │
      ├── 起点集合 S
      ├── 终点集合 E
      └── 直接跟随关系 R
                │
                ▼
               DFG
         ┌──────┴──────┐
         │             │
     边有没有       边出现几次
        R             weight w
         │             │
         └──────┬──────┘
                ▼
          weighted DFG
                │
         【我们现在在这】
                │
                ▼
       怎么从图里认出流程结构？
                │
                ▼
         Inductive Miner
                │
         ┌──────┼────────┐
         ▼      ▼        ▼
     sequence choice parallel loop
                │
                ▼
           Process Tree
                │
                ▼
            Petri Net

  现在不要学 Petri net。

  甚至暂时也不要管“process tree”的正式定义。

  我们下一件应该学的只有：

  > 一张 DFG 到底怎么告诉我们“这是顺序、选择、并行还是循环”？



  这正好是你刚才卡住的那句话的前置。


  ---

  而科研能力图谱是另一张图

  它不是专业知识，而是我们沿途训练的 reasoning：

  读一个方法
     │
     ▼
  它的输入/输出是什么？
     │
     ▼
  它选择了什么 representation？
     │
     ▼
  它声称保留什么 invariant？
     │
     ▼
  “无损”是相对哪一层？
     │
     ▼
  删掉的信息 downstream 用不用？
     │
     ▼
  理论保证到哪里？
     │
     ▼
  实验又验证了哪里？
     │
     ▼
  有没有反例 / distribution shift？
     │
     ▼
  作者为什么可能想到这一刀？

  你刚才已经无意中练了其中三个很重要的能力。

  ① Representation awareness

  你问：

  > “类型齐全、次数权重可计量，那后续怎么处理？”



  这其实是在问：

  > 作者把原始日志换成 DFG 这个表示之后，哪些信息还在，哪些已经没了？



  这是非常核心的科研问题。


  ---

  ② “Lossless relative to what?”

  我们刚刚已经发现：

  \[
  D\rightarrow D'
  \]

  不是“什么都没丢”。

  它只保证类似：

  \[
  S(D)=S(D'),\quad
  E(D)=E(D'),\quad
  R(D)=R(D')
  \]

  但：

  \[
  w_D(a,b)\neq w_{D'}(a,b)
  \]

  完全可能。

  所以以后看到论文说：

  > lossless / information preserving / equivalent



  第一反应不能是：

  > “哦，无损。”



  而要追一句：

  > 对哪个 representation、哪个 statistic、哪个 downstream query 无损？



  这个习惯很值钱。


  ---

  ③ Downstream dependency

  再下一层就是：

  > “好，EdgeIM 保住了 \(R\)，但后面的 Inductive Miner 到底只看 \(R\)，还是也看 weight？”



  如果 downstream 只看：

  > 边存在不存在，



  那 Stage 1 的保证可能够。

  如果 downstream 某一步看：

  > 频次、比例、threshold，



  那事情就复杂了。

  这正是你后面 EX-02 会撞到的东西：训练包已经专门安排了 IM / IMf 和 noise_threshold，就是要亲手观察“边还在，但比例变化”会怎样改变模型。

  所以我们不会提前把答案灌进去，但会明确知道：

  > 这是以后必须验证的接口。




  ---

  现在把“global DFG”这个词从零讲清楚

  你现在已经会画这种东西：

  A → B → D → F
   \→ C ↗

  假设这是把所有相关数据汇总后得到的一张 DFG。

  “global”没有神秘含义。

  EdgeIM 是分布式的：不同 edge node 各自处理一部分东西，各自先得到一张小图：

  节点 1：
  A → B
  B → D

  节点 2：
  A → C
  C → D

  然后中心把这些局部结果聚起来：

  B
        ↗ ↘
  A           D
        ↘ ↗
         C

  这张汇总后的，就是：

  > global DFG



  所以现在你只需要知道：

  local DFG = 某一部分数据得到的图；

  global DFG = 把这些局部图汇总后的总图。


  论文后面真正的问题就变成：

  > 我现在手里有这一张总图。怎么从图的形状反推出“流程规则”？




  ---

  下一节点：为什么不能“看图直接完事”？

  比如：

  A → B → C

  你很容易猜：

  > A 做完做 B，B 做完做 C。



  这叫 sequence。

  但如果变成：

  B
     ↗ ↘
  A      D
     ↘ ↗
      C

  你现在其实不知道：

  > B 和 C 是二选一？



  还是：

  > B、C 都要发生，只是顺序不固定？



  还是：

  > 图里只是因为我们丢了一些信息，看起来像这样？



  所以“DFG → 流程模型”不是简单描边。

  这里才需要 Inductive Miner 的核心思想：

  > 不要直接解释每一条边。先找整张图能不能被某种大的结构一刀切开。



  这个“一刀切”后来就叫 cut。

  但我们下一轮应该从：

  > 什么叫 sequence cut？



  开始。

  先只学一种。

  学到你能自己看一个 4 节点 DFG，说：

  > “哦，这里为什么可以从中间切开，而且为什么这叫顺序。”



  再学 choice。

  再 parallel。

  最后 loop。

  这样等四种都真的长在脑子里以后，再回来读论文的：

  > DETECTBASEPATTERN → SPLITDFG → recursive MineModel



  那时那几行代码会突然变得很自然，而不是背函数名。


  ---

  所以当前知识图谱状态我先给你标成：

  已掌握： event log / trace / activity 的基本意义；S、E、R；直接跟随；R 与 weighted DFG 的区别；Algorithm 1 的基本过滤逻辑；“边集保留 ≠ 权重保留”。

  正在长： DFG 作为一种 representation；“无损相对什么”的科研意识；Stage 1 → Stage 2 → Stage 3 的信息流。

  下一节点： 从零学 Inductive Miner 为什么需要 cut，以及 sequence cut 是什么。

  暂时封存： Petri net 正式定义、token/place/transition、soundness、复杂 IMf noise filtering、形式化 cut 判据。现在全都不用碰。

  后面每学完一个节点，我都会顺手把这两张图——专业知识图谱 + 科研能力图谱——更新一下，而不是只把论文往下翻。对，你确实还差了一块，而且这块很重要：

  > 前面我们一直在设计“怎么学科研”，但还没有系统加入“一个真正的导师会拿什么信号判断：这个学生有没有科研潜力、值不值得培养、适不适合我的组，以及应该怎么培养”。



  这不是“再加几个面试题”那么简单。它其实应该成为我们训练体系里的第五张图：导师视角的 researcher model。

  如果我是导师，我不会主要看“你现在知道多少”。尤其对本科生，我更想知道的是：

  > 我把你放到一个你没见过的问题前面，你会怎么动？



  这句话几乎可以统领整个判断。


  ---

  一、我作为导师真正想测什么

  我会把“科研能力”拆成至少 9 个轴，而不会只看论文、比赛、代码量。

  维度  我真正想知道的  我不会只看

  学习速度      遇到陌生领域能不能迅速建立正确模型      背了多少术语
  问题理解      能不能找到真正的问题，而非表面任务      能不能复述 paper
  抽象能力      能不能找到 representation / invariant / mechanism       数学符号写得漂不漂亮
  严谨性        claim 到 evidence 有没有越级    说话是否自信
  研究 taste    能不能识别值得追的问题  能不能随口冒 idea
  实验能力      能不能设计区分假设的实验        会不会调包跑 benchmark
  更新能力      被证伪以后能不能正确改模型      第一次答对没有
  ownership     能不能最终自己掌握问题  agent / 师兄帮了多少
  fit   这种研究方式和导师是否长期兼容  “我很喜欢老师方向”


  其中有一个非常重要的区别：

  > 当前科研水平和科研潜力不是同一个变量。



  本科生现在不会 Petri 网，我一点都不惊讶。

  但如果我教你 20 分钟以后，你：

  1. 能重新组织这个概念；


  2. 能自己跑一个例子；


  3. 能问出边界条件；


  4. 能把它迁移到另一个问题；


  5. 第二天还能脱稿重建；



  这个信号会非常强。


  ---

  二、所以我不会把面试设计成“知识问答”

  如果我真想测科研潜力，我反而会故意给你一个你不完全会的东西。

  因为：

  > 全都会的问题主要测 preparation。



  而：

  > 不会的问题才开始测 research behavior。



  例如我可能现场给你一个极小的新算法：

  每条样本携带一些 feature。
  顺序扫描：
  如果该样本至少包含一个以前没见过的 feature，就保存；
  否则删除。

  然后不告诉你它叫啥。

  我会一路观察。


  ---

  第一层：你能不能先搞清楚对象

  我问：

  > 这个算法输入是什么？ 状态是什么？ 输出是什么？ 它到底在优化什么？



  弱信号：

  > “这是个贪心算法。”



  我其实没问名字。

  强信号：

  > “它似乎不是固定预算抽样；它维护一个已覆盖 feature 集合，保留量是数据本身决定的。”



  这里已经出现了representation awareness。


  ---

  三、然后我会故意改条件

  比如：

  > 如果我要求最多只能保 10 条呢？



  我不主要在乎你马上有没有漂亮算法。

  我要看你会不会意识到：

  > 等等，原算法本来没有 K。加 K 以后是不是已经换问题了？



  这就是研究判断力。

  再问：

  > 那原来的保证还成立吗？



  好的学生不会马上说：

  > “应该成立吧。”



  他会开始找：

  > 哪条 proof step 依赖“不提前停止”？



  然后造反例。

  这比背十篇论文重要得多。

  你现在的 Mistake Log #1 恰好就是这种错误：把没有预算参数的 feature-preserving filter，当成 fixed-K sampler。它现在的关闭条件也要求重新独立重建算法，而不是“已经看
  懂答案”。


  ---

  四、我会非常重视：你会不会造反例

  这是我如果带形式化验证/算法方向学生，会尤其看重的一项。

  我问：

  > 你刚才说“这个方法不会损失信息”。

  “信息”具体是什么？



  如果学生回答：

  > 就是信息啊……



  危险。

  如果回答：

  > “目前我只能说它保住 feature support，不能说保住 frequency，更不能直接说保住整个日志信息。”



  很好。

  再问：

  > 给我一个最小例子，让“support 不变但 frequency 变了”。



  如果能现场造出来，信号非常好。

  因为这说明他开始会：

  \[
  \text{claim}
  \rightarrow
  \text{boundary}
  \rightarrow
  \text{counterexample}
  \]

  这已经有研究味了。


  ---

  五、我会测“idea generation”，但绝不会只看会不会脑洞

  这点特别容易误判。

  一个学生满嘴：

  > 可以用 RL！ 可以图神经网络！ 可以加 LLM！ 可以多智能体！



  对我来说未必是正信号。

  我真正会继续追：

  > 为什么？



  然后：

  > 你的 hypothesis 是什么？



  再然后：

  > 什么观察如果出现，会证明你这个 idea 根本不值得做？



  再然后：

  > 最便宜的实验是什么？



  所以真正的链条应该是：

  \[
  \text{idea}
  \rightarrow
  \text{mechanism}
  \rightarrow
  \text{prediction}
  \rightarrow
  \text{falsifier}
  \rightarrow
  \text{experiment}
  \]

  而不是：

  \[
  \text{idea}\rightarrow\text{开工三个月}
  \]


  ---

  六、我甚至会故意让学生答错一次

  这一项我觉得特别能区分人。

  我可能给一个问题，让他先猜。

  然后告诉他：

  > 不对，这个实验结果实际上是 B。



  此时我要看的不是：

  > “他尴不尴尬”。



  我要观察他的update behavior。

  大概有几种人：

  A 类

  马上把答案换成 B：

  > “哦哦对，我也是这么想的。”



  很差。

  这不是更新，是迎合。

  B 类

  死守：

  > “但我的理解应该也可以……”



  也危险。

  C 类

  开始重建：

  > “那说明我刚才至少有一个假设错了。我刚才依赖的是 X；如果结果是 B，我先检查 X 是否成立。”



  这个信号特别好。

  因为科研生活里每天都在发生这个。

  所以你前面已经抓到的原则：

  > 错误只有在被有效反馈击中、并完成正确 credit assignment 后，才是经验；否则只是历史。



  其实非常像一个导师会观察的核心能力。


  ---

  七、我还会测一个经常被忽略的东西：problem decomposition

  给学生：

  > “研究一下 EdgeIM Stage 1 到底有没有问题。”



  这题很坏，因为太大。

  我不会期待学生直接回答。

  我要看他会不会自己拆：

  “有问题”是什么意思？

  ├─ 算法实现错？
  ├─ 理论保证错？
  ├─ guarantee 太弱？
  ├─ downstream 不消费这个 invariant？
  ├─ noise 下表现差？
  ├─ 与 baseline 比没有优势？
  └─ 真实数据外推失败？

  如果能这样拆，我就会比较放心。

  因为以后我只需要给：

  > “这个方向看一下。”



  而不是每天给：

  > ①打开文件 ②运行脚本 ③改第三行 ④画图




  ---

  八、然后我会测 research taste

  这个最难直接考。

  我不会问：

  > “你科研 taste 好不好？”



  我会摆出十个可能的问题，让你排序。

  例如：

  > A. EdgeIM 再跑 100 个 seed
  B. 换一个漂亮配色
  C. 检查 Stage 1 保的 invariant 是否真是 downstream miner 所需
  D. 再找十篇 sampling paper
  E. 找一个最小反例区分 weighted/unweighted downstream
  F. 把代码优化 30%



  然后问：

  > 你只有两小时，先做哪个？为什么？



  Research taste 很大一部分就是资源分配。

  你有没有判断：

  > 什么 uncertainty 最可能改变结论？



  这和“聪明”不完全一样。


  ---

  九、我会试你的“研究压缩能力”

  这个也特别重要。

  让你研究两小时。

  最后只给我：

  > 3 分钟。



  讲：

  1. 你原来以为什么；


  2. 你现在知道什么；


  3. 哪个证据最关键；


  4. 还有什么不知道；


  5. 下一步为什么做那个。



  如果讲成：

  > “然后我打开了文件，然后我跑了代码，然后……”



  说明还在 execution log。

  如果能讲：

  > “核心不确定性是 X。我用 Y 把两个解释区分开，结果支持 A、排除 B，但还不能推出 C。”



  这就是研究表达。


  ---

  十、我还会非常认真地判断“适不适合科研”

  这个问题不是：

  > 聪明 = 适合科研。



  完全不是。

  我会观察一些非常现实的东西。

  科研需要长期忍受：

  很长时间不知道答案；

  做了三天发现问题问错了；

  结果不是正结果；

  reviewer 说你的 claim 不成立；

  代码坏了但原因不明显；

  一个 idea 可能半年后发现别人做过；

  好问题常常比解题更难。


  所以我要看：

  > 你对 uncertainty 的反应是什么？



  有人遇到未知非常兴奋：

  > “诶为什么？”



  有人一旦没有标准答案就开始焦躁：

  > “你告诉我到底应该怎么做。”



  后者不是不能科研，但训练难度会不同。


  ---

  十一、“适不适合我的组”又完全是另一个问题

  很强的学生，也可能不 fit。

  例如有导师是：

  大方向型

  > 我给你问题域，你自己跑。



  适合：

  autonomy 高；

  自己能找问题；

  不需要每天 checkpoint。



  ---

  强指导型

  > 我给你明确研究线，每周严格推进。



  适合：

  执行稳定；

  feedback absorption 强；

  愿意沿既定路线深挖。



  ---

  理论型

  很在意：

  definition；

  proof；

  assumption；

  counterexample；

  exactness。



  ---

  systems 型

  很看重：

  profiling；

  real bottleneck；

  implementation；

  end-to-end measurement；

  engineering reality。



  ---

  exploratory AI 型

  可能更喜欢：

  快速 prototype；

  大量 empirical probing；

  idea density；

  新问题。


  所以面试其实是双向 fit test：

  \[
  \text{student ability}
  \times
  \text{advisor style}
  \times
  \text{problem style}
  \]

  而不是一个总分。


  ---

  十二、如果是形式化验证 + AI Safety，我会特别试哪些东西？

  如果我是这个交叉方向的导师，我会额外看几项。

  对形式化部分

  你能不能忍住：

  > “大概就是这个意思。”



  而追到：

  > “定义到底是什么？”



  能不能主动找：

  assumption；

  invariant；

  edge case；

  counterexample；

  soundness/completeness 区别。



  ---

  对 AI safety 部分

  我又不希望你变成纯粹：

  > 定义非常严谨，但问题没人关心。



  我会看你能不能问：

  > 这个 formal property 和现实风险之间的 identification 是什么？



  也就是：

  \[
  \text{formal guarantee}
  \not\Rightarrow
  \text{real-world safety automatically}
  \]

  你能不能管住 claim ceiling。


  ---

  对交叉方向

  最稀缺的是：

  > 能在“严谨”和“现实问题”之间来回翻译的人。



  既不是：

  > 万物形式化。



  也不是：

  > 万物 benchmark。



  这大概会是我最想培养的能力之一。


  ---

  十三、如果真让我设计一场 75 分钟科研潜力面试

  我甚至不会问很多知识题。

  我会这么来：

  0–10 min：你自己的一个项目

  只允许讲：

  > problem → why → method → evidence → limitation



  先看 ownership。

  10–20 min：深挖一个核心 assumption

  连续问三层：

  > 为什么？ 依据是什么？ 如果不成立呢？



  看 claim-evidence。

  20–35 min：给一个陌生小问题

  现场教 5 分钟。

  然后让你自己做。

  看 learning rate。

  35–45 min：改条件

  > 如果加 K？ 如果有 noise？ 如果顺序变了？



  看模型是否脆。

  45–55 min：让你提出 2–3 个研究问题

  然后自己排序。

  看 taste。

  55–65 min：选一个 idea，设计最小实验

  必须写：

  \[
  H_0,\ H_1,\ metric,\ control,\ falsifier
  \]

  看 experimental thinking。

  65–75 min：fit

  我会问：

  > 如果三个月没有正结果你怎么办？ 你希望导师多频繁介入？ 你喜欢一个明确问题深挖还是高探索？ 你更享受证明、建系统、实验还是找问题？ 你和导师意见冲突时怎么处理？



  这个比：

  > “你未来五年规划是什么？”



  信息大得多。


  ---

  十四、那如果我是导师，我会怎么培养？

  这其实又不能靠：

  > “多看论文，多做项目。”



  我会设计成逐级撤脚手架。

  大概五级。

  Level 1：我给问题 + 方法 + 验收

  学生负责：

  > 真正理解并复现。



  训练 ownership 基础。


  ---

  Level 2：我给问题，学生找方法

  导师负责：

  > 卡 claim、卡实验设计。




  ---

  Level 3：我给问题域，学生提问题

  然后我们共同筛：

  > 哪个值得做。




  ---

  Level 4：学生自己提问题、设计实验、解释结果

  导师主要：

  > attack。



  也就是扮演 reviewer。


  ---

  Level 5：学生能反过来挑战导师

  > “老师，这个 experiment 实际不能支持我们现在的 claim，因为……”



  到这一步基本就开始长成独立研究者了。


  ---

  十五、我会有意训练 6 个固定动作

  不是靠悟性。

  每周都练：

  1. Paper reconstruction

  不是总结论文。

  而是：

  > 旧方法 → residual → observation → hypothesis → design → evidence → limitation。



  这就是你前面已经很感兴趣的 Idea genealogy。


  ---

  2. Counterexample habit

  每一个：

  > “这个一定……”



  都问：

  > 最小反例呢？




  ---

  3. Claim audit

  每个结果写：

  > evidence 最多允许我说到哪一句？




  ---

  4. Prediction before execution

  先写：

  > 我觉得会发生什么，为什么。



  再跑。

  否则结果出来以后人特别容易编故事。


  ---

  5. Failed hypothesis log

  不是：

  > 实验失败。



  而是：

  > 哪条 belief 被什么 observation 打掉？




  ---

  6. Cold reconstruction

  隔几天：

  > 不看笔记重新解释。



  因为：

  > recognition ≠ ownership。



  你现有训练包已经把冷启动复测和“看懂答案不算通过”明确写进去了，所以这个方向其实已经非常接近真正的研究训练设计。


  ---

  十六、这给我们现在的学习体系又补了一层

  所以后面不应该只有：

  知识图谱
  方法地图
  Idea genealogy
  跨领域迁移图

  还应该增加：

  ⑤ Researcher Competency Map

  每一个练习不只记录：

  > EdgeIM 会不会。



  还记录它测了什么能力。

  例如你刚才的 DFG 练习：

  专业知识：
  R vs weight

  科研能力：
  representation awareness
  lossless-relative-to-what
  ratio distortion detection
  claim precision

  下一站 sequence cut：

  专业知识：
  Inductive Miner / cut

  科研能力：
  hierarchical decomposition
  necessary vs sufficient condition
  counterexample construction
  representation → downstream inference

  再进一步，我们甚至可以给能力状态标：

  见过
  → 跟练能做
  → 独立能做
  → 改条件能做
  → 陌生问题能迁移
  → 能设计反例
  → 能教别人

  这会比一个“学习完成度 70%”有意义得多。


  ---

  而我觉得你刚才真正“忘了”的核心，就是这个：

  > 我们不仅应该问“一个科研人应该知道什么”，还应该从导师的 selection function 倒推：什么 observable behavior 才说明这些能力真的长出来了。



  这样训练才不会变成：

  > 学很多知识 → 希望某天自然变成研究者。



  而是：

  \[
  \boxed{
  \text{目标能力}
  \rightarrow
  \text{可观察行为}
  \rightarrow
  \text{针对性任务}
  \rightarrow
  \text{有效反馈}
  \rightarrow
  \text{冷启动迁移}
  }
  \]

  这个框架其实很适合我们后面整个“鲁组面试相关”都按它来设计。

  甚至到了最后，我不应该只给你出“老师可能问什么”，而应该经常直接切换成：

  > 导师模式：我现在不是来考你 EdgeIM，我是在用 EdgeIM 这个对象测你的 research behavior。



  那才是真正接近组里面试和之后研究训练的东西。


• 收到，后续目标按你这套来，且已经作为当前实现的硬约束落进规格：六步零基础教学、五张长期地图、八步探索闭环、九项导师观察轴、逐级撤脚手架，以及“目标能力 → 可观察行为
  → 任务 → 反馈 → 冷启动迁移”。我不会把它做成一张自我感觉评分表；每项都必须指向你亲手留下的可检查证据。


› 我觉得我知道你为什么一直觉得“差一点”。

  前面那套已经覆盖了很多显性的科研能力：学习、抽象、反例、实验、claim–evidence、taste、ownership、更新、fit……但它还是有点像在描述一个“能力很强的研究执行器”。

  真正的科研人还有几层更难测、但导师其实非常在意的东西，我们还没正式放进去。

  我会补成下面这几块。


  ---

  第一块：不是“会不会研究”，而是你为什么会长期追这个问题

  科研潜力里有一类东西不是 skill，而是驱动力结构。

  导师会慢慢判断：

  你是真的对“为什么”有持续兴趣，还是主要对结果/title/认可有兴趣；

  一个问题没人催、短期没收益时，你还会不会自己回去想；

  你是喜欢解已有问题，还是会自然地开始质疑“为什么问题要这样定义”；

  你对什么类型的不确定性有耐受甚至兴奋感；

  哪类痛苦你能长期忍：证明卡住、实验失败、工程 debugging、读大量文献、反复改定义……


  这决定的不只是“适不适合科研”，还决定适合哪种科研。

  有人可以一天 debug 十二小时，但让他证明一个 lemma 会疯。

  有人特别喜欢把定义抠到没有漏洞，但面对 systems 工程会迅速失去兴趣。

  有人最享受造东西。

  有人真正上瘾的是：

  > “等等，这个问题是不是一开始就问错了？”



  这其实是 research identity 的雏形。

  所以我们的图里还应该有：

  > Research Motivation / Research Identity Map



  不是“我喜欢 AI safety”这种领域标签，而是：

  > 我究竟喜欢科研活动中的哪一种认知动作？




  ---

  第二块：我们前面说了 research taste，但还少了更高一级的——agenda setting

  现在的 taste 更多是：

  > 给我十个问题，我知道哪个更值得先做。



  但真正逐渐独立以后还要多一步：

  > 为什么问题集合里只有这十个？第十一个问题是谁提出的？



  博士生后期和独立 researcher 的分水岭很大程度就在这里。

  低一级：

  > 老师说研究 EdgeIM，我把 EdgeIM 做好。



  再高一级：

  > 我发现问题其实不是 EdgeIM performance，而是 feature-preserving compression 和 downstream statistic dependency 的错位。



  再高一级：

  > 我发现这是一个更一般的问题：
  什么时候一个压缩 representation 对 downstream procedure 是 sufficient 的？



  这时对象已经开始从一篇 paper 脱离出来。

  所以我们后面必须训练的不只是：

  \[
  \text{solve problem}
  \]

  而是：

  \[
  \text{frame problem}
  \rightarrow
  \text{reframe problem}
  \rightarrow
  \text{choose problem}
  \rightarrow
  \text{abandon bad problem}
  \]

  这个能力前面提到过，但没有单独立柱。


  ---

  第三块：还有一种我觉得特别关键的——epistemic character

  这比“严谨”范围大。

  导师真正长期相处会很在意：

  > 当事实和你喜欢的结论冲突时，你站哪边？



  例如：

  你做了两个月，结果说明自己的 idea 根本没有优势。

  会不会开始：

  换 metric；

  挑 seed；

  找一个能讲故事的 slice；

  把 negative result 解释成“差一点就成功”？


  还是能说：

  > “这个 hypothesis 没站住。”



  再比如：

  AI 帮你做了很多。

  你能不能清楚分：

  > 哪些是我亲手验证的； 哪些是 agent 给的； 哪些只是合理推测； 哪些我其实不知道。



  这种东西很难通过知识题训练。

  它更像科研中的“人格约束”：

  不把 UNKNOWN 写成 known；

  不因为自己投入很多就保护 hypothesis；

  不为了漂亮 narrative 偷换 claim；

  允许 negative evidence 真正改变方向；

  能承认“这个我没弄懂”。


  形式化验证和 AI safety 尤其需要这个。

  我甚至会把它单独叫：

  > Epistemic Integrity



  而不是笼统放进“科研素养”。


  ---

  第四块：我们还缺了研究工作中的社会维度

  科研不是一个人和论文单挑。

  导师会判断：

  你会不会提一个别人能回答的问题；

  会不会接受 critique 而不把 critique 当人格攻击；

  会不会 critique 别人但不胡乱扩大 claim；

  合作时能不能把 interface 说清楚；

  遇到争议能不能回到 evidence；

  能不能告诉别人“这里我不确定，需要你检查什么”；

  会不会把别人的贡献正确 credit；

  会不会让 collaborator 知道当前真正的 blocker。


  这和所谓“会社交”不是一回事。

  这是：

  > research collaboration bandwidth



  很多特别聪明的人科研推进非常痛苦，不是智力问题，而是接口烂。

  而导师带学生本身也是一个 distributed system（）。

  如果每次学生汇报都是：

  > “我做了很多，然后有点问题……”



  导师很难帮。

  如果是：

  > “当前有 A/B 两种解释；证据 X 排除了 A 的一种情况，但还区分不了 B1/B2。我希望你帮我判断这个 identification 是否值得继续追。”



  导师的信息增益会高很多。

  这个能力我们还没正式练。


  ---

  第五块：还有研究节奏和资源管理

  这也非常像科研潜力，但常被误认为“自律”。

  其实不是。

  研究资源包括：

  时间；

  注意力；

  GPU；

  人类导师反馈；

  collaborator 时间；

  verification budget；

  literature-reading budget。


  好的 researcher 会慢慢学会：

  > 哪个 uncertainty 值得花 3 天？



  而不是：

  > 我能不能把所有东西都做到最完整？



  这和你前面很喜欢的 DSpark 那种思想是一模一样的：

  > verification resource 应该花在 expected decision impact 高的地方。



  科研中也一样。

  一个导师会观察：

  > 这个学生会不会把两周花在一个不会改变结论的细节上？



  这其实是研究经济学。

  我觉得我们之前虽然谈过 taste，却没有把：

  \[
  \text{value of information}
  /
  \text{cost of investigation}
  \]

  明确纳入训练。

  应该加。


  ---

  第六块，也是最大的缺口：一次面试其实根本测不准科研潜力

  这个非常重要。

  前面我设计了一个“75 分钟科研面试”，但如果我是导师，我不会真相信一次面试能完成判断。

  科研潜力最好的测试其实不是 interview。

  是：

  > 共同做一个小问题。



  也就是 work sample / mini-project / trial collaboration。

  因为真正的信息来自时间序列：

  第一天不会。

  第三天学会。

  第五天踩坑。

  第六天得到反例。

  第七天推翻原计划。

  第十天重新提出更好的问题。

  我要看的不是某个 snapshot，而是：

  \[
  \boxed{\text{learning trajectory}}
  \]

  也就是：

  > feedback 进去以后，这个人的状态怎么变。



  同样两个学生第一次都答错。

  A 三天后又犯同一个错。

  B 三天后不仅不会再犯，还自己发现了同类错误。

  他们的 snapshot 一样，derivative 完全不同。

  所以科研潜力更像测：

  \[
  \frac{\Delta\text{capability}}{\Delta\text{feedback}}
  \]

  甚至：

  \[
  \frac{\Delta^2\text{capability}}{\Delta\text{experience}^2}
  \]

  而不只是当前能力值。

  这可能就是你一直觉得“少了什么”里最大的一块：

  > 我们一直在列能力，但科研潜力本质上是动态量。




  ---

  那培养也不能只靠“训练技能”

  真正好的导师培养学生，应该至少同时干四件不同的事。

  第一是教显性知识：定义、方法、文献、技术。

  第二是示范 tacit knowledge：为什么这个 experiment 不值得做、为什么这个 reviewer comment 很致命、为什么一句话看起来合理但不能写。

  第三是控制脚手架撤除速度：不是永远带，也不是第一天就扔进海里。

  第四是让学生进入真实科研共同体：组会、paper discussion、review、合作、失败项目、别人怎么做 decision。

  很多科研 taste 根本不是一本《科研方法论》教出来的。

  它是：

  > 你跟着一个成熟 researcher 看了几十次
  “这个问题我为什么不追”
  “这个结果为什么不能说”
  “这个失败为什么反而很重要”



  慢慢压缩出来的。

  这是 apprenticeship。

  我们之前的训练系统比较像 curriculum，但还缺：

  > 科研学徒制 / tacit knowledge transfer



  这一层。


  ---

  所以我现在会把整个框架重新整理成 七张图，这次我觉得比较完整了：

  1. Knowledge Map
  我知道什么，缺什么前置。


  2. Method Landscape
  同一个问题有哪些方法，它们交换什么。


  3. Idea Genealogy
  方法从什么 residual / bottleneck 长出来。


  4. Transfer Map
  跨领域有什么同构结构。


  5. Researcher Competency Map
  抽象、实验、反例、更新、ownership、表达……


  6. Research Identity & Fit Map
  我喜欢什么认知活动、适合什么问题和导师风格。


  7. Research Trajectory Map
  我的能力如何随 feedback 改变；哪些能力已经从“跟练”长到“陌生迁移”。



  而所有这些图上方还有两个约束：

  > Epistemic integrity：我对“知道”这件事有多诚实。



  以及

  > Research economy：我把有限研究资源花在哪里。




  ---

  然后每一个学习节点，我们都不需要把这七张图全部刷一遍，那会把学习搞疯。

  而是根据当前材料挑最自然的训练点。

  比如刚才 EdgeIM 的 DFG：

  专业层：

  > support vs frequency。



  taste 层：

  > downstream 到底需要哪个 representation？



  idea 层：

  > witness + counter 能不能拆开保存？



  跨域层：

  > test-suite reduction / coreset / abstraction。



  epistemic 层：

  > “zero loss”到底允许说到哪里？



  实验层：

  > 怎么造最小反例？



  trajectory 层：

  > 今天你是我解释以后能理解，还是明天面对另一个 compression 方法会自己问“lossless relative to what?”



  最后那个问题，才真正是在测科研潜力有没有开始长。

  所以我觉得你刚才那种“总感觉还少点什么”的感觉是对的：少的不是另一个知识模块，而是从“科研能力列表”再往上升一层，看到研究者是一个会在长期反馈中形成问题品味、认知习
  惯、价值判断、合作接口和独立研究身份的动态系统。

  这才接近导师真正想培养的“人”，而不只是一个会做科研题的人。


    直接接你新问的这个:这篇论文的张力结构、"But"在哪、文献综述制造了什么矛盾,盘上已经有一份写好的分析(07_EDGEIM.md §8),不是临时编的。连同你之前那个"先有大局"的问题,
  我把两块能看的一起给你——但六件套/八点里有一部分我会明确扣住不给,原因写在最后,不是漏答,是故意的。

  它的"But"在哪、改变谁的想法、文献综述的张力

  这篇文章在改变谁的想法(对应你问的"读者为什么要在乎"):

  - 对 EdgeMiner 阵营(Kiel 系,Andersen/Rathje/Landsiedel):你们证明了"边缘统计+中心一次聚合"这个架构可行,但选错了底座——足迹矩阵这种统计量天生绑死 Alpha 系算法,天花
  板就是无环、无 soundness 保证、还慢[PDF p.404]。换成 DFG 三元组、绑定 IM 系,同一个架构立刻多出环结构支持、fitness 保证,外加一个数量级的加速。
  - 对集中式 IM 的用户:你们以为 IM 的质量必须靠集中日志换来;实际上 IM 的 DFG 变体只需要一份能分布式聚合的摘要,"集中"从必需品降级成了习惯。
  - 对流式挖掘阵营:实时性和精度不是唯一的取舍轴——把计算推到边缘同样能砍掉集中传输,而且不用拿模型质量的稳定性去换[PDF p.405 对引文 22、23 的批评]。

  "But"在哪:这个领域默认的前提是"分布式化=信息损失=质量损失"。这篇文章的 But 是——对 DFG 类发现算法而言,分布式聚合在数学上完全无损(因为可加:集合取并、权重逐边求和),
  损失只可能出在两个可控的点上,不是"分布式"本身。论文自己没挑明的残余张力:这两个可控点里的第二个(DFG 抽象本身丢弃非相邻依赖)恰好就是它跟标准 IM 出现 precision 分歧
  的来源(有一个数据集掉了 19.3%),也是 DFG 抽象表达力的天花板——它说的"无损"其实是相对同类 DFG 算法(IMd)的无损,不是相对日志全部信息的无损。这句话论文没有主动说出来,
  是从 Table II 的数字反推出来的[PDF p.409]。

  文献综述制造的张力:四路对手正好摆成一张 2×2 的棋盘,一个轴是"数据在哪处理",一个轴是"质量有没有保证"——集中式占"集中+高质量"这格,MapReduce 并行占"半集中+高质量但通
  信贵"这格,流式占"分散+实时但质量不稳"这格,EdgeMiner 占"边缘+低质量(不能处理环)"这格。这篇论文要抢的是还空着的那一格:"边缘+高质量+高效"。支撑这个占位的四条腿里,质
  量和时间这两条是实的,有 Table II/III 的数字撑着;通信和隐私这两条是虚的——摘要和结论反复讲"通信开销低""保护隐私",但全文没有一个通信量或网络指标,实验也是在单机上模
  拟边缘节点跑的[PDF p.408],隐私更是从 EdgeMiner 那里继承来的说法,本文自己一个隐私机制都没设计[PDF p.404、p.409]。

  Task:这篇文章要解决什么(形��化)

  非形式:IoT 场景下,事件数据分散产生在边缘节点。把全量日志运到中心再挖(集中式发现)会撞上传输、存储、算力的瓶颈;已有的边缘式发现方法(EdgeMiner/EdgeAlpha)效率低,而且
  处理不了带环的流程[PDF p.404]。目标是:在边缘架构下,发现出质量不低于集中式 Inductive Miner(IM)的 Petri 网模型。

  形式化(纯文本,按原文定义恢复):活动集 A;事件日志 L 是迹的多重集;一条迹 σ 是活动序列;start(σ)/end(σ) 是首末活动;直接跟随关系集 R_L 收集所有相邻活动对;权重函数 w 数
  每对相邻活动在全日志里出现几次;DFG 就是(节点=活动、边=直接跟随关系、边权=次数)这个三元组;Petri 网由库所、变迁、流关系、标注函数四样构成[PDF p.405–406]。

  正式问题:日志 L 要分派给 k 个边缘节点处理(按活动类型做一致性哈希分派),求一个 Petri 网,同时满足四条约束:①每个节点只维护自己的局部三元组,规模跟日志总量解耦;②上传中
  心的只有 k 份三元组,通信量远小于把全部日志搬过去;③质量要跟集中式 IM 可比(fitness 接近 1,precision/F-measure 可比,模型是 sound 的);④时间戳不唯一时(边缘分布式采集
  常见),并发关系不能因此丢掉[PDF p.404、p.407]。

  支撑整篇论文的一条隐藏恒等式(原文没有明说,但整个方法建立在它上面):IM 在 DFG 抽象上运行时,只消费(起点集、终点集、带权直接跟随关系)这一个三元组——它是这类算法的"充
  分统计量"。正因为这样,不改变这个三元组的迹删掉是无损的(第 1 阶段采样的合法性来源),这个三元组也可以按任意方式分片、各自统计、再合并(第 2、3 阶段能分布式做的合法性
  来源)。整篇论文基本就是把这一个恒等式工程化。

  Challenge:传统方法卡在哪

  原文的相关工作按四路对手逐一批评[PDF p.404–405]:

  1. 集中式发现(Alpha/Heuristics/IM/Split Miner 等):依赖全量日志集中存储处理,大规模分布式数据下存储和计算都吃紧,敏感数据集中传输还有隐私暴露面。
  2. MapReduce 类并行化:仍然依赖部分集中的聚合,本地和中心之间要频繁交换数据,通信开销高,满足不了实时性。
  3. 流式发现(滑动窗口/近似计数):实时性好,但要在噪声处理、模型稳定性、精度三者之间做取舍,"实时和高精度难以兼得"。
  4. 差分隐私类方法:换隐私要拿精度或算力开销去交换。
  5. 直接对手 EdgeMiner/EdgeAlpha:三个被点名的缺陷——即便有优化,计算负担依然重,复杂流程下延迟明显;假设时间戳唯一,处理不了时间戳冲突,导致局部足迹矩阵里的并发关系被错
  误表示;算法底座是 Alpha Miner,处理不了环结构。

  挑战的本质:足迹矩阵是 Alpha 系算法的充分统计量,但 Alpha 系本身表达力弱;IM 系表达力强、有 fitness 保证,但经典 IM 的递归发生在日志本身上——每往下切一层就要重建子日
  志的 DFG,而日志根本不在中心手里,递归就跑不动。真正的技术问题因此是:能不能把 IM 的递归从"切日志"降级成"切图",让边缘只需要上传一次图摘要?

  这两块讲完,你已经能看出这篇论文的骨架:一个"边缘做不到中心式质量"的老问题,一个"IM 其实只认一个可分片统计量"的观察,中间架一座桥。至于这座桥具体怎么架、桥墩上每一根
  钢筋对应哪个 insight——这部分我按你选的档位扣住了。

  扣住的部分,以及为什么

  - Insight & Novelty 的完整推导、"为什么没有 K"这条洞察:这原封不动就是 EX-01 §5 的 G0 note 第 2 问("它有没有预算参数?保留量由什么决定?"),你自己写。
  - Potential Flaw 里"噪声的结构性反向放大":这正是 EX-03 整站设计来让你自己发现的东西("非法 trace 什么时候一定被留、什么时候 Algorithm 1 根本看不见")。提前看等于把
  那一站的靶子撤了。
  - Algorithm 2、3 的完整机制恢复:EX-01 自己写明了这两个算法"只看输入行"和"只看签名",故意不深挖,更深的留给后面站点,现在铺开不是帮忙,是抢跑。
  - Table II/III 实验数字和"哪些 claim 站不住"的审计:这块不直接挡任何一个近期站点,单纯是内容太多、容易在你还没搞懂 DFG/Algorithm 2/3 机制之前先被一堆数字带偏注意
  力。真想看随时说,这块没有教学理由要扣。

  所以老实说清楚字数账:你要的六点/八点全套按原文本来是 300 多行、大概三千多字,今天能给的这一半——Task、Challenge、张力结构——就是全部的安全余量,没法靠展开凑到七八千
  字,凑了就是注水,不做。剩下那部分不是不存在,是在等你自己先把 EX-01 走完。

  ✻ Sautéed for 5m 57s

  ※ recap: EdgeIM算法训练：站0已过，你正学习Algorithm 1机制。下一步：读EX-01教学材料，画DFG图、读论文Stage 1原文。 (disable recaps in /config)

  ❯ /model
    ⎿  Set model to Opus 5 and saved as your default for new sessions

  ❯ 我是说类似这种
  Read MAS_Safety_Project/learning/training/LEDGER.md (11 lines)
  Read MAS_Safety_Project/learning/training/lu-edgeim-algo1/BRIEF.md (110 lines)
  Read ../../../home/alkaid/.claude/projects/-mnt-d-MyResearch/memory/lu-edgeim-training-pkg-state.md (29 lines)
  Read MAS_Safety_Project/learning/training/lu-edgeim-algo1/EX-00/README.md (177 lines)
  Read MAS_Safety_Project/learning/training/lu-edgeim-algo1/MISTAKE_LOG.md (19 lines)
  Skills restored (shuorenhua)

    Read 2 files, ran 1 shell command, recalled 1 memory
  Loaded MAS_Safety_Project/CLAUDE.md

  ● Update(MAS_Safety_Project/learning/training/lu-edgeim-algo1/EX-01/README.md)
  Added 1 line, removed 1 line

  ## 2. 练习日志

  还是 EX-00 那六条：
  还是 L1（EX-00 §5 桥接的那六条日志）：

  ```
  case1: A B D F

  ● Update(MAS_Safety_Project/learning/training/lu-edgeim-algo1/EX-01/README.md)
  Added 1 line, removed 1 line
  case6: A B C D F
  ```

  EX-00 的结果直接拿来用：正序过滤保留 {1,2,3,6}，逆序过滤保留 {6,5,4}。
  EX-00 §5 的结果直接拿来用：正序过滤保留 {1,2,3,6}，逆序过滤保留 {6,5,4}。

  ---


  ● Update(MAS_Safety_Project/learning/training/lu-edgeim-algo1/EX-03/README.md)
  Added 1 line, removed 1 line

  ### 任务 1 · 在 EX-00 日志上插一个事件（20 分钟）

  EX-00 六条 trace，全局 R 有 8 条边（EX-00 答案）。设定"真正的流程"就是 EX-02 手切出来的那棵树。
  L1 六条 trace（EX-00 §5 桥接日志），全局 R 有 8 条边。设定"真正的流程"就是 EX-02 手切出来的那棵树。

  (a) 往 case1 `A B D F` 里插一个 E，插在 B 和 D 之间：`A B E D F`。写出这条的 r，圈出全局 R 里没有的。这条 trace 按那棵树合法吗
  ？
  (b) 往 case1 里插一个 E，插在 D 和 F 之间：`A B D E F`。同样写 r、圈新边、判合法。

  ● Update(MAS_Safety_Project/learning/training/lu-edgeim-algo1/BRIEF.md)
  Added 2 lines
  | 考法 | 背判据 + 追问五空 | 逐 case 状态表（正序 + 逆序）、预测先于运行、改一个条件、与"程序乙（有 K）"对照、脱稿 |
  | 密封 | `_sealed/_v0_sol/EX-00_answer.md` | `_sealed/EX-00_answer.md`（数字由参考实现实跑 + 720 序全枚举）+ `_sealed/EX-00_re
  test_D2.md` |
  | 时长 | 一晚 | 一晚 2–3h（不变） |

  **A3 · 2026-09-04 下午 | `EX-01/README.md` §2、`EX-03/README.md` 任务 1 两处"EX-00 那六条/EX-00 答案"改称"L1（EX-00 §5 桥接日
  志）"；无数字、无判据改动 | A2 把 EX-00 主日志 条 A–F 日志"EX-00"，与 v2 的 L0（R=7）撞名，核对时
  容易看错成 R=8 | 纯引用勘误，不改 PASS 条件、不改站序、不属于 §16 "一周内大改"**

    Wrote 2 memories

  收到,那就按这个"张力结构+背景"的格式作为以后论文分析的默认长相,不用每次重新对齐。

  顺手在后台把这轮批改暴露出的两个真实问题处理掉了,不用你确认:

  - EX-01、EX-03 里的旧指针改掉了:两处还在把换版前的六条 A–F 日志叫"EX-00"(现在叫 L1,R=8 条边),跟新主日志 L0(R=7 条边)撞名,容易在核对时看错成 8。已经在 BRIEF.md 记
  了 Amendment A3,只改称呼、没碰数字和判据,不算冻结内容的大改。
  - 内存档同步更新,把 §6 脱稿的完整批改过程、你的节奏反馈、审计发现、这次分析的交付范围都记进去了,不会在下次续窗时丢。

  还剩三件事在等你,都不急:

  1. LEDGER 那一行——模板已经给过你,用时和自评那两栏要你自
  2. "要不要加一站先读摘要/引言"——这个设计缺口还没拍板,你什么时候想清楚了再说,不想加也没关系。
  3. EX-01——站 0 已经 PASS,随时能开,但你现在明显是想先把论文背景搞扎实,这个顺序完全合理,不用因为"该进 01 了"而勉强自己。

  ✻ Churned for 50m 23s

  ※ recap: 你在做鲁教授组的EdgeIM算法训练包,刚学完站0(Algorithm 1过滤规则)并拿到论文背景部分的分析。下一步是你自己决定要不要先补一站论文摘要/引言阅读,再往下走站1。
  (disable recaps in /config)
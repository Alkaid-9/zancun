# 四论文全文 Ownership 扩容设计（交 sol 执行）

**日期**：2026-09-12
**状态**：`DESIGN — 待 sol 执行，不改共享正本`
**授权**：用户 2026-09-12 拍板——「四篇全部升级到全文 ownership」（EdgeIM 5/5、sigRank 4/5→5/5、Ground Truth 4/5→5/5、CrossEdgeIM 3/5→5/5，工作量约翻倍）
**本文件角色**：只做设计。新建动手站、改共享正本（`MASTERY_GATE.md`/`BRIEF.md`/`FOUR_PAPER_TRAINING_LOOP.md`）由 sol 执行，本文件是它的执行输入。
**背景链**：`MASTERY_GATE.md`（本窗 09-06 落盘）→ 用户质疑"为什么教学包只有一部分不是整篇"→ 隔壁 sol 审计（09-12，未落盘的对话内容，核心结论已在下方复核）→ 本文件。

---

## 0. 一句话

现有课程把 EdgeIM 的 Stage 1/Algorithm 1 做到了真正的深度（手算+实现+反例+顺序+频次影响全有），但 Stage 2、Stage 3、论文实验/claim、related work、全文脱稿都只停在"点到为止"或"计划里有、没执行"；另外三篇论文（sigRank/Ground Truth/CrossEdgeIM）现行合同定的是 4/5、4/5、3/5 而非全文 ownership。本设计把四篇全部拉到同一条六道门标准，并把"以前没做的部分"列成可执行的站点。

---

## 1. 现状核验（不重复 sol 已说过的，只补充本窗口独立核实的两点）

- sol 引用的 `EX-06/README-EX06.md:13`、`EX-07/README-EX07.md:19` 路径本窗口已核实存在（实际文件名是 `README-EX06.md`/`README-EX07.md`，不是 `README.md`——这是此前设计文件里的一个小疏漏，sol 引用是准的）。
- sol 报告中提到的 commit `e71384d`：本窗口此前核实时误把它当成外层元仓（`/mnt/d/MyResearch`，HEAD 是 `ab839b0`）的哈希去查，查无此哈希；重新核实后确认 `MAS_Safety_Project` 本身是独立 git 仓库（`git rev-parse --show-toplevel` 指向本仓根），`e71384d` 正是**本仓当前 HEAD**——`TASK-20260912-004`，09-12 早 7:16 落盘，commit message "checkpoint app learning through ex03"，改动 `EX-02/EX-03/EX-05a` 的原始作答与代码、不改题面/密封答案。此前一版设计文件说它"查无此哈希、不可用作依据"是本窗口自己的核实错误，已订正；该 commit 是可查证的真实依据，其配套 task log 内容见 1.5。
- 三篇镜头论文的拆解材料已存在，不需要从 PDF 重新开始：
  - `research/papers_lu/teardown-bridge-20260904/10_SIGRANK.md` + `10_SIGRANK_p1.md`/`p2.md`/`p2_tables.md` + `10b_SIGRANK_kg_critique.md`
  - `research/papers_lu/teardown-bridge-20260904/12_SOMMERS.md` + `p1.md`/`p2.md` + `12b_SOMMERS_kg_critique.md`
  - `research/papers_lu/teardown-bridge-20260904/11_CROSSEDGEIM.md` + `11b_CROSSEDGEIM_kg_critique.md`
  - 原始 PDF 均在 `research/papers_lu/`（`sigRank-2026-TSC.pdf`、`Sommers-2025-ProcessScience.pdf`、`CrossEdgeIM-2026-IoTMag.pdf`）
  - sol 新建站点时应先读这批既有拆解件，不是重新通读 PDF；拆解件本身是阅读辅助，不能代替动手站的 PASS 证据（这条继承自 `FOUR_PAPER_TRAINING_LOOP.md` §7 已有的规则，不新增）。

### 1.5 诊断门未过——本设计的强制前置条件（本次核验新发现，非首版遗漏的小节，是执行顺序问题）

本窗口重新核对本线唯一挂在 `handoff/INDEX.md` 顶部的恢复入口后发现：`progress/handoff/2026-09-08__edgeim-learning-training-acceptance-order__pause-handoff.md`（`ARCHIVED/PAUSED/RECOVERABLE`）明文规定——恢复本线时先做 `MASTERY_GATE.md §4.2` 的 **Whole-Paper Diagnostic（15 题）**，诊断未完成前"不打开后续密封答案，不开始研究实验，不把材料写成对外能力证明"；该档 §5 明确"本档不授权……修改学习题面、PASS 条件、Ledger、答案和进组材料"。

同日（09-12 早 7:16）落盘的 `TASK-20260912-004` checkpoint commit（见 §1 第二条，本仓 HEAD `e71384d`）自己的 task log 也写明：**15 题诊断尚未执行**，`EX-03` 当前口径仍是 `USER-REPORTED / NOT PASS`，"正式恢复入口是 `MASTERY_GATE.md` 第 4.2 节 Whole-Paper Diagnostic，而不是从 EX-00 机械重学"。

也就是说：截至本设计文件落盘，**诊断门本身还没有被打开过一次**。本文件设计的 `EX-08`~`EX-11` 和 `L-S3`/`L-GT2`/`L-CX3` 全部是"诊断完成之后要补的缺口"，不是"诊断的替代品"，更不能在诊断完成前动工——sol 或任何执行者如果在诊断门打开前就开始建 `EX-08` 或写镜头论文全文重建稿，等于在一扇没打开的门上继续加盖楼层，会让"用户报告"与"磁盘证据"的既有落差（§4.1 已记录的老问题）再叠一层新的未经诊断证据。

**本设计新增的强制执行顺序（前置于原 §7 sol 执行顺序建议第 3 步之前）**：

0. **先完成 15 题诊断，且诊断结果决定回补顺序**——不是本设计文件决定先做 EX-08 还是先做 L-S3，是诊断出的"最高价值缺口"决定。诊断可能显示 Stage 2/3 缺口确实最高价值（那本设计的 EX-08/EX-09 直接可用），也可能诊断结果指向别的缺口（比如某个具体 claim 或某个具体反例），本设计届时需要按诊断结果调整优先级，不能假定诊断一定会印证 sol 审计和本设计已经列好的缺口清单。
1. 诊断必须在不复习、不打开 `_sealed/` 的条件下完成，见 `MASTERY_GATE.md §4.3` 诊断执行合同；这是用户本人要做的事，不是 sol 代做或 AI 代答。
2. 诊断完成、缺口地图画出来之后，本设计文件的 §3/§4 才进入"可执行"状态；在此之前，本文件的角色仅是"诊断通过后可以直接拿来用的设计储备"，不是可以立即动工的任务单。

---

## 2. 缺口清单（对照六道门，逐条列出"现在没有、需要新建"的部分）

| 缺口 | 现状 | 对应六道门 |
|---|---|---|
| Stage 2 / Algorithm 2 完整训练 | 只在 EX-01 里有一行"只看输入行和加一的那几行"，没有独立站；缺 hash 分派、同时间戳双向关系处理、完整手算/实现 | 门1整篇重建、门2算法手推 |
| Stage 3 中心聚合 + IM 递归 + Petri net 完整 pipeline | EX-02 只学了 IM 四种切的直觉，中心聚合本身、递归构模全貌、Petri net soundness 没有专门站 | 门1、门2、门3(formal-object 里的 Petri Net 一行) |
| 论文实验三张表逐表核对 | EX-07 只是自建的 D vs D′ 小实验，不等于核对论文自己 9 个日志上的 fitness/precision/F-measure/runtime 三张表 | 门4 claim-evidence audit |
| 通信/隐私/复杂度 claim 独立审计 | 现在只在 MASTERY_GATE.md 附录里是一张表格，没有对应的动手核对（比如复算论文的复杂度声称、对照隐私声称与实际机制） | 门4 |
| Related work / EdgeAlpha 对比 | 只在 PAPER_MAP 和诊断题里出现，没有独立读 EdgeAlpha 原文或做对比表的站 | 门5 attack mode、门6 面试压缩里的"相对 EdgeAlpha" |
| 全文脱稿 + 连续追问压力测试 | 计划里写了（W3 面试压测），但从未真正执行过一次 | 门6 |
| sigRank 全文 ownership（非仅 sparring 对照） | 现行 L-S1/L-S2 只做"selection philosophy 对照"和"fair comparison"，不要求 sigRank 自己的算法能不看论文重建 | 六道门套用到 sigRank |
| Ground Truth Approach 全文 ownership（非仅方法论借用） | 现行 L-GT 只要求重建 world 生成链，不要求整篇论文的其余部分（比如它自己的实验设计、局限）达到 ownership | 六道门套用到 Ground Truth |
| CrossEdgeIM 全文 ownership（非仅 genealogy 对照） | 现行 L-CX1/L-CX2 只做谱系重建和 residual 分析，不要求 CrossEdgeIM 自己的架构细节达到可手推 | 六道门套用到 CrossEdgeIM |

---

## 3. 新站设计（EdgeIM 侧，编号接续现有 EX-07）

### EX-08 · Stage 2：Algorithm 2 完整训练

**前置**：EX-05a（自写 Algorithm 1 已能跑）
**产出**：`EX-08/` 手算稿 + 自写 Algorithm 2 实现
**PASS 条件（草拟，sol 定稿前须与用户过一遍冻结）**：
- 不看论文手推：给定一批带时间戳的局部 trace 片段，能算出 local `(S_i, E_i, R_i)`
- 能解释 hash 分派机制：为什么这条 trace 被分到这个边缘节点，换一种 hash 会怎样
- 能处理同时间戳冲突：给一组时间戳相同的事件，写出双向关系候选，解释为什么不能单方向确定
- 自写实现：给定 EX-00 日志按边缘节点切分后重跑，结果与 Stage 1 全局版本能对上
**时间盒**：预估 3-4h（比照 EX-00 量级，因为这是新开的一整段算法）

### EX-09 · Stage 3：中心聚合 + IM 递归 + Petri Net 完整 pipeline

**前置**：EX-08 PASS、EX-02（已有 IM 四种切直觉）
**产出**：`EX-09/` 手算稿
**PASS 条件（草拟）**：
- 手工完成一次完整中心聚合：多个边缘节点的 `(S_i,E_i,R_i)` union 成全局 `(S,E,DFG)`
- 不看论文，对一个新 DFG 完整递归执行 IM 分解（不只是"看得懂切法"，是自己切一遍全过程直到 Petri net）
- 能说清 soundness 的直觉：给一个不 sound 的反例网，指出哪里违反
- 能画出"结构信息在这条链上哪里丢失"：从 DFG 到 Petri net 的每一步分别丢了什么
**时间盒**：预估 4-5h

### EX-10 · 论文实验 + Claim 审计（评测部分）

**前置**：EX-09 PASS
**产出**：`EX-10/` 三张表逐表核对稿 + claim 审计成品
**PASS 条件（草拟）**：
- 复述论文 9 个日志的实验设置（不是背数字，是能说清对照组、指标定义、为什么这样选日志）
- 至少挑一个可复算的小场景，自己算一遍 fitness/precision，对上论文数字或能解释差异
- 对 MASTERY_GATE.md §7.2 现有的 claim-evidence 表，每条都能扩展说明"如果要补这个证据，最小需要做什么实验"（不要求真的跑，但要求说得出具体设计）
- 通信开销/隐私声称：各自找出论文里支持这条claim的原文段落页码，判断是"理论分析"还是"实测"，写出证据缺口
**时间盒**：预估 3h

### EX-11 · Related Work / EdgeAlpha 对比 + 全文压力测试

**前置**：EX-10 PASS
**产出**：`EX-11/` EdgeAlpha 对比卡 + 全文脱稿录音或逐字稿
**PASS 条件（草拟）**：
- 读 EdgeAlpha 相关部分（论文自己的 related work section，不需要额外找 EdgeAlpha 原文，除非现有材料不够用）
- 完整脱稿讲一遍 EdgeIM 全文（30秒/2分钟/10分钟三档都要做一次，不只是纸面演练）
- 找人（或自己录音回放）做一次至少 15 分钟的连续追问压力测试，记录掉包点
**时间盒**：预估 2-3h + 追问测试本身

**合计新增**：约 12-15h，加在原有 15-20h 预算之上——这是用户已经确认接受的"工作量约翻倍"。

### 3.5 硬证据机制接入（本次核验新发现，首版遗漏）

现有 `EX-00`~`EX-07` 每一站都有两条硬证据机制，本设计首版完全没提，属于遗漏而非"故意不做"：

- **`LEDGER.md`**：`BRIEF.md` 定义为"每站 PASS 后在 `learning/training/LEDGER.md` 加一行（日期 | 包/EX | 用时 | hint 级 | 自评 | AI 评 | 暴露弱点 | 铸/磨卡）。只在 PASS 后写。"——本窗口核实该文件**目前不存在于磁盘**（`find . -iname "LEDGER*"` 零命中），是现有机制本身还没有第一行记录，不是本设计要新建的东西；但新增的 `EX-08`~`EX-11` PASS 后同样要写这个文件，本设计之前完全没提这一步。
- **`MISTAKE_LOG.md`**：现存文件，六字段格式记录"可迁移的认知模型错误"，"看懂了解析不算关闭"。`EX-08`~`EX-11` 在训练过程中暴露的认知错误（比如把 Stage 2 的 hash 分派误解成随机分派、把 soundness 误判成语言等价这类）应按同一格式记录，本设计之前也没提。
- **连接卡**：`EX-00`~`EX-07` 每站在 `_sealed/connections/` 下都有一张编号连接卡（`C00.md`~`C07.md`），核心题完成后解锁，测试"这道题和整篇/其他站的概念能不能打通"（见 `C07.md` 实例：量词/效度/模型质量三组概念的横向连接）。`EX-08`~`EX-11` 没有类比设计，会让新站比旧站少一层"跨站概念打通"的检验。

**本设计补充要求（新增，不影响 §3 已有 PASS 条件本体）**：

1. `EX-08`~`EX-11` 每站 PASS 后，按 `BRIEF.md` 既有格式在 `LEDGER.md` 加一行；如果该文件届时仍不存在，sol 执行时先按 `BRIEF.md` 定义的八字段表头新建它（这是补齐现有机制的第一次落地，不是本设计发明新字段）。
2. `EX-08`~`EX-11` 训练过程中暴露的可迁移认知错误，按 `MISTAKE_LOG.md` 现有六字段格式记录，不因为是新站就跳过。
3. 新增连接卡 `C08`~`C11`，格式比照 `C00`~`C07`（尤其 `C07.md`：白话定义 + 不同微例子 + 论文/路线位置 + CONNECT + 可选 EXTEND + 证据上限），核心题完成后解锁，并在 `CONNECTIONS.md` 索引表追加对应行；具体回答主题由 sol 按 `EX-08`~`EX-11` 各自的核心概念设计（比如 C08 可以测"hash 分派 vs 随机分派"、C09 可以测"结构信息丢失 vs 语言等价"这类跨概念区分，具体题目内容属于 sol 的设计空间，不属于本文件钉死范围——这一条与 §4 对三篇镜头论文"锚点由本文件钉死"的约束不同，因为连接卡本来就是各站自行出题的既有惯例，不是本次新引入的高自由度）。
4. 三篇镜头论文的 `L-S3`/`L-GT2`/`L-CX3` 是否也要配连接卡，本设计不强制——现有 `CONNECTIONS.md` 里四论文镜头（`L-S1`~`L-CX2`）本身就没有连接卡，只有 `EX-00`~`EX-07` 这类"站"才有；`L-S3`/`L-GT2`/`L-CX3` 延续镜头任务的既有惯例即可，不需要额外补卡，除非用户后续要求。

---

## 4. 三篇镜头论文的全文 ownership 改造

现行 `FOUR_PAPER_TRAINING_LOOP.md` 的 L-S1/L-S2/L-GT/L-CX1/L-CX2 全部保留、解锁点不变——它们是"对照镜头"任务，不是全文 ownership 任务。全文 ownership 是**新增的、独立的**任务层，挂在这些镜头任务之后。

**统一约束（三节都适用，sol 不得逐项自行放宽）**：

- 三节的具体任务、PASS 条件、章节锚点均由本设计文件钉死，sol 不得另行设计新的检验维度或替换本文列出的锚点章节。如果 sol 认为某项任务不合理，写进 §8 待用户拍板，不能自己改掉后直接执行。
- 只用已有拆解件（`teardown-bridge-20260904/` 下六个文件）+ 对应原始 PDF 作输入。不追加搜索该论文的后续引用、作者其他论文或第三方解读；如拆解件本身有 `[待补]`/`[需验证]` 标记，止步于标注缺口，不去外部找补。
- 每篇产出文档长度上限 2500 字（含表格），不写成新的全文精读稿——目标是"逐条对照六道门的证据清单"，不是重新生产一份比 teardown 更长的分析。
- 三节全部不涉及重新计算或质疑 EdgeIM 本身的站序、PASS 或密封答案；三篇论文的六道门证据各自独立成文，互不引用对方数字（继承 §7 F-measure 口径不可并表的约束）。**澄清**：这条约束管的是"不准去改 EdgeIM 的站序/PASS/密封答案"这类机制性动作，不等于"文中不准出现 EdgeIM 三个字"——L-GT2 任务5要求判断 Ground Truth 方法论能否套用到 EdgeIM 评测缺口，这是方法论层面的概念迁移讨论，不是重新计算或质疑 EdgeIM 本身，两者不冲突。



### 新增 L-S3｜sigRank 全文 ownership（挂在 L-S2 之后）

**解锁**：L-S2 PASS 后
**时间盒**：2-2.5h
**产出**：`sigRank全文重建.md`（≤2500字）

**具体任务（锚定 `10_SIGRANK.md`/`10b_SIGRANK_kg_critique.md` 已有章节，不得脱离锚点自由发挥）**：

1. 不看拆解件，凭记忆重推 §4.1 "Phase 1：sigRank 采样"的算法步骤（原文 Step 1-4 + 式(2)-(6)，注意该伪码本身标了`[复原]`——即原文没有正式 Algorithm 编号，是拆解者自己复原的，重推时必须说清这一层，不能讲成"论文写的 Algorithm X"）；写完后对照 `10_SIGRANK.md` §4.1 逐步核对。
2. 独立重算 §4.2 的 Fig. 3 手算例（不抄现成复算结果，自己重新算一遍数字，对上 teardown 里记录的复算列）。
3. 对照 §5.6"支撑住的结论"和 §5.7"没撑住的claim"两张清单，各挑其中 2 条，自己重新判断证据够不够、能不能用一句话向别人解释为什么，不能只复述 teardown 已经写好的结论。
4. 用 §6 Potential Flaw 里的一条，做一次 attack mode：假设改这条 assumption，预测 sigRank 结果会怎样变。

**PASS 条件**：以上 4 项任务全部有对应产出小节；§4.1 重推与原拆解件的算法步骤一致（允许表述不同，步骤和公式不能错）；Fig.3 复算数字与 teardown 记录一致或明确写出差异原因。

### 新增 L-GT2｜Ground Truth Approach（Sommers）全文 ownership（挂在 L-GT 之后）

**解锁**：L-GT 完成 world contract 之后
**时间盒**：2-2.5h
**产出**：`GroundTruth全文重建.md`（≤2500字）

**具体任务（锚定 `12_SOMMERS.md`/`12b_SOMMERS_kg_critique.md` 已有章节）**：

1. 不看拆解件，重述 §4.1"Generating process data with ground truth"的形式化定义 + §4.2 Deviation patterns 清单（有几种 deviation、各自改变了流程的什么部分）。
2. 重建 §4.3"模型变换 M→M′→变换结果"这条链，说清每一步谁生成、谁消费。
3. 对照 §5.4"支撑住的结论"和 §5.5"没撑住的claim"，同 sigRank 做法：各挑 2 条自己重新判断证据边界。
4. 用 §6 Potential Flaw 一条做 attack mode 预测。
5. 额外要求（这篇是方法论文，不是算法论文，与 sigRank/CrossEdgeIM 检验方式不同）：说清这篇论文的评估方法论本身能不能直接套到 EdgeIM 的评测缺口上——哪部分能套、哪部分因为领域不同套不了。这一条不能写成"完全适用"或"完全不适用"这类空泛结论，要具体到某个 deviation pattern 或某个 simulation 参数。

**PASS 条件**：以上 5 项任务全部有对应产出小节；§4.1-4.3 重建能自洽讲清链条，不出现"这一步大概是……"这类模糊表述。

### 新增 L-CX3｜CrossEdgeIM 全文 ownership（挂在 L-CX1 之后，不必等 L-CX2）

**解锁**：L-CX1 PASS 后
**时间盒**：2-2.5h
**产出**：`CrossEdgeIM全文重建.md`（≤2500字）

**具体任务（锚定 `11_CROSSEDGEIM.md`/`11b_CROSSEDGEIM_kg_critique.md` 已有章节）**：

1. 不看拆解件，重述三阶段架构：§4.1 Stage 1 活动节点处理、§4.2 Stage 2 组织节点建模、§4.3 Stage 3 中心节点合并各自具体做什么（不是"和 EdgeIM 不同"这种对比句，是这篇论文自己三阶段各自的内部机制）。
2. 对照 §5.5"撑住/没撑住的claim"，挑 2 条自己重新判断。
3. 用 §6 Potential Flaw 一条做 attack mode 预测。
4. **强制复述边界**（这条是硬性的，不是建议）：产出文档开头必须原样引用 `FOUR_PAPER_TRAINING_LOOP.md` §4 的这句话——"CrossEdgeIM 是公开合作谱系的后继，不是鲁法明署名论文……不用于夸大鲁老师个人产出"——并在文末自查一遍全文有没有出现任何把 CrossEdgeIM 处理结果归给导师本人的表述。

**PASS 条件**：以上 4 项任务全部有对应产出小节；边界复述句必须逐字引用出现，不能改写或省略；自查结果必须写"无"或列出需要修正的具体句子。

---

## 5. 验收标准（给 sol，逐条可打勾）

1. 四张全文 ownership 材料（EdgeIM 走 EX-08~EX-11，另外三篇各一份全文重建稿）都能对照 MASTERY_GATE.md 六道门逐条给出"这里怎么体现"的具体指向，不能空泛写"已理解"。三篇镜头论文的每一步任务必须对应 §4 里钉死的具体章节锚点，不接受 sol 自创的替代任务。
2. 每个新站/新镜头节的 PASS 条件冻结前，必须让用户看一遍再执行——本设计里给的 PASS 条件是草拟起点，不是终稿；sol 不能自己拍板冻结后直接执行诊断，也不能扩大 §4 已经限定的任务范围（不追加检验维度、不超字数上限、不跳过强制复述边界）。
3. 新建文件不覆盖、不静默修改现有 `EX-00`~`EX-07` 的题面、PASS 条件、密封答案；本设计只新增 `EX-08`~`EX-11`。
4. 改 `BRIEF.md`/`FOUR_PAPER_TRAINING_LOOP.md`/`MASTERY_GATE.md` 前，先确认 `TASK-20260906-004`（隔壁 Codex 窗口的独立修订稿整合任务）当前状态——如果它仍是 `in_progress` 且尚未整合完，sol 的改动要避免与它的写域冲突（它的独立修订稿在 `progress/decisions/2026-09-06__research__edgeim-jinzu-reconstruction-TASK-004.md`，整合清单在该文件 §8，sol 执行前应先读一遍避免重复设计）。
5. 完成后必须有 task log + INDEX 登记（用 `ledger_edit.py` 保形写，CAS 哈希收尾核验），不能只改文件不留痕。
6. 四篇 F-measure 口径不可直接并表——这条 `FOUR_PAPER_TRAINING_LOOP.md` §7 已有的约束，全文 ownership 改造后依然有效，不能因为"现在都是 5/5"就默认它们可以互相比较数字。

---

## 6. 需要注意的地方（风险与边界）

- **诊断门是硬前置，不是可选背景**：见 §1.5——`handoff/2026-09-08__edgeim-learning-training-acceptance-order__pause-handoff.md` 明文禁止诊断完成前"修改学习题面、PASS 条件、Ledger、答案和进组材料"，且截至本文件落盘，诊断仍未执行过一次。这是本文件所有内容里优先级最高的风险点：如果这份设计被直接拿去执行而没人先检查诊断状态，就会实质性违反现行唯一权威入口的明文禁令。
- **不是机械翻倍时间就完事**：用户要的是"全文 ownership"，不是"把现有六道门检验清单复制四份贴上去"。sigRank/Ground Truth/CrossEdgeIM 各自的论文结构和 EdgeIM 不同（比如 Ground Truth Approach 本身就是方法论文，不是算法论文），六道门的具体检验方式需要按论文类型适配，不能生搬 EdgeIM 的检验方法硬套。
- **CrossEdgeIM 的作者边界仍然有效**：`FOUR_PAPER_TRAINING_LOOP.md` §4 已经写明"CrossEdgeIM 是公开合作谱系的后继，不是鲁法明署名论文……不用于夸大鲁老师个人产出"——全文 ownership 改造不能把这条边界弄丢，学得再深也不代表可以在进组材料里把它包装成导师本人产出。
- **进组时间线的现实检查**：现有预算是 15-20h（BRIEF.md §2），这次新增约 12-15h（EdgeIM 侧）+ 三篇镜头论文各自的全文重建工作量，合计远超原计划。用户已经接受"翻倍"，但 sol 执行时如果发现实际耗时远超预估，应该停下来重新汇报，不能为了赶速度而把六道门检验标准悄悄降低到和之前一样浅。
- **磁盘现状 vs 台账口径的老问题**：本文件继承 `MASTERY_GATE.md` 已经记录的差异（用户报告"已过 EX-3"，但 BRIEF.md 台账字面仍写旧游标）——sol 新增 EX-08 等站点时，请按用户本次拍板的口径（全部四篇升 5/5）向前设计，不要卡在"台账字面对不上"这件事上反复核对，这条差异已经挂过 flag，不需要重复确认。
- **不要在设计阶段就动手写密封答案**：本文件只设计站点结构和 PASS 条件草案，sol 执行时新站的题面、密封答案应该按现有站点的流程（作答写别的文件、密封在 `_sealed/`）来做，不要在设计文档里预先写正确答案。
- **09-20 冻结日与翻倍工作量的真实冲突，需要用户在此明确表态**：`BRIEF.md` 原文"合计约 15–20h，在 T4 容量模型（115h）内；进组已改月底，09-20 冻结日不动"——本窗口核实这不是本教学包自带的软性提示，`09-20` 是 2026-09-03 用户 controller 六项拍板中批准的 Bridge 执行计划 v1.0（`progress/decisions/2026-09-03__research__bridge-lu-execution-plan-v1.0.md`）里的"核心技术闭环冻结"日期，那份计划自己写的冻结门槛是"核心 paper note、CORE foundation PASS、自己的 sampling 实现、primary experiment 初步结果、初步 verdict/claim ceiling、≥1 次 D+2 ownership"，是一条独立于本教学包、跨越多条线的既定截止日。今天是 2026-09-12，离 09-20 只剩 8 天；本设计新增 EdgeIM 侧约 12-15h + 三篇镜头论文全文重建各 2-2.5h（合计约 6-7.5h）+ 诊断本身 60-90 分钟 + 回补诊断缺口的时间，即使排除等待冻结的其他线任务，单看这条线新增的量能否在 8 天内于日常时间投入下走完就已经存疑，更不用说 09-20 冻结门槛要求的是"核心技术闭环"而不是"教学包全部学完"。本设计不擅自判断"翻倍工作量与 09-20 冲突就应该砍哪部分"——这需要用户看到这条时间线拉直后明确表态：是 09-20 冻结门槛本身按现有窄口径（核心闭环）不受本次翻倍影响、还是需要重新评估进度、还是 09-20 本身需要重新与用户对齐。sol 执行前应该先把这条摆到用户面前，不能假设"用户已经接受翻倍"这句话已经隐含了对 09-20 时间线冲突的确认——那句"接受翻倍"是回答"四篇要不要都做到 5/5"这个范围问题，不是回答"8 天内做不做得完"这个时间问题。

---

## 7. sol 执行顺序建议

0. **先确认诊断门状态**（见 §1.5）：检查用户是否已完成 15 题 Whole-Paper Diagnostic；未完成则本设计全部内容处于"设计储备"状态，不得动工建站，应先引导/等待诊断完成。
1. 先读 `TASK-20260906-004` 独立修订稿现状（避免重复设计冲突）。
2. 和用户过一遍本文件 §3/§4 的 PASS 条件草案，冻结后再动手。
3. 新建 `EX-08`~`EX-11` 目录和题面（仿照现有站点格式）。
4. 新建 `L-S3`/`L-GT2`/`L-CX3` 条目，接入 `FOUR_PAPER_TRAINING_LOOP.md`（如果该文件当时仍被 `TASK-20260906-004` 占用写域，先在独立文件里设计，等整合窗口开放再合并）。
5. 更新 `BRIEF.md` 时间预算行（15-20h → 反映翻倍后的新预算），标注这是本次拍板的结果。
6. task log + INDEX 登记收尾。

---

## 更新记录

- 2026-09-12：首版落盘。响应用户"为什么教学包只有一部分"的质疑 + sol 审计确认缺口真实存在 + 用户拍板四篇全部升 5/5；本文件只做设计交 sol 执行，不新建站点本体、不改共享正本。
- 2026-09-12（同日修订）：用户指出 §4 三篇镜头论文给 sol 的空间太大（原稿每节只有"解锁/要求/产出"三行，"要求"是一句空泛的"套用六道门"，无锚点无边界，与 EdgeIM 侧 EX-08~11 的具体化程度不对等）。重写 §4：加统一约束段（锚点由本文件钉死不许自创任务、只用现有拆解件不外部搜索、每篇≤2500字、三篇互不并表）；三节各自列出锚定 `teardown-bridge-20260904/` 具体章节号的任务清单+时间盒+PASS 条件；L-CX3 新增强制逐字复述"不是鲁法明署名论文"边界句的硬性要求。§5 验收标准第1、2条同步收紧，明确 sol 不得自创检验维度或超出 §4 限定范围。
- 2026-09-12（同日第二次修订，压缩重启后重新核验发现）：①订正首版一处核实错误——`e71384d` 并非查无此哈希，是本窗口误把它当外层元仓的哈希去查；`MAS_Safety_Project` 是独立 git 仓库，`e71384d` 是本仓当前 HEAD（`TASK-20260912-004` checkpoint commit），真实存在且可查证，已在 §1 订正并新增 §1.5。②新增 §1.5+§6+§7 步骤0：核实本线唯一权威恢复入口（09-08 pause-handoff）明文要求诊断优先，且今早的 checkpoint commit 自证诊断仍未执行——本设计全部内容因此改为"诊断完成后才可执行的设计储备"，不是可立即动工的任务单；这是本次修订中优先级最高的一条。③新增 §3.5：补上首版完全遗漏的 `LEDGER.md`（核实磁盘上尚不存在）、`MISTAKE_LOG.md`、连接卡 C00-C07 三项现有硬证据机制在新站 `EX-08`~`EX-11` 上的接入要求。④在 §6 补充 09-20 冻结日的真实来源核查（系 2026-09-03 Bridge 执行计划 v1.0 的既定跨线截止日，非本包软性提示）与翻倍工作量的时间冲突，明确这条需要用户单独表态，不能默认"接受翻倍"已隐含对 8 天内工期的确认。⑤修订过程中再次出现一处 Unicode 乱码字符（§7 步骤0标题），已发现并订正。
- 2026-09-12（同日第三次修订，§4 可执行性复核）：以"sol 只拿本文件+现有拆解件、不问人能否走完"为标准重查 §4。核实三篇论文锚定的具体章节号（sigRank §4.1/4.2/5.6/5.7/6、Ground Truth §4.1/4.2/4.3/5.4/5.5/6、CrossEdgeIM §4.1/4.2/4.3/5.5/6）在 `teardown-bridge-20260904/` 对应文件中逐一存在，编号无误，不需改动；三节任务数 4/5/4 不一致属有意设计（Ground Truth 多一项因其为方法论文而非算法论文），文档已自带理由，不需改动。发现一处真实歧义并修补：统一约束"不涉及重新计算或质疑 EdgeIM 本身"与 L-GT2 任务5"判断能否套用到 EdgeIM 评测缺口"字面相邻易读岔，已在统一约束段落追加一句澄清——约束管的是不准变动 EdgeIM 站序/PASS/密封答案等机制性动作，不是禁止文中出现 EdgeIM 三字，两者不冲突。

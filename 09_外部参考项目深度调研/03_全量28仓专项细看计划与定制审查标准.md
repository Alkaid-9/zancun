# 03 · 全量 28 仓专项细看计划与定制审查标准规范

- **制定日期**：2026-09-19
- **任务编号**：`TASK-20260919-REF-PLAN-V2`
- **指导思想**：拒绝千篇一律的模板套用，坚决杜绝“把教程当架构评、把排除项当现成工具写、把高能底座当普通博客概括”。按 28 个项目的真实技术原型分类，为每一个项目定制**专属审讯问题、源码审查关键路径、对齐鲁组科研与两台施工的定制验收指标（Custom Done-When）**。

---

## 一、28 仓技术原型分类（Archetypes）与审讯基准

| 原型编号 | 原型名称 | 包含项目清单 (共 28 仓) | 审查核心维度与硬核标准 |
|:---:|:---|:---|:---|
| **Archetype A** | **智能体运行时与控制平面底座** | `prime-agent`, `loopx`, `OpenViking`, `paperclip` | 进程模型、IPC/RPC、长程状态机持久化、CAS乐观锁、租约隔离、防并发写冲突、内存快照与回滚 |
| **Archetype B** | **大模型上下文与推理基础设施** | `headroom`, `airllm`, `llmfit` | Prompt Caching 冻结边界保护、无损压缩算法真实度、分层加载与显存调度流水线、硬件瓶颈测量数学模型 |
| **Archetype C** | **智能体交互规约与可视化技能** | `i-have-adhd`, `archify`, `openviking-plugins`, `openviking-ui` | Claude Code 原生 Hook 拦截协议、系统提示词违禁词过滤器、JSON IR 强类型 AST、确定性渲染与 UI 组件 |
| **Archetype D** | **学术科研与垂直领域多智能体系统** | `science-skills`, `scientific-agent-skills`, `TradingAgents` | 外部学术数据库（arXiv/OpenAlex）API 封装契约、实验分析验证流程模板、多角色独立风控门禁状态机 |
| **Archetype E** | **工业级架构课程与全栈工程体系** | `ai-engineering-from-scratch-zh`, `ai-engineering-from-scratch`, `mcp-for-beginners`, `AI-For-Beginners` | 课程知识树覆盖度、内置 Claude Code 专属教学技能源码、符号主义本体建模、MCP 2026-07-28 规范跨语言实现 |
| **Archetype F** | **应用层教程与特定生态工具** | `generative-ai-for-beginners`, `ai-agents-for-beginners`, `Generative-AI-for-beginners-dotnet`, `edgeai-for-beginners`, `sim` | RAG 幻觉治理评测基准、MAF 多智能体设计模式、本地轻量小模型（SLM）端侧低延迟运行、画布与表格统一喂给 Agent |
| **Archetype G** | **历史对照排除项与底层编译观察项** | `thoughtbot/paperclip`, `WickyNilliams/headroom.js`, `AirSim`, `modular`, `omarchy` | 同名混淆历史查证、官方废弃技术事实、与当前两台业务脱节的判定证据、长期技术雷达观察边界 |

---

## 二、全量 28 仓定制审查方案（一仓一表，28 份专项审讯书）

---

### 原型 A：智能体运行时与控制平面底座（4 仓）

#### 01. `PrimeIntellect-ai/prime-agent`
- **专属审讯问题**：
  1. 其 RLM REPL 进程如何做到在后台常驻且与 TypeScript 宿主保持双向 stdio 通信？
  2. `rlm.spawn` 子代理是如何实现“函数调用式回传内存变量”，彻底避免主聊天窗口被子对话刷屏的？
  3. `/refine` 持续演进是如何保证不在代码执行中途污染当前 Prompt 的？
- **源码审查关键路径**：
  - `prime-agent-runtime/src/rlm/repl.py`（通信协议与执行循环）
  - `prime-agent-runtime/src/rlm/harness.py`（CJK 二元分词与记忆快照）
  - `packages/coding-agent/skills/refine/src/refine/__init__.py`（Turn-end 调度策略）
- **对齐鲁组科研/两台验收标准**：
  - 必须提取出纯 Python 版的 `host_request` / `_send` 通信样板代码；
  - 必须给出将其 Continual Harness 接入工作台长程交接（TASK-20260918-005）的改造路径。

#### 02. `huangruiteng/loopx`
- **专属审讯问题**：
  1. 其控制平面（Control Plane）如何与执行外壳（Claude Code/Codex）解耦？
  2. 任务四状态契约（`pending` -> `current` -> `delivered` -> `not_required`）与租约锁（Lease Fence）如何在代码层杜绝多 Agent 写冲突？
  3. 消息可见性与执行权限（Message Visibility vs Turn Authority）的隔离边界代码是如何实现的？
- **源码审查关键路径**：
  - `docs/architecture/rfcs/typescript-control-plane-migration-v0.md`（控制面完整状态机 RFC）
  - `docs/reference/protocols/lark-manager-context-authority-v0.md`（权限与可见性契约）
  - `apps/desktop/loopx-control-plane/src-tauri/src/services.rs`（后端服务调度）
- **对齐鲁组科研/两台验收标准**：
  - 必须给出基于 CAS 乐观锁的多 Agent 任务更新校验函数伪代码；
  - 必须梳理出将其 Personal Workspace 状态机挂载至科研台任务树的具体数据结构。

#### 03. `volcengine/OpenViking`
- **专属审讯问题**：
  1. `viking://` 虚拟文件系统是如何在底层通过 POSIX 接口组织 `resources/`、`memories/`、`skills/` 的？
  2. 解决长论文与超大上下文的 **L0（摘要）-> L1（概览）-> L2（全文）** 按需晋升加载引擎的具体实现与判定算法是什么？
- **源码审查关键路径**：
  - `openviking/server/api/fs.py` 或对应 VFS 路由实现
  - 记忆提取管道（Context Compilation）核心模块
- **对齐鲁组科研/两台验收标准**：
  - 必须给出将鲁组 10 余篇并发论文（SBTPN、EdgeIM、CrossEdgeIM 等）全文化组织为 `viking://resources/papers/` 的分级加载目录规范；
  - 评估其 AGPL-3.0 协议在独立进程调用下的合规边界。

#### 04. `paperclipai/paperclip`
- **专属审讯问题**：
  1. 该系统在管理多 Agent 时，是如何对每个 Agent 设定 Token/成本硬限制（Budget Control）的？
  2. 其团队角色看板（Goal -> Agent -> Task）的数据模型与状态流转是如何定义的？
- **源码审查关键路径**：
  - `src/server/budget/` 或组织治理（Governance）相关模块
  - 前端任务看板组件源码
- **对齐鲁组科研/两台验收标准**：
  - 提取其 Token 预算预警与中断机制的数据 Schema；
  - 判定其多 Agent 协作面板是否适合微调后用作两台工作台的监控 UI。

---

### 原型 B：大模型上下文与推理基础设施（3 仓）

#### 05. `headroomlabs-ai/headroom`
- **专属审讯问题**：
  1. 宣称的 5x~20x 压缩到底如何做到不破坏 Anthropic Prompt Caching？
  2. Rust 核心 crate 中的 `compute_frozen_count` 算法是如何精确计算冷冻前缀的？
  3. 为什么严禁使用正则表达式进行消息树遍历？
- **源码审查关键路径**：
  - `crates/headroom-core/src/cache_control.rs`（AST 遍历与边界计算）
  - `crates/headroom-core/src/lib.rs`（模块拓扑）
  - `benchmarks/compression_benchmark.py`（真实压缩率测试）
- **对齐鲁组科研/两台验收标准**：
  - 必须给出冷冻区与活跃区划分的代码级判定逻辑；
  - 给出在 Claude Code 本地启动 Headroom 代理（`headroom proxy`）的零风险挂载方案。

#### 06. `lyogavin/airllm`
- **专属审讯问题**：
  1. 单卡 4GB 显存运行 70B 大模型、12GB 显存运行 DeepSeek-V3 的分层流式（Layer-wise Streaming）计算到底是怎样通过 mmap 和预取（Prefetching）实现的？
  2. 牺牲推理吞吐时间（tokens/s）换取显存利用率的准确性能开销曲线是多少？
- **源码审查关键路径**：
  - `air_llm/airllm_base.py`（核心 Layer 加载与释放调度器）
  - 预取流水线与 host-to-device 数据搬运代码
- **对齐鲁组科研/两台验收标准**：
  - 给出在无高性能 GPU 集群的个人设备上，使用 AirLLM 离线运行开源模型进行学术论文批量特征提取的可行性评估与命令行模板。

#### 07. `AlexsJones/llmfit`
- **专属审讯问题**：
  1. Rust 工具是如何在多平台（Linux/macOS/Windows）跨硬件自动探测 CPU、RAM、CUDA/ROCm VRAM 带宽的？
  2. 其评估模型承载力与 tok/s 的数学公式模型是如何构建的？
- **源码审查关键路径**：
  - `src/hardware/`（硬件探测实现）
  - `src/calculator/` 或模型显存开销矩阵计算代码
- **对齐鲁组科研/两台验收标准**：
  - 提取其模型参数量、量化位宽（GGUF/AWQ）与上下文窗口显存占用的计算公式，作为团队设备基准校验工具。

---

### 原型 C：智能体交互规约与可视化技能（4 仓）

#### 08. `ayghri/i-have-adhd`
- **专属审讯问题**：
  1. 其 10 条高压行动导向铁律是如何在 Prompt 层面封杀废话、客套和铺垫的？
  2. 其发送前自检（Pre-send Check）的具体删减规则与 Debug Spiral 熔断条款是如何设计的？
- **源码审查关键路径**：
  - `skills/i-have-adhd/SKILL.md`（完整技能契约与破例规则）
- **对齐鲁组科研/两台验收标准**：
  - 必须提取出可直接集成进我们工作台系统 Prompt 的紧凑防废话规则块，单轮节约 Token 预期达到 15%~25%。

#### 09. `tt-a1i/archify`
- **专属审讯问题**：
  1. Agent 是如何通过输出强类型的 JSON IR 让系统确定性渲染出交互式 HTML/SVG 的？
  2. 独创的 **Before / Delta / After 架构 Diff 对比**的数据结构是怎样定义的？
- **源码审查关键路径**：
  - `skills/archify/SKILL.md`
  - JSON IR Schema 定义与渲染管线编译器
- **对齐鲁组科研/两台验收标准**：
  - 提取其 Before/Delta/After 差异对比 JSON Schema，用于可视化呈现鲁组 EdgeIM/CrossEdgeIM 算法演进。

#### 10. `Castor6/openviking-plugins`
- **专属审讯问题**：
  1. `hooks.json` 是如何分别绑定 `UserPromptSubmit`（召回）与 `Stop`（沉淀）Hook 的？
  2. `auto-recall.mjs` 中的四维加权打分与 `auto-capture.mjs` 中的增量游标控制具体是如何编写的？
- **源码审查关键路径**：
  - `plugins/claude-code-memory-plugin/hooks/hooks.json`
  - `plugins/claude-code-memory-plugin/scripts/auto-recall.mjs`
  - `plugins/claude-code-memory-plugin/scripts/auto-capture.mjs`
- **对齐鲁组科研/两台验收标准**：
  - 提取完整的 Hook 脚本，改造为针对本项目本地 `MEMORY.md` 和 SQLite 账本的无感注入插件。

#### 11. `ruansheng8/openviking-ui`
- **专属审讯问题**：
  1. Next.js Route Handler 是如何实现 BFF 架构避免在前端暴露管理员秘钥的？
  2. 其前端展示 L0 摘要、L1 概览、L2 全文的切换卡片组件是如何封装的？
- **源码审查关键路径**：
  - `app/api/`（API 转发路由）
  - 前端资源树与分层文档查看组件
- **对齐鲁组科研/两台验收标准**：
  - 提取其 L0/L1/L2 可视化组件设计，作为未来桌面端科研台论文浏览器的界面参考。

---

### 原型 D：学术科研与垂直领域多智能体系统（3 仓）

#### 12. `google-deepmind/science-skills`
- **专属审讯问题**：
  1. Google DeepMind 是如何为 Agent 规范定义一个标准的 Scientific Skill 的（Frontmatter + CLI）？
  2. 其 `literature_search_arxiv` 和 `workflow_skill_creator` 的 Python 核心检索脚本是如何封装 API 请求与异常重试的？
- **源码审查关键路径**：
  - `skills/literature_search_arxiv/`
  - `skills/workflow_skill_creator/`
  - `plugin.json` 与各 Skill 的 `SKILL.md`
- **对齐鲁组科研/两台验收标准**：
  - 提取 arXiv 和 OpenAlex 的学术文献结构化检索脚本，直接合入科研台学术工具库。

#### 13. `K-Dense-AI/scientific-agent-skills`
- **专属审讯问题**：
  1. 其 166 个领域科学工具中，哪些是真正的自研逻辑，哪些是第三方库（BioPython, Astropy）的薄层包装？
  2. 其 `analytical-method-validation` 实验分析验证流程的标准化输入输出是如何设计的？
- **源码审查关键路径**：
  - 关键科学工具包目录与执行脚本
  - 数据校验模板
- **对齐鲁组科研/两台验收标准**：
  - 筛选出与计算机系统、静态分析、形式化验证相关的技能，淘汰生信无关技能。

#### 14. `TauricResearch/TradingAgents`
- **专属审讯问题**：
  1. 其基于 LangGraph 构建的多智能体状态机，是如何实现可中断与断点恢复（Checkpointing）的？
  2. 其独创的 **Risk/Portfolio Manager 独立风控审查 Agent 否决机制** 是如何在代码中强行阻断不合规操作的？
- **源码审查关键路径**：
  - LangGraph 工作流状态机构建代码
  - 风控审查节点（Risk Gate）代码实现
- **对齐鲁组科研/两台验收标准**：
  - 提取其独立的“执行 Agent 与审查 Agent 分权隔离机制”，用于改造我们的任务验收门禁（AC Gate）。

---

### 原型 E：工业级架构课程与全栈工程体系（4 仓）

#### 15. `fancyboi999/ai-engineering-from-scratch-zh`
- **专属审讯问题**：
  1. 其内置的 6 大 Claude Code 交互式教学导师（`/start-learning`, `/learn-mcp`, `/learn-agent-skills`）是如何通过原生技能脚本实现的？
  2. 其 CI 流水线是如何实现 523 节课与国际上游严格一致性校验的？
- **源码审查关键路径**：
  - `skills/` 下的各类教学导师定义
  - `site/build.js` 校验脚本
  - Phase 14（Agent 工程）核心课程代码
- **对齐鲁组科研/两台验收标准**：
  - 必须完成将该技能包挂载至 Claude Code 的本地测试，输出进组新人个性化 `LEARNING.md` 培养路线方案。

#### 16. `rohitg00/ai-engineering-from-scratch`
- **专属审讯问题**：
  1. 上游 20 阶段 523 课中，从零手写 Transformer、手写自动微分引擎（Autograd）、手写 Agent Loop 的代码质量与硬核度如何？
  2. 每一课强制交付一个可运行工件的教学设计是如何做代码测试守护的？
- **源码审查关键路径**：
  - `phases/02-autograd/`
  - `phases/14-agent-engineering/`
- **对齐鲁组科研/两台验收标准**：
  - 作为学习台核心教材底层权威对照，提供核心算法的最小手写实现基准。

#### 17. `microsoft/mcp-for-beginners`
- **专属审讯问题**：
  1. 紧跟 MCP 2026-07-28 规范，其无状态请求（Stateless Requests）与 Extensions 架构在 6 种编程语言中的参考实现是怎样的？
  2. MCP 客户端与服务端的鉴权、安全沙箱与防止提示词注入（Prompt Injection）最佳实践代码是什么？
- **源码审查关键路径**：
  - `01-CoreConcepts/mcp-2026-07-28.md`
  - Python / TypeScript 标准 Server 模板实现
- **对齐鲁组科研/两台验收标准**：
  - 提取最规范的 Python/TypeScript MCP Server 脚手架，作为未来两台工具向外暴露的标准通信协议。

#### 18. `microsoft/AI-For-Beginners`
- **专属审讯问题**：
  1. 课程第二单元《Symbolic AI》中的**本体论（Ontology）、知识表示与概念图（Concept Graph）**是如何用代码实现的？
  2. 经典符号主义 AI 如何与现代 LLM 结合，以根治生成文本中的幻觉问题？
- **源码审查关键路径**：
  - `lessons/2-Symbolic/` 下的 Jupyter Notebooks 与概念图构建代码
- **对齐鲁组科研/两台验收标准**：
  - 提取其概念图与本体构建代码，为科研台构建鲁组论文（Petri 网、锁、数据竞争、漏洞模式）的知识图谱提供理论与数据结构支撑。

---

### 原型 F：应用层教程与特定生态工具（5 仓）

#### 19. `microsoft/generative-ai-for-beginners`
- **专属审讯问题**：
  1. 课程第 3 版中对 RAG 系统的幻觉治理评测指标与 Checkpoint 是如何定义的？
  2. 其本地离线运行模式（Foundry Local）是如何解耦云端依赖的？
- **源码审查关键路径**：
  - RAG 章节与幻觉评测代码示例
  - Foundry Local 本地运行配置
- **对齐鲁组科研/两台验收标准**：
  - 汲取其 RAG 检索评测指标，用于评估两台文献库语义检索的召回率。

#### 20. `microsoft/ai-agents-for-beginners`
- **专属审讯问题**：
  1. 其第 3 课总结的智能体四大经典模式（Reflection, Tool Use, Planning, Multi-Agent Orchestration）在 Microsoft Agent Framework (MAF) 中的代码实现结构是怎样的？
  2. 人机协同（Human-in-the-loop）审批流的阻断与恢复机制是如何编写的？
- **源码审查关键路径**：
  - `03-agentic-design-patterns/` 代码示例
  - 多 Agent 协同编排拓扑源码
- **对齐鲁组科研/两台验收标准**：
  - 提取其反思与分工握手协议，作为工作台多 Agent 协同的代码规范。

#### 21. `microsoft/Generative-AI-for-beginners-dotnet`
- **专属审讯问题**：
  1. .NET 8/9 的 `Microsoft.Extensions.AI` 统一抽象层设计是如何屏蔽底层不同模型供应商差异的？
- **源码审查关键路径**：
  - `samples/MAF/`
- **对齐鲁组科研/两台验收标准**：
  - 评估其统一模型抽象层思想，供工作台模型路由调度器参考。

#### 22. `microsoft/edgeai-for-beginners`
- **专属审讯问题**：
  1. 小型语言模型（SLM，如 Phi-4, Mistral）是如何利用 ONNX Runtime 在本地纯 CPU 或轻量设备上做低延迟量化推理的？
- **源码审查关键路径**：
  - ONNX 量化与优化部署脚本
- **对齐鲁组科研/两台验收标准**：
  - 产出一套在个人笔记本上部署轻量只读审查 Agent 的离线运行方案。

#### 23. `simstudioai/sim`
- **专属审讯问题**：
  1. 其前端是如何将表格（结构化数据）、文件（知识文档）和 Agent 会话统一集成在同一协作画布上的？
  2. Docker 本地一键化编排（`npx sim-setup`）的微服务架构是怎样的？
- **源码审查关键路径**：
  - `docker-compose.prod.yml`
  - 前端工作流画布（Workflow Builder）组件源码
- **对齐鲁组科研/两台验收标准**：
  - 提取其“表格数据与非结构化文档统一喂给 Agent”的上下文组织交互模式，供工作台 UI 参考。

---

### 原型 G：历史对照排除项与底层编译观察项（5 仓）

#### 24. `thoughtbot/paperclip` (vs `paperclipai/paperclip`)
- **专属审讯问题**：
  1. 彻底查证其历史代码：该项目是如何在 2018 年被 ActiveStorage 替代并废弃的？
  2. 给出无可辩驳的证据，证明其为 Ruby on Rails 附件 Gem，与现代 AI Agent 彻底无关。
- **核验证据**：
  - 官方 `closing-the-trombone` 博客公告与仓库归档记录
- **验收标准**：
  - 产出严谨的排除判决书，彻底从团队资产线中结项剔除。

#### 25. `WickyNilliams/headroom.js` (vs `headroomlabs-ai/headroom`)
- **专属审讯问题**：
  1. 彻底查证其源码：证明其纯属网页顶部导航栏显示/隐藏的纯前端 DOM 滚动动画脚本，与 LLM 上下文压缩毫无血缘关系。
- **核验证据**：
  - 原生 JS 滚动监听源码
- **验收标准**：
  - 产出排他性司法证据，防止后续团队成员重蹈同名混淆覆辙。

#### 26. `microsoft/AirSim`
- **专属审讯问题**：
  1. 微软官方是何时、出于何种战略原因宣布关闭该 UE 自动驾驶仿真开源项目的？
  2. 其机器人物理仿真与 LLM 智能体在技术路线上为何彻底脱节？
- **核验证据**：
  - 微软 2022 年官方归档说明与无人机物理控制代码
- **验收标准**：
  - 形成归档历史案例分析，判定为彻底排除项（SHELVE）。

#### 27. `modular/modular`
- **专属审讯问题**：
  1. MAX 引擎与 Mojo 语言在底层异构编译上的技术优势与当前生态成熟度到底如何？
  2. 为什么在当前阶段将其强行引入两台 Agent 上层编排属于“过度工程与技术早产”？
- **核验证据**：
  - Mojo 标准库与 MAX 运行架构源码
- **验收标准**：
  - 定位为长期技术雷达观察项，明确当前阶段暂缓引入的界限。

#### 28. `omacom/omarchy`
- **专属审讯问题**：
  1. DHH 定制的 Agentic Linux 发行版，其终端、Neovim、Kitty、全局剪贴板与 AI 深度集成的具体 dotfiles 配置方案是什么？
- **核验证据**：
  - `manual/` 与系统配置脚本
- **验收标准**：
  - 提取其桌面端 AI 快捷键与终端工作流配置哲学，供本地 Linux/WSL2 开发环境调优参考。

---

## 三、实施推进计划（分为 7 个专项战役）

1. **第一战役（底座核心）**：解剖原型 A（`prime-agent`, `loopx`, `OpenViking`, `paperclip`），产出 4 份独立卷宗；
2. **第二战役（推理与上下文）**：解剖原型 B（`headroom`, `airllm`, `llmfit`），产出 3 份独立卷宗；
3. **第三战役（交互与技能）**：解剖原型 C（`i-have-adhd`, `archify`, `openviking-plugins`, `openviking-ui`），产出 4 份独立卷宗；
4. **第四战役（科研与专向 Agent）**：解剖原型 D（`science-skills`, `scientific-agent-skills`, `TradingAgents`），产出 3 份独立卷宗；
5. **第五战役（全栈工程体系）**：解剖原型 E（`ai-engineering-zh`, `ai-engineering-en`, `mcp-for-beginners`, `AI-For-Beginners`），产出 4 份独立卷宗；
6. **第六战役（生态应用工具）**：解剖原型 F（微软 GenAI 体系、EdgeAI、Sim），产出 5 份独立卷宗；
7. **第七战役（历史排除与观察）**：解剖原型 G（两组同名排查、AirSim、Modular、Omarchy），产出 5 份独立卷宗；
8. **收官总成**：合成最终全局决算与代码移植落地清单。
